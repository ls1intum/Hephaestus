import type { PartRendererMap } from "./types";

/**
 * Default per-tool renderers for mentor chat messages.
 *
 * The Pi mentor surface currently exposes no client-side tools. When tools
 * are reintroduced, register their renderers here.
 */
export const defaultPartRenderers: PartRendererMap = {};
