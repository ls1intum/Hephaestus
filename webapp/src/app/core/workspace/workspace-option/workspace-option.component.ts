import { Component } from '@angular/core';

@Component({
  selector: 'app-workspace-option',
  standalone: true,
  imports: [],
  template: `<p>workspace-option works!</p>`
})
export class WorkspaceOptionComponent {
  // Two variants:
  // - Compact for desktop with tooltip
  // - Full for mobile for sidebar with thumb and text
}
