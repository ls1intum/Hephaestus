// Global constants injected by Vite at build time

/**
 * Application version from package.json, injected at build time.
 * Used for cache-busting: ensures JS bundle content-hashes change between versions.
 */
declare const __APP_VERSION__: string;
