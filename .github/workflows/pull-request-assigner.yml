name: "Pull Request"

on:
  pull_request:
    types: [opened]

jobs:
  assign-author:
    name: Assign author
    runs-on: ubuntu-latest
    steps:
      - name: Assign author to PR
        uses: technote-space/assign-author@v1