# =============================================================================
# PREVIEW DEPLOYMENT ENVIRONMENT VARIABLES
# =============================================================================
#
# Copy this file and fill in the values for your preview environment.
#
# For Coolify: Add these as environment variables in the Service/Application settings.
#
# =============================================================================

# -----------------------------------------------------------------------------
# DOMAIN CONFIGURATION
# -----------------------------------------------------------------------------
# Base domain for the preview environment (e.g., preview.hephaestus.cit.tum.de)
# PR deployments will be at pr-{NUMBER}.{PREVIEW_DOMAIN}
PREVIEW_DOMAIN=preview.hephaestus.cit.tum.de

# -----------------------------------------------------------------------------
# INFRASTRUCTURE SECRETS (compose.infrastructure.yaml)
# -----------------------------------------------------------------------------

# GitHub webhook secret (same as configured in GitHub App)
WEBHOOK_SECRET=

# Keycloak admin credentials
KC_ADMIN_USERNAME=admin
KC_ADMIN_PASSWORD=

# Keycloak database password
KEYCLOAK_DB_PASSWORD=

# Keycloak GitHub OAuth (for user login via GitHub)
KEYCLOAK_GITHUB_CLIENT_ID=
KEYCLOAK_GITHUB_CLIENT_SECRET=

# Keycloak confidential client secret (shared by all PR deployments)
KEYCLOAK_HEPHAESTUS_CONFIDENTIAL_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# APPLICATION SECRETS (compose.application.yaml)
# -----------------------------------------------------------------------------

# GitHub App credentials
GH_APP_ID=
GH_APP_PRIVATE_KEY=
GH_AUTH_TOKEN=

# AI Model configuration
MODEL_NAME=openai:gpt-4o
DETECTION_MODEL_NAME=openai:gpt-4o-mini

# OpenAI
OPENAI_API_KEY=
OPENAI_API_VERSION=

# Azure OpenAI (alternative to OpenAI)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=

# Ollama (alternative to OpenAI)
OLLAMA_HOST=
OLLAMA_BASIC_AUTH_USERNAME=
OLLAMA_BASIC_AUTH_PASSWORD=

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Skip Keycloak login (for testing)
KEYCLOAK_SKIP_LOGIN=false

# NATS
NATS_ENABLED=true

# Monitoring (relaxed for previews)
MONITORING_TIMEFRAME=14
MONITORING_RUN_ON_STARTUP=false
MONITORING_SYNC_CRON=0 0 2 * * *

# Observability (optional)
SENTRY_DSN=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=
