if [ "${HEPHAESTUS_SKIP_PRE_PUSH:-}" = "1" ]; then
	echo "WARNING: pre-push verification skipped. Run: pnpm run check"
	exit 0
fi

echo "Pre-push: pnpm run check"
if ! pnpm run check; then
	echo "Pre-push failed. Run: pnpm run check"
	exit 1
fi
echo "Pre-push passed."
