---
---

Intentionally empty: this change replaces the contributor-facing JS/TS toolchain orchestration
(Vite+ task graph instead of npm-run-all2) and is invisible to operators and users — no runtime
behavior, configuration, or upgrade action changes.
