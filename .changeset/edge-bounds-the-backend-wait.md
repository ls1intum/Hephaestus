---
"hephaestus": patch
---

Restores the timeouts the bundled edge applies to the application server, so a backend that accepts a connection but never answers releases the browser with an error page instead of leaving requests and mentor chat streams hanging indefinitely. A host that keeps itself on its channel starts the stacks in the order the migration requires, converges again when you promote the release it already runs, and retries after a partly-applied deploy. Rolling back to a release published before GitHub offered immutable tags works again.
