---
---

No release note: the agent image's Node.js pin gained a comment naming the release list it is read
from, so every place that pins the same Node.js version follows one list. The image is built from the
same version as before.
