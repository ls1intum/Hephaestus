---
"hephaestus": patch
---

Practice feedback about commit messages and commit scope now sees the pull request's commits — each subject, body, timestamps and size — instead of coming back inconclusive because no commit list reached the review.
