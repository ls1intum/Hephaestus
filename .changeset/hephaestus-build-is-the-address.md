---
"hephaestus": patch
---

The TUM-operated instance is addressed as `hephaestus.build`. The privacy statement, `security.txt`
and the documentation name it, and the older `hephaestus.aet.cit.tum.de` address reaches the same
instance. The imprint's and the in-app footer's source links follow the repository to the
`hephaestus-build` organisation; images and signatures for releases cut before the move keep their
original namespace and signing identity, so verifying an older release is unchanged.
