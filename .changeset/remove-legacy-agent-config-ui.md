---
"hephaestus": patch
---

Removes the retired agent-runtimes screen and its named-agent-config editor from the admin UI. Workspace AI setup lives on the single **AI models** page, where each purpose (practice detection, mentor) is bound to a model directly.
