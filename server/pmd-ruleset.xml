<?xml version="1.0"?>
<ruleset name="Hephaestus PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD ruleset for Hephaestus following industry best practices.
        Run with: ./mvnw pmd:check or npm run lint:java

        Based on PMD Best Practices and Maven PMD Plugin defaults.
        See: https://pmd.github.io/pmd/pmd_rules_java_bestpractices.html
    </description>

    <!-- Exclude generated code -->
    <exclude-pattern>.*/target/.*</exclude-pattern>

    <!-- ====================================================================
         BEST PRACTICES - High-value rules for code quality
         ==================================================================== -->

    <!-- Unused code detection -->
    <rule ref="category/java/bestpractices.xml/UnusedPrivateField"/>
    <rule ref="category/java/bestpractices.xml/UnusedLocalVariable"/>
    <rule ref="category/java/bestpractices.xml/UnusedPrivateMethod"/>

    <!-- Avoid common mistakes -->
    <rule ref="category/java/bestpractices.xml/AvoidReassigningParameters"/>
    <rule ref="category/java/bestpractices.xml/DefaultLabelNotLastInSwitchStmt"/>
    <rule ref="category/java/bestpractices.xml/MissingOverride"/>
    <rule ref="category/java/bestpractices.xml/PreserveStackTrace"/>
    <rule ref="category/java/bestpractices.xml/SwitchStmtsShouldHaveDefault"/>
    <rule ref="category/java/bestpractices.xml/UseCollectionIsEmpty"/>

    <!-- ====================================================================
         ERROR PRONE - Catch potential bugs early
         ==================================================================== -->

    <rule ref="category/java/errorprone.xml/AvoidDecimalLiteralsInBigDecimalConstructor"/>
    <rule ref="category/java/errorprone.xml/AvoidMultipleUnaryOperators"/>
    <rule ref="category/java/errorprone.xml/BrokenNullCheck"/>
    <rule ref="category/java/errorprone.xml/CheckSkipResult"/>
    <rule ref="category/java/errorprone.xml/ClassCastExceptionWithToArray"/>
    <rule ref="category/java/errorprone.xml/DontUseFloatTypeForLoopIndices"/>
    <rule ref="category/java/errorprone.xml/JumbledIncrementer"/>
    <rule ref="category/java/errorprone.xml/MisplacedNullCheck"/>
    <rule ref="category/java/errorprone.xml/OverrideBothEqualsAndHashcode"/>
    <rule ref="category/java/errorprone.xml/ReturnFromFinallyBlock"/>
    <rule ref="category/java/errorprone.xml/UnconditionalIfStatement"/>
    <rule ref="category/java/errorprone.xml/UnnecessaryConversionTemporary"/>
    <rule ref="category/java/errorprone.xml/UselessOperationOnImmutable"/>

    <!-- ====================================================================
         DESIGN - Encourage maintainable code
         ==================================================================== -->

    <!--
        SimplifyBooleanReturns is NOT enabled, deliberately.

        The rule's original target is `if (c) { return true; } else { return false; }`. PMD 7 also
        applies it to a guard clause followed by a return — `if (c) { return false; } return other;` —
        and demands `return !c && other;`. That is the wrong direction for this codebase. When the
        gate was first armed, 30 sites tripped it and 28 of them were guard clauses; the two that were
        real tautologies (WorkspaceAccessService#canManageRole, GitHubExceptionClassifier) were fixed
        and are not coming back.

        Of the 28, the ones that decide it are the guard TABLES: PrivateAddressGuard lists the
        non-public IPv4 ranges one per line, each with the CIDR in a trailing comment, and LogSafe does
        the same for the Unicode control ranges. The rule flags only the last line of such a table and
        wants it fused into the final `return`, which orphans one comment and breaks the alignment that
        makes the table checkable against the RFC. Doing that inside an SSRF guard buys nothing and
        costs review confidence.

        The remaining sites are the same shape at smaller scale: a null check, an "unlimited means no
        limit" check, an @AssertTrue whose constraint does not apply unless a flag is on. Enable the
        rule again only if PMD narrows it back to the if/else form.
    -->
    <rule ref="category/java/design.xml/UselessOverridingMethod"/>

    <!-- ====================================================================
         CODE STYLE - Keep code clean
         ==================================================================== -->

    <rule ref="category/java/codestyle.xml/UnnecessaryImport"/>
    <rule ref="category/java/codestyle.xml/ExtendsObject"/>
    <rule ref="category/java/codestyle.xml/UnnecessaryReturn"/>
    <!-- PMD 7: EmptyControlStatement replaces EmptyIfStmt, EmptyWhileStmt, etc. -->
    <rule ref="category/java/codestyle.xml/EmptyControlStatement"/>

</ruleset>
