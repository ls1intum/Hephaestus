query GetGroupWorkItemTypes($fullPath: ID!) {
    group(fullPath: $fullPath) {
        workItemTypes {
            nodes {
                id
                name
                iconName
            }
        }
    }
}
