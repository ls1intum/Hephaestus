{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hephaestus.aet.cit.tum.de/contracts/artifact-source/1.0.0/practice-automated-review-policy.schema.json",
  "title": "Practice automated-review policy",
  "description": "Author-defined evidence Hephaestus needs before it may review work against one practice revision.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sourceContractVersion",
    "automatedReview",
    "needs",
    "whenEvidenceIsInsufficient",
    "knownLimitations"
  ],
  "properties": {
    "sourceContractVersion": {
      "title": "Source contract version",
      "description": "Exact version that defines the available evidence sources and their states.",
      "const": "1.0.0"
    },
    "automatedReview": {
      "title": "Automated review",
      "description": "How Hephaestus may review the practice. This author declaration does not describe or validate review by a developer, peer, or human mentor.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mode",
        "evidenceSufficiency"
      ],
      "properties": {
        "mode": {
          "title": "Review mode",
          "description": "Implementation Hephaestus uses for automated review.",
          "enum": [
            "LANGUAGE_MODEL",
            "NONE"
          ]
        },
        "evidenceSufficiency": {
          "title": "Evidence sufficiency",
          "description": "Whether meeting every declared requirement is enough for Hephaestus to review the work.",
          "enum": [
            "SUFFICIENT_WHEN_REQUIREMENTS_MET",
            "DECLARED_EVIDENCE_INSUFFICIENT",
            "NONE"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "mode": {
                "const": "NONE"
              }
            },
            "required": [
              "mode"
            ]
          },
          "then": {
            "properties": {
              "evidenceSufficiency": {
                "const": "NONE"
              }
            }
          },
          "else": {
            "properties": {
              "evidenceSufficiency": {
                "enum": [
                  "SUFFICIENT_WHEN_REQUIREMENTS_MET",
                  "DECLARED_EVIDENCE_INSUFFICIENT"
                ]
              }
            }
          }
        }
      ]
    },
    "needs": {
      "title": "Evidence needs",
      "description": "Sources this practice reads, each with the stance it takes towards that source. How strictly a source must be captured is fixed by the source contract, not restated here.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/need"
      }
    },
    "whenEvidenceIsInsufficient": {
      "title": "Insufficient evidence action",
      "description": "Conservative action when a required source is unavailable, incomplete, or not current.",
      "const": "SKIP_AUTOMATED_REVIEW"
    },
    "knownLimitations": {
      "title": "Known limitations",
      "description": "Claims this evidence cannot support even when every requirement is met.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/limitation"
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "automatedReview": {
            "type": "object",
            "properties": {
              "mode": {
                "const": "NONE"
              }
            },
            "required": [
              "mode"
            ]
          }
        },
        "required": [
          "automatedReview"
        ]
      },
      "then": {
        "properties": {
          "needs": {
            "type": "array",
            "maxItems": 0
          },
          "knownLimitations": {
            "type": "array",
            "maxItems": 0
          }
        }
      },
      "else": {
        "properties": {
          "needs": {
            "type": "array",
            "minItems": 1,
            "contains": {
              "type": "object",
              "properties": {
                "stance": {
                  "const": "REQUIRED"
                }
              },
              "required": [
                "stance"
              ]
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "automatedReview": {
            "type": "object",
            "properties": {
              "evidenceSufficiency": {
                "const": "DECLARED_EVIDENCE_INSUFFICIENT"
              }
            },
            "required": [
              "evidenceSufficiency"
            ]
          }
        },
        "required": [
          "automatedReview"
        ]
      },
      "then": {
        "properties": {
          "knownLimitations": {
            "type": "array",
            "minItems": 1
          }
        }
      }
    }
  ],
  "$defs": {
    "need": {
      "title": "Evidence need",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sourceKind",
        "stance"
      ],
      "properties": {
        "sourceKind": {
          "title": "Source kind",
          "description": "Stable source identifier from the selected source contract.",
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:\\.[a-z][a-z0-9-]*)+$"
        },
        "stance": {
          "title": "Stance",
          "enum": [
            "REQUIRED",
            "EXHAUSTIVE",
            "CONTEXTUAL"
          ],
          "description": "REQUIRED refuses the review when the source is missing or below the quality its contract demands; EXHAUSTIVE additionally refuses a partial capture, because a claim that something is absent cannot rest on a fragment; CONTEXTUAL is used when present and never blocks."
        }
      },
      "description": "One source this practice reads and how it relates to it."
    },
    "limitation": {
      "title": "Known limitation",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "code",
        "description"
      ],
      "properties": {
        "code": {
          "title": "Limitation code",
          "description": "Stable machine-readable identifier.",
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]{2,63}$"
        },
        "description": {
          "title": "Limitation description",
          "description": "Plain-language explanation of the unsupported claim.",
          "type": "string",
          "minLength": 1,
          "maxLength": 500
        }
      },
      "description": "A claim the selected evidence cannot support even when every requirement passes."
    }
  }
}
