<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-1">
        <preConditions onFail="MARK_RAN" onFailMessage="practice.automated_review_policy already present">
            <not><columnExists tableName="practice" columnName="automated_review_policy"/></not>
        </preConditions>
        <comment>Pin every workspace practice to an explicit artifact-source contract.</comment>
        <addColumn tableName="practice">
            <column name="automated_review_policy" type="jsonb"/>
        </addColumn>
        <sql>
            UPDATE practice
            SET automated_review_policy = CASE applies_to
                WHEN 'ISSUE' THEN '{"sourceContractVersion":"1.0.0","evidenceProfile":"issue-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"scm.issue.core","completeness":"COMPLETE","freshness":"NO_REQUIREMENT","content":"NO_REQUIREMENT"}],"optionalContext":[{"sourceKind":"scm.issue.comments"}],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"IMPLEMENTATION_NOT_OBSERVED","description":"Issue evidence does not establish whether the described work was implemented correctly."}]}'::jsonb
                WHEN 'CONVERSATION_THREAD' THEN '{"sourceContractVersion":"1.0.0","evidenceProfile":"conversation-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"slack.conversation.thread","completeness":"COMPLETE","freshness":"NO_REQUIREMENT","content":"NO_REQUIREMENT"}],"optionalContext":[],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"PRIVATE_CONTEXT_NOT_OBSERVED","description":"The captured thread does not include decisions or context shared outside the conversation."}]}'::jsonb
                ELSE '{"sourceContractVersion":"1.0.0","evidenceProfile":"pull-request-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"scm.pull-request.core","completeness":"COMPLETE","freshness":"NO_REQUIREMENT","content":"NO_REQUIREMENT"},{"sourceKind":"scm.pull-request.diff","completeness":"COMPLETE","freshness":"CURRENT","content":"NON_EMPTY"}],"optionalContext":[{"sourceKind":"scm.pull-request.comments"},{"sourceKind":"scm.repository.tree"}],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"RUNTIME_BEHAVIOR_NOT_OBSERVED","description":"Repository evidence does not establish behavior in a deployed runtime."}]}'::jsonb
            END
        </sql>
        <addNotNullConstraint tableName="practice" columnName="automated_review_policy" columnDataType="jsonb"/>
        <rollback><dropColumn tableName="practice" columnName="automated_review_policy"/></rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-2">
        <preConditions onFail="MARK_RAN" onFailMessage="practice_revision.automated_review_policy already present">
            <not><columnExists tableName="practice_revision" columnName="automated_review_policy"/></not>
        </preConditions>
        <comment>Create a new current revision for the automated-review policy while preserving historical meaning.</comment>
        <addColumn tableName="practice_revision">
            <column name="automated_review_policy" type="jsonb"/>
        </addColumn>
        <sql splitStatements="false"><![CDATA[
            INSERT INTO practice_revision (
                practice_id, revision_number, criteria, created_at,
                slug, name, applies_to, trigger_events, precompute_script,
                why_it_matters, what_good_looks_like,
                area_slug, area_name, area_description, area_icon, area_color,
                detection_fingerprint, automated_review_policy
            )
            SELECT practice.id,
                   COALESCE((
                       SELECT MAX(existing.revision_number) + 1
                       FROM practice_revision existing
                       WHERE existing.practice_id = practice.id
                   ), 1),
                   practice.criteria,
                   now(),
                   practice.slug,
                   practice.name,
                   practice.applies_to,
                   practice.trigger_events,
                   practice.precompute_script,
                   practice.why_it_matters,
                   practice.what_good_looks_like,
                   area.slug,
                   area.name,
                   area.description,
                   area.icon,
                   area.color,
                   NULL,
                   practice.automated_review_policy
            FROM practice
            LEFT JOIN practice_area area ON area.id = practice.practice_area_id;

            UPDATE practice
            SET current_revision_id = revision.id
            FROM practice_revision revision
            WHERE revision.practice_id = practice.id
              AND revision.automated_review_policy IS NOT NULL
              AND revision.revision_number = (
                  SELECT MAX(candidate.revision_number)
                  FROM practice_revision candidate
                  WHERE candidate.practice_id = practice.id
                    AND candidate.automated_review_policy IS NOT NULL
              );
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                UPDATE practice
                SET current_revision_id = previous.id,
                    slug = previous.slug,
                    name = previous.name,
                    applies_to = previous.applies_to,
                    trigger_events = previous.trigger_events,
                    criteria = previous.criteria,
                    precompute_script = previous.precompute_script,
                    why_it_matters = previous.why_it_matters,
                    what_good_looks_like = previous.what_good_looks_like,
                    practice_area_id = (
                        SELECT area.id
                        FROM practice_area area
                        WHERE area.workspace_id = practice.workspace_id
                          AND area.slug = previous.area_slug
                    )
                FROM practice_revision boundary
                JOIN practice_revision previous
                  ON previous.practice_id = boundary.practice_id
                 AND previous.revision_number = boundary.revision_number - 1
                WHERE boundary.practice_id = practice.id
                  AND boundary.automated_review_policy IS NOT NULL
                  AND previous.automated_review_policy IS NULL
                  AND boundary.revision_number = (
                      SELECT MIN(candidate.revision_number)
                      FROM practice_revision candidate
                      JOIN practice_revision before_candidate
                        ON before_candidate.practice_id = candidate.practice_id
                       AND before_candidate.revision_number = candidate.revision_number - 1
                      WHERE candidate.practice_id = practice.id
                        AND candidate.automated_review_policy IS NOT NULL
                        AND before_candidate.automated_review_policy IS NULL
                  );

                DELETE FROM practice_revision
                WHERE automated_review_policy IS NOT NULL;
            ]]></sql>
            <dropColumn tableName="practice_revision" columnName="automated_review_policy"/>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-3">
        <preConditions onFail="MARK_RAN" onFailMessage="curated_practice_override.automated_review_policy already present">
            <not><columnExists tableName="curated_practice_override" columnName="automated_review_policy"/></not>
        </preConditions>
        <comment>Keep evidence requirements with complete instance-curated practice overrides.</comment>
        <addColumn tableName="curated_practice_override">
            <column name="automated_review_policy" type="jsonb"/>
        </addColumn>
        <sql>
            UPDATE curated_practice_override
            SET automated_review_policy = CASE applies_to
                WHEN 'ISSUE' THEN '{"sourceContractVersion":"1.0.0","evidenceProfile":"issue-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"scm.issue.core","completeness":"COMPLETE","freshness":"CURRENT"}],"optionalContext":[{"sourceKind":"scm.issue.comments"}],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"IMPLEMENTATION_NOT_OBSERVED","description":"Issue evidence does not establish whether the described work was implemented correctly."}]}'::jsonb
                WHEN 'CONVERSATION_THREAD' THEN '{"sourceContractVersion":"1.0.0","evidenceProfile":"conversation-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"slack.conversation.thread","completeness":"COMPLETE","freshness":"CURRENT"}],"optionalContext":[],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"PRIVATE_CONTEXT_NOT_OBSERVED","description":"The captured thread does not include decisions or context shared outside the conversation."}]}'::jsonb
                ELSE '{"sourceContractVersion":"1.0.0","evidenceProfile":"pull-request-review","automatedReview":{"mode":"LANGUAGE_MODEL","evidenceSufficiency":"SUFFICIENT_WHEN_REQUIREMENTS_MET"},"requiredEvidence":[{"sourceKind":"scm.pull-request.core","completeness":"COMPLETE","freshness":"CURRENT"},{"sourceKind":"scm.pull-request.diff","completeness":"COMPLETE","freshness":"CURRENT"}],"optionalContext":[{"sourceKind":"scm.pull-request.comments"}],"whenEvidenceIsInsufficient":"SKIP_AUTOMATED_REVIEW","knownLimitations":[{"code":"RUNTIME_BEHAVIOR_NOT_OBSERVED","description":"Repository evidence does not establish behavior in a deployed runtime."}]}'::jsonb
            END
            WHERE applies_to IS NOT NULL
        </sql>
        <rollback><dropColumn tableName="curated_practice_override" columnName="automated_review_policy"/></rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-4">
        <preConditions onFail="HALT">
            <columnExists tableName="practice" columnName="automated_review_policy"/>
            <columnExists tableName="practice_revision" columnName="automated_review_policy"/>
        </preConditions>
        <comment>Enforce evidence requirements in immutable revisions and the current practice projection.</comment>
        <sql splitStatements="false"><![CDATA[
            CREATE OR REPLACE FUNCTION enforce_practice_revision_immutability()
            RETURNS trigger
            LANGUAGE plpgsql
            AS $immutable_revision$
            BEGIN
                IF NEW.practice_id = OLD.practice_id
                   AND NEW.revision_number = OLD.revision_number
                   AND NEW.created_at = OLD.created_at
                   AND NEW.slug IS NOT DISTINCT FROM OLD.slug
                   AND NEW.name IS NOT DISTINCT FROM OLD.name
                   AND NEW.applies_to IS NOT DISTINCT FROM OLD.applies_to
                   AND NEW.trigger_events IS NOT DISTINCT FROM OLD.trigger_events
                   AND NEW.criteria = OLD.criteria
                   AND NEW.precompute_script IS NOT DISTINCT FROM OLD.precompute_script
                   AND NEW.automated_review_policy IS NOT DISTINCT FROM OLD.automated_review_policy
                   AND NEW.why_it_matters IS NOT DISTINCT FROM OLD.why_it_matters
                   AND NEW.what_good_looks_like IS NOT DISTINCT FROM OLD.what_good_looks_like
                   AND NEW.area_slug IS NOT DISTINCT FROM OLD.area_slug
                   AND NEW.area_name IS NOT DISTINCT FROM OLD.area_name
                   AND NEW.area_description IS NOT DISTINCT FROM OLD.area_description
                   AND NEW.area_icon IS NOT DISTINCT FROM OLD.area_icon
                   AND NEW.area_color IS NOT DISTINCT FROM OLD.area_color
                   AND (
                       NEW.detection_fingerprint IS NOT DISTINCT FROM OLD.detection_fingerprint
                       OR OLD.detection_fingerprint IS NULL
                   )
                THEN
                    RETURN NEW;
                END IF;
                RAISE EXCEPTION 'practice revisions are immutable' USING ERRCODE = '55000';
            END;
            $immutable_revision$;

            CREATE OR REPLACE FUNCTION enforce_practice_current_revision_projection()
            RETURNS trigger
            LANGUAGE plpgsql
            AS $current_projection$
            BEGIN
                IF NOT EXISTS (
                    SELECT 1
                    FROM practice practice
                    JOIN practice_revision revision
                      ON revision.id = practice.current_revision_id
                     AND revision.practice_id = practice.id
                    LEFT JOIN practice_area area ON area.id = practice.practice_area_id
                    WHERE practice.id = NEW.id
                      AND revision.slug = practice.slug
                      AND revision.name = practice.name
                      AND revision.applies_to = practice.applies_to
                      AND revision.trigger_events = practice.trigger_events
                      AND revision.criteria = practice.criteria
                      AND revision.precompute_script IS NOT DISTINCT FROM practice.precompute_script
                      AND revision.automated_review_policy = practice.automated_review_policy
                      AND revision.why_it_matters IS NOT DISTINCT FROM practice.why_it_matters
                      AND revision.what_good_looks_like IS NOT DISTINCT FROM practice.what_good_looks_like
                      AND revision.area_slug IS NOT DISTINCT FROM area.slug
                ) THEN
                    RAISE EXCEPTION 'practice current revision does not match its current projection'
                        USING ERRCODE = '23514';
                END IF;
                RETURN NULL;
            END;
            $current_projection$;

            DROP TRIGGER practice_requires_current_revision_projection ON practice;
            CREATE CONSTRAINT TRIGGER practice_requires_current_revision_projection
                AFTER INSERT OR UPDATE OF current_revision_id, slug, name, applies_to, trigger_events,
                    criteria, precompute_script, automated_review_policy, why_it_matters, what_good_looks_like,
                    practice_area_id
                ON practice
                DEFERRABLE INITIALLY DEFERRED
                FOR EACH ROW EXECUTE FUNCTION enforce_practice_current_revision_projection();
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                DROP TRIGGER practice_requires_current_revision_projection ON practice;

                CREATE OR REPLACE FUNCTION enforce_practice_revision_immutability()
                RETURNS trigger
                LANGUAGE plpgsql
                AS $immutable_revision$
                BEGIN
                    IF NEW.practice_id = OLD.practice_id
                       AND NEW.revision_number = OLD.revision_number
                       AND NEW.created_at = OLD.created_at
                       AND NEW.slug IS NOT DISTINCT FROM OLD.slug
                       AND NEW.name IS NOT DISTINCT FROM OLD.name
                       AND NEW.applies_to IS NOT DISTINCT FROM OLD.applies_to
                       AND NEW.trigger_events IS NOT DISTINCT FROM OLD.trigger_events
                       AND NEW.criteria = OLD.criteria
                       AND NEW.precompute_script IS NOT DISTINCT FROM OLD.precompute_script
                       AND NEW.why_it_matters IS NOT DISTINCT FROM OLD.why_it_matters
                       AND NEW.what_good_looks_like IS NOT DISTINCT FROM OLD.what_good_looks_like
                       AND NEW.area_slug IS NOT DISTINCT FROM OLD.area_slug
                       AND NEW.area_name IS NOT DISTINCT FROM OLD.area_name
                       AND NEW.area_description IS NOT DISTINCT FROM OLD.area_description
                       AND NEW.area_icon IS NOT DISTINCT FROM OLD.area_icon
                       AND NEW.area_color IS NOT DISTINCT FROM OLD.area_color
                       AND (
                           NEW.detection_fingerprint IS NOT DISTINCT FROM OLD.detection_fingerprint
                           OR OLD.detection_fingerprint IS NULL
                       )
                    THEN
                        RETURN NEW;
                    END IF;
                    RAISE EXCEPTION 'practice revisions are immutable' USING ERRCODE = '55000';
                END;
                $immutable_revision$;

                CREATE OR REPLACE FUNCTION enforce_practice_current_revision_projection()
                RETURNS trigger
                LANGUAGE plpgsql
                AS $current_projection$
                BEGIN
                    IF NOT EXISTS (
                        SELECT 1
                        FROM practice practice
                        JOIN practice_revision revision
                          ON revision.id = practice.current_revision_id
                         AND revision.practice_id = practice.id
                        LEFT JOIN practice_area area ON area.id = practice.practice_area_id
                        WHERE practice.id = NEW.id
                          AND revision.slug = practice.slug
                          AND revision.name = practice.name
                          AND revision.applies_to = practice.applies_to
                          AND revision.trigger_events = practice.trigger_events
                          AND revision.criteria = practice.criteria
                          AND revision.precompute_script IS NOT DISTINCT FROM practice.precompute_script
                          AND revision.why_it_matters IS NOT DISTINCT FROM practice.why_it_matters
                          AND revision.what_good_looks_like IS NOT DISTINCT FROM practice.what_good_looks_like
                          AND revision.area_slug IS NOT DISTINCT FROM area.slug
                    ) THEN
                        RAISE EXCEPTION 'practice current revision does not match its current projection'
                            USING ERRCODE = '23514';
                    END IF;
                    RETURN NULL;
                END;
                $current_projection$;

                CREATE CONSTRAINT TRIGGER practice_requires_current_revision_projection
                    AFTER INSERT OR UPDATE OF current_revision_id, slug, name, applies_to, trigger_events,
                        criteria, precompute_script, why_it_matters, what_good_looks_like, practice_area_id
                    ON practice
                    DEFERRABLE INITIALLY DEFERRED
                    FOR EACH ROW EXECUTE FUNCTION enforce_practice_current_revision_projection();
            ]]></sql>
        </rollback>
    </changeSet>
    <changeSet author="hephaestus-evidence-contract" id="1785743133884-5">
        <preConditions onFail="HALT">
            <columnExists tableName="practice_revision" columnName="detection_fingerprint"/>
        </preConditions>
        <comment>Version review-rule fingerprints before evidence becomes part of their meaning.</comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE practice_revision DISABLE TRIGGER practice_revision_immutable_update;
            ALTER TABLE practice_revision DROP CONSTRAINT ck_practice_revision_fingerprint;
            ALTER TABLE practice_revision ALTER COLUMN detection_fingerprint TYPE VARCHAR(96);
            UPDATE practice_revision
            SET detection_fingerprint = 'v1:' || detection_fingerprint
            WHERE detection_fingerprint ~ '^[0-9a-f]{64}$';
            ALTER TABLE practice_revision
                ADD CONSTRAINT ck_practice_revision_fingerprint
                CHECK (detection_fingerprint IS NULL OR detection_fingerprint ~ '^v[1-9][0-9]*:[0-9a-f]{64}$');
            ALTER TABLE practice_revision ENABLE TRIGGER practice_revision_immutable_update;

            ALTER TABLE practice DROP CONSTRAINT ck_practice_curated_fingerprint;
            ALTER TABLE practice ALTER COLUMN source_curated_fingerprint TYPE VARCHAR(96);
            UPDATE practice
            SET source_curated_fingerprint = 'v1:' || source_curated_fingerprint
            WHERE source_curated_fingerprint ~ '^[0-9a-f]{64}$';
            ALTER TABLE practice
                ADD CONSTRAINT ck_practice_curated_fingerprint
                CHECK (source_curated_fingerprint IS NULL OR source_curated_fingerprint ~ '^v[1-9][0-9]*:[0-9a-f]{64}$');
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                ALTER TABLE practice DROP CONSTRAINT ck_practice_curated_fingerprint;
                UPDATE practice
                SET source_curated_fingerprint = substring(source_curated_fingerprint FROM position(':' IN source_curated_fingerprint) + 1)
                WHERE source_curated_fingerprint ~ '^v[1-9][0-9]*:[0-9a-f]{64}$';
                ALTER TABLE practice ALTER COLUMN source_curated_fingerprint TYPE VARCHAR(64);
                ALTER TABLE practice
                    ADD CONSTRAINT ck_practice_curated_fingerprint
                    CHECK (source_curated_fingerprint IS NULL OR source_curated_fingerprint ~ '^[0-9a-f]{64}$');

                ALTER TABLE practice_revision DISABLE TRIGGER practice_revision_immutable_update;
                ALTER TABLE practice_revision DROP CONSTRAINT ck_practice_revision_fingerprint;
                UPDATE practice_revision
                SET detection_fingerprint = substring(detection_fingerprint FROM position(':' IN detection_fingerprint) + 1)
                WHERE detection_fingerprint ~ '^v[1-9][0-9]*:[0-9a-f]{64}$';
                ALTER TABLE practice_revision ALTER COLUMN detection_fingerprint TYPE VARCHAR(64);
                ALTER TABLE practice_revision
                    ADD CONSTRAINT ck_practice_revision_fingerprint
                    CHECK (detection_fingerprint IS NULL OR detection_fingerprint ~ '^[0-9a-f]{64}$');
                ALTER TABLE practice_revision ENABLE TRIGGER practice_revision_immutable_update;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-6">
        <preConditions onFail="HALT">
            <not><columnExists tableName="agent_job" columnName="evidence_snapshot"/></not>
        </preConditions>
        <comment>Pin the admitted practice revisions and source availability consumed by each job.</comment>
        <addColumn tableName="agent_job">
            <column name="evidence_snapshot" type="JSONB"/>
        </addColumn>
        <rollback>
            <dropColumn tableName="agent_job" columnName="evidence_snapshot"/>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-7">
        <preConditions onFail="HALT">
            <columnExists tableName="practice" columnName="is_active"/>
            <not><columnExists tableName="practice" columnName="used_in_new_reviews"/></not>
        </preConditions>
        <comment>Name workspace practice participation by its actual effect.</comment>
        <renameColumn tableName="practice" oldColumnName="is_active" newColumnName="used_in_new_reviews" columnDataType="boolean"/>
        <sql>ALTER INDEX idx_practice_workspace_active RENAME TO idx_practice_workspace_usage</sql>
        <rollback>
            <sql>ALTER INDEX idx_practice_workspace_usage RENAME TO idx_practice_workspace_active</sql>
            <renameColumn tableName="practice" oldColumnName="used_in_new_reviews" newColumnName="is_active" columnDataType="boolean"/>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-8">
        <preConditions onFail="HALT">
            <columnExists tableName="practice_revision" columnName="detection_fingerprint"/>
            <not><columnExists tableName="practice_revision" columnName="review_rule_fingerprint"/></not>
        </preConditions>
        <comment>Name the fingerprint by the review rules whose currentness it tracks.</comment>
        <renameColumn tableName="practice_revision" oldColumnName="detection_fingerprint" newColumnName="review_rule_fingerprint" columnDataType="varchar(96)"/>
        <sql splitStatements="false"><![CDATA[
            CREATE OR REPLACE FUNCTION enforce_practice_revision_immutability()
            RETURNS trigger
            LANGUAGE plpgsql
            AS $immutable_revision$
            BEGIN
                IF NEW.practice_id = OLD.practice_id
                   AND NEW.revision_number = OLD.revision_number
                   AND NEW.created_at = OLD.created_at
                   AND NEW.slug IS NOT DISTINCT FROM OLD.slug
                   AND NEW.name IS NOT DISTINCT FROM OLD.name
                   AND NEW.applies_to IS NOT DISTINCT FROM OLD.applies_to
                   AND NEW.trigger_events IS NOT DISTINCT FROM OLD.trigger_events
                   AND NEW.criteria = OLD.criteria
                   AND NEW.precompute_script IS NOT DISTINCT FROM OLD.precompute_script
                   AND NEW.automated_review_policy IS NOT DISTINCT FROM OLD.automated_review_policy
                   AND NEW.why_it_matters IS NOT DISTINCT FROM OLD.why_it_matters
                   AND NEW.what_good_looks_like IS NOT DISTINCT FROM OLD.what_good_looks_like
                   AND NEW.area_slug IS NOT DISTINCT FROM OLD.area_slug
                   AND NEW.area_name IS NOT DISTINCT FROM OLD.area_name
                   AND NEW.area_description IS NOT DISTINCT FROM OLD.area_description
                   AND NEW.area_icon IS NOT DISTINCT FROM OLD.area_icon
                   AND NEW.area_color IS NOT DISTINCT FROM OLD.area_color
                   AND (
                       NEW.review_rule_fingerprint IS NOT DISTINCT FROM OLD.review_rule_fingerprint
                       OR OLD.review_rule_fingerprint IS NULL
                   )
                THEN
                    RETURN NEW;
                END IF;
                RAISE EXCEPTION 'practice revisions are immutable' USING ERRCODE = '55000';
            END;
            $immutable_revision$;
        ]]></sql>
        <rollback>
            <renameColumn tableName="practice_revision" oldColumnName="review_rule_fingerprint" newColumnName="detection_fingerprint" columnDataType="varchar(96)"/>
            <sql splitStatements="false"><![CDATA[
                CREATE OR REPLACE FUNCTION enforce_practice_revision_immutability()
                RETURNS trigger
                LANGUAGE plpgsql
                AS $immutable_revision$
                BEGIN
                    IF NEW.practice_id = OLD.practice_id
                       AND NEW.revision_number = OLD.revision_number
                       AND NEW.created_at = OLD.created_at
                       AND NEW.slug IS NOT DISTINCT FROM OLD.slug
                       AND NEW.name IS NOT DISTINCT FROM OLD.name
                       AND NEW.applies_to IS NOT DISTINCT FROM OLD.applies_to
                       AND NEW.trigger_events IS NOT DISTINCT FROM OLD.trigger_events
                       AND NEW.criteria = OLD.criteria
                       AND NEW.precompute_script IS NOT DISTINCT FROM OLD.precompute_script
                       AND NEW.automated_review_policy IS NOT DISTINCT FROM OLD.automated_review_policy
                       AND NEW.why_it_matters IS NOT DISTINCT FROM OLD.why_it_matters
                       AND NEW.what_good_looks_like IS NOT DISTINCT FROM OLD.what_good_looks_like
                       AND NEW.area_slug IS NOT DISTINCT FROM OLD.area_slug
                       AND NEW.area_name IS NOT DISTINCT FROM OLD.area_name
                       AND NEW.area_description IS NOT DISTINCT FROM OLD.area_description
                       AND NEW.area_icon IS NOT DISTINCT FROM OLD.area_icon
                       AND NEW.area_color IS NOT DISTINCT FROM OLD.area_color
                       AND (
                           NEW.detection_fingerprint IS NOT DISTINCT FROM OLD.detection_fingerprint
                           OR OLD.detection_fingerprint IS NULL
                       )
                    THEN
                        RETURN NEW;
                    END IF;
                    RAISE EXCEPTION 'practice revisions are immutable' USING ERRCODE = '55000';
                END;
                $immutable_revision$;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-9">
        <preConditions onFail="HALT">
            <tableExists tableName="config_audit_event"/>
            <columnExists tableName="config_audit_event" columnName="entity_type"/>
        </preConditions>
        <comment>Name practice usage audit events by the setting they change.</comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE config_audit_event DROP CONSTRAINT ck_config_audit_event_entity_type;
            ALTER TABLE config_audit_event ADD CONSTRAINT ck_config_audit_event_entity_type
                CHECK (entity_type IN (
                    'PRACTICE_REVIEW_SETTINGS', 'AGENT_BINDING', 'AGENT_CONFIG', 'AI_CONFIG_BINDING',
                    'WORKSPACE_ROLE', 'WORKSPACE_FEATURES', 'WORKSPACE_STATUS',
                    'WORKSPACE_TOKEN', 'WORKSPACE_VISIBILITY', 'PRACTICE_ACTIVE', 'PRACTICE_USAGE',
                    'PRACTICE_DEFINITION', 'PRACTICE_AREA', 'CURATED_PRACTICE', 'CURATED_PRACTICE_AREA',
                    'WORKSPACE_INSTANCE_LLM_BUDGET', 'WORKSPACE_OWN_PROVIDER_LLM_BUDGET',
                    'WORKSPACE_LLM_BUDGET', 'WORKSPACE_BYO_LLM_BUDGET',
                    'WORKSPACE_LLM_CONNECTION', 'WORKSPACE_LLM_MODEL'
                ));
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                DO $rollback$
                BEGIN
                    IF EXISTS (SELECT 1 FROM config_audit_event WHERE entity_type = 'PRACTICE_USAGE') THEN
                        RAISE WARNING 'The audit entity-type constraint remains widened because append-only history contains PRACTICE_USAGE rows';
                        RETURN;
                    END IF;

                    ALTER TABLE config_audit_event DROP CONSTRAINT ck_config_audit_event_entity_type;
                    ALTER TABLE config_audit_event ADD CONSTRAINT ck_config_audit_event_entity_type
                        CHECK (entity_type IN (
                            'PRACTICE_REVIEW_SETTINGS', 'AGENT_BINDING', 'AGENT_CONFIG', 'AI_CONFIG_BINDING',
                            'WORKSPACE_ROLE', 'WORKSPACE_FEATURES', 'WORKSPACE_STATUS',
                            'WORKSPACE_TOKEN', 'WORKSPACE_VISIBILITY', 'PRACTICE_ACTIVE',
                            'PRACTICE_DEFINITION', 'PRACTICE_AREA', 'CURATED_PRACTICE', 'CURATED_PRACTICE_AREA',
                            'WORKSPACE_INSTANCE_LLM_BUDGET', 'WORKSPACE_OWN_PROVIDER_LLM_BUDGET',
                            'WORKSPACE_LLM_BUDGET', 'WORKSPACE_BYO_LLM_BUDGET',
                            'WORKSPACE_LLM_CONNECTION', 'WORKSPACE_LLM_MODEL'
                        ));
                END
                $rollback$;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-10">
        <preConditions onFail="HALT">
            <columnExists tableName="practice_area" columnName="is_active"/>
            <not><columnExists tableName="practice_area" columnName="visible_in_practice_dashboards"/></not>
        </preConditions>
        <comment>Name practice-area visibility by where it takes effect.</comment>
        <renameColumn tableName="practice_area" oldColumnName="is_active" newColumnName="visible_in_practice_dashboards" columnDataType="boolean"/>
        <sql>ALTER INDEX idx_practice_area_workspace_active RENAME TO idx_practice_area_workspace_dashboard_visibility</sql>
        <rollback>
            <sql>ALTER INDEX idx_practice_area_workspace_dashboard_visibility RENAME TO idx_practice_area_workspace_active</sql>
            <renameColumn tableName="practice_area" oldColumnName="visible_in_practice_dashboards" newColumnName="is_active" columnDataType="boolean"/>
        </rollback>
    </changeSet>

    <changeSet author="hephaestus-evidence-contract" id="1785743133884-11">
        <preConditions onFail="HALT">
            <tableExists tableName="workspace_agent_binding"/>
            <columnExists tableName="agent_job" columnName="purpose"/>
        </preConditions>
        <comment>Name the AI purpose after the product feature it runs.</comment>
        <sql splitStatements="false"><![CDATA[
            UPDATE workspace_agent_binding SET purpose = 'PRACTICE_REVIEW' WHERE purpose = 'PRACTICE_DETECTION';
            UPDATE agent_job SET purpose = 'PRACTICE_REVIEW' WHERE purpose = 'PRACTICE_DETECTION';
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                DO $rollback$
                BEGIN
                    IF to_regclass('workspace_agent_binding') IS NOT NULL THEN
                        UPDATE workspace_agent_binding
                        SET purpose = 'PRACTICE_DETECTION'
                        WHERE purpose = 'PRACTICE_REVIEW';
                    END IF;

                    IF EXISTS (
                        SELECT 1
                        FROM information_schema.columns
                        WHERE table_schema = current_schema()
                          AND table_name = 'agent_job'
                          AND column_name = 'purpose'
                    ) THEN
                        UPDATE agent_job
                        SET purpose = 'PRACTICE_DETECTION'
                        WHERE purpose = 'PRACTICE_REVIEW';
                    END IF;

                END
                $rollback$;
            ]]></sql>
        </rollback>
    </changeSet>




    <changeSet author="hephaestus-evidence-contract" id="1785743133884-12">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.review_readiness already present">
            <not><columnExists tableName="agent_job" columnName="review_readiness"/></not>
        </preConditions>
        <comment>Keep the readiness decisions out of the evidence snapshot so they can be read in bulk.</comment>
        <addColumn tableName="agent_job">
            <column name="review_readiness" type="jsonb"/>
        </addColumn>
        <sql>
            <![CDATA[
            -- A snapshot carries one entry per staged file, so a repository-tree capture makes it
            -- megabytes. Postgres cannot read one key out of a TOASTed jsonb without detoasting the
            -- whole value, so reporting on 200 reviews meant decompressing 200 snapshots for a few
            -- kilobytes of answer. The decisions move to their own column; the snapshot stays put.
            UPDATE agent_job
            SET review_readiness = evidence_snapshot -> 'automatedReviewReadiness'
            WHERE jsonb_typeof(evidence_snapshot) = 'object'
              AND jsonb_exists(evidence_snapshot, 'automatedReviewReadiness');

            -- The snapshot stored the source manifest twice: once as the bytes staged in the sandbox
            -- and once as the object they were rendered from. Nothing ever read the second copy.
            UPDATE agent_job
            SET evidence_snapshot = evidence_snapshot - 'artifactSourceManifest'
            WHERE jsonb_typeof(evidence_snapshot) = 'object'
              AND jsonb_exists(evidence_snapshot, 'artifactSourceManifest');
            ]]>
        </sql>
        <rollback>
            <dropColumn tableName="agent_job" columnName="review_readiness"/>
        </rollback>
    </changeSet>

    <!-- An observation is an immutable measurement and the substrate for longitudinal research.
         Cascading from practice meant pruning the catalog silently erased the recorded history of
         everyone ever measured against that practice. Practices retire; measurements persist. -->
    <changeSet author="hephaestus" id="1785743133884-13">
        <preConditions onFail="MARK_RAN" onFailMessage="fk_observation_practice already non-cascading">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_constraint
                WHERE conname = 'fk_observation_practice' AND confdeltype = 'c'
            </sqlCheck>
        </preConditions>
        <comment>Stop cascade-deleting observations when a practice is removed.</comment>
        <dropForeignKeyConstraint baseTableName="observation" constraintName="fk_observation_practice"/>
        <addForeignKeyConstraint baseTableName="observation" baseColumnNames="practice_id"
                                 constraintName="fk_observation_practice"
                                 referencedTableName="practice" referencedColumnNames="id"/>
        <rollback>
            <dropForeignKeyConstraint baseTableName="observation" constraintName="fk_observation_practice"/>
            <addForeignKeyConstraint baseTableName="observation" baseColumnNames="practice_id"
                                     constraintName="fk_observation_practice"
                                     referencedTableName="practice" referencedColumnNames="id" onDelete="CASCADE"/>
        </rollback>
    </changeSet>

    <!-- Dedup used to last only as long as a job was QUEUED or RUNNING, so a redelivery after the job
         finished re-ran the whole review; the only remaining guard was a cooldown a workspace can set
         to zero. uq_artifact_signal makes "we already dealt with this occurrence" permanent, and the
         state machine keeps a refusal from consuming the signal forever: submission can be declined
         after the fact (workspace inactive, binding disabled, budget spent), and PENDING is what lets
         that review still happen once the operator lifts the block. -->
    <changeSet author="hephaestus-signal-ledger" id="1785743133884-14">
        <preConditions onFail="MARK_RAN" onFailMessage="artifact_signal already present">
            <not><tableExists tableName="artifact_signal"/></not>
        </preConditions>
        <comment>Record every observed artifact signal and what became of it.</comment>
        <createTable tableName="artifact_signal">
            <column name="id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="pk_artifact_signal" nullable="false"/>
            </column>
            <column name="workspace_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="artifact_kind" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="artifact_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="signal_name" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <!-- Per-SIGNAL identity, never per artifact kind: editing a merge request description moves
                 no commit SHA, so a description-shaped signal keyed on the head SHA could never be
                 re-measured after the author fixed it. -->
            <column name="revision" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="occurred_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="discovered_via" type="VARCHAR(16)">
                <constraints nullable="false"/>
            </column>
            <column name="state" type="VARCHAR(16)">
                <constraints nullable="false"/>
            </column>
            <column name="state_reason" type="VARCHAR(48)"/>
            <column name="job_id" type="UUID"/>
            <column name="state_changed_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addForeignKeyConstraint
            baseTableName="artifact_signal"
            baseColumnNames="workspace_id"
            referencedTableName="workspace"
            referencedColumnNames="id"
            constraintName="fk_artifact_signal_workspace"
            onDelete="CASCADE"/>
        <createIndex tableName="artifact_signal" indexName="idx_artifact_signal_state_changed">
            <column name="state"/>
            <column name="state_changed_at"/>
        </createIndex>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE artifact_signal ADD CONSTRAINT uq_artifact_signal
                UNIQUE (workspace_id, artifact_kind, artifact_id, signal_name, revision);
            ALTER TABLE artifact_signal ADD CONSTRAINT ck_artifact_signal_discovered_via
                CHECK (discovered_via IN ('EVENT','SYNC','MANUAL'));
            ALTER TABLE artifact_signal ADD CONSTRAINT ck_artifact_signal_state
                CHECK (state IN ('RECORDED','TRIGGERED','SUPPRESSED','PENDING','LAPSED'));
        ]]></sql>
        <rollback>
            <dropTable tableName="artifact_signal"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
