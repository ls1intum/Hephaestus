<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Per-workspace LLM configuration, usage accounting and cost control (#1368).

        Every object this branch introduces is created ONCE, in its final shape. Where the branch's
        development history created something and then widened, narrowed or dropped it, only the end
        state is here: an operator replays this file forever, and an intermediate state that existed
        for one afternoon is nothing but risk and noise. Concretely folded away:
          • llm_usage_event — created with its final columns, precision and constraints instead of
            created plus four ALTER passes (pricing_state, provenance, cost widening, attempt keying).
          • instance_llm_settings.default_unpriced_policy — never created; enforcement is structural.
          • agent_config's catalog-binding columns — never created; the legacy table is migrated into
            the workspace catalog (changeSet -33) and dropped (-39) inside this same file, so columns
            that could only ever hold NULL are pure churn.
        Folding stops at the release boundary: 1784237313759 (evaluation provenance, #1363) and
        1784242879917 (the config-audit trail, #1359) shipped while this branch was in review, so
        their change sets are on deployed databases and stay in their own, immutable files. What is
        left here is only the delta on top of them — for the audit trail, one widening of
        ck_config_audit_event_entity_type (changeSet -5) to admit the names #1368 introduces.

        The one place the branch's history is preserved deliberately is ADD CONSTRAINT ... NOT VALID
        followed by a separate VALIDATE CONSTRAINT (changeSets -22/-29). That is not churn: it is what
        keeps the ACCESS EXCLUSIVE lock momentary on tables that already hold production rows, with
        the row scan running afterwards under SHARE UPDATE EXCLUSIVE.

        VARCHAR + CHECK, never a native PG enum: released changelogs are immutable and PostgreSQL
        cannot drop an enum label, so a closed set that may gain values must be a CHECK.
    -->

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         Admin configuration audit trail (#1359): the delta this branch adds to the released
         1784242879917_changelog.xml, which creates config_audit_event, its append-only triggers and
         its read-path indexes.
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-5" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="config_audit_event entity_type CHECK already admits the #1368 names">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_constraint
                WHERE conname = 'ck_config_audit_event_entity_type'
                  AND pg_get_constraintdef(oid) LIKE '%WORKSPACE_LLM_MODEL%'
            </sqlCheck>
        </preConditions>
        <comment>
            Admit the entity types #1368 introduces: the per-purpose binding that replaces the named
            agent config, the two budget caps, and a workspace's own LLM connections and models. DROP +
            ADD, the same shape 1784242879917-3/-4 use — the ADD validates every existing row in one
            pass (no rewrite) under a momentary ACCESS EXCLUSIVE lock, trivial at this table's size.

            The value set is a superset of what anything writes, by design: AGENT_CONFIG,
            AI_CONFIG_BINDING, WORKSPACE_LLM_BUDGET and WORKSPACE_BYO_LLM_BUDGET are names earlier rows
            were written under and stay admitted forever, because
            trg_config_audit_event_block_mutation refuses any UPDATE except erasure — rewriting history
            to tidy the vocabulary is precisely the mutation this table exists to prevent. The set must
            match ConfigAuditEntityType exactly; ConfigAuditImmutabilityIntegrationTest reads
            pg_constraint on the migrated schema and fails if the two drift.
        </comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE config_audit_event DROP CONSTRAINT ck_config_audit_event_entity_type;
            ALTER TABLE config_audit_event ADD CONSTRAINT ck_config_audit_event_entity_type
                CHECK (entity_type IN (
                    'PRACTICE_REVIEW_SETTINGS', 'AGENT_BINDING', 'AGENT_CONFIG', 'AI_CONFIG_BINDING',
                    'WORKSPACE_ROLE', 'WORKSPACE_FEATURES', 'WORKSPACE_STATUS',
                    'WORKSPACE_TOKEN', 'WORKSPACE_VISIBILITY', 'PRACTICE_ACTIVE',
                    'WORKSPACE_INSTANCE_LLM_BUDGET', 'WORKSPACE_OWN_PROVIDER_LLM_BUDGET',
                    'WORKSPACE_LLM_BUDGET', 'WORKSPACE_BYO_LLM_BUDGET',
                    'WORKSPACE_LLM_CONNECTION', 'WORKSPACE_LLM_MODEL'
                ));
        ]]></sql>
        <rollback>
            <!-- Narrows the CHECK back to the released set, but ONLY while no row uses one of the
                 names #1368 added. Unconditionally re-adding it would abort the rollback the moment a
                 single admin action had been recorded, and there is no data-preserving alternative:
                 this table is append-only at the storage layer, so remapping those rows to an older
                 spelling is the exact mutation trg_config_audit_event_block_mutation refuses. When
                 rows are present the wider CHECK stays — it only ever admits more — and the operator
                 is told, rather than the rollback failing or the history being quietly rewritten. -->
            <sql splitStatements="false"><![CDATA[
                DO $cfg_entity_rollback$
                DECLARE
                    added CONSTANT text[] := ARRAY[
                        'AGENT_BINDING', 'WORKSPACE_INSTANCE_LLM_BUDGET',
                        'WORKSPACE_OWN_PROVIDER_LLM_BUDGET', 'WORKSPACE_LLM_BUDGET',
                        'WORKSPACE_BYO_LLM_BUDGET', 'WORKSPACE_LLM_CONNECTION', 'WORKSPACE_LLM_MODEL'];
                    in_use text;
                BEGIN
                    SELECT string_agg(DISTINCT entity_type, ', ' ORDER BY entity_type) INTO in_use
                    FROM config_audit_event WHERE entity_type = ANY (added);

                    IF in_use IS NOT NULL THEN
                        RAISE WARNING 'Hephaestus #1368: ck_config_audit_event_entity_type is left widened — config_audit_event already holds rows of type %, and this trail is append-only, so they cannot be remapped to an older spelling. The wider CHECK only admits more and is harmless; nothing else in this rollback depends on it.', in_use;
                        RETURN;
                    END IF;

                    ALTER TABLE config_audit_event DROP CONSTRAINT ck_config_audit_event_entity_type;
                    ALTER TABLE config_audit_event ADD CONSTRAINT ck_config_audit_event_entity_type
                        CHECK (entity_type IN (
                            'PRACTICE_REVIEW_SETTINGS', 'AI_CONFIG_BINDING', 'AGENT_CONFIG',
                            'WORKSPACE_ROLE', 'WORKSPACE_FEATURES', 'WORKSPACE_STATUS',
                            'WORKSPACE_TOKEN', 'WORKSPACE_VISIBILITY', 'PRACTICE_ACTIVE'
                        ));
                END
                $cfg_entity_rollback$;
            ]]></sql>
        </rollback>
    </changeSet>

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         Unified LLM usage ledger + budget caps (#1368).
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-8" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_usage_event already present">
            <not><tableExists tableName="llm_usage_event"/></not>
        </preConditions>
        <comment>
            Create the append-only LLM accounting ledger: one row per provider attempt of an
            LLM-consuming unit of work. (source_type, source_id, source_attempt) is the idempotency
            key — a retried attempt bills once, and a second attempt of the same job is a distinct,
            separately billable row. pricing_state is tri-state so an unpriced call surfaces as
            unverifiable instead of silently counting as $0. cost_usd is NUMERIC(18,6): a
            NUMERIC(12,6) ceiling of $999,999.999999 would silently clamp — and so undercharge — a
            genuinely larger monthly figure against a cap set above it. applied_* freezes the rates
            actually charged, so a past dollar stays falsifiable no matter how the catalog is repriced
            later; they are soft refs with no FK because the ledger must survive catalog-row deletion.
        </comment>
        <createTable tableName="llm_usage_event">
            <column name="id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="pk_llm_usage_event" nullable="false"/>
            </column>
            <column name="workspace_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="job_type" type="VARCHAR(40)">
                <constraints nullable="false"/>
            </column>
            <column name="source_type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="source_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="source_attempt" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="model" type="VARCHAR(128)"/>
            <column name="input_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="output_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="cache_read_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="cache_write_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="reasoning_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="total_calls" type="INTEGER" defaultValueNumeric="1">
                <constraints nullable="false"/>
            </column>
            <column name="cost_usd" type="NUMERIC(18,6)"/>
            <column name="pricing_state" type="VARCHAR(16)" defaultValue="UNPRICED">
                <constraints nullable="false"/>
            </column>
            <column name="funding_source" type="VARCHAR(16)" defaultValue="INSTANCE">
                <constraints nullable="false"/>
            </column>
            <column name="applied_price_id" type="BIGINT"/>
            <column name="applied_workspace_model_id" type="BIGINT"/>
            <column name="applied_per_1m_input_usd" type="NUMERIC(18,8)"/>
            <column name="applied_per_1m_output_usd" type="NUMERIC(18,8)"/>
            <column name="applied_per_1m_cache_read_usd" type="NUMERIC(18,8)"/>
            <column name="applied_per_1m_cache_write_usd" type="NUMERIC(18,8)"/>
            <column name="occurred_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addForeignKeyConstraint
            baseTableName="llm_usage_event"
            baseColumnNames="workspace_id"
            referencedTableName="workspace"
            referencedColumnNames="id"
            constraintName="fk_llm_usage_event_workspace"
            onDelete="CASCADE"/>
        <createIndex tableName="llm_usage_event" indexName="idx_llm_usage_ws_time">
            <column name="workspace_id"/>
            <column name="occurred_at"/>
        </createIndex>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE llm_usage_event ADD CONSTRAINT ck_llm_usage_event_source_type
                CHECK (source_type IN ('AGENT_JOB','MENTOR_TURN'));
            ALTER TABLE llm_usage_event ADD CONSTRAINT ck_llm_usage_event_source_attempt
                CHECK (source_attempt >= 0);
            ALTER TABLE llm_usage_event ADD CONSTRAINT ux_llm_usage_event_source_attempt
                UNIQUE (source_type, source_id, source_attempt);
            ALTER TABLE llm_usage_event ADD CONSTRAINT ck_llm_usage_event_pricing_state
                CHECK (pricing_state IN ('PRICED','NO_CHARGE','UNPRICED'));
            ALTER TABLE llm_usage_event ADD CONSTRAINT ck_llm_usage_event_funding_source
                CHECK (funding_source IN ('INSTANCE','WORKSPACE'));
        ]]></sql>
        <rollback>
            <dropTable tableName="llm_usage_event"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-9" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace.monthly_llm_budget_usd already present">
            <not><columnExists tableName="workspace" columnName="monthly_llm_budget_usd"/></not>
        </preConditions>
        <comment>The workspace's monthly cap on its instance-funded LLM spend in USD; null = uncapped.</comment>
        <addColumn tableName="workspace">
            <column name="monthly_llm_budget_usd" type="NUMERIC(10,2)"/>
        </addColumn>
        <rollback>
            <dropColumn tableName="workspace" columnName="monthly_llm_budget_usd"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-10" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="nothing to backfill from: agent_job.llm_cost_usd has already been dropped by changeSet -43, so this backfill has already run">
            <and>
                <tableExists tableName="llm_usage_event"/>
                <!-- The source column this reads is dropped by changeSet -43 later in this same file.
                     A first pass runs 10 before 43 and sees it; a SECOND pass over an already-upgraded
                     database (an operator re-running migrations, or Liquibase tracking that had to be
                     rebuilt) reaches this changeSet with the column gone, and the statement below would
                     abort on a column that does not exist. Its absence is also exactly the proof that
                     the backfill has already happened, so MARK_RAN is the honest outcome rather than a
                     suppression. -->
                <columnExists tableName="agent_job" columnName="llm_cost_usd"/>
            </and>
        </preConditions>
        <comment>
            Backfill the ledger from detection/replay job history. NOT EXISTS keeps a re-run
            additive-only; the WHERE mirrors the executor's write guard (usage reported OR a cost
            recorded). Classification is conservative: a positive cost is genuinely PRICED, a zero cost
            with no tokens burned is a genuine no-op (NO_CHARGE), and anything else stays UNPRICED so
            it surfaces as unverifiable rather than as a silent $0.

            funding_source is INSTANCE for every backfilled row, written explicitly rather than left to
            the column default so the decision is visible where the rows are made. Historical spend
            predates the two-purse model entirely, and which purse paid is NOT recoverable from the
            database: the worker-pod override (HEPHAESTUS_WORKER_LLM_API_KEY) beat the per-workspace key
            whenever it was set, and it lived in a pod environment variable that was never persisted.
            INSTANCE is the right side to err on. It is the operator's own instrument — visible in the
            admin console and raisable by the person who reads this changelog — whereas the WORKSPACE
            purse is a different party's money, and a workspace admin given a month of pre-upgrade spend
            they cannot explain has no way to dispute it. It is also the only self-consistent choice at
            the moment of the upgrade: every carried-over workspace connection arrives disabled
            (changeSet -33), so there is no own-provider configuration for a WORKSPACE-funded row to
            belong to. Inventing per-row provenance in an append-only ledger whose purpose is to keep a
            past dollar falsifiable would be worse than one documented, uniform assumption.
        </comment>
        <sql><![CDATA[
            -- The guarded cost is computed once, in the inner SELECT, and both stored and classified
            -- from that one value. Deriving pricing_state from the raw double instead would misclassify
            -- the values the guard rejects — PostgreSQL orders NaN above every number, so a NaN cost
            -- reads as "> 0" (PRICED) while the column it lands in is NULL (unverifiable).
            INSERT INTO llm_usage_event (id, workspace_id, job_type, source_type, source_id, source_attempt,
                model, input_tokens, output_tokens, cache_read_tokens, cache_write_tokens,
                total_calls, cost_usd, pricing_state, funding_source, occurred_at)
            SELECT gen_random_uuid(), src.workspace_id, src.job_type, 'AGENT_JOB', src.id, 0, src.llm_model,
                src.input_tokens, src.output_tokens, src.cache_read_tokens, src.cache_write_tokens,
                src.total_calls,
                src.cost,
                CASE
                    WHEN src.cost > 0 THEN 'PRICED'
                    WHEN src.cost = 0
                         AND src.input_tokens + src.output_tokens
                           + src.cache_read_tokens + src.cache_write_tokens = 0 THEN 'NO_CHARGE'
                    ELSE 'UNPRICED'
                END,
                -- Deliberate and uniform; see this changeSet's comment for why the true purse is not
                -- recoverable and why INSTANCE is the side to err on.
                'INSTANCE',
                src.occurred_at
            FROM (
                SELECT j.id, j.workspace_id, j.job_type, j.llm_model,
                    COALESCE(j.llm_total_input_tokens, 0) AS input_tokens,
                    COALESCE(j.llm_total_output_tokens, 0) AS output_tokens,
                    COALESCE(j.llm_cache_read_tokens, 0) AS cache_read_tokens,
                    COALESCE(j.llm_cache_write_tokens, 0) AS cache_write_tokens,
                    GREATEST(COALESCE(j.llm_total_calls, 1), 1) AS total_calls,
                    -- Guarded cast: llm_cost_usd is DOUBLE PRECISION, so historical bad data (a
                    -- divide-by-zero upstream producing NaN/Infinity, or an absurd magnitude) would
                    -- abort the whole migration mid-CAST. Store NULL — unverifiable, which surfaces as
                    -- such — rather than throw or invent a number. The $1M-per-job ceiling is a
                    -- plausibility bound, not the column's capacity (cost_usd holds far more): no single
                    -- agent job legitimately costs that, so a value at or above it is corrupt, and
                    -- letting it through would blow past every budget cap as if it were real spend. It
                    -- is checked against the value ROUNDED to the stored 6-decimal scale, not the raw
                    -- double, so that 999999.9999999 — which rounds UP past the bound — is judged on the
                    -- number actually stored.
                    CASE
                        WHEN j.llm_cost_usd IS NULL THEN NULL
                        WHEN j.llm_cost_usd = 'NaN'::double precision THEN NULL
                        WHEN j.llm_cost_usd = 'Infinity'::double precision THEN NULL
                        WHEN j.llm_cost_usd = '-Infinity'::double precision THEN NULL
                        WHEN ABS(ROUND(j.llm_cost_usd::numeric, 6)) >= 1000000 THEN NULL
                        ELSE ROUND(j.llm_cost_usd::numeric, 6)
                    END AS cost,
                    COALESCE(j.completed_at, j.created_at) AS occurred_at
                FROM agent_job j
                WHERE (COALESCE(j.llm_total_calls, 0) > 0 OR j.llm_cost_usd IS NOT NULL)
                  AND NOT EXISTS (
                      SELECT 1 FROM llm_usage_event e
                      WHERE e.source_type = 'AGENT_JOB' AND e.source_id = j.id AND e.source_attempt = 0)
            ) src;
        ]]></sql>
        <!-- Intentionally empty: a DELETE keyed on agent_job would also wipe the rows the running
             application has appended since the deploy, i.e. live spend history the budget cap reads.
             Rolling the schema back (changeSet -8) drops the whole table anyway; rolling back only the
             backfill has no safe, targeted inverse. -->
        <rollback/>
    </changeSet>

    <changeSet id="1785015307013-11" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_usage_event missing — nothing to backfill into">
            <tableExists tableName="llm_usage_event"/>
        </preConditions>
        <comment>
            Backfill the ledger from mentor turn history in chat_message.metadata. Tolerates both the
            nested {usage:{input,...}} shape and legacy flat inputTokens keys; float token counts floor
            to bigint (mirroring the reader). Every value is regex- and length-guarded before it is
            cast: metadata is free-form JSONB written by historical app versions, so one malformed row
            (a non-numeric string, "NaN"/"Infinity", or a value wider than the target column) would
            abort the ENTIRE migration on a raw cast. Anything that fails the guard becomes NULL, which
            the token COALESCEs treat as "no value" and the cost column treats as UNPRICED — never a
            silent $0. After this, code never aggregates over the JSON again; the ledger is the
            accounting source.

            funding_source is INSTANCE for the same reason, and on the same evidence, as changeSet -10.
        </comment>
        <sql><![CDATA[
            -- As in changeSet -10: every value is extracted once in the inner SELECT, and the outer one
            -- both stores and classifies from those, so the recorded cost and its pricing_state can
            -- never disagree.
            INSERT INTO llm_usage_event (id, workspace_id, job_type, source_type, source_id, source_attempt,
                model, input_tokens, output_tokens, cache_read_tokens, cache_write_tokens,
                total_calls, cost_usd, pricing_state, funding_source, occurred_at)
            SELECT gen_random_uuid(), src.workspace_id, 'MENTOR_TURN', 'MENTOR_TURN', src.id, 0, src.model,
                src.input_tokens, src.output_tokens, src.cache_read_tokens, src.cache_write_tokens,
                1,
                src.cost,
                CASE
                    WHEN src.cost > 0 THEN 'PRICED'
                    WHEN src.cost = 0
                         AND src.input_tokens + src.output_tokens
                           + src.cache_read_tokens + src.cache_write_tokens = 0 THEN 'NO_CHARGE'
                    ELSE 'UNPRICED'
                END,
                -- Uniform INSTANCE, as in changeSet -10.
                'INSTANCE',
                src.occurred_at
            FROM (
                SELECT m.id, t.workspace_id,
                    NULLIF(COALESCE(m.metadata->>'model', ''), '') AS model,
                    COALESCE(
                        floor(CASE WHEN length(m.metadata->'usage'->>'input') < 50
                                        AND m.metadata->'usage'->>'input' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->'usage'->>'input')::numeric END),
                        floor(CASE WHEN length(m.metadata->>'inputTokens') < 50
                                        AND m.metadata->>'inputTokens' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->>'inputTokens')::numeric END),
                        0
                    )::bigint AS input_tokens,
                    COALESCE(
                        floor(CASE WHEN length(m.metadata->'usage'->>'output') < 50
                                        AND m.metadata->'usage'->>'output' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->'usage'->>'output')::numeric END),
                        floor(CASE WHEN length(m.metadata->>'outputTokens') < 50
                                        AND m.metadata->>'outputTokens' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->>'outputTokens')::numeric END),
                        0
                    )::bigint AS output_tokens,
                    COALESCE(
                        floor(CASE WHEN length(m.metadata->'usage'->>'cacheRead') < 50
                                        AND m.metadata->'usage'->>'cacheRead' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->'usage'->>'cacheRead')::numeric END),
                        floor(CASE WHEN length(m.metadata->>'cacheReadTokens') < 50
                                        AND m.metadata->>'cacheReadTokens' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->>'cacheReadTokens')::numeric END),
                        0
                    )::bigint AS cache_read_tokens,
                    COALESCE(
                        floor(CASE WHEN length(m.metadata->'usage'->>'cacheWrite') < 50
                                        AND m.metadata->'usage'->>'cacheWrite' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->'usage'->>'cacheWrite')::numeric END),
                        floor(CASE WHEN length(m.metadata->>'cacheWriteTokens') < 50
                                        AND m.metadata->>'cacheWriteTokens' ~ '^[0-9]{1,15}(\.[0-9]+)?$'
                                   THEN (m.metadata->>'cacheWriteTokens')::numeric END),
                        0
                    )::bigint AS cache_write_tokens,
                    -- Same plausibility bound as changeSet -10, applied to the ROUNDED value: the
                    -- regex's integer-digit guard only bounds the unrounded string, and
                    -- "999999.9999999" passes it but rounds to seven integer digits.
                    CASE WHEN length(m.metadata->>'costUsd') < 50
                              AND m.metadata->>'costUsd' ~ '^-?[0-9]{1,6}(\.[0-9]+)?$'
                              AND ABS(ROUND((m.metadata->>'costUsd')::numeric, 6)) < 1000000
                         THEN ROUND((m.metadata->>'costUsd')::numeric, 6) END AS cost,
                    m.created_at AS occurred_at
                FROM chat_message m
                JOIN chat_thread t ON t.id = m.thread_id
                WHERE m.metadata IS NOT NULL
                  AND (m.metadata->>'costUsd' IS NOT NULL
                       OR m.metadata->'usage'->>'input' IS NOT NULL
                       OR m.metadata->>'inputTokens' IS NOT NULL)
                  AND NOT EXISTS (
                      SELECT 1 FROM llm_usage_event e
                      WHERE e.source_type = 'MENTOR_TURN' AND e.source_id = m.id AND e.source_attempt = 0)
            ) src;
        ]]></sql>
        <!-- Intentionally empty for the same reason as changeSet -10's rollback. -->
        <rollback/>
    </changeSet>

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         LLM configuration catalog (#1368). Two ownership scopes, both first-class:
           • INSTANCE catalog (app_admin-owned, GLOBAL): llm_connection -> llm_model ->
             llm_model_price, plus llm_model_workspace_grant for GRANTED visibility. Shared,
             curated, instance-funded.
           • WORKSPACE own-provider (workspace-admin-owned, tenant-scoped): workspace_llm_connection
             -> workspace_llm_model. Private to one workspace, self-funded, priced by that admin.

         Pricing authority follows credential ownership: the instance prices instance-funded usage; a
         workspace prices only its own usage. A capped party can never zero out the price of usage it
         does not fund.
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-12" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_connection already present">
            <not><tableExists tableName="llm_connection"/></not>
        </preConditions>
        <comment>Instance-owned LLM provider connection: endpoint, credential and wire protocol. app_admin-owned, global.</comment>
        <createTable tableName="llm_connection">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_llm_connection" nullable="false"/>
            </column>
            <column name="slug" type="VARCHAR(64)">
                <constraints nullable="false" unique="true" uniqueConstraintName="ux_llm_connection_slug"/>
            </column>
            <column name="display_name" type="VARCHAR(128)"><constraints nullable="false"/></column>
            <column name="base_url" type="VARCHAR(2048)"><constraints nullable="false"/></column>
            <column name="api_protocol" type="VARCHAR(40)"><constraints nullable="false"/></column>
            <column name="auth_mode" type="VARCHAR(16)" defaultValue="BEARER">
                <constraints nullable="false"/>
            </column>
            <column name="api_key" type="TEXT"/>
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>
        <!-- No egress_mode: LLM traffic ALWAYS routes through the in-app proxy (running on whichever
             host executes the job); the API key never enters the sandbox. One path, no toggle. -->
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE llm_connection ADD CONSTRAINT ck_llm_connection_api_protocol
                CHECK (api_protocol IN ('openai-completions','openai-responses'));
            ALTER TABLE llm_connection ADD CONSTRAINT ck_llm_connection_auth_mode
                CHECK (auth_mode IN ('BEARER','API_KEY'));
        ]]></sql>
        <rollback><dropTable tableName="llm_connection"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-13" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_model already present">
            <not><tableExists tableName="llm_model"/></not>
        </preConditions>
        <comment>
            Instance-curated model behind a connection: opaque upstream id, capability, lifecycle and
            visibility. (connection_id, upstream_model_id) is unique because the usage recorder matches
            a call's frozen binding by that pair alone and just takes the first enabled match — two
            entries for the same pair would make pricing nondeterministic and let a free sibling
            silently shadow a priced one.
        </comment>
        <createTable tableName="llm_model">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_llm_model" nullable="false"/>
            </column>
            <column name="connection_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="slug" type="VARCHAR(64)"><constraints nullable="false"/></column>
            <column name="display_name" type="VARCHAR(128)"><constraints nullable="false"/></column>
            <column name="upstream_model_id" type="VARCHAR(256)"><constraints nullable="false"/></column>
            <column name="context_window" type="INTEGER"/>
            <column name="max_output_tokens" type="INTEGER"/>
            <column name="supports_reasoning" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="visibility" type="VARCHAR(16)" defaultValue="GRANTED"><constraints nullable="false"/></column>
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="false"><constraints nullable="false"/></column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>
        <addForeignKeyConstraint baseTableName="llm_model" baseColumnNames="connection_id"
            referencedTableName="llm_connection" referencedColumnNames="id"
            constraintName="fk_llm_model_connection" onDelete="RESTRICT"/>
        <addUniqueConstraint tableName="llm_model" columnNames="connection_id, slug"
            constraintName="ux_llm_model_connection_slug"/>
        <createIndex tableName="llm_model" indexName="idx_llm_model_connection">
            <column name="connection_id"/>
        </createIndex>
        <addUniqueConstraint tableName="llm_model" columnNames="connection_id, upstream_model_id"
            constraintName="ux_llm_model_connection_upstream"/>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE llm_model ADD CONSTRAINT ck_llm_model_visibility
                CHECK (visibility IN ('PUBLIC','GRANTED'));
        ]]></sql>
        <rollback><dropTable tableName="llm_model"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-14" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_model_price already present">
            <not><tableExists tableName="llm_model_price"/></not>
        </preConditions>
        <comment>
            Effective-dated price rows for an instance model, with a tri-state pricing_mode. The
            surrogate PK fixes the retired model_pricing table's fatal model-id-as-PK (which made a
            second price row a PK collision). Repricing closes the open row's effective_to and inserts
            a new one in one transaction; the partial unique index is the backstop against a
            double-open race. Rates are per 1M tokens (what every vendor quotes) at NUMERIC(18,8), so
            sub-cent-per-1M rates do not truncate to zero.
        </comment>
        <createTable tableName="llm_model_price">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_llm_model_price" nullable="false"/>
            </column>
            <column name="model_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="pricing_mode" type="VARCHAR(16)"><constraints nullable="false"/></column>
            <column name="per_1m_input_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_output_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_cache_read_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_cache_write_usd" type="NUMERIC(18,8)"/>
            <column name="currency" type="VARCHAR(3)" defaultValue="USD"><constraints nullable="false"/></column>
            <column name="note" type="VARCHAR(500)"/>
            <column name="effective_from" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="effective_to" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="created_by" type="VARCHAR(255)"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
        </createTable>
        <addForeignKeyConstraint baseTableName="llm_model_price" baseColumnNames="model_id"
            referencedTableName="llm_model" referencedColumnNames="id"
            constraintName="fk_llm_model_price_model" onDelete="CASCADE"/>
        <createIndex tableName="llm_model_price" indexName="idx_llm_model_price_model_from">
            <column name="model_id"/>
            <column name="effective_from"/>
        </createIndex>
        <!-- Integrity guards that do NOT need the btree_gist extension (deliberately deferred):
             a closed window must end after it starts, and at most one OPEN (current) row may exist
             per model. -->
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE llm_model_price ADD CONSTRAINT ck_llm_model_price_mode
                CHECK (pricing_mode IN ('PRICED','NO_CHARGE','UNPRICED'));
            ALTER TABLE llm_model_price ADD CONSTRAINT ck_llm_model_price_shape CHECK (
                (pricing_mode = 'PRICED'
                    AND per_1m_input_usd IS NOT NULL AND per_1m_input_usd >= 0
                    AND per_1m_output_usd IS NOT NULL AND per_1m_output_usd >= 0
                    AND (per_1m_cache_read_usd IS NULL OR per_1m_cache_read_usd >= 0)
                    AND (per_1m_cache_write_usd IS NULL OR per_1m_cache_write_usd >= 0)
                    AND GREATEST(per_1m_input_usd, per_1m_output_usd,
                                 COALESCE(per_1m_cache_read_usd, 0), COALESCE(per_1m_cache_write_usd, 0)) > 0)
                OR (pricing_mode = 'NO_CHARGE'
                    AND note IS NOT NULL AND btrim(note) <> ''
                    AND per_1m_input_usd IS NULL AND per_1m_output_usd IS NULL
                    AND per_1m_cache_read_usd IS NULL AND per_1m_cache_write_usd IS NULL)
                OR (pricing_mode = 'UNPRICED'
                    AND per_1m_input_usd IS NULL AND per_1m_output_usd IS NULL
                    AND per_1m_cache_read_usd IS NULL AND per_1m_cache_write_usd IS NULL)
            );
            ALTER TABLE llm_model_price ADD CONSTRAINT ck_llm_model_price_window
                CHECK (effective_to IS NULL OR effective_to > effective_from);
            CREATE UNIQUE INDEX ux_llm_model_price_open ON llm_model_price (model_id) WHERE effective_to IS NULL;
        ]]></sql>
        <rollback><dropTable tableName="llm_model_price"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-15" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="llm_model_workspace_grant already present">
            <not><tableExists tableName="llm_model_workspace_grant"/></not>
        </preConditions>
        <comment>
            Which workspaces may use a GRANTED-visibility instance model. Composite PK, modelled on
            account_feature: an allowlist by reference, so revoking needs no data migration. sfk_ =
            scalar FK (ADR 0017) — a pure join table with no JPA @ManyToOne, so Hibernate emits the
            columns but never these constraints; the prefix keeps them out of the drift diff while
            preserving referential integrity in the database.
        </comment>
        <createTable tableName="llm_model_workspace_grant">
            <column name="model_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="workspace_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="granted_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="granted_by" type="VARCHAR(255)"/>
        </createTable>
        <addPrimaryKey tableName="llm_model_workspace_grant" columnNames="model_id, workspace_id"
            constraintName="pk_llm_model_workspace_grant"/>
        <addForeignKeyConstraint baseTableName="llm_model_workspace_grant" baseColumnNames="model_id"
            referencedTableName="llm_model" referencedColumnNames="id"
            constraintName="sfk_llm_model_grant_model" onDelete="CASCADE"/>
        <addForeignKeyConstraint baseTableName="llm_model_workspace_grant" baseColumnNames="workspace_id"
            referencedTableName="workspace" referencedColumnNames="id"
            constraintName="sfk_llm_model_grant_workspace" onDelete="CASCADE"/>
        <createIndex tableName="llm_model_workspace_grant" indexName="idx_llm_model_grant_workspace">
            <column name="workspace_id"/>
        </createIndex>
        <rollback><dropTable tableName="llm_model_workspace_grant"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-16" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace_llm_connection already present">
            <not><tableExists tableName="workspace_llm_connection"/></not>
        </preConditions>
        <comment>
            Workspace-owned LLM connection: same shape as the instance one, but tenant-scoped — the
            workspace admin owns the key and the bill. (id, workspace_id) is unique so a model row can
            carry a same-workspace FK: without it a model could claim workspace A while pointing at
            workspace B's connection, and thus decrypt B's key.
        </comment>
        <createTable tableName="workspace_llm_connection">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_workspace_llm_connection" nullable="false"/>
            </column>
            <column name="workspace_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="slug" type="VARCHAR(64)"><constraints nullable="false"/></column>
            <column name="display_name" type="VARCHAR(128)"><constraints nullable="false"/></column>
            <column name="base_url" type="VARCHAR(2048)"><constraints nullable="false"/></column>
            <column name="api_protocol" type="VARCHAR(40)"><constraints nullable="false"/></column>
            <column name="auth_mode" type="VARCHAR(16)" defaultValue="BEARER">
                <constraints nullable="false"/>
            </column>
            <column name="api_key" type="TEXT"/>
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="false"><constraints nullable="false"/></column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>
        <addForeignKeyConstraint baseTableName="workspace_llm_connection" baseColumnNames="workspace_id"
            referencedTableName="workspace" referencedColumnNames="id"
            constraintName="fk_ws_llm_connection_workspace" onDelete="CASCADE"/>
        <addUniqueConstraint tableName="workspace_llm_connection" columnNames="workspace_id, slug"
            constraintName="ux_ws_llm_connection_ws_slug"/>
        <addUniqueConstraint tableName="workspace_llm_connection" columnNames="id, workspace_id"
            constraintName="ux_ws_llm_connection_id_ws"/>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE workspace_llm_connection ADD CONSTRAINT ck_ws_llm_connection_api_protocol
                CHECK (api_protocol IN ('openai-completions','openai-responses'));
            ALTER TABLE workspace_llm_connection ADD CONSTRAINT ck_ws_llm_connection_auth_mode
                CHECK (auth_mode IN ('BEARER','API_KEY'));
        ]]></sql>
        <rollback><dropTable tableName="workspace_llm_connection"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-17" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace_llm_model already present">
            <not><tableExists tableName="workspace_llm_model"/></not>
        </preConditions>
        <comment>
            Workspace-owned model with its current price inline (a single workspace's own rate needs no
            audit history). workspace_id is denormalised rather than implied through connection_id so
            every query carries a workspace_id predicate for the tenancy inspector, and the
            (connection_id, workspace_id) FK guarantees a model can never bind another tenant's
            connection — and so never decrypt its key.
        </comment>
        <createTable tableName="workspace_llm_model">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_workspace_llm_model" nullable="false"/>
            </column>
            <column name="workspace_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="connection_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="slug" type="VARCHAR(64)"><constraints nullable="false"/></column>
            <column name="display_name" type="VARCHAR(128)"><constraints nullable="false"/></column>
            <column name="upstream_model_id" type="VARCHAR(256)"><constraints nullable="false"/></column>
            <column name="context_window" type="INTEGER"/>
            <column name="max_output_tokens" type="INTEGER"/>
            <column name="supports_reasoning" type="BOOLEAN" defaultValueBoolean="false"><constraints nullable="false"/></column>
            <column name="pricing_mode" type="VARCHAR(16)" defaultValue="UNPRICED"><constraints nullable="false"/></column>
            <column name="per_1m_input_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_output_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_cache_read_usd" type="NUMERIC(18,8)"/>
            <column name="per_1m_cache_write_usd" type="NUMERIC(18,8)"/>
            <column name="currency" type="VARCHAR(3)" defaultValue="USD"><constraints nullable="false"/></column>
            <column name="price_note" type="VARCHAR(500)"/>
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="false"><constraints nullable="false"/></column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>
        <addForeignKeyConstraint baseTableName="workspace_llm_model" baseColumnNames="workspace_id"
            referencedTableName="workspace" referencedColumnNames="id"
            constraintName="fk_ws_llm_model_workspace" onDelete="CASCADE"/>
        <addForeignKeyConstraint baseTableName="workspace_llm_model" baseColumnNames="connection_id"
            referencedTableName="workspace_llm_connection" referencedColumnNames="id"
            constraintName="fk_ws_llm_model_connection" onDelete="CASCADE"/>
        <addForeignKeyConstraint baseTableName="workspace_llm_model" baseColumnNames="connection_id, workspace_id"
            referencedTableName="workspace_llm_connection" referencedColumnNames="id, workspace_id"
            constraintName="sfk_ws_llm_model_connection_scope" onDelete="CASCADE"/>
        <addUniqueConstraint tableName="workspace_llm_model" columnNames="workspace_id, slug"
            constraintName="ux_ws_llm_model_ws_slug"/>
        <addUniqueConstraint tableName="workspace_llm_model" columnNames="id, workspace_id"
            constraintName="ux_ws_llm_model_id_ws"/>
        <createIndex tableName="workspace_llm_model" indexName="idx_ws_llm_model_connection">
            <column name="connection_id"/>
        </createIndex>
        <addUniqueConstraint tableName="workspace_llm_model" columnNames="connection_id, upstream_model_id"
            constraintName="ux_ws_llm_model_connection_upstream"/>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE workspace_llm_model ADD CONSTRAINT ck_ws_llm_model_pricing_mode
                CHECK (pricing_mode IN ('PRICED','NO_CHARGE','UNPRICED'));
            ALTER TABLE workspace_llm_model ADD CONSTRAINT ck_ws_llm_model_price_shape CHECK (
                (pricing_mode = 'PRICED'
                    AND per_1m_input_usd IS NOT NULL AND per_1m_input_usd >= 0
                    AND per_1m_output_usd IS NOT NULL AND per_1m_output_usd >= 0
                    AND (per_1m_cache_read_usd IS NULL OR per_1m_cache_read_usd >= 0)
                    AND (per_1m_cache_write_usd IS NULL OR per_1m_cache_write_usd >= 0)
                    AND GREATEST(per_1m_input_usd, per_1m_output_usd,
                                 COALESCE(per_1m_cache_read_usd, 0), COALESCE(per_1m_cache_write_usd, 0)) > 0)
                OR (pricing_mode = 'NO_CHARGE'
                    AND price_note IS NOT NULL AND btrim(price_note) <> ''
                    AND per_1m_input_usd IS NULL AND per_1m_output_usd IS NULL
                    AND per_1m_cache_read_usd IS NULL AND per_1m_cache_write_usd IS NULL)
                OR (pricing_mode = 'UNPRICED'
                    AND per_1m_input_usd IS NULL AND per_1m_output_usd IS NULL
                    AND per_1m_cache_read_usd IS NULL AND per_1m_cache_write_usd IS NULL)
            );
        ]]></sql>
        <rollback><dropTable tableName="workspace_llm_model"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-18" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="instance_llm_settings already present">
            <not><tableExists tableName="instance_llm_settings"/></not>
        </preConditions>
        <comment>
            Instance LLM settings singleton (id pinned to 1): the egress allowlist governing EVERY
            connection, and the workspace-connection enable flag. An empty allowlist means allow all,
            so an upgrade is non-breaking. There is deliberately no unpriced-usage policy knob:
            enforcement is structural — a capped workspace whose month is unverifiable is always
            blocked (a cap you cannot verify is not a cap) and an uncapped one never is.
        </comment>
        <createTable tableName="instance_llm_settings">
            <column name="id" type="SMALLINT"><constraints primaryKey="true" primaryKeyName="pk_instance_llm_settings" nullable="false"/></column>
            <column name="allowed_egress_hosts" type="TEXT"/>
            <column name="allow_workspace_connections" type="BOOLEAN" defaultValueBoolean="true"><constraints nullable="false"/></column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="updated_by" type="VARCHAR(255)"/>
        </createTable>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE instance_llm_settings ADD CONSTRAINT ck_instance_llm_settings_singleton CHECK (id = 1);
            INSERT INTO instance_llm_settings (id, allowed_egress_hosts, allow_workspace_connections)
                VALUES (1, NULL, TRUE) ON CONFLICT (id) DO NOTHING;
        ]]></sql>
        <rollback><dropTable tableName="instance_llm_settings"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-19" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="ck_auth_event_event_type already admits the LLM catalog event types">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM pg_constraint
                 WHERE conname = 'ck_auth_event_event_type'
                   AND pg_get_constraintdef(oid) LIKE '%LLM_SETTINGS_CHANGED%'
            </sqlCheck>
        </preConditions>
        <comment>
            Admit the instance LLM catalog's audit events on auth_event. The catalog is global, and
            config_audit_event.workspace_id is NOT NULL, so these changes cannot live there; auth_event
            is the same ledger the account admin already uses for APP_ROLE_CHANGED. The ADD validates
            every existing row in one pass under a momentary ACCESS EXCLUSIVE lock, no rewrite.
        </comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE auth_event DROP CONSTRAINT ck_auth_event_event_type;
            ALTER TABLE auth_event ADD CONSTRAINT ck_auth_event_event_type CHECK (event_type IN (
                'LOGIN', 'LOGIN_FAILED', 'LOGOUT', 'TOKEN_REFRESH', 'JWT_REVOKED',
                'IDENTITY_LINKED', 'IDENTITY_UNLINKED',
                'IMPERSONATION_BEGIN', 'IMPERSONATION_END',
                'ACCOUNT_DELETED',
                'EXPORT_REQUESTED', 'APP_ROLE_CHANGED',
                'RESEARCH_CONSENT_REVOKED',
                'LLM_CONNECTION_CREATED', 'LLM_CONNECTION_UPDATED', 'LLM_CONNECTION_DELETED',
                'LLM_MODEL_CREATED', 'LLM_MODEL_UPDATED', 'LLM_MODEL_DELETED',
                'LLM_MODEL_PRICE_CHANGED', 'LLM_MODEL_SHARING_CHANGED', 'LLM_SETTINGS_CHANGED'
            ));
        ]]></sql>
        <!-- Empty: re-adding the narrow CHECK fails the moment any row carries one of the new LLM_*
             event types, and a data-preserving downgrade does not exist without deleting or remapping
             those rows — which a rollback must never do silently. Rolling back past this changeSet
             means accepting that the wider CHECK stays in place. -->
        <rollback/>
    </changeSet>

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         Agent job queue on PostgreSQL: hardening for the NATS→Postgres cutover. See
         docs/decisions/0025-agent-job-queue-on-postgresql.md.
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-20" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.available_at already present">
            <not><columnExists tableName="agent_job" columnName="available_at"/></not>
        </preConditions>
        <comment>When a QUEUED job becomes eligible for a poll-loop claim; backoff-computed on requeue.</comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE agent_job ADD COLUMN available_at TIMESTAMPTZ NOT NULL DEFAULT now();
            ALTER TABLE agent_job ALTER COLUMN available_at DROP DEFAULT;
        ]]></sql>
        <rollback>
            <dropColumn tableName="agent_job" columnName="available_at"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-21" author="hephaestus" runInTransaction="false">
        <preConditions onFail="MARK_RAN" onFailMessage="ix_agent_job_queued_available already present and valid">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid
                WHERE c.relname = 'ix_agent_job_queued_available' AND i.indisvalid
            </sqlCheck>
        </preConditions>
        <comment>
            Index queued jobs by their claim-eligibility time without blocking queue traffic.

            The guard is on pg_index.indisvalid, not on the index merely existing: a CREATE INDEX
            CONCURRENTLY that fails partway leaves an index the planner will never use, and JDBC index
            metadata cannot tell it apart from a good one. An existence-only precondition would record
            this changeSet as applied and leave the queue's poll query unindexed for the life of the
            install. The leading DROP clears exactly that leftover and is a no-op otherwise.
        </comment>
        <sql>
            DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_queued_available;
            CREATE INDEX CONCURRENTLY ix_agent_job_queued_available
                ON agent_job (available_at, id)
                WHERE status = 'QUEUED';
        </sql>
        <rollback>
            <sql>DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_queued_available;</sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-22" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="ck_agent_job_status already present">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_constraint WHERE conname = 'ck_agent_job_status'
            </sqlCheck>
        </preConditions>
        <comment>CHECK the six documented job statuses at the storage layer; NOT VALID keeps the lock momentary, changeSet -29 validates.</comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE agent_job ADD CONSTRAINT ck_agent_job_status
                CHECK (status IN ('QUEUED','RUNNING','COMPLETED','FAILED','TIMED_OUT','CANCELLED')) NOT VALID;
        ]]></sql>
        <rollback>
            <sql>ALTER TABLE agent_job DROP CONSTRAINT IF EXISTS ck_agent_job_status;</sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-23" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.delivery_attempts already present">
            <not><columnExists tableName="agent_job" columnName="delivery_attempts"/></not>
        </preConditions>
        <comment>Bounded attempt counter for the delivery-recovery sweep, so a poisoned delivery cannot retry forever.</comment>
        <addColumn tableName="agent_job">
            <column name="delivery_attempts" type="SMALLINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="agent_job" columnName="delivery_attempts"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-24" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job table is absent">
            <tableExists tableName="agent_job"/>
        </preConditions>
        <comment>
            Oban's autovacuum recommendation, adapted: a high-churn queue table needs vacuum triggered
            by absolute row-change count, not the default 20% scale factor.

            The precondition checks only that the table is there, and deliberately does not try to
            detect the storage parameters themselves. A precondition can see WHETHER a reloption is
            set but not whether it holds the intended value, so matching on presence would let a stale
            or half-applied setting silently skip the change. ALTER TABLE ... SET (...) is idempotent,
            so this reasserts the intended values outright. Consequence, stated rather than hidden:
            a re-run overwrites operator-tuned values for these two parameters on this table. Tune
            them by changing this changelog, not the live table.
        </comment>
        <sql>
            ALTER TABLE agent_job SET (autovacuum_vacuum_scale_factor = 0, autovacuum_vacuum_threshold = 500);
        </sql>
        <rollback>
            <sql>ALTER TABLE agent_job RESET (autovacuum_vacuum_scale_factor, autovacuum_vacuum_threshold);</sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-25" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="worker_registry table is absent">
            <tableExists tableName="worker_registry"/>
        </preConditions>
        <comment>Same autovacuum tuning for worker_registry — a small table taking heartbeat-rate UPDATEs. Reasserted unconditionally on re-run for the reason given in changeSet -24.</comment>
        <sql>
            ALTER TABLE worker_registry SET (autovacuum_vacuum_scale_factor = 0, autovacuum_vacuum_threshold = 100);
        </sql>
        <rollback>
            <sql>ALTER TABLE worker_registry RESET (autovacuum_vacuum_scale_factor, autovacuum_vacuum_threshold);</sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-26" author="hephaestus" runInTransaction="false">
        <preConditions onFail="MARK_RAN" onFailMessage="ix_agent_job_delivery_pending already present and valid">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid
                WHERE c.relname = 'ix_agent_job_delivery_pending' AND i.indisvalid
            </sqlCheck>
        </preConditions>
        <comment>Support the zombie sweeper's stuck-PENDING-delivery scan. Guarded on index validity, not existence — see changeSet -21.</comment>
        <sql>
            DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_delivery_pending;
            CREATE INDEX CONCURRENTLY ix_agent_job_delivery_pending
                ON agent_job (completed_at)
                WHERE status = 'COMPLETED' AND delivery_status = 'PENDING';
        </sql>
        <rollback><sql>DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_delivery_pending;</sql></rollback>
    </changeSet>

    <changeSet id="1785015307013-27" author="hephaestus" runInTransaction="false">
        <preConditions onFail="MARK_RAN" onFailMessage="ix_agent_job_retention already present and valid">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid
                WHERE c.relname = 'ix_agent_job_retention' AND i.indisvalid
            </sqlCheck>
        </preConditions>
        <comment>Support the retention service's terminal-row batch scans (strip, then delete). Guarded on index validity, not existence — see changeSet -21.</comment>
        <sql>
            DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_retention;
            CREATE INDEX CONCURRENTLY ix_agent_job_retention
                ON agent_job (completed_at)
                WHERE status IN ('COMPLETED','FAILED','TIMED_OUT','CANCELLED');
        </sql>
        <rollback><sql>DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_retention;</sql></rollback>
    </changeSet>

    <changeSet id="1785015307013-28" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="sfk_feedback_agent_job already RESTRICT">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_constraint
                WHERE conname = 'sfk_feedback_agent_job' AND confdeltype = 'c'
            </sqlCheck>
        </preConditions>
        <comment>
            Harden feedback.agent_job_id from CASCADE to RESTRICT. CASCADE transitively took
            feedback_observation, feedback_placement and reaction with it — append-only research and
            product data. The retention service's delete pass already excludes jobs referenced by
            feedback, but leaving the FK on CASCADE means any FUTURE agent_job delete (a bug, an ad-hoc
            admin query, a retention path added later) could silently repeat it.
        </comment>
        <sql splitStatements="false"><![CDATA[
            ALTER TABLE feedback DROP CONSTRAINT sfk_feedback_agent_job;
            ALTER TABLE feedback ADD CONSTRAINT sfk_feedback_agent_job
                FOREIGN KEY (agent_job_id) REFERENCES agent_job (id) ON DELETE RESTRICT;
        ]]></sql>
        <rollback>
            <sql splitStatements="false"><![CDATA[
                ALTER TABLE feedback DROP CONSTRAINT sfk_feedback_agent_job;
                ALTER TABLE feedback ADD CONSTRAINT sfk_feedback_agent_job
                    FOREIGN KEY (agent_job_id) REFERENCES agent_job (id) ON DELETE CASCADE;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-29" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="ck_agent_job_status already validated">
            <sqlCheck expectedResult="1">
                SELECT count(*) FROM pg_constraint WHERE conname = 'ck_agent_job_status' AND NOT convalidated
            </sqlCheck>
        </preConditions>
        <comment>Validate ck_agent_job_status under SHARE UPDATE EXCLUSIVE (reads and writes unblocked).</comment>
        <sql>ALTER TABLE agent_job VALIDATE CONSTRAINT ck_agent_job_status;</sql>
        <!-- Intentionally empty: VALIDATE only flips pg_constraint.convalidated on a constraint that
             already exists and already applies to new rows. There is no inverse worth running — a
             hand-crafted UPDATE pg_constraint SET convalidated = false is catalog surgery, and dropping
             the constraint is changeSet -22's rollback, not this one's. -->
        <rollback/>
    </changeSet>

    <changeSet id="1785015307013-30" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.execution_started_at already present">
            <not><columnExists tableName="agent_job" columnName="execution_started_at"/></not>
        </preConditions>
        <comment>Distinguish claimed preparation from provider-capable execution, so a crashed job is billed for the calls it actually made.</comment>
        <addColumn tableName="agent_job">
            <column name="execution_started_at" type="TIMESTAMP WITH TIME ZONE"/>
        </addColumn>
        <sql>
            UPDATE agent_job SET execution_started_at = started_at
            WHERE status = 'RUNNING' AND started_at IS NOT NULL
        </sql>
        <rollback>
            <dropColumn tableName="agent_job" columnName="execution_started_at"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-31" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="model_pricing already removed">
            <tableExists tableName="model_pricing"/>
        </preConditions>
        <comment>
            Retire the legacy per-1k model_pricing table. It has no runtime consumer: mentor and
            practice cost both derive from the admission-frozen per-1M price snapshot, and the usage
            ledger freezes the applied rates per event as the authoritative price history.
        </comment>
        <dropTable tableName="model_pricing"/>
        <rollback>
            <createTable tableName="model_pricing">
                <column name="model_id" type="VARCHAR(128)">
                    <constraints primaryKey="true" primaryKeyName="pk_model_pricing"/>
                </column>
                <column name="per_1k_input_usd" type="DECIMAL(12,6)">
                    <constraints nullable="false"/>
                </column>
                <column name="per_1k_output_usd" type="DECIMAL(12,6)">
                    <constraints nullable="false"/>
                </column>
                <column name="per_1k_cache_read_usd" type="DECIMAL(12,6)" defaultValueNumeric="0"/>
                <column name="per_1k_cache_write_usd" type="DECIMAL(12,6)" defaultValueNumeric="0"/>
                <column name="valid_from" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                    <constraints nullable="false"/>
                </column>
                <column name="valid_to" type="TIMESTAMP WITH TIME ZONE"/>
            </createTable>
        </rollback>
    </changeSet>

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         Per-purpose agent bindings, and the retirement of the named agent_config profile.
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-32" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace_agent_binding already present">
            <not><tableExists tableName="workspace_agent_binding"/></not>
        </preConditions>
        <comment>
            Exactly one model per (workspace, purpose). Replaces the named agent_config profile and the
            scalar practice/mentor config pointers on workspace. A binding points at either an instance
            model or a workspace-owned one, never both and never neither.
        </comment>
        <createTable tableName="workspace_agent_binding">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_workspace_agent_binding"/>
            </column>
            <column name="workspace_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="purpose" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="enabled" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="timeout_seconds" type="INT" defaultValueNumeric="600">
                <constraints nullable="false"/>
            </column>
            <column name="max_concurrent_jobs" type="INT" defaultValueNumeric="3">
                <constraints nullable="false"/>
            </column>
            <column name="allow_internet" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="instance_model_id" type="BIGINT"/>
            <column name="workspace_model_id" type="BIGINT"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>
        <addUniqueConstraint
            tableName="workspace_agent_binding"
            columnNames="workspace_id, purpose"
            constraintName="uk_workspace_agent_binding_purpose"/>
        <addForeignKeyConstraint baseTableName="workspace_agent_binding" baseColumnNames="workspace_id"
            referencedTableName="workspace" referencedColumnNames="id"
            constraintName="fk_workspace_agent_binding_workspace" onDelete="CASCADE"/>
        <addForeignKeyConstraint baseTableName="workspace_agent_binding" baseColumnNames="instance_model_id"
            referencedTableName="llm_model" referencedColumnNames="id"
            constraintName="fk_workspace_agent_binding_instance_model" onDelete="RESTRICT"/>
        <addForeignKeyConstraint baseTableName="workspace_agent_binding" baseColumnNames="workspace_model_id"
            referencedTableName="workspace_llm_model" referencedColumnNames="id"
            constraintName="fk_workspace_agent_binding_workspace_model" onDelete="RESTRICT"/>
        <!-- Tenancy-safe: a binding can only reference a workspace model of the SAME workspace. -->
        <addForeignKeyConstraint baseTableName="workspace_agent_binding" baseColumnNames="workspace_model_id, workspace_id"
            referencedTableName="workspace_llm_model" referencedColumnNames="id, workspace_id"
            constraintName="sfk_workspace_agent_binding_workspace_model_scope" onDelete="RESTRICT"/>
        <sql>
            ALTER TABLE workspace_agent_binding ADD CONSTRAINT ck_workspace_agent_binding_single_model
                CHECK (num_nonnulls(instance_model_id, workspace_model_id) = 1);
        </sql>
        <rollback>
            <dropTable tableName="workspace_agent_binding"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-33" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="the workspace LLM catalog is already populated — legacy configs were migrated or the catalog was configured by hand">
            <and>
                <tableExists tableName="agent_config"/>
                <sqlCheck expectedResult="0">SELECT count(*) FROM workspace_agent_binding</sqlCheck>
                <sqlCheck expectedResult="0">SELECT count(*) FROM workspace_llm_connection</sqlCheck>
                <sqlCheck expectedResult="0">SELECT count(*) FROM workspace_llm_model</sqlCheck>
            </and>
        </preConditions>
        <comment>
            Carry every workspace's existing AI configuration into the new catalog BEFORE agent_config
            is dropped (changeSet -39).

            agent_config is the only place an instance holds a workspace's LLM endpoint, model name and
            API key. The key is encrypted with the same converter the new tables use, so the ciphertext
            copies verbatim and the operator does not have to re-enter a secret they may no longer
            have. Dropping the table without this step destroys all of it irrecoverably.

            WHAT IS CARRIED: every config that was doing something — one a workspace explicitly pointed
            at, OR one that is simply enabled. A NULL practice_config_id / mentor_config_id never meant
            "unused"; it meant "use the fallback", and both features had one. The mentor fell back to the
            workspace's oldest enabled config, and practice detection fanned out to ALL enabled configs.
            The documented default provisioning route (AGENT_DEFAULT_CONFIG_*) seeded exactly that shape:
            an enabled config with both pointers NULL, serving both features. Carrying only pointed-at
            rows would silently destroy the live key of every instance provisioned that way.

            Only a config that is BOTH disabled AND unreferenced is dropped: nothing could reach it, and
            neither resolver would ever have selected it. changeSet -34 names those workspaces in the
            deploy log before the table goes.

            WHAT IS BUILT: one connection per carried config, one model per connection, and one binding
            per (workspace, purpose) carrying that config's timeout, concurrency and internet limits
            verbatim. A config serving both purposes yields one connection and two bindings.

            The bindings reproduce what each resolver actually selected, not what the pointer column
            said:
              • MENTOR — the pointed-at config when it exists and is enabled, else the workspace's oldest
                enabled config, else no binding (the mentor could not run at all in that state).
              • PRACTICE_DETECTION — the pointed-at config when the pointer is set, enabled or not (a
                bound-but-disabled config PAUSED detection, and a disabled binding is that same pause),
                else the oldest enabled config. A dangling or cross-tenant pointer yields no binding,
                which is also what it did before.
            (workspace, purpose) is unique, so a workspace that ran detection across SEVERAL enabled
            configs collapses to one binding — the oldest. Nothing is lost: the other configs still
            arrive as connections and models, ready to bind. changeSet -34 names those workspaces.

            EVERYTHING LANDS DISABLED, including configs that look complete. In PROXY credential mode —
            the default, and what the startup seeder wrote — the sandbox never used the config's own
            base URL: it called the in-app proxy, which forwarded to an INSTANCE-level upstream URL set
            by environment variable. That value is not in the database, so no migration can know which
            endpoint was really in force. Enabling automatically could silently re-point a workspace's
            traffic, and its API key, at a different host. Carrying everything forward disabled loses
            nothing and guesses nothing; an admin reviews the pre-filled endpoint and switches it on.

            Endpoint fallbacks, in order: the config's own base URL, then the provider's documented
            public endpoint for OpenAI and Anthropic, then an explicitly unresolvable placeholder for a
            provider whose endpoint was never recorded anywhere (Azure OpenAI, whose resource host only
            ever lived in configuration). The placeholder uses the reserved .invalid TLD so it can
            never resolve, and the row it sits on is disabled. changeSet -34 reports these.

            A config that never named a model gets the same treatment: upstream_model_id is NOT NULL, so
            it takes the placeholder 'model-not-migrated' rather than being skipped. Skipping would drop
            the binding — and with it that workspace's timeout, concurrency and internet limits, which
            exist nowhere else once the table is gone — and would leave the operator no trace of which
            purposes the workspace had wired. Such a config could not run before either (the seeder warns
            about it), so the placeholder costs nothing and, being disabled, cannot call anything.
            changeSet -34 reports these too.

            api_protocol is openai-completions for every carried-over row: it is the protocol the legacy
            runtime used for OpenAI-compatible endpoints, and it is one of only two the new schema
            admits. An Anthropic endpoint does not speak it — such a row is carried forward for its key
            alone and is reported by changeSet -34 as needing a real endpoint.

            Slugs are legacy-&lt;config id&gt;: deterministic, inside the 64-character limit, and unique
            without collision handling because config ids are unique and both catalog tables are
            created empty in this same file (the precondition above enforces that).
        </comment>
        <sql splitStatements="false"><![CDATA[
            -- 1. One workspace-owned connection per legacy config that was reachable: pointed at by a
            --    workspace, OR enabled (and therefore selectable by the mentor fallback and by
            --    detection's fan-out). This is where the irreplaceable thing lives: the encrypted key.
            INSERT INTO workspace_llm_connection
                (workspace_id, slug, display_name, base_url, api_protocol, auth_mode, api_key,
                 enabled, created_at, updated_at)
            SELECT c.workspace_id,
                   'legacy-' || c.id,
                   left(c.name, 128),
                   COALESCE(
                       NULLIF(btrim(c.llm_base_url), ''),
                       CASE c.llm_provider
                           WHEN 'OPENAI'    THEN 'https://api.openai.com/v1'
                           WHEN 'ANTHROPIC' THEN 'https://api.anthropic.com/v1'
                       END,
                       'https://endpoint-not-migrated.invalid/v1'),
                   'openai-completions',
                   'BEARER',
                   c.llm_api_key,
                   false,
                   COALESCE(c.created_at, now()),
                   now()
            FROM agent_config c
            WHERE c.enabled
               OR EXISTS (
                   SELECT 1 FROM workspace w
                   WHERE w.id = c.workspace_id
                     AND (w.practice_config_id = c.id OR w.mentor_config_id = c.id));

            -- 2. One model per carried-over connection, so every carried config can carry a binding.
            --    upstream_model_id is NOT NULL; a config that never named a model takes the
            --    'model-not-migrated' placeholder rather than being skipped, because skipping it would
            --    also drop its binding and with it the execution limits, which survive nowhere else.
            --    The join to the connection is the filter — exactly the configs step 1 carried.
            --    model_version has no column here, so it is folded into the display name rather than
            --    dropped.
            INSERT INTO workspace_llm_model
                (workspace_id, connection_id, slug, display_name, upstream_model_id,
                 supports_reasoning, pricing_mode, currency, enabled, created_at, updated_at)
            SELECT c.workspace_id,
                   conn.id,
                   'legacy-' || c.id,
                   left(c.name || COALESCE(' (' || NULLIF(btrim(c.model_version), '') || ')', ''), 128),
                   COALESCE(NULLIF(btrim(c.model_name), ''), 'model-not-migrated'),
                   false,
                   'UNPRICED',
                   'USD',
                   false,
                   COALESCE(c.created_at, now()),
                   now()
            FROM agent_config c
            JOIN workspace_llm_connection conn
              ON conn.workspace_id = c.workspace_id AND conn.slug = 'legacy-' || c.id;

            -- 3. One binding per (workspace, purpose), on the config the OLD RESOLVER would have picked
            --    — not on the pointer column, which was null far more often than it was set. Execution
            --    limits carry verbatim. Disabled, per the reasoning above.
            --
            --    Practice detection: the pointed-at config when the pointer is set (enabled or not — a
            --    bound-but-disabled config paused detection, and a disabled binding is that same pause),
            --    otherwise the oldest enabled config, standing in for the fan-out that (workspace,
            --    purpose) uniqueness cannot represent. A pointer to a missing or foreign config joins to
            --    nothing and yields no binding, which is what it did before.
            INSERT INTO workspace_agent_binding
                (workspace_id, purpose, instance_model_id, workspace_model_id,
                 timeout_seconds, max_concurrent_jobs, allow_internet, enabled, created_at, updated_at)
            SELECT b.workspace_id, 'PRACTICE_DETECTION', NULL, m.id,
                   c.timeout_seconds, c.max_concurrent_jobs, c.allow_internet, false, now(), now()
            FROM (
                SELECT w.id AS workspace_id,
                       CASE
                           WHEN w.practice_config_id IS NOT NULL THEN w.practice_config_id
                           ELSE (SELECT min(x.id) FROM agent_config x
                                  WHERE x.workspace_id = w.id AND x.enabled)
                       END AS config_id
                FROM workspace w
            ) b
            JOIN agent_config c ON c.id = b.config_id AND c.workspace_id = b.workspace_id
            JOIN workspace_llm_model m ON m.workspace_id = c.workspace_id AND m.slug = 'legacy-' || c.id;

            --    Mentor: the pointed-at config only while it is enabled, otherwise the oldest enabled
            --    config — the mentor answered a user mid-conversation rather than going silent, so a
            --    disabled or dangling pointer fell through instead of pausing.
            INSERT INTO workspace_agent_binding
                (workspace_id, purpose, instance_model_id, workspace_model_id,
                 timeout_seconds, max_concurrent_jobs, allow_internet, enabled, created_at, updated_at)
            SELECT b.workspace_id, 'MENTOR', NULL, m.id,
                   c.timeout_seconds, c.max_concurrent_jobs, c.allow_internet, false, now(), now()
            FROM (
                SELECT w.id AS workspace_id,
                       COALESCE(
                           (SELECT x.id FROM agent_config x
                             WHERE x.id = w.mentor_config_id AND x.workspace_id = w.id AND x.enabled),
                           (SELECT min(x.id) FROM agent_config x
                             WHERE x.workspace_id = w.id AND x.enabled)
                       ) AS config_id
                FROM workspace w
            ) b
            JOIN agent_config c ON c.id = b.config_id AND c.workspace_id = b.workspace_id
            JOIN workspace_llm_model m ON m.workspace_id = c.workspace_id AND m.slug = 'legacy-' || c.id;
        ]]></sql>
        <rollback>
            <!-- Undo only what this changeSet inserted, identified by the legacy- slug prefix. Bindings
                 first, then models, then connections — the FKs are RESTRICT.

                 The DELETE is conditional on agent_config still being there, and the condition is the
                 whole point rather than a formality. Liquibase rolls a file back in REVERSE order, so
                 changeSet -39's rollback runs before this one — and -39's is deliberately empty,
                 meaning agent_config stays dropped. Once it is gone the legacy- rows are the ONLY
                 surviving copy of every workspace's encrypted provider key: -33 copied the ciphertext
                 out of agent_config, and -39 then destroyed the source. Deleting them then is not an
                 undo, it is the irreversible loss -33 exists to prevent.

                 So when the source table is gone this SKIPS the delete and says so, rather than either
                 destroying the keys or halting the run:
                   • Rolling the whole release back: changeSets -32, -17 and -16 drop
                     workspace_agent_binding, workspace_llm_model and workspace_llm_connection a few
                     steps later anyway, so skipping and deleting reach the identical end state — and
                     halting here would leave the release half-rolled-back, which is strictly worse.
                   • Rolling back only part of the release, to a point where those tables survive:
                     keeping the rows is the safe outcome, and re-applying -33 forward is a no-op (its
                     precondition requires an empty catalog), so the carry-over simply stands.
                 Rolling back to just after -33, with agent_config still present, undoes cleanly. -->
            <sql splitStatements="false"><![CDATA[
                DO $legacy_carryover_rollback$
                BEGIN
                    IF to_regclass('agent_config') IS NULL THEN
                        RAISE WARNING 'Hephaestus #1368: leaving the carried-over legacy-%% LLM connections, models and bindings in place while rolling back changeSet 1785015307013-33 — agent_config was already dropped by changeSet -39, so these rows hold the only copy of every workspace''s encrypted provider key. If you are rolling the whole release back they are dropped with their tables a few steps later; if you are stopping short of that, they stay and re-applying the migration leaves them untouched.';
                        RETURN;
                    END IF;

                    DELETE FROM workspace_agent_binding b
                     USING workspace_llm_model m
                     WHERE b.workspace_model_id = m.id AND m.slug LIKE 'legacy-%';
                    DELETE FROM workspace_llm_model WHERE slug LIKE 'legacy-%';
                    DELETE FROM workspace_llm_connection WHERE slug LIKE 'legacy-%';
                END
                $legacy_carryover_rollback$;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-34" author="hephaestus">
        <preConditions onFail="WARN" onFailMessage="MIGRATION NOTICE (#1368): at least one workspace AI configuration needs a human before it will run again — a provider that is not OpenAI-compatible, no endpoint URL ever recorded, no model name ever recorded, detection that ran across several configurations at once (only one binding survives), or a configuration that was both disabled and unreferenced and is therefore dropped with the old table. Every other carried-over value — endpoint, model, API key, limits — was preserved. Everything arrives disabled; open each workspace's AI settings, fix what the server log names, and re-enable. See MIGRATION.md. (Re-applying this changelog against an already-upgraded database — after Liquibase's tracking table has been cleared or rebuilt — also prints this notice, because the legacy table it inspects is gone by then; expected, and safe to ignore. An ordinary re-run skips this changeSet entirely, and a fresh install never prints it: the legacy table is created empty by an earlier changelog and has nothing to report.)">
            <and>
                <tableExists tableName="agent_config"/>
                <sqlCheck expectedResult="0">
                    SELECT (
                        (SELECT count(*) FROM agent_config c
                          WHERE (c.enabled OR EXISTS (
                                    SELECT 1 FROM workspace w
                                    WHERE w.id = c.workspace_id
                                      AND (w.practice_config_id = c.id OR w.mentor_config_id = c.id)))
                            AND (c.llm_provider &lt;&gt; 'OPENAI'
                                 OR NULLIF(btrim(c.llm_base_url), '') IS NULL
                                 OR NULLIF(btrim(c.model_name), '') IS NULL))
                      + (SELECT count(*) FROM agent_config c
                          WHERE NOT c.enabled
                            AND NOT EXISTS (
                                    SELECT 1 FROM workspace w
                                    WHERE w.id = c.workspace_id
                                      AND (w.practice_config_id = c.id OR w.mentor_config_id = c.id)))
                      + (SELECT count(*) FROM workspace w
                          WHERE w.practice_config_id IS NULL
                            AND (SELECT count(*) FROM agent_config c
                                  WHERE c.workspace_id = w.id AND c.enabled) &gt; 1)
                    )
                </sqlCheck>
            </and>
        </preConditions>
        <comment>
            Report, at deploy time, everything about the carry-over an operator has to act on rather
            than just re-enable. The precondition fails (WARN, and this changeSet then still runs) when
            any of the three cases below applies; the body writes server-log warnings naming the
            affected workspaces, because the precondition message is static and by the time an operator
            reads it agent_config is gone. The predicate mirrors changeSet -33's carry-over rule exactly
            — anything -33 could not reproduce faithfully must be named here, or it is lost in silence.

            The body re-checks that the table is there rather than trusting the precondition: onFail
            WARN executes the changeSet regardless of why the precondition failed, and "the legacy
            table no longer exists" is one of those reasons.
        </comment>
        <sql splitStatements="false"><![CDATA[
            DO $migration_report$
            DECLARE
                needs_endpoint text;
                collapsed      text;
                dropped        text;
            BEGIN
                IF to_regclass('agent_config') IS NULL THEN
                    RETURN;
                END IF;

                -- (1) Carried over, but the endpoint, the protocol or the model id is a placeholder or
                --     a guess.
                SELECT string_agg(DISTINCT w.slug, ', ' ORDER BY w.slug) INTO needs_endpoint
                FROM agent_config c
                JOIN workspace w ON w.id = c.workspace_id
                WHERE (c.enabled
                       OR w.practice_config_id = c.id
                       OR w.mentor_config_id = c.id)
                  AND (c.llm_provider <> 'OPENAI'
                       OR NULLIF(btrim(c.llm_base_url), '') IS NULL
                       OR NULLIF(btrim(c.model_name), '') IS NULL);

                IF needs_endpoint IS NOT NULL THEN
                    RAISE WARNING 'Hephaestus #1368: AI configuration carried over with a placeholder endpoint, model id or non-OpenAI protocol in these workspaces: %. Their API keys, limits and any recorded model name were preserved; set a working endpoint and model in each workspace''s AI settings before re-enabling.', needs_endpoint;
                END IF;

                -- (2) Detection ran across several enabled configurations at once; one binding per
                --     (workspace, purpose) means only the oldest becomes the binding.
                SELECT string_agg(w.slug, ', ' ORDER BY w.slug) INTO collapsed
                FROM workspace w
                WHERE w.practice_config_id IS NULL
                  AND (SELECT count(*) FROM agent_config c
                        WHERE c.workspace_id = w.id AND c.enabled) > 1;

                IF collapsed IS NOT NULL THEN
                    RAISE WARNING 'Hephaestus #1368: practice detection ran on SEVERAL configurations at once in these workspaces: %. The new model binds one model per purpose, so detection is bound to the oldest of them; the others were carried over as connections and models and can be bound to a purpose or deleted.', collapsed;
                END IF;

                -- (3) Dropped with the table: disabled AND unreferenced, so neither resolver could ever
                --     select it. Named anyway — it may hold a key the operator wants to revoke.
                SELECT string_agg(w.slug || '/' || c.name, ', ' ORDER BY w.slug || '/' || c.name) INTO dropped
                FROM agent_config c
                JOIN workspace w ON w.id = c.workspace_id
                WHERE NOT c.enabled
                  AND w.practice_config_id IS DISTINCT FROM c.id
                  AND w.mentor_config_id IS DISTINCT FROM c.id;

                IF dropped IS NOT NULL THEN
                    RAISE WARNING 'Hephaestus #1368: these disabled, unreferenced AI configurations were NOT carried over and are dropped with the old table: %. Nothing could reach them, but each may hold an API key you want to revoke at the provider.', dropped;
                END IF;
            END
            $migration_report$;
        ]]></sql>
        <rollback/>
    </changeSet>

    <changeSet id="1785015307013-35" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.purpose already present">
            <not><columnExists tableName="agent_job" columnName="purpose"/></not>
        </preConditions>
        <comment>
            Which per-purpose binding a job runs on. Every agent_job is practice detection (mentor
            turns do not use this table), so existing rows all backfill to PRACTICE_DETECTION.
        </comment>
        <addColumn tableName="agent_job">
            <column name="purpose" type="VARCHAR(32)"/>
        </addColumn>
        <sql>UPDATE agent_job SET purpose = 'PRACTICE_DETECTION' WHERE purpose IS NULL</sql>
        <rollback>
            <dropColumn tableName="agent_job" columnName="purpose"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-36" author="hephaestus" runInTransaction="false">
        <preConditions onFail="MARK_RAN" onFailMessage="ix_agent_job_running_purpose already present and valid">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pg_index i JOIN pg_class c ON c.oid = i.indexrelid
                WHERE c.relname = 'ix_agent_job_running_purpose' AND i.indisvalid
            </sqlCheck>
        </preConditions>
        <comment>Per-(workspace, purpose) RUNNING count for the poll query's concurrency-cap fairness predicate. Must follow changeSet -35: an index cannot precede the column it references. Guarded on index validity, not existence — see changeSet -21.</comment>
        <sql>
            DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_running_purpose;
            CREATE INDEX CONCURRENTLY ix_agent_job_running_purpose
                ON agent_job (workspace_id, purpose)
                WHERE status = 'RUNNING';
        </sql>
        <rollback>
            <sql>DROP INDEX CONCURRENTLY IF EXISTS ix_agent_job_running_purpose;</sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-37" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.config_id already removed">
            <columnExists tableName="agent_job" columnName="config_id"/>
        </preConditions>
        <comment>Drop agent_job.config_id — a job is attributed to a (workspace, purpose) binding now.</comment>
        <dropColumn tableName="agent_job" columnName="config_id"/>
        <rollback>
            <!-- The column's index goes with it (PostgreSQL drops dependent objects), so the rollback
                 has to put it back explicitly or it silently restores a slower schema than it found.
                 The FK to agent_config is NOT restored here: agent_config is dropped by changeSet -39,
                 whose own rollback deliberately does not recreate it. -->
            <addColumn tableName="agent_job">
                <column name="config_id" type="BIGINT"/>
            </addColumn>
            <sql>
                CREATE INDEX IF NOT EXISTS idx_agent_job_workspace_config ON agent_job (workspace_id, config_id);
            </sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-38" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace config pointers already removed">
            <columnExists tableName="workspace" columnName="practice_config_id"/>
        </preConditions>
        <comment>Drop the scalar workspace→config pointers; workspace_agent_binding carries the binding, and changeSet -33 has already read them.</comment>
        <dropColumn tableName="workspace" columnName="practice_config_id"/>
        <dropColumn tableName="workspace" columnName="mentor_config_id"/>
        <rollback>
            <!-- Restore the partial indexes that went with the columns; see changeSet -37's rollback. -->
            <addColumn tableName="workspace">
                <column name="practice_config_id" type="BIGINT"/>
                <column name="mentor_config_id" type="BIGINT"/>
            </addColumn>
            <sql splitStatements="false"><![CDATA[
                CREATE INDEX IF NOT EXISTS ix_workspace_practice_config
                    ON workspace (practice_config_id) WHERE practice_config_id IS NOT NULL;
                CREATE INDEX IF NOT EXISTS ix_workspace_mentor_config
                    ON workspace (mentor_config_id) WHERE mentor_config_id IS NOT NULL;
            ]]></sql>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-39" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_config already removed">
            <tableExists tableName="agent_config"/>
        </preConditions>
        <comment>
            Drop the retired agent_config table. Safe only because changeSet -33 has already copied its
            endpoint, model, encrypted key and execution limits into the workspace catalog. Pre-1.0, so
            this lands in one release rather than deprecate-then-remove across two.
        </comment>
        <dropTable tableName="agent_config"/>
        <rollback>
            <!-- Deliberately empty: rolling back this far restores an empty shell with no data and no
                 reader. The configuration itself survives in workspace_llm_connection /
                 workspace_llm_model / workspace_agent_binding. Roll forward instead. -->
        </rollback>
    </changeSet>

    <!-- ══════════════════════════════════════════════════════════════════════════════════════
         Workspace-owned cost control: a workspace can cap its OWN-provider spend, the same
         instrument the instance admin has over shared-model spend. Two caps rather than one because
         they govern different people's money and are never summed.
         ══════════════════════════════════════════════════════════════════════════════════════ -->

    <changeSet id="1785015307013-40" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="workspace.monthly_byo_llm_budget_usd already present">
            <not><columnExists tableName="workspace" columnName="monthly_byo_llm_budget_usd"/></not>
        </preConditions>
        <comment>The workspace's monthly cap on its own-provider LLM spend; null = uncapped.</comment>
        <addColumn tableName="workspace">
            <column name="monthly_byo_llm_budget_usd" type="NUMERIC(10,2)"/>
        </addColumn>
        <rollback>
            <dropColumn tableName="workspace" columnName="monthly_byo_llm_budget_usd"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-41" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.hold_reason already present">
            <not><columnExists tableName="agent_job" columnName="hold_reason"/></not>
        </preConditions>
        <comment>
            Why a QUEUED job's availability was pushed out, when an admin can undo it. Only 'BUDGET'
            today: raising a cap releases exactly those jobs instead of also fast-forwarding a
            crash-retry backoff into a failing upstream.
        </comment>
        <addColumn tableName="agent_job">
            <column name="hold_reason" type="VARCHAR(32)"/>
        </addColumn>
        <rollback>
            <dropColumn tableName="agent_job" columnName="hold_reason"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-42" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="fx_rate already present">
            <not><tableExists tableName="fx_rate"/></not>
        </preConditions>
        <comment>
            Daily ECB euro reference rates, for DISPLAY ONLY: USD remains the sole unit of record,
            pricing and enforcement, and nothing here is ever read by a budget gate, an admission
            decision or the usage ledger. usd_per_eur is stored in the ECB's own direction — dollars
            per ONE euro — so the historical record is exactly what was published; the inversion
            happens once, at read time. Global: an exchange rate is a property of the world, not of a
            tenant. The unique rate_date is also the arbiter when two replicas fetch at once — the
            loser's insert fails and is swallowed, rather than duplicating the day.
        </comment>
        <createTable tableName="fx_rate">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_fx_rate" nullable="false"/>
            </column>
            <column name="rate_date" type="DATE"><constraints nullable="false"/></column>
            <column name="usd_per_eur" type="NUMERIC(12,6)"><constraints nullable="false"/></column>
            <column name="fetched_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
        </createTable>
        <addUniqueConstraint tableName="fx_rate" columnNames="rate_date" constraintName="ux_fx_rate_date"/>
        <rollback><dropTable tableName="fx_rate"/></rollback>
    </changeSet>

    <changeSet id="1785015307013-43" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job.llm_cost_usd already removed">
            <columnExists tableName="agent_job" columnName="llm_cost_usd"/>
        </preConditions>
        <comment>
            Drop agent_job.llm_cost_usd — the ledger is the single accounting source, and this column
            was DOUBLE PRECISION (money in a binary float). Its values were already copied into
            llm_usage_event.cost_usd as NUMERIC(18,6) by changeSets -10/-11 above, so this must stay
            after them. Nothing reads it: the JPA mapping and the DTO field are gone.
        </comment>
        <dropColumn tableName="agent_job" columnName="llm_cost_usd"/>
        <rollback>
            <!-- Restores the column but not its values: the ledger holds them at higher precision, and
                 writing a numeric(18,6) back into a double would reintroduce the representation error
                 the drop removes. -->
            <addColumn tableName="agent_job">
                <column name="llm_cost_usd" type="DOUBLE PRECISION"/>
            </addColumn>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-44" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="chat_message usage accumulators already exist">
            <not><columnExists tableName="chat_message" columnName="llm_total_calls"/></not>
        </preConditions>
        <comment>
            Per-call usage accumulators for a mentor turn, mirroring agent_job's. The proxy meters each
            call as it is served so the budget gate can see a turn's own spend before the turn ends;
            these columns make that survive a worker crash, which an in-memory meter cannot. The
            in-flight reaper bills from them instead of booking a crashed turn as zero tokens.
        </comment>
        <addColumn tableName="chat_message">
            <column name="llm_total_calls" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="llm_total_input_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="llm_total_output_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="llm_total_reasoning_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="llm_cache_read_tokens" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="chat_message" columnName="llm_total_calls"/>
            <dropColumn tableName="chat_message" columnName="llm_total_input_tokens"/>
            <dropColumn tableName="chat_message" columnName="llm_total_output_tokens"/>
            <dropColumn tableName="chat_message" columnName="llm_total_reasoning_tokens"/>
            <dropColumn tableName="chat_message" columnName="llm_cache_read_tokens"/>
        </rollback>
    </changeSet>

    <changeSet id="1785015307013-45" author="hephaestus">
        <preConditions onFail="MARK_RAN" onFailMessage="agent_job dead columns already removed">
            <columnExists tableName="agent_job" columnName="llm_api_key"/>
        </preConditions>
        <comment>
            Drop three agent_job columns nothing reads. llm_api_key is never written — the proxy resolves
            the credential live from the catalog, so a stale copy here would only ever be a second place
            for a key to leak from. network_id and container_id are never written either; container_id
            was projected into the API, where it read as permanently null.
        </comment>
        <dropColumn tableName="agent_job" columnName="llm_api_key"/>
        <dropColumn tableName="agent_job" columnName="network_id"/>
        <dropColumn tableName="agent_job" columnName="container_id"/>
        <rollback>
            <!-- The widths are the originals (1773354889621 / 1773500736939), not a round number: a
                 rollback that silently widens a column is a rollback that did not restore the schema
                 it claims to. -->
            <addColumn tableName="agent_job">
                <column name="llm_api_key" type="TEXT"/>
                <column name="network_id" type="VARCHAR(64)"/>
                <column name="container_id" type="VARCHAR(64)"/>
            </addColumn>
        </rollback>
    </changeSet>

</databaseChangeLog>
