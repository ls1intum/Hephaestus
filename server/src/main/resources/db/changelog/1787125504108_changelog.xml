<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet author="hephaestus-delivery-policy" id="1787125504108-1">
        <preConditions onFail="MARK_RAN" onFailMessage="Practice autonomy columns already use the canonical names">
            <columnExists tableName="practice" columnName="review_tier"/>
            <columnExists tableName="practice_area" columnName="review_tier"/>
            <columnExists tableName="workspace" columnName="practice_default_review_tier"/>
            <not><columnExists tableName="practice" columnName="autonomy"/></not>
        </preConditions>
        <comment>Give practice delivery authority one explicit vocabulary across its inheritance chain.</comment>
        <sql splitStatements="true" endDelimiter=";"><![CDATA[
            ALTER TABLE practice DROP CONSTRAINT IF EXISTS chk_practice_review_tier;
            ALTER TABLE practice_area DROP CONSTRAINT IF EXISTS chk_practice_area_review_tier;
            ALTER TABLE workspace DROP CONSTRAINT IF EXISTS chk_workspace_default_review_tier;
            DROP INDEX IF EXISTS idx_practice_workspace_tier;

            ALTER TABLE practice RENAME COLUMN review_tier TO autonomy;
            ALTER TABLE practice_area RENAME COLUMN review_tier TO autonomy;
            ALTER TABLE workspace RENAME COLUMN practice_default_review_tier TO practice_default_autonomy;

            UPDATE practice SET autonomy = CASE autonomy
                WHEN 'PROPOSE' THEN 'HUMAN_APPROVAL'
                WHEN 'DELIVER' THEN 'AUTOMATIC'
                ELSE autonomy
            END;
            UPDATE practice_area SET autonomy = CASE autonomy
                WHEN 'PROPOSE' THEN 'HUMAN_APPROVAL'
                WHEN 'DELIVER' THEN 'AUTOMATIC'
                ELSE autonomy
            END;
            UPDATE workspace SET practice_default_autonomy = CASE practice_default_autonomy
                WHEN 'PROPOSE' THEN 'HUMAN_APPROVAL'
                WHEN 'DELIVER' THEN 'AUTOMATIC'
                ELSE practice_default_autonomy
            END;

            ALTER TABLE practice ADD CONSTRAINT chk_practice_autonomy
                CHECK (autonomy IS NULL OR autonomy IN ('OFF', 'HUMAN_APPROVAL', 'AUTOMATIC'));
            ALTER TABLE practice_area ADD CONSTRAINT chk_practice_area_autonomy
                CHECK (autonomy IS NULL OR autonomy IN ('OFF', 'HUMAN_APPROVAL', 'AUTOMATIC'));
            ALTER TABLE workspace ADD CONSTRAINT chk_workspace_default_autonomy
                CHECK (practice_default_autonomy IS NULL OR practice_default_autonomy IN ('OFF', 'HUMAN_APPROVAL', 'AUTOMATIC'));
            CREATE INDEX idx_practice_workspace_autonomy ON practice (workspace_id, autonomy);
        ]]></sql>
        <rollback>
            <sql splitStatements="true" endDelimiter=";"><![CDATA[
                ALTER TABLE practice DROP CONSTRAINT IF EXISTS chk_practice_autonomy;
                ALTER TABLE practice_area DROP CONSTRAINT IF EXISTS chk_practice_area_autonomy;
                ALTER TABLE workspace DROP CONSTRAINT IF EXISTS chk_workspace_default_autonomy;
                DROP INDEX IF EXISTS idx_practice_workspace_autonomy;

                UPDATE practice SET autonomy = CASE autonomy WHEN 'HUMAN_APPROVAL' THEN 'PROPOSE' WHEN 'AUTOMATIC' THEN 'DELIVER' ELSE autonomy END;
                UPDATE practice_area SET autonomy = CASE autonomy WHEN 'HUMAN_APPROVAL' THEN 'PROPOSE' WHEN 'AUTOMATIC' THEN 'DELIVER' ELSE autonomy END;
                UPDATE workspace SET practice_default_autonomy = CASE practice_default_autonomy WHEN 'HUMAN_APPROVAL' THEN 'PROPOSE' WHEN 'AUTOMATIC' THEN 'DELIVER' ELSE practice_default_autonomy END;

                ALTER TABLE practice RENAME COLUMN autonomy TO review_tier;
                ALTER TABLE practice_area RENAME COLUMN autonomy TO review_tier;
                ALTER TABLE workspace RENAME COLUMN practice_default_autonomy TO practice_default_review_tier;
                ALTER TABLE practice ADD CONSTRAINT chk_practice_review_tier CHECK (review_tier IS NULL OR review_tier IN ('OFF', 'PROPOSE', 'DELIVER'));
                ALTER TABLE practice_area ADD CONSTRAINT chk_practice_area_review_tier CHECK (review_tier IS NULL OR review_tier IN ('OFF', 'PROPOSE', 'DELIVER'));
                ALTER TABLE workspace ADD CONSTRAINT chk_workspace_default_review_tier CHECK (practice_default_review_tier IS NULL OR practice_default_review_tier IN ('OFF', 'PROPOSE', 'DELIVER'));
                CREATE INDEX idx_practice_workspace_tier ON practice (workspace_id, review_tier);
            ]]></sql>
        </rollback>
    </changeSet>
    <changeSet author="hephaestus" id="1787125504108-2">
        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="feedback_approval"/></not>
        </preConditions>
        <comment>Persist the immutable human decision for feedback that requires approval.</comment>
        <sql splitStatements="true" endDelimiter=";"><![CDATA[
            ALTER TABLE feedback ALTER COLUMN delivery_state TYPE VARCHAR(32);
            ALTER TABLE feedback DROP CONSTRAINT chk_feedback_state;
            ALTER TABLE feedback ADD CONSTRAINT chk_feedback_state CHECK (delivery_state IN ('AWAITING_APPROVAL', 'PREPARED', 'DELIVERED', 'DISCARDED', 'SUPERSEDED', 'SUPPRESSED', 'FAILED'));
            ALTER TABLE feedback DROP CONSTRAINT chk_feedback_suppression_reason;
            UPDATE feedback SET suppression_reason = 'PRACTICE_REQUIRES_APPROVAL'
                WHERE suppression_reason = 'PRACTICE_TIER_QUIET';
            ALTER TABLE feedback ADD CONSTRAINT chk_feedback_suppression_reason
                CHECK (suppression_reason IS NULL OR suppression_reason IN (
                    'VOLUME_CAPPED', 'COMPOSER_DEDUPED', 'REACTED_DISPUTED', 'REACTED_NOT_APPLICABLE',
                    'CONVERSATION_EXPIRED', 'ARTIFACT_GONE', 'ARTIFACT_CLOSED', 'ARTIFACT_MERGED',
                    'ARTIFACT_DRAFT', 'RECIPIENT_OPTED_OUT', 'EMPTY_AFTER_SANITIZE', 'INSTANCE_SILENCED',
                    'PRACTICE_REQUIRES_APPROVAL', 'BACKFILL_QUIET', 'WORKSPACE_DISABLED', 'APPROVAL_STALE'));
        ]]></sql>
        <createTable tableName="feedback_approval">
            <column name="id" type="UUID"><constraints nullable="false" primaryKey="true" primaryKeyName="pk_feedback_approval"/></column>
            <column name="feedback_id" type="UUID"><constraints nullable="false" unique="true" uniqueConstraintName="uk_feedback_approval_feedback"/></column>
            <column name="workspace_id" type="BIGINT"><constraints nullable="false"/></column>
            <column name="actor_account_id" type="BIGINT"/>
            <column name="decision" type="VARCHAR(16)"><constraints nullable="false"/></column>
            <column name="rejection_reason" type="VARCHAR(32)"/>
            <column name="content_digest" type="VARCHAR(64)"><constraints nullable="false"/></column>
            <column name="decided_at" type="TIMESTAMP WITH TIME ZONE"><constraints nullable="false"/></column>
        </createTable>
        <addForeignKeyConstraint baseTableName="feedback_approval" baseColumnNames="feedback_id"
                                 referencedTableName="feedback" referencedColumnNames="id"
                                 constraintName="fk_feedback_approval_feedback" onDelete="CASCADE"/>
        <addForeignKeyConstraint baseTableName="feedback_approval" baseColumnNames="actor_account_id"
                                 referencedTableName="account" referencedColumnNames="id"
                                 constraintName="fk_feedback_approval_actor" onDelete="SET NULL"/>
        <sql splitStatements="true" endDelimiter=";"><![CDATA[
            ALTER TABLE feedback_approval ADD CONSTRAINT ck_feedback_approval_decision
                CHECK (decision IN ('APPROVED', 'REJECTED'));
            ALTER TABLE feedback_approval ADD CONSTRAINT ck_feedback_approval_reason
                CHECK ((decision = 'APPROVED' AND rejection_reason IS NULL) OR
                       (decision = 'REJECTED' AND (rejection_reason IS NULL OR rejection_reason IN ('INCORRECT', 'MISSING_CONTEXT', 'UNHELPFUL', 'DUPLICATE', 'INAPPROPRIATE_PLACEMENT', 'OTHER'))));
            ALTER TABLE feedback_approval ADD CONSTRAINT ck_feedback_approval_digest
                CHECK (content_digest ~ '^[0-9a-f]{64}$');
        ]]></sql>
        <createIndex tableName="feedback_approval" indexName="idx_feedback_approval_workspace_decided">
            <column name="workspace_id"/><column name="decided_at" descending="true"/>
        </createIndex>
        <rollback>
            <dropTable tableName="feedback_approval"/>
            <sql splitStatements="true" endDelimiter=";"><![CDATA[
                DELETE FROM feedback WHERE delivery_state IN ('AWAITING_APPROVAL', 'DISCARDED');
                UPDATE feedback SET delivery_state = 'FAILED', suppression_reason = NULL
                    WHERE suppression_reason IN ('WORKSPACE_DISABLED', 'APPROVAL_STALE');
                ALTER TABLE feedback DROP CONSTRAINT chk_feedback_state;
                ALTER TABLE feedback ADD CONSTRAINT chk_feedback_state CHECK (delivery_state IN ('PREPARED', 'DELIVERED', 'SUPERSEDED', 'SUPPRESSED', 'FAILED'));
                ALTER TABLE feedback ALTER COLUMN delivery_state TYPE VARCHAR(16);
                ALTER TABLE feedback DROP CONSTRAINT chk_feedback_suppression_reason;
                UPDATE feedback SET suppression_reason = 'PRACTICE_TIER_QUIET'
                    WHERE suppression_reason = 'PRACTICE_REQUIRES_APPROVAL';
                ALTER TABLE feedback ADD CONSTRAINT chk_feedback_suppression_reason
                    CHECK (suppression_reason IS NULL OR suppression_reason IN (
                        'VOLUME_CAPPED', 'COMPOSER_DEDUPED', 'REACTED_DISPUTED', 'REACTED_NOT_APPLICABLE',
                        'CONVERSATION_EXPIRED', 'ARTIFACT_GONE', 'ARTIFACT_CLOSED', 'ARTIFACT_MERGED',
                        'ARTIFACT_DRAFT', 'RECIPIENT_OPTED_OUT', 'EMPTY_AFTER_SANITIZE', 'INSTANCE_SILENCED',
                        'PRACTICE_TIER_QUIET', 'BACKFILL_QUIET'));
            ]]></sql>
        </rollback>
    </changeSet>
    <changeSet author="hephaestus" id="1787125504108-3">
        <preConditions onFail="MARK_RAN">
            <columnExists tableName="workspace" columnName="practice_run_for_all_users"/>
        </preConditions>
        <comment>Remove the obsolete detection audience override now that enabled practices detect for every user.</comment>
        <dropColumn tableName="workspace" columnName="practice_run_for_all_users"/>
        <rollback>
            <addColumn tableName="workspace">
                <column name="practice_run_for_all_users" type="BOOLEAN"/>
            </addColumn>
        </rollback>
    </changeSet>
</databaseChangeLog>
