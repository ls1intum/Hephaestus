package de.tum.cit.aet.hephaestus.practices.curated;

/** Effect of replacing the effective definition with the bundled default. */
public enum CatalogChangeKind {
    NONE,
    WORDING,
    PRESENTATION,
    DETECTION,
}
