package de.tum.cit.aet.hephaestus.evidence;

/** Why a source-use entry is operationally present. This is not, by itself, a legal basis. */
public enum SourceUseBasis {
    ENGINEERING_BASELINE,
    CONTROLLER_DECISION,
}
