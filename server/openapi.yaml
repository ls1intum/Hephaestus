openapi: 3.1.0
info:
  contact:
    email: felixtj.dietrich@tum.de
    name: Felix T.J. Dietrich
  description: |-
    API documentation for the Hephaestus application server.

    ### Money and exact decimals

    Every monetary amount and per-unit rate is a JSON number carrying `format: decimal`. It is an exact decimal on the server (`BigDecimal`, backed by a `NUMERIC` column) and MUST NOT be parsed into a binary floating-point type in a language that has an exact decimal one: bind it to `BigDecimal` / `decimal` / `Decimal`, not to `double`.

    JavaScript has no exact decimal type, so the generated TypeScript client necessarily binds these to `number` (IEEE-754 binary64). That is lossless for every value this API produces, and the margin is large: a binary64 round-trips any decimal of at most 15 significant digits exactly (`DBL_DIG`). Amounts are quantised to 6 decimal places, so they are exact below **$1,000,000,000**; per-1M-token rates are quantised to 8 places, so they are exact below **$10,000,000** per 1M tokens; budget caps are quantised to 2 places and their column tops out at $99,999,999.99. Every reachable value sits orders of magnitude inside those bounds.

    What that margin does NOT license is arithmetic. Totals, remaining budget and cap verdicts are computed on the server in exact decimal and shipped as fields; a client that re-derives them by summing rows is accumulating binary rounding error the server does not have. Read the totals, do not add them up.

    Amounts are USD throughout — the ledger, the caps and the gates all meter in USD, and the currency is therefore part of the field name (`...Usd`) rather than a per-amount field. Where a response also carries `FxRateInfo`, that is a display-only estimate in a second currency and is never an input to a budget decision.
  license:
    name: MIT License
    url: https://github.com/ls1intum/Hephaestus/blob/develop/LICENSE
  title: Hephaestus API
  version: 0.0.0-development
servers:
- description: Default Server URL
  url: /
security:
- bearerAuth: []
tags:
- description: User achievement progress and unlocks
  name: Achievements
- description: Global contributor information
  name: Contributors
- description: User preferences
  name: Account
- description: Instance-admin login provider management
  name: Admin
- description: Per-workspace Slack channel consent + activation
  name: Slack channel activation
- description: Per-workspace LLM spend rollup and budget status
  name: LLM Usage
- description: Manage practice areas
  name: Practice Areas
- description: Workspace-scoped mentor chat threads
  name: Mentor Threads
- description: |-
    Workspace registry endpoints under /workspaces that operate outside of a specific workspace slug;
     slugged routes live in {@link WorkspaceController WorkspaceController} via <code>@WorkspaceScopedController</code>.
  name: workspace-registry-controller
- description: Bounded campaigns that review work which already existed
  name: Practice Review Backfill
- description: |-
    Controller for managing workspace memberships and roles.
     Handles CRUD operations for workspace members and role assignments.
  name: workspace-membership-controller
- description: Workspace-scoped per-purpose agent configuration
  name: Agents
- description: Workspace-scoped agent job monitoring
  name: Agent Jobs
- description: Asking for a review of a piece of work by hand
  name: Practice review requests
- description: "User account management (settings, deletion)"
  name: Account
- description: Read-only access to practice evaluation observations
  name: Practice Observations
- description: Instance-admin account management
  name: Admin
- description: Current user's Slack privacy preferences
  name: Slack User Preferences
- description: Per-workspace Outline collection mirroring control plane
  name: Outline collections
- description: Team management within workspace
  name: Teams
- description: The starting catalog copied into new workspaces
  name: Admin Practice Catalog
- description: The developer's own prepared practice feedback
  name: In-App Feedback
- description: "Workspace-scoped \"bring your own\" AI provider connections, models\
    \ and settings"
  name: Workspace LLM
- description: User profile and activity data
  name: Profile
- description: Instance-wide operator settings
  name: Instance Settings
- description: Contributor rankings and league statistics
  name: Leaderboard
- description: Why each practice did or did not run on one piece of work; readable
    by any workspace member
  name: Practice review trace
- description: Current instance definitions visible to workspace administrators
  name: Practice Catalog
- description: Session lifecycle
  name: Auth
- description: Workspace-scoped practice-review policy
  name: Practice Review Settings
- description: Active sessions
  name: Account
- description: |-
    GitLab-specific REST endpoints used during the workspace creation wizard. Gated on the
     <code>GITLAB_WORKSPACE_CREATION</code> feature flag.
  name: git-lab-preflight-controller
- description: Workspace integration connection management
  name: Connections
- description: Instance-admin LLM connection and settings management
  name: Admin LLM
- description: Up/down votes on mentor assistant messages
  name: Mentor Votes
- description: "Workspace-admin access to review runs, observations, and feedback\
    \ delivery history"
  name: Practice reviews
- description: Public JWK set for verifying Hephaestus session JWTs
  name: Auth discovery
- description: GDPR Art. 20 self-service data export
  name: Account
- description: Workspace-scoped history of admin configuration changes
  name: Config Audit
- description: |-
    Vendor OAuth redirect ingress at <code>/oauth/callback/{kind</code>}. Unauthenticated —
     identity comes from the HMAC-signed <code>state</code> param. Browsers get 302 redirects
     on both success and failure; only <code>Accept: application/json</code> requests get
     4xx JSON.
  name: o-auth-callback-controller
- description: Manage practice definitions
  name: Practice Catalog
- description: Workspace-scoped team settings management
  name: Team Settings
- description: Recurring reviews of recent work
  name: Practice Review Sweep
- description: Developer reactions to delivered feedback
  name: Feedback Reaction
- description: Identity provider discovery (public)
  name: Auth discovery
- description: Integration sync observability and manual controls
  name: Sync
- description: "Current user identity, linked accounts, deletion"
  name: Account
paths:
  /.well-known/jwks.json:
    get:
      operationId: getJwks
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: OK
      summary: JWK set (public keys only)
      tags:
      - Auth discovery
  /admin/audit:
    get:
      operationId: adminListAuthEvents
      parameters:
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      - in: query
        name: accountId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: actingAccountId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: eventType
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - LOGIN
            - LOGIN_FAILED
            - LOGOUT
            - TOKEN_REFRESH
            - JWT_REVOKED
            - IDENTITY_LINKED
            - IDENTITY_UNLINKED
            - IMPERSONATION_BEGIN
            - IMPERSONATION_END
            - ACCOUNT_DELETED
            - EXPORT_REQUESTED
            - APP_ROLE_CHANGED
            - RESEARCH_CONSENT_REVOKED
            - LLM_CONNECTION_CREATED
            - LLM_CONNECTION_UPDATED
            - LLM_CONNECTION_DELETED
            - LLM_MODEL_CREATED
            - LLM_MODEL_UPDATED
            - LLM_MODEL_DELETED
            - LLM_MODEL_PRICE_CHANGED
            - LLM_MODEL_SHARING_CHANGED
            - LLM_SETTINGS_CHANGED
            - LOGIN_PROVIDER_CREATED
            - LOGIN_PROVIDER_UPDATED
            - LOGIN_PROVIDER_DELETED
            - SILENT_MODE_CHANGED
      - in: query
        name: result
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - SUCCESS
            - FAILURE
      - in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageAuthEventView"
          description: OK
      summary: "List auth audit events (paged, newest first)"
      tags:
      - Admin
  /admin/audit/export:
    get:
      operationId: adminExportAuthEvents
      parameters:
      - in: query
        name: accountId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: actingAccountId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: eventType
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - LOGIN
            - LOGIN_FAILED
            - LOGOUT
            - TOKEN_REFRESH
            - JWT_REVOKED
            - IDENTITY_LINKED
            - IDENTITY_UNLINKED
            - IMPERSONATION_BEGIN
            - IMPERSONATION_END
            - ACCOUNT_DELETED
            - EXPORT_REQUESTED
            - APP_ROLE_CHANGED
            - RESEARCH_CONSENT_REVOKED
            - LLM_CONNECTION_CREATED
            - LLM_CONNECTION_UPDATED
            - LLM_CONNECTION_DELETED
            - LLM_MODEL_CREATED
            - LLM_MODEL_UPDATED
            - LLM_MODEL_DELETED
            - LLM_MODEL_PRICE_CHANGED
            - LLM_MODEL_SHARING_CHANGED
            - LLM_SETTINGS_CHANGED
            - LOGIN_PROVIDER_CREATED
            - LOGIN_PROVIDER_UPDATED
            - LOGIN_PROVIDER_DELETED
            - SILENT_MODE_CHANGED
      - in: query
        name: result
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - SUCCESS
            - FAILURE
      - in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            text/csv:
              schema:
                type: string
          description: OK
      summary: "Export the filtered audit log as CSV (newest first, capped)"
      tags:
      - Admin
  /admin/config-audit:
    get:
      operationId: adminListConfigAuditEvents
      parameters:
      - in: query
        name: workspaceId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      - in: query
        name: entityType
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - PRACTICE_REVIEW_SETTINGS
            - AGENT_BINDING
            - AGENT_CONFIG
            - AI_CONFIG_BINDING
            - WORKSPACE_ROLE
            - WORKSPACE_FEATURES
            - WORKSPACE_STATUS
            - WORKSPACE_TOKEN
            - WORKSPACE_VISIBILITY
            - PRACTICE_ACTIVE
            - PRACTICE_USAGE
            - PRACTICE_DEFINITION
            - PRACTICE_AREA
            - CURATED_PRACTICE
            - CURATED_PRACTICE_AREA
            - WORKSPACE_INSTANCE_LLM_BUDGET
            - WORKSPACE_OWN_PROVIDER_LLM_BUDGET
            - WORKSPACE_LLM_BUDGET
            - WORKSPACE_BYO_LLM_BUDGET
            - REVIEW_BACKFILL_RUN
            - REVIEW_SWEEP_SCHEDULE
            - WORKSPACE_LLM_CONNECTION
            - WORKSPACE_LLM_MODEL
      - in: query
        name: entityId
        required: false
        schema:
          type: string
      - in: query
        name: changedKey
        required: false
        schema:
          type: string
      - in: query
        name: action
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - CREATED
            - UPDATED
            - DELETED
      - in: query
        name: actorId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageConfigAuditEntryView"
          description: OK
      summary: "List admin configuration changes across workspaces (paged, newest\
        \ first)"
      tags:
      - Admin
  /admin/llm/connections:
    get:
      operationId: adminListLlmConnections
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LlmConnection"
          description: OK
      summary: List LLM connections
      tags:
      - Admin LLM
    post:
      operationId: adminCreateLlmConnection
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLlmConnectionRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmConnection"
          description: Connection created; URL in the Location header
        "409":
          description: An LLM connection with this slug already exists
      summary: Create an LLM connection
      tags:
      - Admin LLM
  /admin/llm/connections/probe:
    post:
      operationId: adminProbeLlmConnectionDraft
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProbeLlmConnectionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmProbeResult"
          description: OK
      summary: Test a draft connection and fetch its models
      tags:
      - Admin LLM
  /admin/llm/connections/{connectionId}/models:
    post:
      operationId: adminCreateLlmModel
      parameters:
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLlmModelRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmModel"
          description: Model created; URL in the Location header
        "404":
          description: LLM connection not found
        "409":
          description: A model with this slug or upstream model id already exists
            on the connection
      summary: Create a model on an LLM connection
      tags:
      - Admin LLM
  /admin/llm/connections/{id}:
    delete:
      operationId: adminDeleteLlmConnection
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Connection deleted
        "404":
          description: LLM connection not found
        "409":
          description: Cannot delete a connection still referenced by one or more
            models
      summary: Delete an LLM connection
      tags:
      - Admin LLM
    get:
      operationId: adminGetLlmConnection
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmConnection"
          description: OK
        "404":
          description: LLM connection not found
      summary: Get an LLM connection
      tags:
      - Admin LLM
    patch:
      operationId: adminUpdateLlmConnection
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmConnectionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmConnection"
          description: OK
        "404":
          description: LLM connection not found
      summary: Update an LLM connection
      tags:
      - Admin LLM
  /admin/llm/connections/{id}/probe:
    post:
      operationId: adminProbeLlmConnection
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmProbeResult"
          description: OK
      summary: Test a stored connection and fetch its models
      tags:
      - Admin LLM
  /admin/llm/models:
    get:
      operationId: adminListLlmModels
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LlmModel"
          description: OK
      summary: List LLM catalog models
      tags:
      - Admin LLM
  /admin/llm/models/{id}:
    delete:
      operationId: adminDeleteLlmModel
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Model deleted
        "404":
          description: LLM model not found
        "409":
          description: Cannot delete a model still bound to an agent configuration
      summary: Delete an LLM catalog model
      tags:
      - Admin LLM
    get:
      operationId: adminGetLlmModel
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmModel"
          description: OK
        "404":
          description: LLM model not found
      summary: Get an LLM catalog model
      tags:
      - Admin LLM
    patch:
      operationId: adminUpdateLlmModel
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmModelRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmModel"
          description: OK
        "404":
          description: LLM model not found
        "409":
          description: Another model on the connection already uses this upstream
            model id
      summary: Update a model's metadata
      tags:
      - Admin LLM
  /admin/llm/models/{id}/price:
    put:
      operationId: adminUpdateLlmModelPrice
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmModelPriceRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmModel"
          description: OK
        "404":
          description: LLM model not found
      summary: Reprice a model
      tags:
      - Admin LLM
  /admin/llm/models/{id}/sharing:
    put:
      operationId: adminUpdateLlmModelSharing
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmModelSharingRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LlmModel"
          description: OK
        "404":
          description: LLM model not found
      summary: Share a model with all or selected workspaces
      tags:
      - Admin LLM
  /admin/llm/settings:
    get:
      operationId: adminGetLlmSettings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstanceLlmSettings"
          description: OK
      summary: Get instance-wide LLM governance settings
      tags:
      - Admin LLM
    put:
      operationId: adminUpdateLlmSettings
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInstanceLlmSettingsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstanceLlmSettings"
          description: OK
      summary: Update instance-wide LLM governance settings
      tags:
      - Admin LLM
  /admin/llm/usage:
    get:
      operationId: adminGetLlmUsageReport
      parameters:
      - in: query
        name: month
        required: false
        schema:
          type: string
          pattern: "\\d{4}-(0[1-9]|1[0-2])"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminLlmUsageReport"
          description: OK
      summary: Per-workspace LLM spend rollup for one month (all workspaces)
      tags:
      - Admin
  /admin/login-providers:
    get:
      operationId: adminListLoginProviders
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LoginProviderView"
          description: OK
      summary: List login providers
      tags:
      - Admin
    post:
      operationId: adminCreateLoginProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLoginProviderRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginProviderView"
          description: Login provider created; URL in the Location header
      summary: Create a login provider
      tags:
      - Admin
  /admin/login-providers/{registrationId}:
    delete:
      operationId: adminDeleteLoginProvider
      parameters:
      - in: path
        name: registrationId
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: Delete a login provider
      tags:
      - Admin
    patch:
      operationId: adminUpdateLoginProvider
      parameters:
      - in: path
        name: registrationId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLoginProviderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginProviderView"
          description: OK
      summary: Update a login provider
      tags:
      - Admin
  /admin/practice-catalog:
    get:
      description: "Practice summaries, complete areas, ordering, and catalog state.\
        \ Fetch a practice for its full definition."
      operationId: adminGetCuratedCatalog
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: OK
      summary: Read the instance catalog
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas:
    post:
      operationId: adminCreateCuratedArea
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCuratedAreaRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedArea"
          description: Area added
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Slug already exists
      summary: Add an area to the catalog
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas/reorder:
    patch:
      operationId: adminReorderCuratedAreas
      parameters:
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderPracticeAreasRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: OK
      summary: Reorder catalog areas
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas/{slug}:
    get:
      operationId: adminGetCuratedArea
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedArea"
          description: The area
      summary: Read a catalog area
      tags:
      - Admin Practice Catalog
    put:
      operationId: adminUpdateCuratedArea
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CuratedAreaRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedArea"
          description: The area
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Replace an area definition
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas/{slug}/override:
    delete:
      operationId: adminDeleteCuratedAreaOverride
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedArea"
          description: The area
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Hephaestus ships no definition for this slug
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Use the Hephaestus definition of an area
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas/{slug}/override/acknowledgement:
    put:
      operationId: adminKeepCuratedArea
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedArea"
          description: The area
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Keep the saved area customization
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/areas/{slug}/status:
    patch:
      description: Excluding an area also excludes its practices from new workspaces;
        existing workspaces do not change.
      operationId: adminUpdateCuratedAreaStatus
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCuratedStatusRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: The updated catalog
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: "Exclude an area from new workspaces, or include it again"
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/definition-options:
    get:
      description: "Returns available review events, recommended requirements, and\
        \ allowed evidence sources by work type"
      operationId: adminGetPracticeDefinitionOptions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeDefinitionOptions"
          description: OK
      summary: Read practice definition options
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/order:
    delete:
      operationId: adminResetCuratedCatalogOrder
      parameters:
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: OK
      summary: Use the Hephaestus default order
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices:
    post:
      operationId: adminCreateCuratedPractice
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCuratedPracticeRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: Practice added
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Slug already exists
      summary: Add a practice to the catalog
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/reorder:
    patch:
      operationId: adminReorderCuratedPractices
      parameters:
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderPracticesRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: OK
      summary: Reorder practices within one catalog area
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/{slug}:
    get:
      operationId: adminGetCuratedPractice
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: The practice
      summary: Read a catalog practice
      tags:
      - Admin Practice Catalog
    put:
      operationId: adminUpdateCuratedPractice
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CuratedPracticeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: The practice
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Replace a practice definition
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/{slug}/override:
    delete:
      description: "Discards the customization, so the practice follows the Hephaestus\
        \ default again."
      operationId: adminDeleteCuratedPracticeOverride
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: The practice
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Hephaestus ships no definition for this slug
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Use the Hephaestus definition of a practice
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/{slug}/override/acknowledgement:
    put:
      description: Records that the Hephaestus update was reviewed and keeps the saved
        definition.
      operationId: adminKeepCuratedPractice
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: The practice
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: Keep the saved practice customization
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/{slug}/placement:
    patch:
      operationId: adminPlaceCuratedPractice
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlacePracticeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedCatalog"
          description: OK
      summary: Move a practice to another catalog area
      tags:
      - Admin Practice Catalog
  /admin/practice-catalog/practices/{slug}/status:
    patch:
      operationId: adminUpdateCuratedPracticeStatus
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      - in: header
        name: If-Match
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCuratedStatusRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: The practice
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required
      summary: "Exclude a practice from new workspaces, or include it again"
      tags:
      - Admin Practice Catalog
  /admin/settings:
    get:
      operationId: adminGetInstanceSettings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstanceSettings"
          description: OK
      summary: Get instance settings
      tags:
      - Instance Settings
  /admin/settings/silent-mode:
    patch:
      operationId: adminUpdateSilentMode
      parameters:
      - description: Current settings ETag; required when releasing Silent Mode
        in: header
        name: If-Match
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSilentModeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstanceSettings"
          description: Silent Mode updated
        "412":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The supplied ETag is stale
        "428":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: If-Match is required when releasing Silent Mode
      summary: Engage or release the instance-wide silent mode
      tags:
      - Instance Settings
  /admin/users:
    get:
      operationId: adminListUsers
      parameters:
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminAccountView"
          description: OK
      summary: List accounts (paged)
      tags:
      - Admin
  /admin/users/{id}:
    patch:
      operationId: adminUpdateUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAccountView"
          description: OK
      summary: Update an account's app role
      tags:
      - Admin
  /admin/users/{id}/sessions:
    delete:
      operationId: adminRevokeUserSessions
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RevokeSessionsResult"
          description: OK
      summary: "Force sign-out: revoke all of an account's active sessions"
      tags:
      - Admin
  /admin/workspaces:
    get:
      operationId: adminListWorkspaces
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminWorkspaceView"
          description: OK
      summary: List all workspaces (metadata only)
      tags:
      - Admin
  /admin/workspaces/{workspaceSlug}/llm/budget:
    put:
      operationId: adminUpdateWorkspaceLlmBudget
      parameters:
      - in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmBudgetRequest"
        required: true
      responses:
        "204":
          description: Cap updated
      summary: Set or clear a workspace's monthly cap on host-funded LLM spend
      tags:
      - Admin
  /auth/impersonate:
    post:
      operationId: impersonate
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImpersonateRequest"
        required: true
      responses:
        "200":
          description: OK
      summary: Begin impersonating another account
      tags:
      - Auth
  /auth/impersonate:exit:
    post:
      operationId: exitImpersonation
      responses:
        "200":
          description: OK
      summary: "Exit impersonation, restore operator session"
      tags:
      - Auth
  /auth/logout:
    post:
      operationId: logout
      responses:
        "200":
          description: OK
      summary: Log out — revoke the current token + clear the cookie
      tags:
      - Auth
  /auth/refresh:
    post:
      operationId: refresh
      responses:
        "200":
          description: OK
      summary: "Rotate the access token (new jti, old revoked)"
      tags:
      - Auth
  /contributors:
    get:
      description: Returns contributors to the Hephaestus project (for the About page)
      operationId: listGlobalContributors
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Contributor"
          description: list of contributors sorted by contribution count
      security: []
      summary: List global contributors
      tags:
      - Contributors
  /identity-providers:
    get:
      operationId: listIdentityProviders
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IdentityProviderView"
          description: OK
      summary: List available identity providers
      tags:
      - Auth discovery
  /oauth/callback/{kind}:
    get:
      operationId: callbackGet
      parameters:
      - in: path
        name: kind
        required: true
        schema:
          type: string
      - in: query
        name: state
        required: false
        schema:
          type: string
      - in: query
        name: error
        required: false
        schema:
          type: string
      - in: query
        name: error_description
        required: false
        schema:
          type: string
      - in: query
        name: allParams
        required: true
        schema:
          type: object
          additionalProperties:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
          description: OK
      tags:
      - o-auth-callback
    post:
      operationId: callbackPost
      parameters:
      - in: path
        name: kind
        required: true
        schema:
          type: string
      - in: query
        name: state
        required: false
        schema:
          type: string
      - in: query
        name: error
        required: false
        schema:
          type: string
      - in: query
        name: error_description
        required: false
        schema:
          type: string
      - in: query
        name: allParams
        required: true
        schema:
          type: object
          additionalProperties:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
          description: OK
      tags:
      - o-auth-callback
  /user:
    delete:
      operationId: deleteCurrentUser
      parameters:
      - in: header
        name: X-Confirm-Delete
        required: false
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: Delete the current account (GDPR Art. 17)
      tags:
      - Account
    get:
      operationId: getCurrentUser
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentUserView"
          description: OK
      summary: Get the current user
      tags:
      - Account
  /user/exports:
    post:
      operationId: requestDataExport
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExportCreated"
          description: OK
      summary: Request a data export (async)
      tags:
      - Account
  /user/exports/{id}:
    get:
      operationId: getDataExportStatus
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExportStatus"
          description: OK
      summary: Get data-export status
      tags:
      - Account
  /user/exports/{id}/download:
    get:
      operationId: downloadDataExport
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
                format: byte
          description: OK
      summary: Download the data-export bundle
      tags:
      - Account
  /user/features:
    get:
      description: Returns all feature flags evaluated for the authenticated user.
        Combines role checks and server-side config toggles.
      operationId: getUserFeatures
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureFlags"
          description: Feature flags evaluated successfully
      summary: Get feature flags for the current user
      tags:
      - Account
  /user/identities:
    get:
      operationId: listLinkedIdentities
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IdentityView"
          description: OK
      summary: List linked identity providers
      tags:
      - Account
  /user/identities/{id}:
    delete:
      operationId: unlinkIdentity
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Identity unlinked
        "404":
          description: No such identity on the current account
        "409":
          description: Cannot unlink the account's only remaining sign-in method
      summary: Unlink one of the current user's linked identity providers
      tags:
      - Account
  /user/sessions:
    delete:
      operationId: revokeOtherSessions
      responses:
        "200":
          description: OK
      summary: Revoke all sessions except the current one
      tags:
      - Account
    get:
      operationId: listSessions
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SessionView"
          description: OK
      summary: List active sessions for the current user
      tags:
      - Account
  /user/sessions/{jti}:
    delete:
      operationId: revokeSession
      parameters:
      - in: path
        name: jti
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: OK
      summary: Revoke a single session
      tags:
      - Account
  /user/settings:
    get:
      operationId: getUserSettings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserSettings"
          description: OK
      summary: Get user settings
      tags:
      - Account
    post:
      operationId: updateUserSettings
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserSettings"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserSettings"
          description: OK
      summary: Update user settings
      tags:
      - Account
  /user/slack/preferences:
    get:
      operationId: getSlackUserPreferences
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlackUserPreferences"
          description: OK
      summary: Get current user's Slack preferences
      tags:
      - Slack User Preferences
  /workspaces:
    get:
      operationId: listWorkspaces
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceListItem"
          description: Workspace list
      summary: List all workspaces
      tags:
      - workspace-registry
    post:
      operationId: createWorkspace
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace created
      summary: Create a new workspace
      tags:
      - workspace-registry
  /workspaces/gitlab/groups:
    post:
      operationId: listGitLabGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GitLabPreflightRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GitLabGroup"
          description: Accessible groups
      summary: List GitLab groups accessible to a PAT
      tags:
      - git-lab-preflight
  /workspaces/gitlab/preflight:
    post:
      operationId: gitLabPreflight
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GitLabPreflightRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GitLabPreflightResponse"
          description: Validation result
      summary: Validate a GitLab PAT before workspace creation
      tags:
      - git-lab-preflight
  /workspaces/providers:
    get:
      description: Returns available workspace providers with their configuration.
        Public endpoint — no authentication required.
      operationId: getProviders
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceProviders"
          description: OK
      security: []
      summary: List available workspace creation providers
      tags:
      - workspace-registry
  /workspaces/{workspaceSlug}:
    delete:
      operationId: purgeWorkspace
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "204":
          description: Workspace purged
      summary: Permanently delete a workspace and purge its data
      tags:
      - workspace
    get:
      operationId: getWorkspace
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace returned
      security: []
      summary: Fetch a workspace by slug
      tags:
      - workspace
  /workspaces/{workspaceSlug}/agents:
    get:
      operationId: listAgents
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AgentBinding"
          description: Bindings returned
      summary: List the workspace's agents and how each is configured
      tags:
      - Agents
  /workspaces/{workspaceSlug}/agents/jobs:
    get:
      operationId: listAgentJobs
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: Filter by job status
        in: query
        name: status
        required: false
        schema:
          type: string
          enum:
          - QUEUED
          - RUNNING
          - COMPLETED
          - FAILED
          - TIMED_OUT
          - CANCELLED
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 20
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageAgentJob"
          description: Paginated job list
      summary: List agent jobs for a workspace
      tags:
      - Agent Jobs
  /workspaces/{workspaceSlug}/agents/jobs/{jobId}:
    get:
      operationId: getAgentJob
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: jobId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentJob"
          description: Job detail returned
        "404":
          description: Job not found in this workspace
      summary: Get agent job details
      tags:
      - Agent Jobs
  /workspaces/{workspaceSlug}/agents/jobs/{jobId}/cancel:
    post:
      operationId: cancelAgentJob
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: jobId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentJob"
          description: Job cancelled
        "404":
          description: Job not found in this workspace
        "409":
          description: Job already in terminal state
      summary: Cancel an agent job
      tags:
      - Agent Jobs
  /workspaces/{workspaceSlug}/agents/jobs/{jobId}/delivery/retry:
    post:
      operationId: retryAgentJobDelivery
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: jobId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentJob"
          description: Delivery retried
        "404":
          description: Job not found in this workspace
        "409":
          description: Job not in a retryable state
      summary: Retry delivery for a completed agent job
      tags:
      - Agent Jobs
  /workspaces/{workspaceSlug}/agents/{purpose}:
    delete:
      operationId: deleteAgent
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: purpose
        required: true
        schema:
          type: string
          enum:
          - PRACTICE_REVIEW
          - MENTOR
      responses:
        "204":
          description: Binding removed
      summary: Remove the agent for one purpose (turn it off)
      tags:
      - Agents
    put:
      operationId: configureAgent
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: purpose
        required: true
        schema:
          type: string
          enum:
          - PRACTICE_REVIEW
          - MENTOR
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentBindingRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentBinding"
          description: Binding saved
        "404":
          description: Model not found
      summary: Configure the agent for one purpose
      tags:
      - Agents
  /workspaces/{workspaceSlug}/config-audit:
    get:
      operationId: listWorkspaceConfigAuditEvents
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      - in: query
        name: entityType
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - PRACTICE_REVIEW_SETTINGS
            - AGENT_BINDING
            - AGENT_CONFIG
            - AI_CONFIG_BINDING
            - WORKSPACE_ROLE
            - WORKSPACE_FEATURES
            - WORKSPACE_STATUS
            - WORKSPACE_TOKEN
            - WORKSPACE_VISIBILITY
            - PRACTICE_ACTIVE
            - PRACTICE_USAGE
            - PRACTICE_DEFINITION
            - PRACTICE_AREA
            - CURATED_PRACTICE
            - CURATED_PRACTICE_AREA
            - WORKSPACE_INSTANCE_LLM_BUDGET
            - WORKSPACE_OWN_PROVIDER_LLM_BUDGET
            - WORKSPACE_LLM_BUDGET
            - WORKSPACE_BYO_LLM_BUDGET
            - REVIEW_BACKFILL_RUN
            - REVIEW_SWEEP_SCHEDULE
            - WORKSPACE_LLM_CONNECTION
            - WORKSPACE_LLM_MODEL
      - in: query
        name: entityId
        required: false
        schema:
          type: string
      - in: query
        name: changedKey
        required: false
        schema:
          type: string
      - in: query
        name: action
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - CREATED
            - UPDATED
            - DELETED
      - in: query
        name: actorId
        required: false
        schema:
          type: integer
          format: int64
      - in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageConfigAuditEntryView"
          description: OK
      summary: "List this workspace's admin configuration changes (paged, newest first)"
      tags:
      - Config Audit
  /workspaces/{workspaceSlug}/connections:
    get:
      operationId: list
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConnectionSummary"
          description: OK
      tags:
      - Connections
    post:
      operationId: initiate
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitiateConnectionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InitiateConnectionResponse"
          description: OK
      tags:
      - Connections
  /workspaces/{workspaceSlug}/connections/catalog:
    get:
      operationId: getIntegrationCatalog
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IntegrationCatalogEntry"
          description: OK
      summary: "Every integration kind this workspace could connect, joined against\
        \ existing connections"
      tags:
      - Sync
  /workspaces/{workspaceSlug}/connections/outline/token:
    get:
      description: "Probes Outline directly. Reports whether the token is still accepted\
        \ and, when the token may list its own key, its name, expiry and last use.\
        \ Outline cannot rotate a key from a key, so an expiring token is renewed\
        \ in Outline and re-entered here."
      operationId: getOutlineTokenStatus
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutlineTokenStatus"
          description: Token state returned
        "404":
          description: The workspace has no ACTIVE Outline connection
      summary: Live state of the API token behind this workspace's Outline connection
      tags:
      - Connections
  /workspaces/{workspaceSlug}/connections/slack/test-message:
    post:
      description: |-
        Probe the Slack connection by posting a test message. This is a <em>probe</em>: every outcome
         — success, a Slack-side rejection (e.g. <code>not_in_channel</code>), or a missing channel — is a
         200 result carrying <code>ok</code>/<code>slackError</code>, never an HTTP error. That lets the admin UI
         test a typed-but-not-yet-saved channel and render the Slack error inline without conflating it
         with a transport failure.
      operationId: sendSlackTestMessage
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SlackTestMessageRequest"
        description: "optional channel override; when blank, the persisted notification\
          \ channel is used."
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlackTestMessageResponse"
          description: OK
      summary: Post a test message to verify the Slack connection
      tags:
      - Connections
  /workspaces/{workspaceSlug}/connections/{connectionId}/sync:
    get:
      operationId: getConnectionSyncStatus
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectionSyncStatus"
          description: OK
      summary: Unified sync status for one connection
      tags:
      - Sync
  /workspaces/{workspaceSlug}/connections/{connectionId}/sync/jobs:
    get:
      operationId: listConnectionSyncJobs
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 20
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageSyncJob"
          description: OK
      summary: Paginated sync job history for one connection
      tags:
      - Sync
    post:
      description: |-
        Trigger a manual sync. Idempotent-absorb: a connection that already has an active job answers
         200 with that job rather than erroring, so a double-click "Sync now" is harmless.
      operationId: triggerSyncJob
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriggerSyncJobRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncJob"
          description: "Idempotent-absorb: a same-type job was already running and\
            \ is returned unchanged"
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncJob"
          description: A new sync job was created and dispatched
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Missing or invalid request body (e.g. absent sync type)
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Connection not found in this workspace
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "Connection is not ACTIVE, a different sync type is already\
            \ running, or manual sync is unsupported for the kind"
        "503":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The server is too busy to dispatch the sync; retry later
      summary: Trigger a manual sync or backfill
      tags:
      - Sync
  /workspaces/{workspaceSlug}/connections/{connectionId}/sync/jobs/{jobId}:
    patch:
      operationId: updateConnectionSyncJob
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      - in: path
        name: jobId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSyncJobRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncJob"
          description: Cancellation requested; the running job stops cooperatively
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The update does not request cancellation
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "Job not found in this workspace, or not owned by this connection"
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Job is already in a terminal status
      summary: Update a running sync job
      tags:
      - Sync
  /workspaces/{workspaceSlug}/connections/{connectionId}/sync/resources:
    get:
      operationId: listConnectionSyncResources
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SyncResourceState"
          description: OK
      summary: Per-resource sync state (repos / channels / collections) for one connection
      tags:
      - Sync
  /workspaces/{workspaceSlug}/connections/{id}:
    get:
      operationId: read
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectionDetail"
          description: OK
      tags:
      - Connections
  /workspaces/{workspaceSlug}/connections/{id}/audit:
    get:
      operationId: audit
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ConnectionAuditEntry"
          description: OK
      tags:
      - Connections
  /workspaces/{workspaceSlug}/connections/{id}/status:
    patch:
      description: |-
        Resource-oriented lifecycle transition: <code>ACTIVE</code> (reactivate), <code>SUSPENDED</code>
         (suspend), or <code>UNINSTALLED</code> (disconnect — also best-effort revokes the vendor token).
         <code>PENDING</code> is internal to the OAuth handshake and rejected as a bad request; illegal
         transitions surface as 400 via the state machine.
      operationId: updateConnectionStatus
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateConnectionStatusRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectionSummary"
          description: OK
      summary: |-
        Resource-oriented lifecycle transition: <code>ACTIVE</code> (reactivate), <code>SUSPENDED</code>
         (suspend), or <code>UNINSTALLED</code> (disconnect — also best-effort revokes the vendor token).
      tags:
      - Connections
  /workspaces/{workspaceSlug}/features:
    patch:
      operationId: updateFeatures
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceFeaturesRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update workspace feature flags
      tags:
      - workspace
  /workspaces/{workspaceSlug}/leaderboard:
    get:
      description: Creates a ranked contributor list for the specified time range
      operationId: getLeaderboard
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: start of the time range (inclusive)
        in: query
        name: after
        required: true
        schema:
          type: string
          format: date-time
      - description: end of the time range (exclusive)
        in: query
        name: before
        required: true
        schema:
          type: string
          format: date-time
      - description: Team filter to apply in INDIVIDUAL mode; ignored when mode is
          TEAM.
        in: query
        name: team
        required: true
        schema:
          type: string
          minLength: 1
      - description: Determines the ranking metric. In TEAM mode SCORE uses summed
          contribution scores; LEAGUE_POINTS uses total league points.
        in: query
        name: sort
        required: true
        schema:
          type: string
          enum:
          - SCORE
          - LEAGUE_POINTS
      - description: aggregation mode (INDIVIDUAL or TEAM)
        in: query
        name: mode
        required: true
        schema:
          type: string
          enum:
          - INDIVIDUAL
          - TEAM
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaderboardEntry"
          description: ranked list of leaderboard entries
      security: []
      summary: Generate leaderboard
      tags:
      - Leaderboard
  /workspaces/{workspaceSlug}/leaderboard-digest:
    patch:
      operationId: updateLeaderboardDigest
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLeaderboardDigestRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update the whole weekly leaderboard digest config (schedule + notifications)
        atomically
      tags:
      - workspace
  /workspaces/{workspaceSlug}/leaderboard/users/{login}/league-stats:
    get:
      description: Computes projected league point changes for a specific user using
        the global leaderboard
      operationId: computeUserLeagueStats
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the user's GitHub login
        in: path
        name: login
        required: true
        schema:
          type: string
      - description: start of the time range (inclusive)
        in: query
        name: after
        required: true
        schema:
          type: string
          format: date-time
      - description: end of the time range (exclusive)
        in: query
        name: before
        required: true
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueChange"
          description: league change statistics including projected point delta
      security: []
      summary: Calculate user league stats
      tags:
      - Leaderboard
  /workspaces/{workspaceSlug}/league/reset:
    put:
      operationId: resetAndRecalculateLeagues
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          description: OK
      summary: Reset and recalculate workspace leagues
      tags:
      - workspace
  /workspaces/{workspaceSlug}/llm/available-models:
    get:
      operationId: workspaceListAvailableLlmModels
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvailableLlmModel"
          description: OK
      summary: List models this workspace can bind a Task to (shared + your own)
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/budget:
    put:
      operationId: updateWorkspaceLlmBudget
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLlmBudgetRequest"
        required: true
      responses:
        "204":
          description: Cap updated
      summary: Set or clear this workspace's monthly cap on its own-provider LLM spend
      tags:
      - LLM Usage
  /workspaces/{workspaceSlug}/llm/connections:
    get:
      operationId: workspaceListLlmConnections
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceLlmConnection"
          description: OK
      summary: List your AI provider connections
      tags:
      - Workspace LLM
    post:
      operationId: workspaceCreateLlmConnection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceLlmConnectionRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmConnection"
          description: Connection created; URL in the Location header
        "409":
          description: A connection with this slug already exists in this workspace
      summary: Connect your own AI provider
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/connections/{connectionId}/models:
    post:
      operationId: workspaceCreateLlmModel
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: connectionId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceLlmModelRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmModel"
          description: Model created; URL in the Location header
        "404":
          description: AI provider connection not found
        "409":
          description: A model with this slug or upstream model id already exists
            on the connection
      summary: Create a model on your AI provider
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/connections/{id}:
    delete:
      operationId: workspaceDeleteLlmConnection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Connection removed
        "404":
          description: Connection not found
        "409":
          description: Cannot delete a connection still referenced by one or more
            models
      summary: Remove your AI provider connection
      tags:
      - Workspace LLM
    get:
      operationId: workspaceGetLlmConnection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmConnection"
          description: OK
        "404":
          description: Connection not found
      summary: Get one of your AI provider connections
      tags:
      - Workspace LLM
    patch:
      operationId: workspaceUpdateLlmConnection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceLlmConnectionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmConnection"
          description: OK
        "404":
          description: Connection not found
      summary: Update your AI provider connection
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/connections/{id}/probe:
    post:
      operationId: workspaceProbeLlmConnection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmProbeResult"
          description: OK
      summary: Test your AI provider connection
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/models:
    get:
      operationId: workspaceListLlmModels
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceLlmModel"
          description: OK
      summary: List models on your AI provider
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/models/{id}:
    delete:
      operationId: workspaceDeleteLlmModel
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Model removed
        "404":
          description: Model not found
        "409":
          description: Cannot delete a model still bound to an agent configuration
      summary: Remove a model on your AI provider
      tags:
      - Workspace LLM
    get:
      operationId: workspaceGetLlmModel
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmModel"
          description: OK
        "404":
          description: Model not found
      summary: Get a model on your AI provider
      tags:
      - Workspace LLM
    patch:
      operationId: workspaceUpdateLlmModel
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceLlmModelRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmModel"
          description: OK
        "404":
          description: Model not found
        "409":
          description: Another model on the connection already uses this upstream
            model id
      summary: Update a model on your AI provider
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/settings:
    get:
      operationId: workspaceGetLlmSettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmSettings"
          description: OK
      summary: Get the instance LLM policy as it applies to this workspace
      tags:
      - Workspace LLM
  /workspaces/{workspaceSlug}/llm/usage:
    get:
      operationId: getLlmUsageReport
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: month
        required: false
        schema:
          type: string
          pattern: "\\d{4}-(0[1-9]|1[0-2])"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceLlmUsageReport"
          description: OK
      summary: Get the workspace's LLM usage report for one month
      tags:
      - LLM Usage
  /workspaces/{workspaceSlug}/members:
    get:
      description: |-
        List all members of the workspace with pagination.
         Accessible to all workspace members (MEMBER role and above).
      operationId: listMembers
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: Page number (0-indexed)
        in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - description: "Page size (default 50, max 100)"
        in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorkspaceMembership"
          description: List of workspace memberships
      security: []
      summary: List all members of the workspace with pagination.
      tags:
      - workspace-membership
  /workspaces/{workspaceSlug}/members/assign:
    post:
      description: |-
        Assign or update a role for a workspace member.
         OWNER can assign any role. ADMIN can assign ADMIN or MEMBER roles.
      operationId: assignRole
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignRoleRequest"
        description: Role assignment request
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMembership"
          description: Updated membership
      summary: Assign or update a role for a workspace member.
      tags:
      - workspace-membership
  /workspaces/{workspaceSlug}/members/me:
    get:
      description: |-
        Get the current user's membership in this workspace.
         Super admins (the <code>admin</code> app role, APP_ADMIN) have their effective role elevated to ADMIN
         if their database role is lower, matching the runtime authorization behaviour in
         {@link WorkspaceAccessService WorkspaceAccessService}.
      operationId: getCurrentUserMembership
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMembership"
          description: the current user's membership details with effective role
      security: []
      summary: Get the current user's membership in this workspace.
      tags:
      - workspace-membership
  /workspaces/{workspaceSlug}/members/{userId}:
    delete:
      description: |-
        Revoke a user's membership (remove them from workspace).
         OWNER can remove anyone except themselves if they are the last OWNER.
         ADMIN can remove MEMBER and ADMIN roles.
      operationId: removeMember
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: User ID to remove
        in: path
        name: userId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: 204 No Content on success
      summary: Revoke a user's membership (remove them from workspace).
      tags:
      - workspace-membership
    get:
      description: |-
        Get a specific member's details.
         Accessible to all workspace members.
      operationId: getMember
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: User ID
        in: path
        name: userId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMembership"
          description: Workspace membership details
      security: []
      summary: Get a specific member's details.
      tags:
      - workspace-membership
  /workspaces/{workspaceSlug}/members/{userId}/hidden:
    patch:
      description: |-
        Toggle the hidden flag for a workspace member.
         Hidden members are excluded from the leaderboard.
      operationId: updateMemberVisibility
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: User ID
        in: path
        name: userId
        required: true
        schema:
          type: integer
          format: int64
      - description: whether the member should be hidden
        in: query
        name: hidden
        required: true
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceMembership"
          description: Updated membership
      summary: Toggle the hidden flag for a workspace member.
      tags:
      - workspace-membership
  /workspaces/{workspaceSlug}/mentor/threads:
    get:
      operationId: listThreads
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatThreadSummary"
          description: "Threads returned, newest first"
      summary: List the current user's mentor threads in this workspace
      tags:
      - Mentor Threads
  /workspaces/{workspaceSlug}/mentor/threads/{threadId}:
    delete:
      operationId: deleteThread
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: threadId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Thread deleted
        "404":
          description: Thread not found OR not owned by current user
      summary: "Delete a mentor thread (cascades to messages, votes, parts)"
      tags:
      - Mentor Threads
    get:
      operationId: getThread
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: threadId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatThreadDetail"
          description: Thread + messages returned
        "404":
          description: Thread not found OR not owned by current user
      summary: Get a mentor thread with its full message history
      tags:
      - Mentor Threads
  /workspaces/{workspaceSlug}/mentor/threads/{threadId}/messages/{messageId}/vote:
    delete:
      operationId: removeVote
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: threadId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: messageId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Vote removed or did not exist
        "404":
          description: Thread or message not found (or not owned by current user)
      summary: Remove a vote on an assistant message (idempotent)
      tags:
      - Mentor Votes
    post:
      operationId: vote
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: threadId
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: messageId
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatMessageVoteRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatMessageVote"
          description: Vote recorded
        "404":
          description: Thread or message not found (or not owned by current user)
      summary: Upsert a vote on an assistant message
      tags:
      - Mentor Votes
  /workspaces/{workspaceSlug}/notifications:
    patch:
      operationId: updateNotifications
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceNotificationsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update leaderboard notification preferences
      tags:
      - workspace
  /workspaces/{workspaceSlug}/outline/collections:
    get:
      operationId: listOutlineCollections
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OutlineCollection"
          description: Mirrored collections returned
        "404":
          description: The workspace has no ACTIVE Outline connection
      summary: List the workspace's mirrored Outline collections with their sync state
      tags:
      - Outline collections
    post:
      operationId: registerOutlineCollection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterOutlineCollectionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutlineCollection"
          description: Collection was already registered (idempotent repeat)
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutlineCollection"
          description: Collection registered; the Location header points at the collection
            resource
        "404":
          description: The workspace has no ACTIVE Outline connection
        "409":
          description: A concurrent registration of the same collection won the race
        "422":
          description: Outline does not know the requested collection id
      summary: Register an Outline collection for mirroring (lands ENABLED + PENDING;
        idempotent on the id)
      tags:
      - Outline collections
  /workspaces/{workspaceSlug}/outline/collections/candidates:
    get:
      description: "Proxies Outline's collections.list with the stored token under\
        \ a bounded interactive page budget. Served with Cache-Control: no-store —\
        \ the live upstream view must not be cached."
      operationId: listOutlineCollectionCandidates
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OutlineCollectionCandidate"
          description: "Candidate collections returned (Cache-Control: no-store)"
        "404":
          description: The workspace has no ACTIVE Outline connection
        "502":
          description: The Outline server could not be reached or answered with an
            error
        "503":
          description: Outline is rate-limiting requests; the Retry-After header carries
            the seconds to wait before retrying
      summary: List the Outline collections available to mirror (live proxy; doubles
        as connectivity probe)
      tags:
      - Outline collections
  /workspaces/{workspaceSlug}/outline/collections/{collectionId}:
    delete:
      operationId: deleteOutlineCollection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: collectionId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Collection removed and its mirrored documents erased
        "404":
          description: "The collection is not registered for this workspace, or the\
            \ workspace has no ACTIVE Outline connection"
      summary: Remove a collection from the mirror and erase its mirrored documents
        (terminal)
      tags:
      - Outline collections
    get:
      operationId: getOutlineCollection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: collectionId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutlineCollection"
          description: Mirrored collection returned
        "404":
          description: "The collection is not registered for this workspace, or the\
            \ workspace has no ACTIVE Outline connection"
      summary: One mirrored Outline collection with its sync state and live document
        count
      tags:
      - Outline collections
    patch:
      description: Resuming (PAUSED → ENABLED) resets the sync status to PENDING and
        kicks a targeted sync; requesting the current state is an idempotent no-op.
      operationId: updateOutlineCollectionState
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: collectionId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOutlineCollectionStateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OutlineCollection"
          description: Collection state after the transition returned
        "404":
          description: "The collection is not registered for this workspace, or the\
            \ workspace has no ACTIVE Outline connection"
      summary: Transition a mirrored collection to a target mirror state (pause /
        resume)
      tags:
      - Outline collections
  /workspaces/{workspaceSlug}/practice-areas:
    get:
      description: Returns the workspace's practice areas
      operationId: listAreas
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: Return only areas shown in practice dashboards
        in: query
        name: visibleInPracticeDashboardsOnly
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PracticeArea"
          description: Areas returned
      security: []
      summary: List practice areas
      tags:
      - Practice Areas
    post:
      operationId: createArea
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePracticeAreaRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeArea"
          description: Area created
        "409":
          description: Area slug already exists in this workspace
      summary: Create a new practice area
      tags:
      - Practice Areas
  /workspaces/{workspaceSlug}/practice-areas/reorder:
    patch:
      description: Sets each area's display order to its index in the provided slug
        list (one atomic write)
      operationId: reorderAreas
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderPracticeAreasRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PracticeArea"
          description: Areas reordered; the full ordered list is returned
        "400":
          description: orderedSlugs is empty or contains duplicates
        "404":
          description: A slug is unknown
      summary: Reorder practice areas
      tags:
      - Practice Areas
  /workspaces/{workspaceSlug}/practice-areas/{areaSlug}:
    delete:
      description: "Moves its practices to Unassigned, then deletes the area"
      operationId: deleteArea
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: areaSlug
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Area deleted
        "404":
          description: Area not found
      summary: Delete a practice area
      tags:
      - Practice Areas
    get:
      operationId: getArea
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: areaSlug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeArea"
          description: Area returned
        "404":
          description: Area not found
      security: []
      summary: Get a practice area
      tags:
      - Practice Areas
    patch:
      operationId: updateArea
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: areaSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePracticeAreaRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeArea"
          description: Area updated
        "404":
          description: Area not found
      summary: Update a practice area
      tags:
      - Practice Areas
  /workspaces/{workspaceSlug}/practice-areas/{areaSlug}/review-tier:
    patch:
      description: "Applies to every practice in the area that holds no tier of its\
        \ own; practices that set their own are left alone. OFF stops their reviews\
        \ entirely. PROPOSE runs them and records every observation, and sends nothing.\
        \ DELIVER sends feedback without asking. Send a null tier to clear the area's\
        \ own setting so it follows the workspace default."
      operationId: setAreaReviewTier
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: areaSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePracticeReviewTierRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeArea"
          description: Tier updated; the response carries the tier now in force and
            where it came from
        "400":
          description: "PROPOSE cannot be selected yet: there is no approval queue\
            \ for the feedback it would prepare"
        "404":
          description: Area not found
      summary: Set how much autonomy the system has over one area
      tags:
      - Practice Areas
  /workspaces/{workspaceSlug}/practice-catalog/practices/{slug}:
    get:
      description: The current instance-catalog definition for comparison with a workspace
        copy.
      operationId: getCuratedPracticeCatalogEntry
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CuratedPractice"
          description: OK
      summary: Read the instance's definition of a practice
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices:
    get:
      description: "Returns this workspace's practices, each with the autonomy tier\
        \ in force for it, whether that tier was set on the practice or inherited\
        \ from its area or the workspace, and which level decided it. Optionally narrowed\
        \ to one tier."
      operationId: listPractices
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: "Keep only the practices whose tier IN FORCE is exactly this\
          \ one, inherited or not"
        in: query
        name: reviewTier
        required: false
        schema:
          type: string
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Practice"
          description: Practices returned
      summary: List practice definitions
      tags:
      - Practice Catalog
    post:
      operationId: createPractice
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePracticeRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Practice"
          description: Practice created
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice area not found
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice slug already exists in this workspace
      summary: Create a new practice definition
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/backfill-runs:
    get:
      operationId: listBackfillRuns
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReviewBackfillRun"
          description: Campaigns returned
      summary: List this workspace's recent backfill campaigns
      tags:
      - Practice Review Backfill
    post:
      description: Creates a run awaiting confirmation. Submits nothing and spends
        nothing.
      operationId: preflightBackfillRun
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReviewBackfillRunRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewBackfillRun"
          description: Scope enumerated and priced
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "The window is inverted, too long, or covers too many artifacts"
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: A campaign is already under way for this workspace
      summary: Enumerate and price a backfill campaign
      tags:
      - Practice Review Backfill
  /workspaces/{workspaceSlug}/practices/backfill-runs/{runId}:
    get:
      operationId: getBackfillRun
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: runId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewBackfillRun"
          description: Campaign returned
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: No such campaign in this workspace
      summary: "Get one backfill campaign, including its live progress"
      tags:
      - Practice Review Backfill
  /workspaces/{workspaceSlug}/practices/backfill-runs/{runId}/status:
    patch:
      description: RUNNING authorises the estimated spend and starts the campaign;
        CANCELLED stops it for good.
      operationId: updateBackfillRunStatus
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: runId
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReviewBackfillRunStatusRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewBackfillRun"
          description: Campaign updated
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The campaign cannot make that transition from its current state
      summary: Confirm or cancel a backfill campaign
      tags:
      - Practice Review Backfill
  /workspaces/{workspaceSlug}/practices/definition-options:
    get:
      description: "Returns available review events, recommended requirements, and\
        \ allowed evidence sources by work type"
      operationId: getPracticeDefinitionOptions
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeDefinitionOptions"
          description: OK
      summary: Read practice definition options
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/feedback/engagement:
    get:
      description: Returns the current user's reaction action counts across all feedback
        they received in this workspace.
      operationId: getEngagement
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReactionEngagement"
          description: Engagement statistics returned
      summary: Get engagement statistics
      tags:
      - Feedback Reaction
  /workspaces/{workspaceSlug}/practices/feedback/in-app:
    get:
      description: "Process-level messages prepared for the authenticated developer:\
        \ for each habit that recurs in their work, what the pattern is, the pieces\
        \ of work it was observed on, and one thing to try next. Distinct from in-context\
        \ notes (which say what is wrong in one diff) and from the mentor conversation\
        \ (which asks rather than tells). Reading a message is what delivers it, so\
        \ this GET records the delivery."
      operationId: getInAppFeedback
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InAppFeedback"
          description: "In-app messages returned, newest first"
      security: []
      summary: The current developer's in-app feedback
      tags:
      - In-App Feedback
  /workspaces/{workspaceSlug}/practices/feedback/{feedbackId}/reactions:
    get:
      description: "Returns the current user's most recent reaction to the specified\
        \ feedback unit, or 204 if none exists."
      operationId: getLatestReaction
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: feedbackId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reaction"
          description: Latest reaction returned
        "204":
          description: No reaction exists for this feedback unit
        "404":
          description: Feedback not found in this workspace
      summary: Get the latest reaction to a feedback unit
      tags:
      - Feedback Reaction
    post:
      description: "Records the recipient's reaction (ADDRESSED, DISPUTED, NOT_APPLICABLE)\
        \ to a delivered feedback unit. Append-only: submitting again creates a new\
        \ record, preserving temporal history."
      operationId: submitReaction
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: feedbackId
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReaction"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reaction"
          description: Reaction recorded
        "400":
          description: "Invalid request (e.g., DISPUTED without explanation)"
        "403":
          description: Current user is not the feedback's recipient
        "404":
          description: Feedback not found in this workspace
      summary: Submit a reaction to a feedback unit
      tags:
      - Feedback Reaction
  /workspaces/{workspaceSlug}/practices/learner:
    get:
      description: "Returns the learner-facing name, area, rationale, and example\
        \ for every practice the workspace reviews (any autonomy tier above OFF)"
      operationId: listLearnerPractices
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LearnerPractice"
          description: Learner practices returned
      security: []
      summary: "List reviewed practices, learner-facing"
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/observations:
    get:
      description: Paginated observations for the authenticated developer with optional
        filters
      operationId: listObservations
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: Filter by practice slug
        in: query
        name: practiceSlug
        required: false
        schema:
          type: string
      - description: Filter by presence
        in: query
        name: presence
        required: false
        schema:
          type: string
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 20
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageObservationList"
          description: Paginated observations returned
      security: []
      summary: List observations for current user
      tags:
      - Practice Observations
  /workspaces/{workspaceSlug}/practices/observations/pull-request/{prId}:
    get:
      description: All observations for a specific pull request within the workspace
      operationId: getObservationsForPullRequest
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: prId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ObservationList"
          description: PR observations returned
      security: []
      summary: List observations for a pull request
      tags:
      - Practice Observations
  /workspaces/{workspaceSlug}/practices/observations/reflection:
    get:
      description: "Per-practice cards a developer can READ — why the practice matters,\
        \ what good looks like, where they stand, the specific feedback to act on,\
        \ and what they already do well. The third feedback channel alongside in-context\
        \ SCM notes and the conversational mentor; the same observations reorganised\
        \ by practice for self-paced reflection, not a scoreboard of counts."
      operationId: getReflection
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReflectionPractice"
          description: Per-practice reflection cards returned
      security: []
      summary: Reflective dashboard feedback for the current developer
      tags:
      - Practice Observations
  /workspaces/{workspaceSlug}/practices/observations/summary:
    get:
      description: Aggregated observation counts per practice for dashboard cards
      operationId: getSummary
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DeveloperPracticeSummary"
          description: Practice summaries returned
      security: []
      summary: Per-practice summary for current user
      tags:
      - Practice Observations
  /workspaces/{workspaceSlug}/practices/observations/{observationId}:
    get:
      operationId: getObservation
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: observationId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObservationDetail"
          description: Observation detail returned
        "404":
          description: Observation not found or not owned by current user
      security: []
      summary: Get observation detail
      tags:
      - Practice Observations
  /workspaces/{workspaceSlug}/practices/reorder:
    patch:
      description: Sets each practice's display order to its index in the provided
        slug list (one atomic write)
      operationId: reorderPractices
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderPracticesRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Practice"
          description: Practices reordered; the full ordered practice list is returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "orderedSlugs is empty, has duplicates, or is not the area's\
            \ complete set"
      summary: Reorder the practices within an area
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/review-requests:
    post:
      description: "Answers 200 both when a review starts and when one deliberately\
        \ does not; the body says which, and why. 403 only when the caller has no\
        \ standing on the artifact."
      operationId: requestPracticeReview
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReviewRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewRequestOutcome"
          description: "The ask was understood: a review is running, or the body names\
            \ what stopped it"
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The artifact kind is not one that can be asked for
        "403":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: The caller is neither the work's author or assignee nor a workspace
            admin
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: No such artifact in this workspace
      summary: Ask for a review of a piece of work now
      tags:
      - Practice review requests
  /workspaces/{workspaceSlug}/practices/review-settings:
    get:
      operationId: getPracticeReviewSettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeReviewSettings"
          description: Policy returned
      summary: Get the workspace's practice-review policy
      tags:
      - Practice Review Settings
    patch:
      operationId: updatePracticeReviewSettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePracticeReviewSettingsRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PracticeReviewSettings"
          description: Policy updated
      summary: Update the workspace's practice-review policy
      tags:
      - Practice Review Settings
  /workspaces/{workspaceSlug}/practices/review-tiers:
    get:
      description: "How many practices sit at each autonomy tier, for the whole workspace\
        \ and for each area, plus the workspace default and where feedback may go.\
        \ The summary a hundred-practice catalogue is read through — answered here\
        \ so a client never has to fetch every practice to count them."
      operationId: reviewTierRollup
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewTierRollup"
          description: Rollup returned
      summary: "Summarise how much autonomy this workspace grants, by area"
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/reviews:
    get:
      description: Results are ordered newest first.
      operationId: listPracticeReviews
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 100
          minimum: 1
      - in: query
        name: status
        required: false
        schema:
          type: string
          enum:
          - QUEUED
          - RUNNING
          - COMPLETED
          - FAILED
          - TIMED_OUT
          - CANCELLED
      - description: Inclusive lower bound on when the review was requested
        in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - description: Exclusive upper bound on when the review was requested
        in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedModelReviewRunSummary"
          description: Paginated review summaries returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Invalid filter or pagination
      summary: List practice reviews with observation and feedback outcomes
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/reviews/evidence-outcomes:
    get:
      description: "One entry per practice that recent reviews considered, with the\
        \ sources that skipped it."
      operationId: listPracticeEvidenceOutcomes
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PracticeEvidenceOutcome"
          description: Evidence outcomes returned
      summary: Summarize how each practice's evidence requirements turned out on recent
        reviews
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/reviews/feedback:
    get:
      description: Results are ordered newest first and include every delivery state.
      operationId: listPracticeReviewFeedback
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 100
          minimum: 1
      - in: query
        name: deliveryState
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - PREPARED
            - DELIVERED
            - SUPERSEDED
            - SUPPRESSED
            - FAILED
      - in: query
        name: suppressionReason
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - VOLUME_CAPPED
            - COMPOSER_DEDUPED
            - REACTED_DISPUTED
            - REACTED_NOT_APPLICABLE
            - CONVERSATION_EXPIRED
            - ARTIFACT_GONE
            - ARTIFACT_CLOSED
            - ARTIFACT_MERGED
            - ARTIFACT_DRAFT
            - RECIPIENT_OPTED_OUT
            - EMPTY_AFTER_SANITIZE
            - INSTANCE_SILENCED
            - PRACTICE_TIER_QUIET
            - BACKFILL_QUIET
      - in: query
        name: channel
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - IN_CONTEXT
            - IN_CHAT
            - IN_APP
      - in: query
        name: agentJobId
        required: false
        schema:
          type: string
          format: uuid
      - description: "Kind of reviewed work, e.g. scm.pull_request"
        in: query
        name: artifactKind
        required: false
        schema:
          type: string
      - description: Artifact ID; requires artifactKind
        in: query
        name: artifactId
        required: false
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      - in: query
        name: recipientUserId
        required: false
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      - description: Inclusive lower bound
        in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - description: Exclusive upper bound
        in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedModelReviewFeedback"
          description: Paginated feedback returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Invalid filter or pagination
      summary: List practice review feedback across the workspace
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/reviews/feedback/{feedbackId}:
    get:
      operationId: getPracticeReviewFeedback
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: feedbackId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewFeedbackDetail"
          description: Feedback detail returned
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Feedback not found in this workspace
      summary: "Get feedback with its stored body, observations and placements"
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/reviews/observations:
    get:
      description: Results include linked feedback outcomes and are ordered newest
        first by default.
      operationId: listPracticeReviewObservations
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 100
          minimum: 1
      - description: "Sorting strategy. ACTIONABILITY orders problems from CRITICAL\
          \ to INFO, then strengths, then not-applicable observations; ties are newest\
          \ first."
        in: query
        name: sort
        required: false
        schema:
          type: string
          default: NEWEST
          enum:
          - NEWEST
          - ACTIONABILITY
      - in: query
        name: practiceSlug
        required: false
        schema:
          type: array
          items:
            type: string
      - in: query
        name: areaSlug
        required: false
        schema:
          type: array
          items:
            type: string
      - in: query
        name: presence
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - PRESENT
            - ABSENT
            - NOT_APPLICABLE
            - INCONCLUSIVE
      - in: query
        name: assessment
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - GOOD
            - BAD
      - in: query
        name: severity
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - CRITICAL
            - MAJOR
            - MINOR
            - INFO
      - in: query
        name: agentJobId
        required: false
        schema:
          type: string
          format: uuid
      - description: "Kind of reviewed work, e.g. scm.pull_request"
        in: query
        name: artifactKind
        required: false
        schema:
          type: string
      - description: Artifact ID; requires artifactKind
        in: query
        name: artifactId
        required: false
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      - in: query
        name: subjectUserId
        required: false
        schema:
          type: integer
          format: int64
          exclusiveMinimum: 0
      - description: "What occasioned the measurement: LIVE, MANUAL or BACKFILL"
        in: query
        name: origin
        required: false
        schema:
          type: array
          items:
            type: string
            enum:
            - LIVE
            - MANUAL
            - BACKFILL
      - description: Inclusive lower bound
        in: query
        name: from
        required: false
        schema:
          type: string
          format: date-time
      - description: Exclusive upper bound
        in: query
        name: to
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedModelReviewObservation"
          description: Paginated observations returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Invalid filter or pagination
      summary: List practice review observations across the workspace
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/reviews/observations/{observationId}:
    get:
      operationId: getPracticeReviewObservation
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: observationId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewObservationDetail"
          description: Observation detail returned
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Observation not found in this workspace
      summary: Get an observation with its evidence and linked feedback
      tags:
      - Practice reviews
  /workspaces/{workspaceSlug}/practices/sweep-schedules:
    get:
      operationId: listSweepSchedules
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReviewSweepSchedule"
          description: Schedules returned
      summary: List this workspace's sweep schedules
      tags:
      - Practice Review Sweep
    post:
      description: Authorises the recurring spend. The first sweep runs within the
        hour.
      operationId: createSweepSchedule
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReviewSweepScheduleRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewSweepSchedule"
          description: Schedule created
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "The kind cannot be swept, or the lookback is longer than the\
            \ cadence allows"
        "409":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: This workspace already sweeps that kind of work
      summary: Start sweeping a kind of work on a cadence
      tags:
      - Practice Review Sweep
  /workspaces/{workspaceSlug}/practices/sweep-schedules/{scheduleId}:
    delete:
      description: Removes the instruction. Campaigns it already opened keep their
        records.
      operationId: deleteSweepSchedule
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: scheduleId
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Schedule removed
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: No such schedule in this workspace
      summary: Stop sweeping
      tags:
      - Practice Review Sweep
    put:
      description: "Changes the cadence, the window, or whether the sweep runs at\
        \ all."
      operationId: replaceSweepSchedule
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: scheduleId
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReviewSweepScheduleRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewSweepSchedule"
          description: Schedule updated
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: No such schedule in this workspace
      summary: Replace a sweep schedule's terms
      tags:
      - Practice Review Sweep
  /workspaces/{workspaceSlug}/practices/trace:
    get:
      description: "Built from the signal ledger, so it includes work that was never\
        \ reviewed — which is exactly what a listing derived from review runs cannot\
        \ show. Most recently signalled first."
      operationId: listTracedArtifacts
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: page
        required: false
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - in: query
        name: size
        required: false
        schema:
          type: integer
          format: int32
          default: 25
          maximum: 100
          minimum: 1
      - description: "Restrict to one kind of work, e.g. scm.pull_request"
        in: query
        name: artifactKind
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PagedModelTracedArtifact"
          description: Paginated artifacts returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Unknown artifact kind or invalid pagination
      summary: List work this workspace recorded something about
      tags:
      - Practice review trace
  /workspaces/{workspaceSlug}/practices/trace/{artifactKind}/{artifactId}:
    get:
      description: "Every practice the workspace runs against this kind of work appears,\
        \ including the ones that did nothing, each with the recorded reason. 404\
        \ means nothing about this artifact was ever recorded here — not that the\
        \ trace is unavailable."
      operationId: getArtifactTrace
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: "Kind of work, e.g. scm.pull_request"
        in: path
        name: artifactKind
        required: true
        schema:
          type: string
      - description: The artifact's identifier as the ledger stores it
        in: path
        name: artifactId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArtifactTrace"
          description: Trace returned
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Nothing recorded about this artifact in this workspace
      summary: Explain what every practice did about one piece of work
      tags:
      - Practice review trace
  /workspaces/{workspaceSlug}/practices/{practiceSlug}:
    delete:
      operationId: deletePractice
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Practice deleted
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice not found
      summary: Delete a practice definition
      tags:
      - Practice Catalog
    get:
      operationId: getPractice
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Practice"
          description: Practice returned
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice not found
      summary: Get a practice definition
      tags:
      - Practice Catalog
    patch:
      operationId: updatePractice
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePracticeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Practice"
          description: Practice updated
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice or practice area not found
      summary: Update a practice
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/{practiceSlug}/area:
    put:
      description: "Moves the practice to the requested area, or to Unassigned when\
        \ areaSlug is null"
      operationId: bindArea
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BindPracticeAreaRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Practice"
          description: Practice moved
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice or area not found
      summary: Move a practice
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/{practiceSlug}/placement:
    put:
      description: Moves the practice and sets its exact position in one atomic write;
        omit areaSlug for Unassigned
      operationId: placePractice
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlacePracticeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Practice"
          description: Practice placed; the full updated practice list is returned
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "position is missing, negative, or beyond the destination"
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice or area not found
      summary: Place a practice in the catalog
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/practices/{practiceSlug}/review-tier:
    patch:
      description: "OFF stops the review entirely. PROPOSE runs it and records every\
        \ observation and sends nothing. DELIVER sends feedback without asking, as\
        \ far as this workspace's reach allows. Send a null tier to clear the practice's\
        \ own setting so it follows its area, and through the area the workspace default."
      operationId: setReviewTier
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: practiceSlug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePracticeReviewTierRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Practice"
          description: Tier updated; the response carries the tier now in force and
            where it came from
        "400":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: "The tier cannot be selected: PROPOSE has no approval queue\
            \ yet, or this practice's review settings cannot run an automated review\
            \ at any tier above OFF"
        "404":
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
          description: Practice not found
      summary: Set how much autonomy the system has over one practice
      tags:
      - Practice Catalog
  /workspaces/{workspaceSlug}/profile/{login}:
    get:
      description: "Returns user profile with workspace-scoped activity data including\
        \ open PRs, review activity, and league points"
      operationId: getUserProfile
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: login
        required: true
        schema:
          type: string
      - in: query
        name: after
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: before
        required: false
        schema:
          type: string
          format: date-time
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Profile"
          description: OK
      security: []
      summary: Get user profile
      tags:
      - Profile
  /workspaces/{workspaceSlug}/profile/{login}/activity-monitor:
    get:
      description: Returns workspace-scoped activity monitor data for a contributor
        with optional repository and limit filters
      operationId: getActivityMonitor
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: login
        required: true
        schema:
          type: string
      - in: query
        name: after
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: before
        required: false
        schema:
          type: string
          format: date-time
      - in: query
        name: repositoryIds
        required: false
        schema:
          type: array
          items:
            type: integer
            format: int64
          maxItems: 100
          minItems: 0
          uniqueItems: true
      - in: query
        name: limit
        required: false
        schema:
          type: integer
          format: int32
          maximum: 100
          minimum: 1
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileActivityMonitor"
          description: OK
      security: []
      summary: Get contributor activity monitor
      tags:
      - Profile
  /workspaces/{workspaceSlug}/public-visibility:
    patch:
      operationId: updatePublicVisibility
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspacePublicVisibilityRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Toggle public visibility for a workspace
      tags:
      - workspace
  /workspaces/{workspaceSlug}/repositories:
    delete:
      operationId: removeRepositoryToMonitor
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: nameWithOwner
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: Remove a repository from a workspace monitor list
      tags:
      - workspace
    get:
      operationId: getRepositoriesToMonitor
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
          description: Repository list
      security: []
      summary: List repositories monitored by a workspace
      tags:
      - workspace
    post:
      operationId: addRepositoryToMonitor
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: query
        name: nameWithOwner
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: Add a repository to a workspace monitor list
      tags:
      - workspace
  /workspaces/{workspaceSlug}/schedule:
    patch:
      operationId: updateSchedule
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceScheduleRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update leaderboard schedule configuration
      tags:
      - workspace
  /workspaces/{workspaceSlug}/slack/channels:
    get:
      operationId: listSlackChannels
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SlackMonitoredChannel"
          description: OK
      summary: List the workspace's allow-listed Slack channels with their consent
        state
      tags:
      - Slack channel activation
    post:
      operationId: registerSlackChannel
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterSlackChannelRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlackMonitoredChannel"
          description: OK
      summary: Allow-list a Slack channel (lands in PENDING; idempotent on the natural
        key)
      tags:
      - Slack channel activation
  /workspaces/{workspaceSlug}/slack/channels/candidates:
    get:
      operationId: listSlackChannelCandidates
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SlackChannelCandidate"
          description: OK
      summary: List Slack channels available to add to monitoring
      tags:
      - Slack channel activation
  /workspaces/{workspaceSlug}/slack/channels/{slackChannelId}:
    patch:
      operationId: updateSlackChannelConsent
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: slackChannelId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSlackChannelConsentRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlackMonitoredChannel"
          description: OK
      summary: Transition a Slack channel to a target consent state (activate / pause
        / resume / revoke)
      tags:
      - Slack channel activation
  /workspaces/{workspaceSlug}/slack/channels/{slackChannelId}/consent-events:
    get:
      operationId: listSlackChannelConsentEvents
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: slackChannelId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SlackChannelConsentEvent"
          description: OK
      summary: The immutable consent-transition audit trail of one Slack channel
      tags:
      - Slack channel activation
  /workspaces/{workspaceSlug}/slack/me/preferences:
    patch:
      operationId: updateSlackUserPreferences
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSlackUserPreferencesRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlackUserWorkspacePreferences"
          description: OK
      summary: Update current user's Slack workspace preferences
      tags:
      - Slack User Preferences
  /workspaces/{workspaceSlug}/slug:
    patch:
      operationId: renameSlug
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RenameWorkspaceSlugRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace renamed
      summary: Rename workspace slug and create redirect
      tags:
      - workspace
  /workspaces/{workspaceSlug}/status:
    patch:
      operationId: updateStatus
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceStatusRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update workspace lifecycle status
      tags:
      - workspace
  /workspaces/{workspaceSlug}/team:
    get:
      description: Returns all teams in the workspace organization
      operationId: getAllTeams
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TeamInfo"
          description: list of teams with their members and permissions
      security: []
      summary: List teams
      tags:
      - Teams
  /workspaces/{workspaceSlug}/team/{id}/visibility:
    post:
      description: Show or hide a team in leaderboard calculations
      operationId: updateTeamVisibility
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: id
        required: true
        schema:
          type: integer
          format: int64
      - description: "whether to hide the team (query parameter, fallback)"
        in: query
        name: hidden
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              type: boolean
        description: "whether to hide the team (body parameter, preferred)"
      responses:
        "200":
          description: "200 OK on success, 404 if team not found"
      summary: Update team visibility
      tags:
      - Teams
  /workspaces/{workspaceSlug}/team/{teamId}/repositories/{repositoryId}/visibility:
    post:
      operationId: updateRepositoryVisibility
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - in: path
        name: repositoryId
        required: true
        schema:
          type: integer
          format: int64
      - in: query
        name: hiddenFromContributions
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              type: boolean
      responses:
        "200":
          description: OK
      tags:
      - Teams
  /workspaces/{workspaceSlug}/teams/{teamId}/labels/{labelId}:
    delete:
      operationId: removeLabelFromTeam
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - in: path
        name: labelId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInfo"
          description: OK
      summary: Remove a repository label from a team
      tags:
      - workspace
  /workspaces/{workspaceSlug}/teams/{teamId}/labels/{repositoryId}/{label}:
    post:
      operationId: addLabelToTeam
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - in: path
        name: repositoryId
        required: true
        schema:
          type: integer
          format: int64
      - in: path
        name: label
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInfo"
          description: OK
      summary: Add a repository label to a team
      tags:
      - workspace
  /workspaces/{workspaceSlug}/teams/{teamId}/settings:
    get:
      description: Returns the visibility settings for a team in the workspace
      operationId: getTeamSettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamSettings"
          description: Team settings returned
      security: []
      summary: Get team settings
      tags:
      - Team Settings
    patch:
      description: Updates the visibility settings for a team in the workspace
      operationId: updateTeamSettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTeamSettingsRequest"
        description: the update request containing the hidden flag
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamSettings"
          description: Team settings updated
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamSettings"
          description: Team not found in workspace
      summary: Update team settings
      tags:
      - Team Settings
  /workspaces/{workspaceSlug}/teams/{teamId}/settings/label-filters:
    get:
      description: Returns all labels configured as filters for a team in the workspace
      operationId: getLabelFilters
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LabelInfo"
          description: Label filters returned
      security: []
      summary: Get team label filters
      tags:
      - Team Settings
  /workspaces/{workspaceSlug}/teams/{teamId}/settings/label-filters/{labelId}:
    delete:
      description: Removes a label filter from a team in the workspace
      operationId: removeLabelFilter
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - description: the label ID to remove as filter
        in: path
        name: labelId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Label filter removed
        "404":
          description: Label filter not found
      summary: Remove label filter
      tags:
      - Team Settings
    post:
      description: Adds a label as a filter for a team in the workspace
      operationId: addLabelFilter
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - description: the label ID to add as filter
        in: path
        name: labelId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "201":
          description: Label filter added
        "404":
          description: Team or label not found
      summary: Add label filter
      tags:
      - Team Settings
  /workspaces/{workspaceSlug}/teams/{teamId}/settings/repositories/{repositoryId}:
    get:
      description: Returns the contribution visibility settings for a repository in
        a team
      operationId: getRepositorySettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - description: the repository ID
        in: path
        name: repositoryId
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamRepositorySettings"
          description: Repository settings returned
      security: []
      summary: Get repository settings
      tags:
      - Team Settings
    patch:
      description: Updates the contribution visibility settings for a repository in
        a team
      operationId: updateRepositorySettings
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the team ID
        in: path
        name: teamId
        required: true
        schema:
          type: integer
          format: int64
      - description: the repository ID
        in: path
        name: repositoryId
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRepositorySettingsRequest"
        description: the update request containing the hiddenFromContributions flag
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamRepositorySettings"
          description: Repository settings updated
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceTeamRepositorySettings"
          description: Team or repository not found
      summary: Update repository settings
      tags:
      - Team Settings
  /workspaces/{workspaceSlug}/token:
    patch:
      operationId: updateToken
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWorkspaceTokenRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
          description: Workspace updated
      summary: Update workspace Personal Access Token
      tags:
      - workspace
  /workspaces/{workspaceSlug}/users:
    get:
      operationId: getUsersWithTeams
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserTeams"
          description: OK
      security: []
      summary: List workspace users and the teams they belong to
      tags:
      - workspace
  /workspaces/{workspaceSlug}/users/{login}/achievements:
    get:
      description: Returns all achievements with progress information for the specified
        user
      operationId: getUserAchievements
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the user's GitHub login
        in: path
        name: login
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Achievement"
          description: list of all achievements with user-specific progress
      security: []
      summary: Get user achievements
      tags:
      - Achievements
  /workspaces/{workspaceSlug}/users/{login}/achievements/definitions:
    get:
      description: Returns the master list of all available achievements. Restricted
        to non-prod environments.
      operationId: getAllAchievementDefinitions
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: login
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Achievement"
          description: OK
      security: []
      summary: Get all achievement definitions
      tags:
      - Achievements
  /workspaces/{workspaceSlug}/users/{login}/achievements/recalculate:
    post:
      description: Wipes and historically recalculates a user's achievement timeline.
        Admin only.
      operationId: recalculateUserAchievements
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - description: the user's GitHub login
        in: path
        name: login
        required: true
        schema:
          type: string
      responses:
        "202":
          description: Recalculation task started successfully
      summary: Recalculate user achievements
      tags:
      - Achievements
  /workspaces/{workspaceSlug}/users/{login}/achievements/reload:
    post:
      description: Hot reloads the achievements.yml configuration without requiring
        a restart. Admin only.
      operationId: reloadAchievements
      parameters:
      - description: Workspace slug
        in: path
        name: workspaceSlug
        required: true
        schema:
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      - in: path
        name: login
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
      summary: Reload achievements configuration
      tags:
      - Achievements
components:
  schemas:
    AccountRef:
      type: object
      description: A human-readable account identity. <code>displayName</code>/<code>email</code>
        are null for deleted accounts.
      properties:
        displayName:
          type: string
        email:
          type: string
        id:
          type: integer
          format: int64
      required:
      - id
    Achievement:
      type: object
      description: Achievement with user-specific progress information
      properties:
        category:
          type: string
          description: Category for grouping achievements
          enum:
          - pull_requests
          - commits
          - communication
          - issues
          - milestones
          example: pull_requests
        id:
          type: string
          $ref: "#/components/schemas/AchievementId"
          description: Unique identifier for the achievement
          example: pr.merged.common.1
        isHidden:
          type: boolean
          description: Whether the achievement should be hidden until unlocked
        parent:
          type: string
          description: Parent achievement in progression chain
          example: first_pull
        progressData:
          description: The structured progress data based on the achievements evaluator
          oneOf:
          - $ref: "#/components/schemas/BinaryAchievementProgress"
          - $ref: "#/components/schemas/LinearAchievementProgress"
        rarity:
          type: string
          description: Visual level tier/rarity for badge styling
          enum:
          - common
          - uncommon
          - rare
          - epic
          - legendary
          - mythic
          example: common
        status:
          type: string
          description: Current status of the achievement for this user
          enum:
          - locked
          - available
          - unlocked
          - hidden
          example: unlocked
        unlockedAt:
          type: string
          format: date-time
          description: "Optional of when the achievement was unlocked, empty() if\
            \ not unlocked"
      required:
      - category
      - id
      - progressData
      - rarity
      - status
      - unlockedAt
    AchievementId:
      type: string
      enum:
      - commit.common.1
      - commit.common.2
      - commit.epic
      - commit.legendary
      - commit.mythic
      - commit.rare
      - commit.special.atomic_changes
      - commit.special.brute_force
      - commit.special.cross_boundary
      - commit.special.itsy_bitsy
      - commit.uncommon.1
      - commit.uncommon.2
      - issue.close.common.1
      - issue.close.common.2
      - issue.close.epic
      - issue.close.legendary
      - issue.close.rare
      - issue.close.uncommon
      - issue.open.common.1
      - issue.open.common.2
      - issue.open.epic
      - issue.open.legendary
      - issue.open.rare
      - issue.open.uncommon
      - issue.special.hive_mind
      - issue.special.necromancer
      - issue.special.oracle
      - milestone.all_epic
      - milestone.all_legendary
      - milestone.all_rare
      - milestone.first_action
      - milestone.long_time_return
      - milestone.night_owl
      - milestone.polyglot
      - pr.merged.common.1
      - pr.merged.common.2
      - pr.merged.epic
      - pr.merged.legendary
      - pr.merged.rare
      - pr.merged.uncommon
      - pr.special.speedster
      - review.common.1
      - review.common.2
      - review.epic
      - review.legendary
      - review.mythic
      - review.rare
      - review.uncommon.1
      - review.uncommon.2
    AchievementProgress:
      description: Polymorphic progress data
      discriminator:
        propertyName: type
      properties:
        type:
          type: string
      required:
      - type
    AdminAccountView:
      type: object
      properties:
        appRole:
          type: string
        displayName:
          type: string
        id:
          type: integer
          format: int64
        primaryEmail:
          type: string
        status:
          type: string
    AdminLlmUsageReport:
      type: object
      description: "Instance-admin per-workspace month rollup (metadata only, no tenant\
        \ content)"
      properties:
        fx:
          $ref: "#/components/schemas/FxRateInfo"
          description: Display-only conversion when the instance has a display currency.
            Absent = show USD only. Applies to every USD amount in this response.
        month:
          type: string
          description: "Calendar month (UTC), ISO yyyy-MM"
          example: 2026-07
        workspaces:
          type: array
          description: "One row per workspace, including workspaces with no ledger\
            \ activity this month"
          items:
            $ref: "#/components/schemas/AdminWorkspaceLlmUsage"
      required:
      - month
      - workspaces
    AdminWorkspaceLlmUsage:
      type: object
      description: "One workspace's month of spend, as an instance admin sees it"
      properties:
        displayName:
          type: string
        events:
          type: integer
          format: int64
          description: "Ledger events (jobs / mentor turns) this month, either purse"
        instanceBudgetVerdict:
          type: string
          description: "Whether shared-model spend is within the instance cap, has\
            \ reached it, or can't be confirmed because some shared-model usage has\
            \ no price set."
          enum:
          - WITHIN
          - EXHAUSTED
          - UNVERIFIABLE
        instanceMonthlyBudgetUsd:
          type: number
          format: decimal
          description: Monthly cap in USD on this workspace's spend on shared (instance)
            models — the money this instance pays for; null = uncapped. Yours to set.
        instancePaused:
          type: boolean
          description: "Whether work on SHARED models is paused for this workspace\
            \ right now (current month only) — authoritative, mirroring the live gate."
        instanceTotalCostUsd:
          type: number
          format: decimal
          description: "This month's confirmed spend on shared (instance) models,\
            \ in USD — compared against instanceMonthlyBudgetUsd."
        ownProviderBudgetVerdict:
          type: string
          description: The same verdict for the workspace's own-provider spend against
            its own cap.
          enum:
          - WITHIN
          - EXHAUSTED
          - UNVERIFIABLE
        ownProviderMonthlyBudgetUsd:
          type: number
          format: decimal
          description: "The workspace's own cap in USD on its own-provider spend;\
            \ null = uncapped. Read-only here — it governs the workspace's money,\
            \ so only its own admins may change it."
        ownProviderPaused:
          type: boolean
          description: Whether work on the workspace's OWN provider is paused right
            now.
        ownProviderTotalCostUsd:
          type: number
          format: decimal
          description: "This month's confirmed spend on the workspace's own connected\
            \ provider(s), in USD — compared against ownProviderMonthlyBudgetUsd."
        workspaceSlug:
          type: string
          description: "Addresses the workspace everywhere else in the API, including\
            \ its cap"
      required:
      - displayName
      - events
      - instanceBudgetVerdict
      - instancePaused
      - instanceTotalCostUsd
      - ownProviderBudgetVerdict
      - ownProviderPaused
      - ownProviderTotalCostUsd
      - workspaceSlug
    AdminWorkspaceView:
      type: object
      description: Metadata-only workspace summary for the instance-admin overview
      properties:
        accountLogin:
          type: string
        createdAt:
          type: string
          format: date-time
        displayName:
          type: string
        id:
          type: integer
          format: int64
        memberCount:
          type: integer
          format: int64
        ownerLogin:
          type: string
        providerType:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        status:
          type: string
        workspaceSlug:
          type: string
      required:
      - accountLogin
      - createdAt
      - displayName
      - id
      - memberCount
      - status
      - workspaceSlug
    AgentBinding:
      type: object
      description: A workspace's agent binding for one purpose
      properties:
        allowInternet:
          type: boolean
        enabled:
          type: boolean
        instanceModelId:
          type: integer
          format: int64
        maxConcurrentJobs:
          type: integer
          format: int32
        purpose:
          type: string
          enum:
          - PRACTICE_REVIEW
          - MENTOR
        ready:
          type: boolean
          description: True when the bound model is available to run right now
        timeoutSeconds:
          type: integer
          format: int32
        workspaceModelId:
          type: integer
          format: int64
      required:
      - enabled
      - purpose
      - ready
    AgentBindingRequest:
      type: object
      description: Bind a model and execution limits to an agent purpose
      properties:
        allowInternet:
          type: boolean
          description: Whether the sandbox may reach the public internet
        enabled:
          type: boolean
          description: Whether this purpose is active (paused when false)
        instanceModelId:
          type: integer
          format: int64
          description: Shared (instance-catalog) model id to run this purpose on
        maxConcurrentJobs:
          type: integer
          format: int32
          description: Maximum concurrent runs for this purpose
          minimum: 1
        timeoutSeconds:
          type: integer
          format: int32
          description: Per-run timeout in seconds (30–3600; one hour is the longest
            a single run may take)
          maximum: 3600
          minimum: 30
        workspaceModelId:
          type: integer
          format: int64
          description: Workspace-owned model id to run this purpose on
    AgentJob:
      type: object
      description: Agent job execution record (job_token intentionally omitted)
      properties:
        availableAt:
          type: string
          format: date-time
          description: "When this job becomes eligible to be claimed. In the future\
            \ while the job is waiting — on a retry backoff, or on a hold. Read together\
            \ with holdReason: a QUEUED job with availableAt in the future is waiting,\
            \ not starved for workers."
        completedAt:
          type: string
          format: date-time
          description: Timestamp when the job completed
        configSnapshot:
          description: Frozen agent config at submit time (an INSTANCE-scoped connection's
            baseUrl is redacted to scheme://host; only a WORKSPACE-scoped connection's
            baseUrl is left intact)
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the job was created
        deliveryCommentId:
          type: string
          description: Git provider comment/note ID for posted feedback
        deliveryStatus:
          type: string
          description: "Delivery status: null = not applicable, PENDING = awaiting\
            \ delivery, DELIVERED = posted, FAILED = delivery error"
          enum:
          - PENDING
          - DELIVERED
          - FAILED
        errorMessage:
          type: string
          description: Human-readable error message
        exitCode:
          type: integer
          format: int32
          description: Container exit code
        holdReason:
          type: string
          description: "Why a QUEUED job is waiting rather than eligible, when the\
            \ reason is one an admin can undo. BUDGET = the payer is over its monthly\
            \ LLM cap and the job resumes by itself once the cap is raised or the\
            \ month rolls over. Absent means no such hold — a future availableAt is\
            \ then an ordinary retry backoff."
        id:
          type: string
          format: uuid
          description: Job ID
        jobType:
          type: string
          description: Job type
          enum:
          - PULL_REQUEST_REVIEW
          - ISSUE_REVIEW
          - CONVERSATION_REVIEW
          - DOCUMENT_REVIEW
        llmCacheReadTokens:
          type: integer
          format: int32
          description: Tokens read from prompt cache
        llmCacheWriteTokens:
          type: integer
          format: int32
          description: Tokens written to prompt cache
        llmModel:
          type: string
          description: "LLM model used (e.g. gpt-5.4-mini, openai/gpt-oss-120b)"
        llmModelVersion:
          type: string
          description: Model version/snapshot date (e.g. 2026-03-17). Only jobs from
            before the model catalog carry one; absent on everything newer.
        llmTotalCalls:
          type: integer
          format: int32
          description: Total LLM API calls (steps) during execution
        llmTotalInputTokens:
          type: integer
          format: int32
          description: Total input tokens consumed
        llmTotalOutputTokens:
          type: integer
          format: int32
          description: Total output tokens generated
        llmTotalReasoningTokens:
          type: integer
          format: int32
          description: Total reasoning/thinking tokens
        metadata:
          description: Job metadata (routing/display info)
        model:
          type: string
          description: "Upstream model this job was admitted on, frozen at submit\
            \ time (e.g. gpt-5.4-mini). Available from submission, unlike llmModel,\
            \ which the runner reports only once the job has run."
        output:
          description: Job output (agent results)
        retryCount:
          type: integer
          format: int32
          description: Number of retry attempts
        reviewOutcome:
          type: string
          description: "Why a COMPLETED run produced the observations it did. INSUFFICIENT_EVIDENCE\
            \ means no model ran because required evidence was missing, unreadable,\
            \ stale, or unauthorized — so no observations means nothing was assessed,\
            \ not that nothing was wrong. REVIEWED means the model ran against sufficient\
            \ evidence."
          enum:
          - REVIEWED
          - INSUFFICIENT_EVIDENCE
        startedAt:
          type: string
          format: date-time
          description: Timestamp when the job started running
        status:
          type: string
          description: Current job status
          enum:
          - QUEUED
          - RUNNING
          - COMPLETED
          - FAILED
          - TIMED_OUT
          - CANCELLED
        target:
          $ref: "#/components/schemas/ReviewRunTarget"
          description: Work item reviewed by this job
      required:
      - availableAt
      - configSnapshot
      - createdAt
      - id
      - jobType
      - retryCount
      - reviewOutcome
      - status
      - target
    AreaReviewTierRollup:
      type: object
      description: "One area's practice counts per effective tier, and the area's\
        \ own tier"
      properties:
        areaName:
          type: string
          description: Area name; null for the no-area group
        areaSlug:
          type: string
          description: Area slug; null groups the practices that belong to no area
        counts:
          type: object
          additionalProperties:
            type: integer
            format: int32
          description: Practice count per effective tier in this area; every tier
            is a key
        overriddenCount:
          type: integer
          format: int32
          description: How many of this area's practices set their own tier rather
            than inheriting
        reviewTier:
          $ref: "#/components/schemas/ReviewTierAssignment"
          description: "The tier in force for this area, and where it came from"
      required:
      - counts
      - overriddenCount
      - reviewTier
    ArtifactTrace:
      type: object
      description: "Every practice's answer for one artifact, and the occurrences\
        \ those answers rest on"
      properties:
        artifactId:
          type: integer
          format: int64
        artifactKind:
          type: string
        container:
          type: string
          description: "Repository, collection or channel it sits in"
        number:
          type: integer
          format: int32
          description: "The number the provider shows, for kinds that have one"
        practices:
          type: array
          description: "Every practice this workspace runs against this kind of work,\
            \ the ones with something to report first, then the rest; ties broken\
            \ by practice name"
          items:
            $ref: "#/components/schemas/PracticeTraceEntry"
        signals:
          type: array
          description: "Everything recorded about this artifact, oldest first"
          items:
            $ref: "#/components/schemas/TracedSignal"
        title:
          type: string
          description: The label a person recognises; the kind's display name when
            the mirror cannot name it
        url:
          type: string
          description: Where to open it upstream; absent for a deleted or unlinkable
            artifact
      required:
      - artifactId
      - artifactKind
      - practices
      - signals
      - title
    AssignRoleRequest:
      type: object
      description: Request to assign or update a user's role in a workspace
      properties:
        role:
          type: string
          description: "New role to assign (OWNER, ADMIN, MEMBER)"
          enum:
          - OWNER
          - ADMIN
          - MEMBER
        userId:
          type: integer
          format: int64
          description: User ID of the member to update
      required:
      - role
      - userId
    AuthEventView:
      type: object
      description: "One audit row, flattened for the admin viewer."
      properties:
        account:
          $ref: "#/components/schemas/AccountRef"
        accountId:
          type: integer
          format: int64
        actingAccountId:
          type: integer
          format: int64
        actor:
          $ref: "#/components/schemas/AccountRef"
        details:
          type: string
        eventType:
          type: string
        failureReason:
          type: string
        id:
          type: integer
          format: int64
        ipAddress:
          type: string
        occurredAt:
          type: string
          format: date-time
        result:
          type: string
        userAgent:
          type: string
        workspaceId:
          type: integer
          format: int64
      required:
      - eventType
      - id
      - occurredAt
      - result
    AvailableLlmModel:
      type: object
      description: A model available for this workspace to bind a Task to
      properties:
        connectionDisplayName:
          type: string
          description: Owning connection's display name
        displayName:
          type: string
          description: Human-readable name
        id:
          type: integer
          format: int64
          description: "Model id, unique within its scope"
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: Cache-read rate per 1M tokens (USD)
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: Cache-write rate per 1M tokens (USD)
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD)
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD)
        pricingMode:
          type: string
          description: Pricing mode
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
        scope:
          type: string
          description: SHARED (instance catalog) or WORKSPACE (your own provider)
          enum:
          - SHARED
          - WORKSPACE
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
      required:
      - connectionDisplayName
      - displayName
      - id
      - pricingMode
      - scope
      - supportsReasoning
    BackfillSummary:
      type: object
      description: Connection-level backfill rollup
      properties:
        percent:
          type: integer
          format: int32
          description: "0-100 completion estimate, if computable"
        state:
          type: string
          description: Integration-defined backfill state string
      required:
      - state
    BinaryAchievementProgress:
      allOf:
      - $ref: "#/components/schemas/AchievementProgress"
      - type: object
        properties:
          unlocked:
            type: boolean
      description: Binary progress indicating unlocked state
      required:
      - unlocked
    BindPracticeAreaRequest:
      type: object
      description: Request to move a practice to an area or Unassigned
      properties:
        areaSlug:
          type:
          - string
          - "null"
          description: Destination area slug; omit or set to null for Unassigned
          example: review-ready-work
    CatalogEntryStatus:
      type: object
      properties:
        changeKind:
          type: string
          enum:
          - NONE
          - WORDING
          - PRESENTATION
          - DETECTION
        etag:
          type: string
          description: Strong entity tag to send in If-Match when updating this entry
        offered:
          type: boolean
        state:
          type: string
          enum:
          - FROM_HEPHAESTUS
          - EDITED_HERE
          - UPDATE_WAITING
          - YOURS
          - NO_LONGER_SHIPPED
      required:
      - changeKind
      - etag
      - offered
      - state
    CatalogOrigin:
      type: object
      description: The catalog entry a workspace copy came from and whether it differs
        now
      properties:
        link:
          type: string
          enum:
          - IN_SYNC
          - LOCALLY_EDITED
          - UPDATE_AVAILABLE
        slug:
          type: string
          description: Slug of the catalog entry this copy was made from
        sourceOffered:
          type: boolean
          description: Whether new workspaces receive the source entry
      required:
      - link
      - slug
      - sourceOffered
    ChatMessage:
      type: object
      description: Mentor chat message in AI SDK UIMessage shape.
      properties:
        createdAt:
          type: string
          format: date-time
        id:
          type: string
          format: uuid
        metadata:
          type: object
          additionalProperties: {}
          description: "Per-turn metadata: status, model, costUsd, usage, …"
        parentMessageId:
          type: string
          format: uuid
        parts:
          type: array
          items:
            description: AI SDK UIMessage part (text / data-observation).
        role:
          type: string
    ChatMessageVote:
      type: object
      description: User vote on a mentor assistant message.
      properties:
        isUpvoted:
          type: boolean
        messageId:
          type: string
          format: uuid
        updatedAt:
          type: string
          format: date-time
    ChatMessageVoteRequest:
      type: object
      description: Upsert a vote on a mentor assistant message
      properties:
        isUpvoted:
          type: boolean
          description: "true = upvote (helpful), false = downvote"
      required:
      - isUpvoted
    ChatThreadDetail:
      type: object
      description: Mentor chat thread with all messages.
      properties:
        createdAt:
          type: string
          format: date-time
        id:
          type: string
          format: uuid
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ChatMessage"
        title:
          type: string
    ChatThreadSummary:
      type: object
      description: Mentor chat thread summary (no messages).
      properties:
        createdAt:
          type: string
          format: date-time
        id:
          type: string
          format: uuid
        title:
          type: string
    ConfigAuditActorRef:
      type: object
      description: |-
        A human-readable actor identity on an audit row. Resolved at read time, so an erased account
         degrades here without the trail being rewritten: <code>AccountPurger</code> clears the email, replaces
         the display name with a placeholder, and nulls the row's actor references.

         <p>Named distinctly from the auth trail's own <code>AccountRefDTO</code>: the OpenAPI schema key is the
         simple name minus the <code>DTO</code> suffix, so two <code>AccountRefDTO</code> records would collide and one
         would be silently dropped from the spec.
      properties:
        displayName:
          type: string
        email:
          type: string
        id:
          type: integer
          format: int64
    ConfigAuditEntryView:
      type: object
      description: "One audit row, flattened for a viewer. Crosses the module boundary,\
        \ so it carries no entity."
      properties:
        actingAccountId:
          type: integer
          format: int64
        actingActor:
          $ref: "#/components/schemas/ConfigAuditActorRef"
          description: "resolved impersonator, present only for {@link ConfigAuditActorKind#IMPERSONATED\
            \ ConfigAuditActorKind#IMPERSONATED}"
        action:
          type: string
          enum:
          - CREATED
          - UPDATED
          - DELETED
        actor:
          $ref: "#/components/schemas/ConfigAuditActorRef"
          description: |-
            resolved identity of <code>actorAccountId</code>; null for SYSTEM rows or once the
                                 account is gone. Read together with <code>actorKind</code> — that is what keeps
                                 "a system did this" distinct from "we no longer know who did this".
        actorAccountId:
          type: integer
          format: int64
        actorKind:
          type: string
          enum:
          - USER
          - SYSTEM
          - IMPERSONATED
        changedKeys:
          type: array
          description: dot-paths that differ between <code>oldValue</code> and <code>newValue</code>
          items:
            type: string
        entityId:
          type: string
        entityType:
          type: string
          enum:
          - PRACTICE_REVIEW_SETTINGS
          - AGENT_BINDING
          - AGENT_CONFIG
          - AI_CONFIG_BINDING
          - WORKSPACE_ROLE
          - WORKSPACE_FEATURES
          - WORKSPACE_STATUS
          - WORKSPACE_TOKEN
          - WORKSPACE_VISIBILITY
          - PRACTICE_ACTIVE
          - PRACTICE_USAGE
          - PRACTICE_DEFINITION
          - PRACTICE_AREA
          - CURATED_PRACTICE
          - CURATED_PRACTICE_AREA
          - WORKSPACE_INSTANCE_LLM_BUDGET
          - WORKSPACE_OWN_PROVIDER_LLM_BUDGET
          - WORKSPACE_LLM_BUDGET
          - WORKSPACE_BYO_LLM_BUDGET
          - REVIEW_BACKFILL_RUN
          - REVIEW_SWEEP_SCHEDULE
          - WORKSPACE_LLM_CONNECTION
          - WORKSPACE_LLM_MODEL
        id:
          type: integer
          format: int64
        newValue:
          type: string
        occurredAt:
          type: string
          format: date-time
        oldValue:
          type: string
        workspaceId:
          type: integer
          format: int64
    ConnectionAuditEntry:
      type: object
      description: |-
        Audit-log entry returned by <code>GET /workspaces/{workspaceSlug</code>/connections/{id}/audit}.

         <p>Lean projection of {@link ConnectionAudit ConnectionAudit} — the entity carries a back-reference to
         <code>Connection</code> that this DTO omits.
      properties:
        actorKind:
          type: string
        actorRef:
          type: string
        correlationId:
          type: string
        eventType:
          type: string
        fromState:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
        occurredAt:
          type: string
          format: date-time
        toState:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
    ConnectionDetail:
      type: object
      description: |-
        Detailed view of a single Connection — extends {@link ConnectionSummaryDTO ConnectionSummaryDTO} with the
         sealed config serialized to a free-form JSON object (<code>Map&lt;String, Object&gt;</code> →
         <code>type: object, additionalProperties: true</code> in the spec, so it round-trips through
         client codegen). NEVER carries credentials: the encrypted credential blob stays inside the
         entity, and every secret-bearing key of the config itself is stripped by
         <code>redactSensitive</code> before serialization.

         <p><b>Why redact here and not with <code>@JsonIgnore</code> on the record component?</b> The very
         same <code>ObjectMapper</code> bean serializes <code>ConnectionConfig</code> into the JSONB
         <code>connection.config</code> column (Hibernate's <code>json_format_mapper</code> is wired to it in
         <code>HibernateJacksonFormatMapperConfig</code>). Annotating the component would therefore drop the
         secret on write and destroy the stored value — the API-boundary filter below is the only place
         the two concerns can be separated.

         <p>Mirroring the summary fields (rather than embedding the summary record) keeps
         the JSON shape flat — the API consumer sees one record, not a nested <code>summary</code>
         object.
      properties:
        capabilities:
          type: array
          items:
            type: string
            enum:
            - WEBHOOK_INGEST
            - TOKEN_REFRESH
            - FEEDBACK_DELIVERY
            - INLINE_FINDINGS
            - APPROVAL_WORKFLOW
            - SCOPE_CHANGES
          uniqueItems: true
        config:
          type: object
          additionalProperties: {}
        createdAt:
          type: string
          format: date-time
        displayName:
          type: string
        family:
          type: string
          enum:
          - SCM
          - MESSAGING
          - DOCUMENTATION
        id:
          type: integer
          format: int64
        instanceKey:
          type: string
        kind:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        state:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
        stateReason:
          type: string
        updatedAt:
          type: string
          format: date-time
    ConnectionSummary:
      type: object
      description: |-
        Wire shape returned by the <code>GET /workspaces/{workspaceSlug</code>/connections}
         list + by lifecycle endpoints (suspend, reactivate). Lightweight — no config, no
         credentials, no audit. Capabilities are looked up from the per-kind manifest at
         response build time so adding/removing a capability needs no DB migration.
      properties:
        capabilities:
          type: array
          items:
            type: string
            enum:
            - WEBHOOK_INGEST
            - TOKEN_REFRESH
            - FEEDBACK_DELIVERY
            - INLINE_FINDINGS
            - APPROVAL_WORKFLOW
            - SCOPE_CHANGES
          uniqueItems: true
        createdAt:
          type: string
          format: date-time
        displayName:
          type: string
        family:
          type: string
          enum:
          - SCM
          - MESSAGING
          - DOCUMENTATION
        id:
          type: integer
          format: int64
        instanceKey:
          type: string
        kind:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        state:
          type: string
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
        stateReason:
          type: string
        updatedAt:
          type: string
          format: date-time
    ConnectionSyncStatus:
      type: object
      description: Unified sync-observability status for one connection
      properties:
        activeJob:
          $ref: "#/components/schemas/SyncJob"
          description: "Currently PENDING/RUNNING job, if any"
        backfill:
          $ref: "#/components/schemas/BackfillSummary"
          description: "Connection-level backfill rollup, if applicable"
        backfillSupported:
          type: boolean
          description: "Whether this kind's runner offers an explicitly triggerable\
            \ backfill pass. Reflects the vendor capability only — the scheduled-backfill\
            \ flag does not gate it, so a manual backfill stays available while the\
            \ automatic cycle is administratively paused."
        connectionId:
          type: integer
          format: int64
          description: Connection id
        connectionState:
          type: string
          description: Raw connection lifecycle state
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
        health:
          type: string
          description: Derived health
          enum:
          - PENDING
          - HEALTHY
          - DEGRADED
          - FAILED
          - SUSPENDED
        kind:
          type: string
          description: Integration kind
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        lastEventProcessedAt:
          type: string
          format: date-time
          description: "When the last inbound webhook/event was processed for this\
            \ connection, if any"
        lastJob:
          $ref: "#/components/schemas/SyncJob"
          description: "Most recently finished job, if any"
        lastSuccessfulSyncAt:
          type: string
          format: date-time
          description: Most recent successful job's finish time
        nextScheduledSyncAt:
          type: string
          format: date-time
          description: When the next periodic reconciliation is expected
        rateLimit:
          $ref: "#/components/schemas/RateLimitSnapshot"
          description: "Current rate-limit budget, if known"
        resourceCounts:
          $ref: "#/components/schemas/ResourceCounts"
          description: Resource-level rollup
        syncIntervalSeconds:
          type: integer
          format: int64
          description: "The periodic reconciliation's cadence in seconds, when the\
            \ schedule has a regular one. This is what makes a resource's lastSyncedAt\
            \ judgeable: \"synced 4h ago\" is only stale if the cadence is hourly,\
            \ and a client cannot know that without this. Null when the schedule is\
            \ irregular or unparseable — clients must then decline to judge staleness\
            \ rather than assume a default, exactly as the server's own stale rollup\
            \ does."
        webhookRegistered:
          type: boolean
          description: Whether the vendor webhook registration is present; null if
            not applicable/unknown
      required:
      - backfillSupported
      - connectionId
      - connectionState
      - health
      - kind
      - resourceCounts
    Contributor:
      type: object
      description: Information about a contributor to the Hephaestus project
      properties:
        avatarUrl:
          type: string
          description: URL to the contributor's avatar image
        contributions:
          type: integer
          format: int32
          description: Number of contributions to the project
          example: 42
        htmlUrl:
          type: string
          description: URL to the contributor's GitHub profile
        id:
          type: integer
          format: int64
          description: GitHub user ID
        login:
          type: string
          description: GitHub username
        name:
          type: string
          description: Display name of the contributor
      required:
      - avatarUrl
      - htmlUrl
      - id
      - login
      - name
    CreateCuratedAreaRequest:
      type: object
      description: Request to add an area to the instance catalog
      properties:
        definition:
          $ref: "#/components/schemas/CuratedAreaRequest"
        slug:
          type: string
          example: reviewing-work
      required:
      - definition
      - slug
    CreateCuratedPracticeRequest:
      type: object
      description: Request to add a practice to the instance catalog
      properties:
        definition:
          $ref: "#/components/schemas/CuratedPracticeRequest"
        slug:
          type: string
          example: pr-description-quality
      required:
      - definition
      - slug
    CreateLlmConnectionRequest:
      type: object
      description: Create an instance LLM provider connection
      properties:
        apiKey:
          type: string
          description: API key (write-only; never returned)
        apiProtocol:
          type: string
          description: Wire protocol
          example: openai-completions
          minLength: 1
          pattern: openai-completions|openai-responses
        authMode:
          type: string
          description: Credential shape (default BEARER)
          enum:
          - BEARER
          - API_KEY
        baseUrl:
          type: string
          description: Provider base URL
          maxLength: 2048
          minLength: 0
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the connection is enabled (default false)
        slug:
          type: string
          description: Optional internal slug; generated from displayName when omitted
          maxLength: 63
          minLength: 0
      required:
      - apiProtocol
      - baseUrl
      - displayName
    CreateLlmModelRequest:
      type: object
      description: Create a model on an instance LLM connection
      properties:
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the model is active (default false)
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        slug:
          type: string
          description: Optional internal slug; generated from displayName when omitted
          maxLength: 63
          minLength: 0
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
        upstreamModelId:
          type: string
          description: Upstream provider model id
          maxLength: 256
          minLength: 0
      required:
      - displayName
      - upstreamModelId
    CreateLoginProviderRequest:
      type: object
      properties:
        baseUrl:
          type: string
          description: Instance base URL (GitLab only; GitHub is always github.com)
          example: https://gitlab.example.com
        clientId:
          type: string
          minLength: 1
        clientSecret:
          type: string
          description: "OAuth client secret; sealed at rest, never returned"
          minLength: 1
        displayName:
          type: string
          description: Label shown on the login button; defaults to the registrationId
        registrationId:
          type: string
          description: Stable id used in the OAuth callback path and identity resolution
          example: gitlab-acme
          minLength: 1
        scopes:
          type: string
          description: Space-separated scopes; defaulted by provider type if omitted
        type:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
      required:
      - clientId
      - clientSecret
      - registrationId
      - type
    CreatePracticeAreaRequest:
      type: object
      description: Request to create a new practice area
      properties:
        color:
          type: string
          description: Optional palette colour key for the area's chip
          example: rose
          maxLength: 32
          minLength: 0
          pattern: "^[a-z]+$"
        description:
          type: string
          description: What this area develops
          maxLength: 5000
          minLength: 0
        displayOrder:
          type: integer
          format: int32
          description: Sort order within the workspace. Appended when omitted.
          example: 1
        icon:
          type: string
          description: Optional lucide icon name giving the area a glanceable identity
          example: ShieldAlert
          maxLength: 64
          minLength: 0
          pattern: "^[A-Za-z0-9]+$"
        name:
          type: string
          description: Human-readable name
          example: Submitting review-ready work
          maxLength: 128
          minLength: 3
        slug:
          type: string
          description: URL-safe identifier unique within the workspace
          example: review-ready-work
          maxLength: 64
          minLength: 3
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      required:
      - name
      - slug
    CreatePracticeRequest:
      type: object
      description: Request to create a new practice definition
      properties:
        areaSlug:
          type:
          - string
          - "null"
          description: Practice area to add the practice to. Omit or set to null for
            Unassigned.
        automatedReviewPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
          description: Versioned review settings; omit to use the recommended ones
            for the work type the bindings name
        bindings:
          type: array
          description: "The one occasion this practice is reviewed on, with the evidence\
            \ that review reads. The kind of work reviewed is read off the signals."
          items:
            $ref: "#/components/schemas/PracticeBinding"
          maxItems: 1
          minItems: 1
        criteria:
          type: string
          description: Practice review criteria
          maxLength: 50000
          minLength: 0
        name:
          type: string
          description: Human-readable name
          example: PR Description Quality
          maxLength: 128
          minLength: 3
        precomputeScript:
          type: string
          description: TypeScript/Bun static analysis run before automated review
          maxLength: 100000
          minLength: 0
        slug:
          type: string
          description: URL-safe identifier unique within the workspace
          example: pr-description-quality
          maxLength: 64
          minLength: 3
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        whatGoodLooksLike:
          type: string
          description: "Developer-facing exemplar; a concrete instance, not the review\
            \ criteria"
          maxLength: 2000
          minLength: 0
        whyItMatters:
          type: string
          description: Plain-language rationale shown to the developer
          maxLength: 2000
          minLength: 0
      required:
      - bindings
      - criteria
      - name
      - slug
    CreateReaction:
      type: object
      description: Submit a reaction to a delivered feedback unit
      properties:
        action:
          type: string
          description: The reaction action to record
          enum:
          - ADDRESSED
          - DISPUTED
          - NOT_APPLICABLE
        explanation:
          type: string
          description: Explanation for the reaction. Required when action is DISPUTED.
          maxLength: 2000
          minLength: 0
      required:
      - action
    CreateReviewBackfillRunRequest:
      type: object
      description: |-
        Ask for a campaign to be enumerated and costed. Creates nothing that spends money: the run comes back
         awaiting confirmation, and only a subsequent status change starts it.
      properties:
        artifactKind:
          type: string
          description: Kind of work to review
          enum:
          - scm.pull_request
          - scm.issue
          example: scm.pull_request
        fromAt:
          type: string
          format: date-time
          description: "Window start, inclusive, over the artifact's creation time"
        toAt:
          type: string
          format: date-time
          description: "Window end, exclusive"
      required:
      - artifactKind
      - fromAt
      - toAt
    CreateReviewRequest:
      type: object
      description: The piece of work a review is being asked for
      properties:
        artifactId:
          type: integer
          format: int64
          description: "The artifact's internal id, as the trace and review listings\
            \ report it"
        artifactKind:
          type: string
          description: "The artifact kind's wire id, e.g. scm.pull_request. A raw\
            \ string rather than a closed enum because kinds are an open vocabulary:\
            \ a build that has never heard of a kind should refuse it by name, not\
            \ fail to parse the request."
          example: scm.pull_request
          minLength: 1
      required:
      - artifactId
      - artifactKind
    CreateReviewSweepScheduleRequest:
      type: object
      description: |-
        Create a standing instruction to sweep this workspace's recent work.

         <p>Unlike a backfill campaign there is no separate confirmation step: this request <em>is</em> the
         authorisation to spend on the cadence it names, and it is recorded against the account that made it.

         <p>There is deliberately no repository or author list. Which repositories are reviewed is the
         workspace's review scope and whose work is reviewed is the practice-review role; both already apply to
         every review this workspace runs, a sweep included.
      properties:
        artifactKind:
          type: string
          description: Kind of work to sweep
          enum:
          - scm.pull_request
          - scm.issue
          example: scm.pull_request
        cadence:
          type: string
          description: How often the sweep runs
          enum:
          - DAILY
          - WEEKLY
        lookbackDays:
          type: integer
          format: int32
          description: "How far back each sweep looks, in days"
          example: 2
          maximum: 7
          minimum: 1
      required:
      - artifactKind
      - cadence
      - lookbackDays
    CreateWorkspaceLlmConnectionRequest:
      type: object
      description: Connect your own AI provider
      properties:
        apiKey:
          type: string
          description: API key (write-only; never returned)
        apiProtocol:
          type: string
          description: Wire protocol
          example: openai-completions
          minLength: 1
          pattern: openai-completions|openai-responses
        authMode:
          type: string
          description: Credential shape (default BEARER)
          enum:
          - BEARER
          - API_KEY
        baseUrl:
          type: string
          description: Provider base URL
          maxLength: 2048
          minLength: 0
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the connection is active (default false)
        slug:
          type: string
          description: Optional internal slug; generated from displayName when omitted
          maxLength: 63
          minLength: 0
      required:
      - apiProtocol
      - baseUrl
      - displayName
    CreateWorkspaceLlmModelRequest:
      type: object
      description: Create a model on your AI provider
      properties:
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the model is active (default false)
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: "Cache-read rate per 1M tokens (USD), if applicable"
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: "Cache-write rate per 1M tokens (USD), if applicable"
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD); required when the model has
            a price
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD); required when the model has
            a price
        priceNote:
          type: string
          description: "Note; required when the model is free (e.g. self-hosted, no\
            \ cost)"
          maxLength: 500
          minLength: 0
        pricingMode:
          type: string
          description: Pricing mode (default UNPRICED)
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
        slug:
          type: string
          description: Optional internal slug; generated from displayName when omitted
          maxLength: 63
          minLength: 0
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
        upstreamModelId:
          type: string
          description: Upstream provider model id
          maxLength: 256
          minLength: 0
      required:
      - displayName
      - upstreamModelId
    CreateWorkspaceRequest:
      type: object
      description: Request to create a new workspace
      properties:
        accountLogin:
          type: string
          description: Git provider account login (GitHub org/user or GitLab group
            path)
          example: my-org
          maxLength: 255
          minLength: 0
        accountType:
          type: string
          description: Type of account (USER or ORG)
          enum:
          - ORG
          - USER
        displayName:
          type: string
          description: Human-readable name of the workspace
          example: My Workspace
          maxLength: 120
          minLength: 0
        kind:
          type: string
          description: "Integration kind to provision. SLACK flows through OAuth,\
            \ not this endpoint."
          enum:
          - GITHUB
          - GITLAB
          example: GITLAB
        ownerUserId:
          type: integer
          format: int64
          deprecated: true
          description: "Deprecated: ignored by the server. The authenticated user\
            \ always becomes the owner."
        personalAccessToken:
          type: string
          description: Personal Access Token. Required for both kinds (GitLab API
            or GitHub PAT). Stored encrypted at rest.
          example: glpat-...
          maxLength: 512
          minLength: 0
        serverUrl:
          type: string
          description: Custom server URL for self-hosted GitLab instances. Must use
            HTTPS. Defaults to https://gitlab.com if not specified.
          example: https://gitlab.example.com
        workspaceSlug:
          type: string
          description: URL-friendly identifier for the workspace
          example: my-workspace
          minLength: 1
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      required:
      - accountLogin
      - accountType
      - displayName
      - kind
      - workspaceSlug
    CuratedArea:
      type: object
      properties:
        definition:
          $ref: "#/components/schemas/CuratedAreaRequest"
        position:
          type: integer
          format: int32
        shipped:
          $ref: "#/components/schemas/CuratedAreaRequest"
        slug:
          type: string
        status:
          $ref: "#/components/schemas/CatalogEntryStatus"
      required:
      - definition
      - position
      - slug
      - status
    CuratedAreaRequest:
      type: object
      description: A complete curated practice area definition
      properties:
        color:
          type: string
          maxLength: 32
          minLength: 0
        description:
          type: string
          maxLength: 2000
          minLength: 0
        icon:
          type: string
          maxLength: 64
          minLength: 0
        name:
          type: string
          maxLength: 128
          minLength: 3
      required:
      - name
    CuratedCatalog:
      type: object
      properties:
        areas:
          type: array
          items:
            $ref: "#/components/schemas/CuratedArea"
        customOrder:
          type: boolean
        etag:
          type: string
          description: Strong entity tag to send in If-Match when reordering this
            catalog
        practices:
          type: array
          items:
            $ref: "#/components/schemas/CuratedPracticeSummary"
        summary:
          $ref: "#/components/schemas/CuratedCatalogSummary"
      required:
      - areas
      - customOrder
      - etag
      - practices
      - summary
    CuratedCatalogSummary:
      type: object
      properties:
        editedHere:
          type: integer
          format: int32
        noLongerShipped:
          type: integer
          format: int32
        notOffered:
          type: integer
          format: int32
        total:
          type: integer
          format: int32
        updatesChangingDetection:
          type: integer
          format: int32
        updatesChangingPresentation:
          type: integer
          format: int32
        updatesChangingWordingOnly:
          type: integer
          format: int32
        yours:
          type: integer
          format: int32
      required:
      - editedHere
      - noLongerShipped
      - notOffered
      - total
      - updatesChangingDetection
      - updatesChangingPresentation
      - updatesChangingWordingOnly
      - yours
    CuratedPractice:
      type: object
      properties:
        definition:
          $ref: "#/components/schemas/CuratedPracticeDefinition"
        position:
          type: integer
          format: int32
        shipped:
          $ref: "#/components/schemas/CuratedPracticeDefinition"
        slug:
          type: string
        status:
          $ref: "#/components/schemas/CatalogEntryStatus"
      required:
      - definition
      - position
      - slug
      - status
    CuratedPracticeDefinition:
      type: object
      description: A resolved curated practice definition
      properties:
        areaSlug:
          type: string
        artifactKind:
          type: string
        automatedReviewPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
        automatedReviewValidation:
          $ref: "#/components/schemas/PracticeAutomatedReviewValidation"
        bindings:
          type: array
          items:
            $ref: "#/components/schemas/PracticeBinding"
        criteria:
          type: string
        name:
          type: string
        precomputeScript:
          type: string
        whatGoodLooksLike:
          type: string
        whyItMatters:
          type: string
      required:
      - artifactKind
      - automatedReviewPolicy
      - automatedReviewValidation
      - bindings
      - criteria
      - name
    CuratedPracticeRequest:
      type: object
      description: A complete curated practice definition
      properties:
        areaSlug:
          type: string
          maxLength: 64
          minLength: 0
        automatedReviewPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
          description: Evidence requirements; omit to use the recommended requirements
            for the selected work type
        bindings:
          type: array
          description: The one occasion this practice is reviewed on; the kind of
            work is read off the signals
          items:
            $ref: "#/components/schemas/PracticeBinding"
          maxItems: 1
          minItems: 1
        criteria:
          type: string
          maxLength: 50000
          minLength: 0
        name:
          type: string
          maxLength: 128
          minLength: 3
        precomputeScript:
          type: string
          maxLength: 100000
          minLength: 0
        whatGoodLooksLike:
          type: string
          maxLength: 2000
          minLength: 0
        whyItMatters:
          type: string
          maxLength: 2000
          minLength: 0
      required:
      - bindings
      - criteria
      - name
    CuratedPracticeSummary:
      type: object
      properties:
        areaSlug:
          type: string
        artifactKind:
          type: string
        automatedReview:
          $ref: "#/components/schemas/PracticeAutomatedReview"
        effectivelyOffered:
          type: boolean
        name:
          type: string
        position:
          type: integer
          format: int32
        slug:
          type: string
        status:
          $ref: "#/components/schemas/CatalogEntryStatus"
      required:
      - artifactKind
      - automatedReview
      - effectivelyOffered
      - name
      - position
      - slug
      - status
    CurrentUserView:
      type: object
      properties:
        accessTokenExpiresAt:
          type: integer
          format: int64
        appRole:
          type: string
        avatarUrl:
          type: string
        displayName:
          type: string
        gitProviderId:
          type: string
        hasGitLabIdentity:
          type: boolean
        id:
          type: integer
          format: int64
        identityProvider:
          type: string
        impersonating:
          type: boolean
        impersonatorId:
          type: integer
          format: int64
        linkedProviders:
          type: array
          items:
            $ref: "#/components/schemas/LinkedProvider"
        primaryEmail:
          type: string
        profileUrl:
          type: string
        roles:
          type: array
          items:
            type: string
        status:
          type: string
        username:
          type: string
    DeveloperPracticeSummary:
      type: object
      description: Per-practice observation summary for a developer
      properties:
        badCount:
          type: integer
          format: int64
          description: Number of BAD (problem) observations
        goodCount:
          type: integer
          format: int64
          description: Number of GOOD (strength) observations
        lastObservedAt:
          type: string
          format: date-time
          description: Timestamp of most recent observation
        practiceName:
          type: string
          description: Practice name
        practiceSlug:
          type: string
          description: Practice slug
        totalObservations:
          type: integer
          format: int64
          description: Total number of observations
      required:
      - badCount
      - goodCount
      - practiceName
      - practiceSlug
      - totalObservations
    EvidenceCitation:
      type: object
      description: A verified quote and its exact source location
      properties:
        artifactPath:
          type: string
        endLine:
          type: integer
          format: int32
        path:
          type: string
        quote:
          type: string
        quoteRedacted:
          type: boolean
        side:
          type: string
          enum:
          - OLD
          - NEW
        sourceKind:
          type: string
        startLine:
          type: integer
          format: int32
      required:
      - artifactPath
      - endLine
      - path
      - quoteRedacted
      - sourceKind
      - startLine
    ExportCreated:
      type: object
      description: Acknowledgement returned when a data export is requested.
      properties:
        id:
          type: integer
          format: int64
        status:
          type: string
    ExportStatus:
      type: object
      description: Status of a requested data export; timestamps are null until the
        matching transition occurs.
      properties:
        completedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        id:
          type: integer
          format: int64
        requestedAt:
          type: string
          format: date-time
        status:
          type: string
    FeatureFlags:
      type: object
      description: Feature flags evaluated for the current user
      properties:
        ADMIN:
          type: boolean
          description: User has admin privileges
        GITLAB_WORKSPACE_CREATION:
          type: boolean
          description: GitLab workspace creation feature is enabled
        MENTOR_ACCESS:
          type: boolean
          description: User has access to the AI Mentor feature
        NOTIFICATION_ACCESS:
          type: boolean
          description: User can receive notifications
        PRACTICE_REVIEW_FOR_ALL:
          type: boolean
          description: Practice review runs for all users regardless of role
        RUN_PRACTICE_REVIEW:
          type: boolean
          description: User's PRs trigger practice review
    FxRateInfo:
      type: object
      description: "Display-only currency conversion for the USD amounts in this response.\
        \ Multiply a USD amount by ratePerUsd to get the display-currency estimate;\
        \ always label it as an estimate and show rateDate, which is the date the\
        \ rate was actually published on (not necessarily today), and attribute it\
        \ to source."
      properties:
        currencyCode:
          type: string
          description: ISO 4217 code of the display currency
          example: EUR
        rateDate:
          type: string
          format: date
          description: "The date the underlying reference rate was published. A closed\
            \ month always reports a date inside that month, so its converted figure\
            \ never changes once the month ends."
          example: 2026-07-24
        ratePerUsd:
          type: number
          format: decimal
          description: "Units of the display currency per 1 USD, at 6 decimal places"
          example: 0.878966
        source:
          type: string
          description: "Who published the rate, so a disclosure can name it instead\
            \ of saying \"a reference rate\". ECB = the European Central Bank's daily\
            \ euro foreign-exchange reference rates."
          enum:
          - ECB
          example: ECB
      required:
      - currencyCode
      - rateDate
      - ratePerUsd
      - source
    GitHubProvider:
      type: object
      description: GitHub provider configuration
      properties:
        appInstallationUrl:
          type: string
          description: GitHub App installation URL
          example: https://github.com/apps/hephaestus/installations/new
    GitLabGroup:
      type: object
      description: GitLab group accessible to the provided PAT
      properties:
        avatarUrl:
          type: string
          description: Group avatar URL
        fullPath:
          type: string
          description: Full group path including parent groups
          example: my-org/my-team
        id:
          type: integer
          format: int64
          description: GitLab group numeric ID
        name:
          type: string
          description: Group display name
        visibility:
          type: string
          description: "Group visibility: public, internal, or private"
        webUrl:
          type: string
          description: Group web URL
      required:
      - fullPath
      - id
      - name
    GitLabPreflightRequest:
      type: object
      description: Request to validate a GitLab PAT or list accessible groups before
        workspace creation
      properties:
        groupFullPath:
          type: string
          description: "GitLab group full path, used as fallback for group/project\
            \ tokens that cannot access /api/v4/user"
          example: my-org/my-team
        personalAccessToken:
          type: string
          description: GitLab Personal Access Token to validate
          example: your-gitlab-token
          minLength: 1
        serverUrl:
          type: string
          description: GitLab server URL. Defaults to https://gitlab.com if not specified.
          example: https://gitlab.example.com
      required:
      - personalAccessToken
    GitLabPreflightResponse:
      type: object
      description: Result of GitLab PAT validation
      properties:
        error:
          type: string
          description: Error message if validation failed
        userId:
          type: integer
          format: int64
          description: GitLab user ID or group ID
        username:
          type: string
          description: Username of the token owner (personal tokens) or group name
            (group tokens)
        valid:
          type: boolean
          description: Whether the token is valid
      required:
      - valid
    GitLabProvider:
      type: object
      description: GitLab provider configuration
      properties:
        defaultServerUrl:
          type: string
          description: Default GitLab server URL
          example: https://gitlab.lrz.de
    IdentityProviderView:
      type: object
      description: |-
        One row per sign-in option. <code>providerType</code> drives the SPA's icon choice; <code>baseUrl</code> is
         the OAuth instance origin (scheme + host[:port]) of the authorization endpoint, so the
         workspace-creation wizard can match a target instance to its login.

         <p><b>baseUrl is the OAuth origin, not the SCM API origin.</b> It is only meaningful for GitLab rows,
         where the OAuth origin and the API origin coincide (e.g. <code>https://gitlab.example.com</code>). For a
         GitHub row it is <code>https://github.com</code> (the OAuth host), NOT <code>https://api.github.com</code>; the
         sole consumer (the workspace wizard) matches GitLab self-hosted origins and never relies on the GitHub
         value, so the discrepancy is harmless. Do not treat this as an SCM API base URL.
      properties:
        baseUrl:
          type: string
        displayName:
          type: string
        providerType:
          type: string
        registrationId:
          type: string
    IdentityView:
      type: object
      properties:
        avatarUrl:
          type: string
        displayName:
          type: string
        id:
          type: integer
          format: int64
        lastLoginAt:
          type: string
          format: date-time
        providerType:
          type: string
        subject:
          type: string
        username:
          type: string
    ImpersonateRequest:
      type: object
      properties:
        reason:
          type: string
          minLength: 1
        targetAccountId:
          type: integer
          format: int64
      required:
      - reason
      - targetAccountId
    InAppEvidence:
      type: object
      description: One piece of work the pattern was observed on
      properties:
        artifactId:
          type: integer
          format: int64
          description: Identifier of the work within its kind
        artifactKind:
          type: string
          description: "Kind of work, e.g. scm.pull_request"
        observedAt:
          type: string
          format: date-time
          description: When the measurement behind this occurrence was taken
        summary:
          type: string
          description: What the review recorded on this piece of work
      required:
      - artifactId
      - artifactKind
      - observedAt
    InAppFeedback:
      type: object
      description: A process-level message on the developer's own practice pages
      properties:
        areaName:
          type: string
          description: Area display name; null when the practice has none
        areaSlug:
          type: string
          description: Area the practice sits in; null when the practice has none
        body:
          type: string
          description: "The message, as Markdown; ends with the habit to try next"
        evidence:
          type: array
          description: "The pieces of work the habit was observed on, newest first"
          items:
            $ref: "#/components/schemas/InAppEvidence"
        headline:
          type: string
          description: "Short headline naming the habit, never the person"
        id:
          type: string
          format: uuid
        occurrenceCount:
          type: integer
          format: int32
          description: How many pieces of work carry it — the length of the evidence
            list
        practiceName:
          type: string
        practiceSlug:
          type: string
          description: Practice this habit belongs to
        preparedAt:
          type: string
          format: date-time
          description: When the message was composed
        readAt:
          type: string
          format: date-time
          description: When this developer first opened it; null until they have
        whatGoodLooksLike:
          type: string
          description: "What good looks like, in the learner's framing"
        whyItMatters:
          type: string
          description: "Why this practice matters, in the learner's framing"
      required:
      - body
      - evidence
      - headline
      - id
      - occurrenceCount
      - practiceName
      - practiceSlug
      - preparedAt
    InitiateConnectionRequest:
      type: object
      description: |-
        Inbound payload for <code>POST /workspaces/{workspaceSlug</code>/connections}.

         <p><code>userInput</code> is intentionally a free-form map so per-kind ConnectionStrategy
         implementations can dictate their own field schema (e.g. GitLab needs <code>pat</code> +
         <code>group_id</code>; GitHub needs nothing because the install URL is server-configured).
         Validation is the strategy's responsibility — invalid input surfaces as a 400 via
         <code>IllegalArgumentException</code>.
      properties:
        kind:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        userInput:
          type: object
          additionalProperties:
            type: string
    InitiateConnectionResponse:
      type: object
      description: |-
        Flat response for <code>POST /workspaces/{workspaceSlug</code>/connections}.

         <p>Two outcomes, distinguished by <code>type</code>:
         <ul>
           <li><code>REDIRECT</code> — OAuth / App-install flows (GitHub, Slack). <code>vendorUrl</code> is the
               URL to bounce the browser to; the signed OAuth state is already embedded in it.</li>
           <li><code>LINKED</code> — inline-credential flows (GitLab PAT). <code>connectionId</code> is the
               newly-created Connection; no further round-trip is needed.</li>
         </ul>

         <p>Deliberately a flat record (nullable per-variant fields) rather than a sealed
         <code>oneOf</code> hierarchy: a discriminated union serialises to OpenAPI <code>oneOf +
         discriminator</code>, which the webapp's hey-api react-query transformer mishandles. A flat shape
         round-trips cleanly through code generation, so the webapp consumes the generated client
         instead of a hand-rolled fetch wrapper.
      properties:
        connectionId:
          type: integer
          format: int64
        type:
          type: string
          enum:
          - REDIRECT
          - LINKED
        vendorUrl:
          type: string
          format: uri
    InstanceLlmSettings:
      type: object
      description: Instance-wide LLM governance settings
      properties:
        allowWorkspaceConnections:
          type: boolean
          description: Whether workspaces may register their own LLM connections
        allowedEgressHosts:
          type: string
          description: Comma/newline-delimited egress host allowlist; blank = allow
            any public host
          example: "api.openai.com, llm-gateway.example.com"
      required:
      - allowWorkspaceConnections
    InstanceSettings:
      type: object
      properties:
        etag:
          type: string
        silentModeChangedAt:
          type: string
          format: date-time
        silentModeChangedBy:
          type: string
        silentModeEngaged:
          type: boolean
        silentModeReason:
          type: string
      required:
      - etag
      - silentModeEngaged
    IntegrationCatalogEntry:
      type: object
      description: Integration kind availability + connection status for this workspace
      properties:
        connected:
          type: boolean
          description: Whether this workspace has a (non-UNINSTALLED) connection for
            this kind
        connectionId:
          type: integer
          format: int64
          description: "Connection id, if connected"
        connectionState:
          type: string
          description: "Connection state, if connected"
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
        displayName:
          type: string
          description: Human-readable display name
        kind:
          type: string
          description: Integration kind
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
      required:
      - connected
      - displayName
      - kind
    LabelInfo:
      type: object
      description: Information about a label from a repository
      properties:
        color:
          type: string
          description: "Hex color code of the label (without #)"
          example: d73a4a
        id:
          type: integer
          format: int64
          description: Unique identifier of the label
        name:
          type: string
          description: Name of the label
        repository:
          $ref: "#/components/schemas/RepositoryInfo"
          description: Repository the label belongs to
      required:
      - color
      - id
      - name
    LeaderboardEntry:
      type: object
      description: A ranked entry in the leaderboard (individual or team)
      properties:
        numberOfApprovals:
          type: integer
          format: int32
          description: Count of review approvals
          example: 3
        numberOfChangeRequests:
          type: integer
          format: int32
          description: Count of change requests submitted
          example: 1
        numberOfClosedIssues:
          type: integer
          format: int32
          description: Count of issues closed in the timeframe
          example: 2
        numberOfClosedPullRequests:
          type: integer
          format: int32
          description: Count of authored pull requests closed without merge in the
            timeframe
          example: 1
        numberOfCodeComments:
          type: integer
          format: int32
          description: Count of scored inline feedback comments on pull requests authored
            by someone else
          example: 7
        numberOfComments:
          type: integer
          format: int32
          description: Count of comment-only review submissions
          example: 10
        numberOfMergedPullRequests:
          type: integer
          format: int32
          description: Count of authored pull requests merged in the timeframe
          example: 6
        numberOfOpenPullRequests:
          type: integer
          format: int32
          description: Count of authored pull requests opened in the timeframe that
            are still open
          example: 2
        numberOfOpenedIssues:
          type: integer
          format: int32
          description: Count of issues opened in the timeframe
          example: 3
        numberOfOwnReplies:
          type: integer
          format: int32
          description: Count of visible-only discussion replies and inline thread
            replies on the contributor's own pull requests
          example: 4
        numberOfReviewedPRs:
          type: integer
          format: int32
          description: Count of distinct PRs reviewed
          example: 5
        numberOfUnknowns:
          type: integer
          format: int32
          description: Count of reviews with unknown/unrecognized state
          example: 0
        rank:
          type: integer
          format: int32
          description: Position in the leaderboard (1-based)
          example: 1
        reviewedPullRequests:
          type: array
          description: Sample of reviewed PRs for display
          items:
            $ref: "#/components/schemas/PullRequestInfo"
        score:
          type: integer
          format: int32
          description: Total XP score for the timeframe
          example: 150
        team:
          $ref: "#/components/schemas/TeamInfo"
          description: "Team info (populated in TEAM mode, null in INDIVIDUAL mode)"
        user:
          $ref: "#/components/schemas/UserInfo"
          description: "User info (populated in INDIVIDUAL mode, null in TEAM mode)"
      required:
      - numberOfApprovals
      - numberOfChangeRequests
      - numberOfClosedIssues
      - numberOfClosedPullRequests
      - numberOfCodeComments
      - numberOfComments
      - numberOfMergedPullRequests
      - numberOfOpenPullRequests
      - numberOfOpenedIssues
      - numberOfOwnReplies
      - numberOfReviewedPRs
      - numberOfUnknowns
      - rank
      - reviewedPullRequests
      - score
    LeagueChange:
      type: object
      properties:
        leaguePointsChange:
          type: integer
          format: int32
        login:
          type: string
      required:
      - leaguePointsChange
      - login
    LearnerPractice:
      type: object
      description: Learner-facing view of a practice — criteria absent by construction
      properties:
        areaSlug:
          type: string
          description: "Slug of the practice area this belongs to, if any"
        name:
          type: string
          description: Human-readable name
        slug:
          type: string
          description: URL-safe identifier
        whatGoodLooksLike:
          type: string
          description: A concrete picture of doing this well
        whyItMatters:
          type: string
          description: "Why this practice matters, in plain language"
      required:
      - name
      - slug
    LinearAchievementProgress:
      allOf:
      - $ref: "#/components/schemas/AchievementProgress"
      - type: object
        properties:
          current:
            type: integer
            format: int32
          target:
            type: integer
            format: int32
      description: Linear progress with current and target counts
      required:
      - current
      - target
    LinkedProvider:
      type: object
      description: "A provider instance the current user is linked to: its type +\
        \ server-url origin."
      properties:
        serverUrl:
          type: string
        type:
          type: string
    LlmConnection:
      type: object
      description: Instance LLM provider connection (API key redacted)
      properties:
        apiKeyLast4:
          type: string
          description: "Last four characters of the stored API key, if any"
        apiProtocol:
          type: string
          description: Wire protocol
          example: openai-completions
        authMode:
          type: string
          description: Credential shape
          enum:
          - BEARER
          - API_KEY
        baseUrl:
          type: string
          description: Provider base URL
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        displayName:
          type: string
          description: Human-readable name
        enabled:
          type: boolean
          description: Whether the connection is enabled
        hasApiKey:
          type: boolean
          description: Whether an API key is stored
        id:
          type: integer
          format: int64
          description: Connection ID
        slug:
          type: string
          description: Unique slug
          example: openai-prod
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
      required:
      - apiProtocol
      - authMode
      - baseUrl
      - createdAt
      - displayName
      - enabled
      - hasApiKey
      - id
      - slug
    LlmModel:
      type: object
      description: Instance catalog model
      properties:
        connectionDisplayName:
          type: string
          description: Owning connection's display name
        connectionId:
          type: integer
          format: int64
          description: Owning connection id
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        currentPrice:
          $ref: "#/components/schemas/LlmModelPrice"
          description: Current price; null if none has ever been set
        displayName:
          type: string
          description: Human-readable name
        enabled:
          type: boolean
          description: Active toggle
        grantedWorkspaceIds:
          type: array
          description: Workspace ids shared with; only meaningful when visibility
            is GRANTED
          items:
            type: integer
            format: int64
        id:
          type: integer
          format: int64
          description: Model id
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        slug:
          type: string
          description: Unique slug within the connection
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
        upstreamModelId:
          type: string
          description: Upstream provider model id
        visibility:
          type: string
          description: Share with all workspaces (PUBLIC) or only selected ones (GRANTED)
          enum:
          - PUBLIC
          - GRANTED
      required:
      - connectionDisplayName
      - connectionId
      - createdAt
      - displayName
      - enabled
      - grantedWorkspaceIds
      - id
      - slug
      - supportsReasoning
      - upstreamModelId
      - visibility
    LlmModelPrice:
      type: object
      description: "A model's price, per 1M tokens"
      properties:
        currency:
          type: string
          description: Currency code
        effectiveFrom:
          type: string
          format: date-time
          description: When this price took effect
        effectiveTo:
          type: string
          format: date-time
          description: When this price was superseded; null if still current
        id:
          type: integer
          format: int64
          description: Price row id
        note:
          type: string
          description: Note
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: Cache-read rate per 1M tokens (USD)
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: Cache-write rate per 1M tokens (USD)
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD)
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD)
        pricingMode:
          type: string
          description: Pricing mode
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
      required:
      - currency
      - effectiveFrom
      - id
      - pricingMode
    LlmProbeResult:
      type: object
      description: Result of testing an LLM connection's /models endpoint
      properties:
        message:
          type: string
          description: Human-readable diagnostic when not reachable
        models:
          type: array
          description: Model ids returned by the provider (empty if unreachable)
          items:
            type: string
        reachable:
          type: boolean
          description: Whether the provider answered with a successful models listing
        statusCode:
          type: integer
          format: int32
          description: "HTTP status returned by the provider, if any"
      required:
      - models
      - reachable
    LlmUsageByDay:
      type: object
      description: Spend for one UTC day
      properties:
        day:
          type: string
          format: date
        events:
          type: integer
          format: int64
        instanceTotalCostUsd:
          type: number
          format: decimal
          description: "Confirmed spend on shared (instance) models for this day,\
            \ in USD."
        ownProviderTotalCostUsd:
          type: number
          format: decimal
          description: "Spend on this workspace's own connected provider(s) for this\
            \ day, in USD."
        unpricedEventCount:
          type: integer
          format: int64
          description: Calls this day whose price is not yet known. Excluded from
            both totals above.
      required:
      - day
      - events
      - instanceTotalCostUsd
      - ownProviderTotalCostUsd
      - unpricedEventCount
    LlmUsageByJobType:
      type: object
      description: Month spend aggregated by job type
      properties:
        cacheReadTokens:
          type: integer
          format: int64
        cacheWriteTokens:
          type: integer
          format: int64
        events:
          type: integer
          format: int64
          description: Ledger events (jobs / mentor turns)
        inputTokens:
          type: integer
          format: int64
        instanceTotalCostUsd:
          type: number
          format: decimal
          description: "Confirmed spend on shared (instance) models for this job type,\
            \ in USD."
        jobType:
          type: string
          enum:
          - PULL_REQUEST_REVIEW
          - ISSUE_REVIEW
          - CONVERSATION_REVIEW
          - DOCUMENT_REVIEW
          - MENTOR_TURN
        outputTokens:
          type: integer
          format: int64
        ownProviderTotalCostUsd:
          type: number
          format: decimal
          description: "Spend on this workspace's own connected provider(s) for this\
            \ job type, in USD."
        totalCalls:
          type: integer
          format: int64
          description: "LLM API calls, as reported by the runtime. Detection jobs\
            \ and mentor turns both include every assistant call in an internal tool\
            \ loop."
        unpricedEventCount:
          type: integer
          format: int64
          description: Calls for this job type whose price is not yet known. Excluded
            from both totals above.
      required:
      - cacheReadTokens
      - cacheWriteTokens
      - events
      - inputTokens
      - instanceTotalCostUsd
      - jobType
      - outputTokens
      - ownProviderTotalCostUsd
      - totalCalls
      - unpricedEventCount
    LoginProviderView:
      type: object
      description: Admin view of a login provider. The client secret is never included.
      properties:
        baseUrl:
          type: string
        createdAt:
          type: string
          format: date-time
        displayName:
          type: string
        enabled:
          type: boolean
        redirectUri:
          type: string
          description: Redirect/callback URI to register on the upstream OAuth app
        registrationId:
          type: string
        scopes:
          type: string
        seededFromEnv:
          type: boolean
        type:
          type: string
        updatedAt:
          type: string
          format: date-time
      required:
      - baseUrl
      - createdAt
      - displayName
      - redirectUri
      - registrationId
      - scopes
      - type
      - updatedAt
    ObservationDetail:
      type: object
      description: Full practice observation detail including delivered feedback and
        evidence
      properties:
        artifactId:
          type: integer
          format: int64
          description: Artifact entity ID
        artifactKind:
          type: string
          description: Artifact type (e.g. PULL_REQUEST)
        assessment:
          type: string
          description: "Assessment: GOOD or BAD; null when the presence carries no\
            \ direction (NOT_APPLICABLE, INCONCLUSIVE)"
          enum:
          - GOOD
          - BAD
        claimCurrentness:
          type: string
          description: Whether an observation was produced using the current review
            rules
          enum:
          - CURRENT
          - STALE
          - UNVERIFIABLE
        deliveredFeedback:
          type: string
          description: What to do — the delivered feedback for this observation (null
            if nothing was delivered)
        evidence:
          $ref: "#/components/schemas/ObservationEvidence"
        evidenceRationale:
          type: string
          description: Evidence-based rationale for the observation
        id:
          type: string
          format: uuid
          description: Observation ID
        observedAt:
          type: string
          format: date-time
          description: When the observation was made
        origin:
          type: string
          description: What occasioned the measurement; never mix origins in one trend
            line
          enum:
          - LIVE
          - MANUAL
          - BACKFILL
        practiceName:
          type: string
          description: Practice name
        practiceSlug:
          type: string
          description: Practice slug
        presence:
          type: string
          description: "Presence: PRESENT, ABSENT, NOT_APPLICABLE, or INCONCLUSIVE"
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
        severity:
          type: string
          description: Severity level (null unless assessment is BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        summary:
          type: string
          description: Observation summary
      required:
      - artifactId
      - artifactKind
      - claimCurrentness
      - id
      - observedAt
      - origin
      - practiceName
      - practiceSlug
      - presence
      - summary
    ObservationEvidence:
      type: object
      description: "Verified, source-bound evidence for an observation"
      properties:
        citations:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceCitation"
        detector:
          type: string
      required:
      - citations
    ObservationList:
      type: object
      description: Practice observation summary for list views
      properties:
        artifactId:
          type: integer
          format: int64
          description: Artifact entity ID
        artifactKind:
          type: string
          description: Artifact type (e.g. PULL_REQUEST)
        assessment:
          type: string
          description: "Assessment: GOOD or BAD; null when the presence carries no\
            \ direction (NOT_APPLICABLE, INCONCLUSIVE)"
          enum:
          - GOOD
          - BAD
        claimCurrentness:
          type: string
          description: Whether an observation was produced using the current review
            rules
          enum:
          - CURRENT
          - STALE
          - UNVERIFIABLE
        id:
          type: string
          format: uuid
          description: Observation ID
        observedAt:
          type: string
          format: date-time
          description: When the observation was made
        origin:
          type: string
          description: What occasioned the measurement; never mix origins in one trend
            line
          enum:
          - LIVE
          - MANUAL
          - BACKFILL
        practiceName:
          type: string
          description: Practice name
        practiceSlug:
          type: string
          description: Practice slug
        presence:
          type: string
          description: "Presence: PRESENT, ABSENT, NOT_APPLICABLE, or INCONCLUSIVE"
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
        severity:
          type: string
          description: Severity level (null unless assessment is BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        summary:
          type: string
          description: Observation summary
      required:
      - artifactId
      - artifactKind
      - claimCurrentness
      - id
      - observedAt
      - origin
      - practiceName
      - practiceSlug
      - presence
      - summary
    OutlineCollection:
      type: object
      description: A mirrored Outline collection with its sync state and live document
        count
      properties:
        collectionId:
          type: string
          description: Outline collection id (UUID; the natural key)
        color:
          type: string
          description: Collection color as configured in Outline
        createdAt:
          type: string
          format: date-time
          description: When the collection was registered for mirroring
        description:
          type: string
          description: Collection description as shown in Outline
        documentCount:
          type: integer
          format: int64
          description: Live (non-tombstoned) mirrored document count
        documentsUpstream:
          type: integer
          format: int32
          description: Documents upstream reported for this collection at the last
            enumeration (coverage denominator)
        exportsSkippedForBudget:
          type: integer
          format: int32
          description: Exports the last pass skipped because the shared budget ran
            out (0 on a clean pass)
        icon:
          type: string
          description: Collection icon as configured in Outline
        id:
          type: integer
          format: int64
          description: Internal registry row id
        lastSyncError:
          type: string
          description: "Last sync failure for this collection, cleared on the next\
            \ clean pass"
        lastSyncedAt:
          type: string
          format: date-time
          description: "When this collection last completed a full reconcile pass,\
            \ if any"
        name:
          type: string
          description: "Collection name as shown in Outline, if known"
        state:
          type: string
          description: Mirror lifecycle state (PAUSED freezes sync but keeps documents)
          enum:
          - ENABLED
          - PAUSED
        syncStatus:
          type: string
          description: Whether a clean full pass has completed since registration
            or the last resume
          enum:
          - PENDING
          - COMPLETE
        urlId:
          type: string
          description: Outline url id (the short slug in collection URLs)
      required:
      - collectionId
      - createdAt
      - documentCount
      - id
      - state
      - syncStatus
    OutlineCollectionCandidate:
      type: object
      description: "An Outline collection the token can see, offered in the add-collection\
        \ picker"
      properties:
        alreadyMirrored:
          type: boolean
          description: Whether this collection is already registered for mirroring
        collectionId:
          type: string
          description: Outline collection id (UUID)
        color:
          type: string
          description: Collection color as configured in Outline
        description:
          type: string
          description: Collection description as shown in Outline
        icon:
          type: string
          description: Collection icon as configured in Outline
        name:
          type: string
          description: Collection name as shown in Outline
        urlId:
          type: string
          description: Outline url id (the short slug in collection URLs)
      required:
      - alreadyMirrored
      - collectionId
    OutlineTokenStatus:
      type: object
      description: Live state of the API token behind the workspace's Outline connection
      properties:
        accepted:
          type: boolean
          description: "Whether Outline still accepts the stored token. False means\
            \ revoked, expired or rejected."
        expiresAt:
          type: string
          format: date-time
          description: When the token lapses. Absent for a key created without an
            expiry.
        last4:
          type: string
          description: "Last four characters of the token, as Outline reports them"
        lastActiveAt:
          type: string
          format: date-time
          description: When Outline last saw the token used
        name:
          type: string
          description: "The token's name in Outline. Absent when the token cannot\
            \ list its own key (a scoped key, or one owned by a user who cannot see\
            \ it) — sync is unaffected."
      required:
      - accepted
    PageAgentJob:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/AgentJob"
        empty:
          type: boolean
        first:
          type: boolean
        last:
          type: boolean
        number:
          type: integer
          format: int32
        numberOfElements:
          type: integer
          format: int32
        pageable:
          $ref: "#/components/schemas/PageableObject"
        size:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PageAuthEventView:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/AuthEventView"
        empty:
          type: boolean
        first:
          type: boolean
        last:
          type: boolean
        number:
          type: integer
          format: int32
        numberOfElements:
          type: integer
          format: int32
        pageable:
          $ref: "#/components/schemas/PageableObject"
        size:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PageConfigAuditEntryView:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ConfigAuditEntryView"
        empty:
          type: boolean
        first:
          type: boolean
        last:
          type: boolean
        number:
          type: integer
          format: int32
        numberOfElements:
          type: integer
          format: int32
        pageable:
          $ref: "#/components/schemas/PageableObject"
        size:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PageMetadata:
      properties:
        number:
          type: integer
          format: int64
        size:
          type: integer
          format: int64
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int64
    PageObservationList:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ObservationList"
        empty:
          type: boolean
        first:
          type: boolean
        last:
          type: boolean
        number:
          type: integer
          format: int32
        numberOfElements:
          type: integer
          format: int32
        pageable:
          $ref: "#/components/schemas/PageableObject"
        size:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PageSyncJob:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/SyncJob"
        empty:
          type: boolean
        first:
          type: boolean
        last:
          type: boolean
        number:
          type: integer
          format: int32
        numberOfElements:
          type: integer
          format: int32
        pageable:
          $ref: "#/components/schemas/PageableObject"
        size:
          type: integer
          format: int32
        sort:
          $ref: "#/components/schemas/SortObject"
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PageableObject:
      type: object
      properties:
        offset:
          type: integer
          format: int64
        pageNumber:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
        paged:
          type: boolean
        sort:
          $ref: "#/components/schemas/SortObject"
        unpaged:
          type: boolean
    PagedModelReviewFeedback:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ReviewFeedback"
        page:
          $ref: "#/components/schemas/PageMetadata"
    PagedModelReviewObservation:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ReviewObservation"
        page:
          $ref: "#/components/schemas/PageMetadata"
    PagedModelReviewRunSummary:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/ReviewRunSummary"
        page:
          $ref: "#/components/schemas/PageMetadata"
    PagedModelTracedArtifact:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/TracedArtifact"
        page:
          $ref: "#/components/schemas/PageMetadata"
    PlacePracticeRequest:
      type: object
      description: A practice's position in the catalog
      properties:
        areaSlug:
          type: string
          description: Destination area slug; omit to place the practice in Unassigned
          example: review-ready-work
        position:
          type: integer
          format: int32
          description: Zero-based position in the destination
          example: 1
          minimum: 0
      required:
      - position
    Practice:
      type: object
      description: Practice definition for evaluating developer contributions
      properties:
        areaSlug:
          type: string
          description: "Slug of the practice area this practice is bound to, if any"
        artifactKind:
          type: string
          description: "Kind of work this practice reviews, read off its bindings"
          example: scm.pull_request
        automatedReviewPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
        automatedReviewValidation:
          $ref: "#/components/schemas/PracticeAutomatedReviewValidation"
        bindings:
          type: array
          description: "The one occasion this practice is reviewed on, with the evidence\
            \ that review reads"
          items:
            $ref: "#/components/schemas/PracticeBinding"
        catalogOrigin:
          $ref: "#/components/schemas/CatalogOrigin"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the practice was created
        criteria:
          type: string
          description: Practice review criteria
        displayOrder:
          type: integer
          format: int32
          description: Position within its area (lowest first); ties broken by name
        id:
          type: integer
          format: int64
          description: Practice ID
        name:
          type: string
          description: Human-readable name
        precomputeScript:
          type: string
          description: TypeScript/Bun precompute script for static analysis before
            AI review
        reviewTier:
          $ref: "#/components/schemas/ReviewTierAssignment"
          description: "How much autonomy the system has over this practice, whether\
            \ that was set here or inherited from its area or workspace, and which\
            \ level decided it"
        slug:
          type: string
          description: URL-safe identifier unique within workspace
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the practice was last updated
        whatGoodLooksLike:
          type: string
          description: Developer-facing exemplar (learner layer)
        whyItMatters:
          type: string
          description: Developer-facing rationale (learner layer)
      required:
      - artifactKind
      - automatedReviewPolicy
      - automatedReviewValidation
      - bindings
      - createdAt
      - criteria
      - displayOrder
      - id
      - name
      - reviewTier
      - slug
      - updatedAt
    PracticeArea:
      type: object
      description: A practice area grouping related practices into a learning objective
      properties:
        catalogOrigin:
          $ref: "#/components/schemas/CatalogOrigin"
        color:
          type: string
          description: Optional palette colour key for the area's chip
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the area was created
        description:
          type: string
          description: What this area develops
        displayOrder:
          type: integer
          format: int32
          description: Sort order within the workspace
        icon:
          type: string
          description: Optional lucide icon name for the area's chip
        id:
          type: integer
          format: int64
          description: Area ID
        name:
          type: string
          description: Human-readable name
        reviewTier:
          $ref: "#/components/schemas/ReviewTierAssignment"
          description: "How much autonomy the system has over every practice in this\
            \ area that holds no tier of its own, whether that was set here or inherited\
            \ from the workspace, and which level decided it"
        slug:
          type: string
          description: URL-safe identifier unique within the workspace
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the area was last updated
        visibleInPracticeDashboards:
          type: boolean
          description: Whether this area is shown in practice dashboards
      required:
      - createdAt
      - displayOrder
      - id
      - name
      - reviewTier
      - slug
      - visibleInPracticeDashboards
    PracticeAutomatedReview:
      type: object
      description: Author-declared automated review and evidence sufficiency for one
        practice
      properties:
        evidenceSufficiency:
          type: string
          description: Whether meeting every evidence requirement provides enough
            context to review the work
          enum:
          - SUFFICIENT_WHEN_REQUIREMENTS_MET
          - DECLARED_EVIDENCE_INSUFFICIENT
          - NONE
        mode:
          type: string
          description: Implementation Hephaestus uses for automated review
          enum:
          - LANGUAGE_MODEL
          - NONE
      required:
      - evidenceSufficiency
      - mode
    PracticeAutomatedReviewPolicy:
      type: object
      description: Author-defined automated review settings for one practice revision
      properties:
        automatedReview:
          $ref: "#/components/schemas/PracticeAutomatedReview"
          description: Automated review configuration; human review is a separate
            process
        insufficiencyReason:
          $ref: "#/components/schemas/PracticeEvidenceLimitation"
          description: Why this practice needs a human rather than automated review;
            present only when evidenceSufficiency is DECLARED_EVIDENCE_INSUFFICIENT
        knownLimitations:
          type: array
          description: Claims the selected evidence cannot support even when every
            requirement passes
          items:
            $ref: "#/components/schemas/PracticeEvidenceLimitation"
        sourceContractVersion:
          type: string
          description: Exact contract version that defines source kinds and source-state
            semantics
        whenEvidenceIsInsufficient:
          type: string
          description: Conservative action when required evidence does not pass
          enum:
          - SKIP_AUTOMATED_REVIEW
      required:
      - automatedReview
      - knownLimitations
      - sourceContractVersion
      - whenEvidenceIsInsufficient
    PracticeAutomatedReviewValidation:
      type: object
      description: "Who stands behind a practice's automated-review policy, and which\
        \ exact policy"
      properties:
        policyDigest:
          type: string
          description: SHA-256 digest of the exact automated-review policy
        reviewRuleFingerprint:
          type: string
          description: Versioned fingerprint of the exact review rules
        sourceContractVersion:
          type: string
          description: Source contract the declared practice definition is written
            against
        status:
          type: string
          description: Validation lifecycle; authors cannot mark their own review
            policy as independently validated
          enum:
          - AUTHOR_DECLARED
      required:
      - policyDigest
      - reviewRuleFingerprint
      - sourceContractVersion
      - status
    PracticeBinding:
      type: object
      description: "An occasion that starts a review, and the evidence that review\
        \ reads"
      properties:
        appliesWhen:
          $ref: "#/components/schemas/PracticeSubject"
          description: What must be in the work for this practice to apply; omit to
            always apply
        needs:
          type: array
          description: "Sources a review occasioned this way reads, each with the\
            \ stance it takes"
          items:
            $ref: "#/components/schemas/PracticeEvidenceRequirement"
        onDrafts:
          type: boolean
          description: Whether an artifact still marked draft occasions this review;
            omit for false
        signals:
          type: array
          description: "Signals that occasion this review, e.g. scm.pull_request.merged"
          items:
            type: string
          minItems: 1
        subject:
          type: string
          description: Whose conduct this review judges; omit for AUTHOR
          enum:
          - AUTHOR
          - ASSIGNEE
          - REVIEWER
      required:
      - needs
      - signals
    PracticeDefinitionOptions:
      type: object
      description: "What a practice author may choose, per type of reviewed work"
      properties:
        sourceContractVersion:
          type: string
          description: Source contract these options describe
          example: 1.0.0
        workTypes:
          type: array
          items:
            $ref: "#/components/schemas/PracticeWorkTypeDefinitionOptions"
      required:
      - sourceContractVersion
      - workTypes
    PracticeEvidenceBlocker:
      type: object
      description: "One thing that blocked automated review, and how many reviews\
        \ it affected"
      properties:
        reasonCode:
          type: string
          description: Readiness reason recorded for that source or practice
          enum:
          - SOURCE_NOT_AVAILABLE
          - SOURCE_INCOMPLETE
          - SOURCE_EMPTY
          - NO_AUTOMATED_REVIEW
          - DECLARED_EVIDENCE_INSUFFICIENT
          - SUBJECT_NOT_IN_THE_WORK
        reviewsAffected:
          type: integer
          format: int32
          description: Reviews this blocker affected
        sourceKind:
          type: string
          description: Source that did not meet its requirement; absent when the practice
            itself runs no review
      required:
      - reasonCode
      - reviewsAffected
    PracticeEvidenceLimitation:
      type: object
      description: Known claim that the selected evidence cannot support even when
        every requirement passes
      properties:
        code:
          type: string
          description: Stable machine-readable identifier
          example: RUNTIME_BEHAVIOR_NOT_OBSERVED
          pattern: "[A-Z][A-Z0-9_]{2,63}"
        description:
          type: string
          description: Plain-language explanation of the claim the evidence cannot
            support
          maxLength: 500
          minLength: 1
      required:
      - code
      - description
    PracticeEvidenceOutcome:
      type: object
      description: Recent review readiness for one practice's evidence requirements
      properties:
        blockersObserved:
          type: array
          description: "Blockers seen on the skipped reviews, most frequent first"
          items:
            $ref: "#/components/schemas/PracticeEvidenceBlocker"
        consideredReviews:
          type: integer
          format: int32
          description: Reviews that considered this practice
        practiceSlug:
          type: string
          description: Practice this outcome describes
        reviewedCount:
          type: integer
          format: int32
          description: Reviews where the evidence met every requirement
      required:
      - blockersObserved
      - consideredReviews
      - practiceSlug
      - reviewedCount
    PracticeEvidenceRequirement:
      type: object
      description: A source a practice reads and the stance it takes towards it
      properties:
        sourceKind:
          type: string
          description: Stable source identifier from the selected source contract
        stance:
          type: string
          description: Whether an absent or degraded capture refuses the review
          enum:
          - REQUIRED
          - EXHAUSTIVE
          - CONTEXTUAL
      required:
      - sourceKind
      - stance
    PracticeEvidenceSourceOption:
      type: object
      description: An evidence source a practice on this kind of work may read
      properties:
        description:
          type: string
        displayName:
          type: string
        privacyClass:
          type: string
          enum:
          - INTERNAL
          - PERSONAL
          - SENSITIVE_PERSONAL
        requiredQuality:
          type: string
          description: What requiring this source demands of its capture; fixed by
            the source contract
          enum:
          - ANY_CAPTURE
          - COMPLETE
          - COMPLETE_AND_NON_EMPTY
        selectionScope:
          type: string
          description: "How much of the source one capture takes, and the bound past\
            \ which it is no longer whole"
        sourceKind:
          type: string
        supportsExhaustiveEvidence:
          type: boolean
          description: "Whether this source can be captured whole, and so whether\
            \ a practice may rest a claim about what is absent from it on the capture"
      required:
      - description
      - displayName
      - privacyClass
      - requiredQuality
      - selectionScope
      - sourceKind
      - supportsExhaustiveEvidence
    PracticeManualReviewSignal:
      type: object
      description: The signal a person raises by asking for a review of this work
        type by hand
      properties:
        displayName:
          type: string
        signal:
          type: string
          example: scm.pull_request.manual_review
      required:
      - displayName
      - signal
    PracticeReviewSettings:
      type: object
      description: "A workspace's practice-review policy: effective values plus raw\
        \ overrides"
      properties:
        cooldownMinutes:
          type: integer
          format: int32
          description: "Effective: minimum minutes between reviews for the same PR"
        cooldownMinutesOverride:
          type: integer
          format: int32
          description: Raw override; null = inheriting the fleet default
        defaultReviewTier:
          type: string
          description: "Effective: how much autonomy the system has over practices\
            \ and areas that hold no tier of their own — the bottom of the practice\
            \ → area → workspace chain"
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        defaultReviewTierOverride:
          type: string
          description: "Raw override; null = this workspace has never chosen, so DELIVER\
            \ applies"
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        deliverToMerged:
          type: boolean
          description: "Effective: deliver feedback to merged PRs/MRs"
        deliverToMergedOverride:
          type: boolean
          description: Raw override; null = inheriting the fleet default
        reviewScope:
          $ref: "#/components/schemas/WorkspaceReviewScope"
          description: "Which work is reviewed at all, ANDed onto every practice binding.\
            \ Empty lists mean no restriction on that axis. Exact names only — no\
            \ patterns, and no path scope (changed paths are not known where the decision\
            \ is made)."
        runForAllUsers:
          type: boolean
          description: "Effective: run practice review for all developers"
        runForAllUsersOverride:
          type: boolean
          description: Raw override; null = inheriting the fleet default
      required:
      - cooldownMinutes
      - defaultReviewTier
      - deliverToMerged
      - reviewScope
      - runForAllUsers
    PracticeSignalOption:
      type: object
      description: A signal a practice can start an automated review on
      properties:
        displayName:
          type: string
        recommended:
          type: boolean
        signal:
          type: string
          example: scm.pull_request.ready
      required:
      - displayName
      - recommended
      - signal
    PracticeSubject:
      type: object
      description: What must be in a piece of work for this practice to have anything
        to judge
      properties:
        absentSays:
          type: string
          description: "Sentence shown when the subject was proven absent, in the\
            \ author's voice"
          maxLength: 300
          minLength: 0
        anyOf:
          type: array
          description: Shapes the subject may take; the subject is present when any
            one is found
          items:
            $ref: "#/components/schemas/PracticeSubjectClause"
          maxItems: 10
          minItems: 1
      required:
      - absentSays
    PracticeSubjectClause:
      type: object
      description: One shape the subject a practice judges can take in a piece of
        work
      properties:
        changedPathMatches:
          type: array
          description: Globs; holds when the change touches a matching path
          items:
            type: string
        diffContains:
          type: array
          description: Literal strings; holds when the diff contains one of them
          items:
            type: string
        evidenceHasItems:
          type: string
          description: Named evidence collection; holds when it has at least one entry
          enum:
          - scm.review-threads
          - scm.inline-review-comments
          - scm.general-review-comments
    PracticeTraceEntry:
      type: object
      description: "What became of one practice on this artifact, and whether anyone\
        \ heard about it"
      properties:
        decidedAt:
          type: string
          format: date-time
          description: When the answer was settled
        deliveredCount:
          type: integer
          format: int32
          description: Interventions actually delivered to a person
        explanation:
          type: string
          description: "The outcome in a sentence, phrased as what would change it"
        observationCount:
          type: integer
          format: int32
          description: Measurements this practice produced on this artifact
        occasionedBy:
          type: string
          description: The occurrence this answer is about; null when nothing it watches
            happened
        occasionedById:
          type: string
          format: uuid
          description: That occurrence's id in this trace's signals list. The name
            alone cannot identify it — the same signal recurs on every revision —
            so this is what a link should follow.
        outcome:
          type: string
          enum:
          - REVIEWED
          - RUNNING
          - PENDING
          - SKIPPED
          - NOT_ASSESSABLE
          - TURNED_OFF
          - NOT_OCCASIONED
          - DORMANT
          - LAPSED
          - FAILED
        practiceName:
          type: string
        practiceSlug:
          type: string
        reviewId:
          type: string
          format: uuid
          description: "The review this answer came from, when one ran"
        reviewTier:
          type: string
          description: "How much autonomy the workspace currently gives this practice,\
            \ after inheritance"
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        watches:
          type: array
          description: The signals this practice watches
          items:
            type: string
        withheldReasons:
          type: array
          description: Why prepared feedback was withheld. Non-empty with observations
            present means we measured and deliberately said nothing.
          items:
            type: string
            enum:
            - VOLUME_CAPPED
            - COMPOSER_DEDUPED
            - REACTED_DISPUTED
            - REACTED_NOT_APPLICABLE
            - CONVERSATION_EXPIRED
            - ARTIFACT_GONE
            - ARTIFACT_CLOSED
            - ARTIFACT_MERGED
            - ARTIFACT_DRAFT
            - RECIPIENT_OPTED_OUT
            - EMPTY_AFTER_SANITIZE
            - INSTANCE_SILENCED
            - PRACTICE_TIER_QUIET
            - BACKFILL_QUIET
      required:
      - deliveredCount
      - explanation
      - observationCount
      - outcome
      - practiceName
      - practiceSlug
      - reviewTier
      - watches
      - withheldReasons
    PracticeWorkTypeDefinitionOptions:
      type: object
      description: "Review timing, evidence choices, and recommended settings for\
        \ one type of reviewed work"
      properties:
        allowedSources:
          type: array
          items:
            $ref: "#/components/schemas/PracticeEvidenceSourceOption"
        artifactKind:
          type: string
        manualReviewSignal:
          $ref: "#/components/schemas/PracticeManualReviewSignal"
          description: "How a person asks for a review of this work type by hand,\
            \ or absent where the work type admits no such request. Not an occasion\
            \ to bind to: such a request reviews every practice on the work type whatever\
            \ state the work is in."
        recommendedNeeds:
          type: array
          description: Evidence a new binding on this work type starts with when the
            author says nothing
          items:
            $ref: "#/components/schemas/PracticeEvidenceRequirement"
        recommendedPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
        signals:
          type: array
          description: The occasions a practice on this work type can be bound to.
            A review somebody asks for by hand is not among them — see manualReviewSignal.
          items:
            $ref: "#/components/schemas/PracticeSignalOption"
        supportedAutomatedReviewModes:
          type: array
          items:
            type: string
            description: Author-declared approach for automated review; this does
              not describe human review
            enum:
            - LANGUAGE_MODEL
            - NONE
      required:
      - allowedSources
      - artifactKind
      - recommendedNeeds
      - recommendedPolicy
      - signals
      - supportedAutomatedReviewModes
    ProbeLlmConnectionRequest:
      type: object
      description: Draft connection probe using a supplied (never-persisted) credential
      properties:
        apiKey:
          type: string
          description: API key used only for this probe
        apiProtocol:
          type: string
          description: Wire protocol
          minLength: 1
          pattern: openai-completions|openai-responses
        authMode:
          type: string
          description: Credential shape (default BEARER)
          enum:
          - BEARER
          - API_KEY
        baseUrl:
          type: string
          description: Provider base URL
          minLength: 1
      required:
      - apiProtocol
      - baseUrl
    ProblemDetail:
      type: object
      properties:
        detail:
          type: string
        instance:
          type: string
          format: uri
        properties:
          type: object
          additionalProperties: {}
        status:
          type: integer
          format: int32
        title:
          type: string
        type:
          type: string
          format: uri
    Profile:
      type: object
      description: "User profile header: identity, league standing, contribution surface,\
        \ XP"
      properties:
        contributedRepositories:
          type: array
          description: Repositories the user has contributed to
          items:
            $ref: "#/components/schemas/RepositoryInfo"
        firstContribution:
          type: string
          format: date-time
          description: Timestamp of the user's first contribution
        userInfo:
          $ref: "#/components/schemas/UserInfo"
          description: Basic information about the user
        xpRecord:
          $ref: "#/components/schemas/ProfileXpRecord"
          description: XP progress information for the users' profile
      required:
      - contributedRepositories
      - userInfo
      - xpRecord
    ProfileActivityMonitor:
      type: object
      description: Configurable activity monitor data for a contributor profile
      properties:
        activityStats:
          $ref: "#/components/schemas/ProfileActivityStats"
          description: Aggregated activity stats after applying monitor filters
        authoredPullRequests:
          type: array
          description: "Open pull requests authored in the selected timeframe, after\
            \ repository filters and limit"
          items:
            $ref: "#/components/schemas/PullRequestInfo"
        repositories:
          type: array
          description: Repositories with monitor-relevant activity in the selected
            timeframe
          items:
            $ref: "#/components/schemas/RepositoryInfo"
        reviewActivity:
          type: array
          description: Review activity entries after applying monitor filters and
            limit
          items:
            $ref: "#/components/schemas/ProfileReviewActivity"
        totalAuthoredPullRequestCount:
          type: integer
          format: int32
          description: "Total open authored pull requests after filters, before limit"
        totalReviewActivityCount:
          type: integer
          format: int32
          description: "Total review activity entries after filters, before limit"
      required:
      - activityStats
      - authoredPullRequests
      - repositories
      - reviewActivity
      - totalAuthoredPullRequestCount
      - totalReviewActivityCount
    ProfileActivityStats:
      type: object
      description: Aggregated activity statistics with XP scores for a user profile
      properties:
        numberOfApprovals:
          type: integer
          format: int32
          description: Number of approvals given
          example: 15
        numberOfChangeRequests:
          type: integer
          format: int32
          description: Number of change requests submitted
          example: 8
        numberOfClosedIssues:
          type: integer
          format: int32
          description: Number of issues closed in the timeframe
          example: 2
        numberOfClosedPullRequests:
          type: integer
          format: int32
          description: Number of authored pull requests closed without merge in the
            timeframe
          example: 1
        numberOfCodeComments:
          type: integer
          format: int32
          description: Number of scored inline feedback comments on pull requests
            authored by someone else
          example: 30
        numberOfComments:
          type: integer
          format: int32
          description: Number of comment-only review submissions
          example: 12
        numberOfMergedPullRequests:
          type: integer
          format: int32
          description: Number of authored pull requests merged in the timeframe
          example: 6
        numberOfOpenPullRequests:
          type: integer
          format: int32
          description: Number of authored pull requests opened in the timeframe that
            are still open
          example: 2
        numberOfOpenedIssues:
          type: integer
          format: int32
          description: Number of issues opened in the timeframe
          example: 3
        numberOfOwnReplies:
          type: integer
          format: int32
          description: Number of visible-only discussion replies and inline thread
            replies on the user's own pull requests
          example: 4
        numberOfReviewedPRs:
          type: integer
          format: int32
          description: Number of distinct pull requests reviewed
          example: 42
        numberOfUnknowns:
          type: integer
          format: int32
          description: Number of reviews with unknown state
          example: 0
        score:
          type: integer
          format: int32
          description: Total XP score
          example: 1250
      required:
      - numberOfApprovals
      - numberOfChangeRequests
      - numberOfClosedIssues
      - numberOfClosedPullRequests
      - numberOfCodeComments
      - numberOfComments
      - numberOfMergedPullRequests
      - numberOfOpenPullRequests
      - numberOfOpenedIssues
      - numberOfOwnReplies
      - numberOfReviewedPRs
      - numberOfUnknowns
      - score
    ProfileReviewActivity:
      type: object
      description: A scored review activity entry with XP score for profile display
      properties:
        author:
          $ref: "#/components/schemas/UserInfo"
          description: Author of the review
        codeComments:
          type: integer
          format: int32
          description: Number of inline code comments in the review
          example: 3
        htmlUrl:
          type: string
          description: URL to the review on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the review
        isDismissed:
          type: boolean
          description: Whether the review was dismissed
        pullRequest:
          $ref: "#/components/schemas/PullRequestBaseInfo"
          description: Pull request that was reviewed
        score:
          type: integer
          format: int32
          description: XP score earned for this review
          example: 25
        state:
          type: string
          description: "State of the review (APPROVED, CHANGES_REQUESTED, COMMENTED,\
            \ etc.)"
          enum:
          - COMMENTED
          - APPROVED
          - CHANGES_REQUESTED
          - PENDING
          - DISMISSED
          - UNKNOWN
        submittedAt:
          type: string
          format: date-time
          description: Timestamp when the review was submitted
      required:
      - codeComments
      - htmlUrl
      - id
      - isDismissed
      - score
      - state
      - submittedAt
    ProfileXpRecord:
      type: object
      description: User's XP and Level progress details
      properties:
        currentLevel:
          type: integer
          format: int32
          description: Current calculated level
          example: 5
        currentLevelXP:
          type: integer
          format: int64
          description: XP accumulated in the current level
          example: 450
        totalXP:
          type: integer
          format: int64
          description: Overall total XP accumulated
          example: 5450
        xpNeeded:
          type: integer
          format: int64
          description: XP needed to reach the next level
          example: 1000
      required:
      - currentLevel
      - currentLevelXP
      - totalXP
      - xpNeeded
    PullRequestBaseInfo:
      type: object
      description: Basic information about a pull request
      properties:
        htmlUrl:
          type: string
          description: URL to the pull request on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the pull request
        isDraft:
          type: boolean
          description: Whether the pull request is in draft mode
        isMerged:
          type: boolean
          description: Whether the pull request has been merged
        number:
          type: integer
          format: int32
          description: Pull request number within the repository
          example: 42
        repository:
          $ref: "#/components/schemas/RepositoryInfo"
          description: Repository the pull request belongs to
        state:
          type: string
          description: "Current state of the pull request (OPEN, CLOSED, MERGED)"
          enum:
          - OPEN
          - CLOSED
          - MERGED
        title:
          type: string
          description: Title of the pull request
      required:
      - htmlUrl
      - id
      - isDraft
      - isMerged
      - number
      - state
      - title
    PullRequestInfo:
      type: object
      description: Detailed information about a pull request
      properties:
        additions:
          type: integer
          format: int32
          description: Number of lines added
          example: 150
        assignees:
          type: array
          description: Users assigned to the pull request
          items:
            $ref: "#/components/schemas/UserInfo"
        author:
          $ref: "#/components/schemas/UserInfo"
          description: Author of the pull request
        closedAt:
          type: string
          format: date-time
          description: Timestamp when the pull request was closed
        commentsCount:
          type: integer
          format: int32
          description: Number of comments on the pull request
          example: 5
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the pull request was created
        deletions:
          type: integer
          format: int32
          description: Number of lines deleted
          example: 50
        htmlUrl:
          type: string
          description: URL to the pull request on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the pull request
        isDraft:
          type: boolean
          description: Whether the pull request is in draft mode
        isMerged:
          type: boolean
          description: Whether the pull request has been merged
        labels:
          type: array
          description: Labels applied to the pull request
          items:
            $ref: "#/components/schemas/LabelInfo"
        mergedAt:
          type: string
          format: date-time
          description: Timestamp when the pull request was merged
        number:
          type: integer
          format: int32
          description: Pull request number within the repository
          example: 42
        repository:
          $ref: "#/components/schemas/RepositoryInfo"
          description: Repository the pull request belongs to
        state:
          type: string
          description: "Current state of the pull request (OPEN, CLOSED, MERGED)"
          enum:
          - OPEN
          - CLOSED
          - MERGED
        title:
          type: string
          description: Title of the pull request
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the pull request was last updated
      required:
      - additions
      - commentsCount
      - deletions
      - htmlUrl
      - id
      - isDraft
      - isMerged
      - number
      - state
      - title
    RateLimitSnapshot:
      type: object
      description: "Vendor API rate-limit observation, read from in-memory trackers\
        \ (not persisted across restarts). Every field except observedAt is present\
        \ only if the vendor actually reported it."
      properties:
        limit:
          type: integer
          format: int32
          description: "Window ceiling, if the vendor reported one. Survives window\
            \ rollover — a ceiling is window-invariant."
        observedAt:
          type: string
          format: date-time
          description: When the underlying vendor response was seen
        remaining:
          type: integer
          format: int32
          description: "Remaining budget, if reported and still inside the observed\
            \ window. Null once the window has rolled over."
        resetAt:
          type: string
          format: date-time
          description: "When the observed window ends, if reported and still in the\
            \ future"
        throttledUntil:
          type: string
          format: date-time
          description: An observed 429's back-off deadline (observedAt + Retry-After);
            null if the vendor never told us to back off
      required:
      - observedAt
    Reaction:
      type: object
      description: Developer reaction to a delivered unit of feedback
      properties:
        action:
          type: string
          description: The reaction action taken
          enum:
          - ADDRESSED
          - DISPUTED
          - NOT_APPLICABLE
        createdAt:
          type: string
          format: date-time
          description: When the reaction was submitted
        explanation:
          type: string
          description: Optional explanation for the reaction
        feedbackId:
          type: string
          format: uuid
          description: ID of the feedback unit this reaction is about
        id:
          type: string
          format: uuid
          description: Unique reaction ID
      required:
      - action
      - createdAt
      - feedbackId
      - id
    ReactionEngagement:
      type: object
      description: "Reaction engagement for a developer, split into the response (uptake)\
        \ and validity axes"
      properties:
        addressed:
          type: integer
          format: int64
          description: "RESPONSE: observations the developer acted on (the recipience\
            \ act, not the outcome)"
        disputed:
          type: integer
          format: int64
          description: "RESPONSE: observations the developer rejected with a reasoned\
            \ explanation"
        notApplicable:
          type: integer
          format: int64
          description: "VALIDITY: observations marked out-of-scope — a detector-scope\
            \ signal, NOT an uptake count"
      required:
      - addressed
      - disputed
      - notApplicable
    ReflectionItem:
      type: object
      description: A single piece of practice feedback to read and act on
      properties:
        artifactId:
          type: integer
          format: int64
          description: Id of the PR / issue this is about
        artifactKind:
          type: string
          description: The kind of work this is about (PR / issue)
        deliveredFeedback:
          type: string
          description: What to do — the delivered feedback for this observation (null
            if nothing was delivered)
        locator:
          type: string
          description: "Where in the work, e.g. \"FrameRecorder.swift:212\", when\
            \ known"
        observationId:
          type: string
          format: uuid
          description: Observation id — handle to open the full detail
        origin:
          type: string
          description: "What occasioned the measurement. BACKFILL means it came from\
            \ a review of past work rather than from something that just happened,\
            \ and nothing was posted anywhere at the time."
          enum:
          - LIVE
          - MANUAL
          - BACKFILL
        severity:
          type: string
          description: Impact level (null unless assessed BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        title:
          type: string
          description: The headline of the feedback
      required:
      - artifactId
      - artifactKind
      - observationId
      - origin
      - title
    ReflectionPractice:
      type: object
      description: A developer's readable feedback for one practice
      properties:
        areaName:
          type: string
          description: "Area name this practice belongs to, if any"
        areaSlug:
          type: string
          description: "Area slug this practice belongs to, if any"
        name:
          type: string
          description: Practice name
        slug:
          type: string
          description: Practice slug
        standing:
          type: string
          description: Where the developer stands on this practice
          enum:
          - DEVELOPING
          - STRENGTH
          - MIXED
        strengths:
          type: array
          description: What the developer already does well here
          items:
            $ref: "#/components/schemas/ReflectionItem"
        toWorkOn:
          type: array
          description: Specific feedback to act on (highest-impact first)
          items:
            $ref: "#/components/schemas/ReflectionItem"
        whatGoodLooksLike:
          type: string
          description: A concrete picture of doing this well
        whyItMatters:
          type: string
          description: "Why this practice matters, in plain language"
      required:
      - name
      - slug
      - standing
      - strengths
      - toWorkOn
    RegisterOutlineCollectionRequest:
      type: object
      description: Register an Outline collection for mirroring (lands ENABLED + PENDING)
      properties:
        collectionId:
          type: string
          description: Outline collection id (UUID)
          maxLength: 64
          minLength: 0
      required:
      - collectionId
    RegisterSlackChannelRequest:
      type: object
      description: Allow-list a Slack channel (lands in PENDING)
      properties:
        channelName:
          type: string
          description: Optional human-readable channel name to store alongside the
            id
          maxLength: 256
          minLength: 0
        slackChannelId:
          type: string
          description: Slack public/private channel id (stable C… or G… id)
          maxLength: 32
          minLength: 0
          pattern: "^[CG][A-Z0-9]{8,}$"
      required:
      - slackChannelId
    RenameWorkspaceSlugRequest:
      type: object
      description: Request to rename a workspace's URL slug
      properties:
        newSlug:
          type: string
          description: New URL-friendly identifier for the workspace
          example: new-workspace-slug
          minLength: 1
          pattern: "^[a-z0-9][a-z0-9-]{2,50}$"
      required:
      - newSlug
    ReorderPracticeAreasRequest:
      type: object
      description: Reorder practice areas to match the supplied list
      properties:
        orderedSlugs:
          type: array
          description: Area slugs in the desired display order
          example:
          - review-ready-work
          - acting-on-review-feedback
          items:
            type: string
          minItems: 1
      required:
      - orderedSlugs
    ReorderPracticesRequest:
      type: object
      description: Reorder one area's practices to match the supplied list
      properties:
        areaSlug:
          type:
          - string
          - "null"
          description: Slug of the area whose practices are being reordered; null
            reorders the unassigned bucket
          example: review-ready-work
        orderedSlugs:
          type: array
          description: Practice slugs in the desired display order — the complete
            set for the area
          example:
          - describe-what-and-why
          - ready-and-traceable-handoff
          items:
            type: string
          minItems: 1
      required:
      - orderedSlugs
    RepositoryInfo:
      type: object
      description: Information about a git repository
      properties:
        description:
          type: string
          description: Description of the repository
        hiddenFromContributions:
          type: boolean
          description: Whether contributions from this repository are hidden from
            leaderboard calculations
        htmlUrl:
          type: string
          description: URL to the repository on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the repository
        labels:
          type: array
          description: Labels defined in the repository
          items:
            $ref: "#/components/schemas/LabelInfo"
        name:
          type: string
          description: Name of the repository
        nameWithOwner:
          type: string
          description: "Full name including owner (e.g., 'owner/repo')"
      required:
      - hiddenFromContributions
      - htmlUrl
      - id
      - name
      - nameWithOwner
    ResourceCounts:
      type: object
      description: Resource-level rollup for the connection overview badge
      properties:
        errored:
          type: integer
          format: int64
          description: Resources currently reporting a sync error
        pending:
          type: integer
          format: int64
          description: "Resources that have never completed a sync (no lastSyncedAt).\
            \ Defined on the timestamp rather than on a provider's status vocabulary\
            \ so it means the same thing for a repository, a Slack channel and an\
            \ Outline collection."
        stale:
          type: integer
          format: int64
          description: "Resources whose last sync is older than twice the connection's\
            \ scheduled cadence. Always 0 when the cadence is unknown or the schedule\
            \ is irregular — staleness is a judgement against a known cron, and without\
            \ one this declines to guess rather than flagging healthy resources."
        total:
          type: integer
          format: int64
          description: Total resources known to this connection
      required:
      - errored
      - pending
      - stale
      - total
    ReviewArtifact:
      type: object
      properties:
        channelName:
          type: string
          description: Slack channel name for conversation artifacts
        id:
          type: integer
          format: int64
          description: Internal artifact entity ID
        number:
          type: integer
          format: int32
          description: Provider-visible work-item number
        provider:
          type: string
          description: "Source provider, when recorded"
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        repositoryName:
          type: string
          description: Provider-qualified repository path for SCM artifacts
        title:
          type: string
        type:
          type: string
        url:
          type: string
          description: "Provider URL, when one is available"
      required:
      - id
      - title
      - type
    ReviewBackfillRun:
      type: object
      description: "A campaign as an admin sees it — before confirming, while it runs,\
        \ and after it ends."
      properties:
        artifactKind:
          type: string
        confirmedByAccountId:
          type: integer
          format: int64
          description: Who authorised the spend; absent until the run is confirmed
        createdAt:
          type: string
          format: date-time
        discoveredVia:
          type: string
          description: "BACKFILL for a campaign an admin scoped by hand, SWEEP for\
            \ one a recurring schedule opened"
          enum:
          - EVENT
          - SYNC
          - MANUAL
          - BACKFILL
          - SWEEP
        estimatedArtifacts:
          type: integer
          format: int32
        estimatedCostUsd:
          type: number
          format: decimal
          description: Forecast total spend in USD; absent when the workspace has
            no priced review history
        failedCount:
          type: integer
          format: int32
          description: |-
            artifacts whose submission threw, leaving no job and no recorded decision. Non-zero
                 means this campaign's baseline has holes in it, which is why it is reported apart from a pass.
        finishedAt:
          type: string
          format: date-time
        fromAt:
          type: string
          format: date-time
        id:
          type: string
          format: uuid
        passedCount:
          type: integer
          format: int32
          description: |-
            artifacts walked past without one: already measured at their current state, or
                 refused by the review gate. The campaign looked at each of these and decided.
        pauseReason:
          type: string
          description: Set only while the run is PAUSED
          enum:
          - BUDGET_EXHAUSTED
          - REVIEW_MODEL_UNBOUND
          - WORKSPACE_UNAVAILABLE
        requestedByAccountId:
          type: integer
          format: int64
        startedAt:
          type: string
          format: date-time
        status:
          type: string
          enum:
          - AWAITING_CONFIRMATION
          - RUNNING
          - PAUSED
          - COMPLETED
          - CANCELLED
        submittedCount:
          type: integer
          format: int32
          description: artifacts for which a review job was created
        sweepScheduleId:
          type: string
          format: uuid
          description: The schedule that opened this run; absent for a campaign an
            admin scoped by hand
        toAt:
          type: string
          format: date-time
      required:
      - artifactKind
      - createdAt
      - discoveredVia
      - estimatedArtifacts
      - failedCount
      - fromAt
      - id
      - passedCount
      - requestedByAccountId
      - status
      - submittedCount
      - toAt
    ReviewBoundFeedback:
      type: object
      description: Feedback composed from an observation
      properties:
        agentJobId:
          type: string
          format: uuid
        channel:
          type: string
          enum:
          - IN_CONTEXT
          - IN_CHAT
          - IN_APP
        createdAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
          description: When the message was placed; null if it was not delivered
        deliveryState:
          type: string
          enum:
          - PREPARED
          - DELIVERED
          - SUPERSEDED
          - SUPPRESSED
          - FAILED
        feedbackId:
          type: string
          format: uuid
        role:
          type: string
          description: Whether the observation led the feedback or reinforced it
          enum:
          - PRIMARY
          - SUPPORTING
        suppressionReason:
          type: string
          description: Why the message was withheld; null unless the state is SUPPRESSED
          enum:
          - VOLUME_CAPPED
          - COMPOSER_DEDUPED
          - REACTED_DISPUTED
          - REACTED_NOT_APPLICABLE
          - CONVERSATION_EXPIRED
          - ARTIFACT_GONE
          - ARTIFACT_CLOSED
          - ARTIFACT_MERGED
          - ARTIFACT_DRAFT
          - RECIPIENT_OPTED_OUT
          - EMPTY_AFTER_SANITIZE
          - INSTANCE_SILENCED
          - PRACTICE_TIER_QUIET
          - BACKFILL_QUIET
      required:
      - agentJobId
      - channel
      - createdAt
      - deliveryState
      - feedbackId
      - role
    ReviewBoundObservation:
      type: object
      description: An observation that contributed to a piece of feedback
      properties:
        area:
          $ref: "#/components/schemas/ReviewPracticeArea"
          description: Practice area; null when the practice is Unassigned
        assessment:
          type: string
          description: "Assessment: GOOD or BAD (null when NOT_APPLICABLE)"
          enum:
          - GOOD
          - BAD
        claimCurrentness:
          type: string
          description: Whether an observation was produced using the current review
            rules
          enum:
          - CURRENT
          - STALE
          - UNVERIFIABLE
        observationId:
          type: string
          format: uuid
        observedAt:
          type: string
          format: date-time
        ordinal:
          type: integer
          format: int32
          description: Render order within the feedback (lower renders earlier)
        practiceName:
          type: string
        practiceSlug:
          type: string
        presence:
          type: string
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
        role:
          type: string
          description: Whether the observation leads the feedback or reinforces it
          enum:
          - PRIMARY
          - SUPPORTING
        severity:
          type: string
          description: Severity band (null unless assessment is BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        summary:
          type: string
      required:
      - claimCurrentness
      - observationId
      - observedAt
      - ordinal
      - practiceName
      - practiceSlug
      - presence
      - role
      - summary
    ReviewFeedback:
      type: object
      properties:
        agentJobId:
          type: string
          format: uuid
        artifact:
          $ref: "#/components/schemas/ReviewArtifact"
          description: Work item the feedback targets; null when it is unanchored
        bodyPreview:
          type: string
          description: Leading characters of the composed body; null when the feedback
            carries no body
        bodyTruncated:
          type: boolean
        channel:
          type: string
          enum:
          - IN_CONTEXT
          - IN_CHAT
          - IN_APP
        createdAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
          description: When the feedback was placed; null if it was not delivered
        deliveryState:
          type: string
          enum:
          - PREPARED
          - DELIVERED
          - SUPERSEDED
          - SUPPRESSED
          - FAILED
        id:
          type: string
          format: uuid
        observationCount:
          type: integer
          format: int64
          description: Number of observations used to compose the feedback
        recipient:
          $ref: "#/components/schemas/ReviewSubject"
          description: Who the feedback is addressed to; null when the identity is
            no longer resolvable
        replacesId:
          type: string
          format: uuid
          description: The feedback this one replaced; null on a first delivery
        subject:
          $ref: "#/components/schemas/ReviewSubject"
          description: Whose work the feedback addresses; may equal the recipient
        suppressionReason:
          type: string
          description: Why the feedback was withheld; null unless the state is SUPPRESSED
          enum:
          - VOLUME_CAPPED
          - COMPOSER_DEDUPED
          - REACTED_DISPUTED
          - REACTED_NOT_APPLICABLE
          - CONVERSATION_EXPIRED
          - ARTIFACT_GONE
          - ARTIFACT_CLOSED
          - ARTIFACT_MERGED
          - ARTIFACT_DRAFT
          - RECIPIENT_OPTED_OUT
          - EMPTY_AFTER_SANITIZE
          - INSTANCE_SILENCED
          - PRACTICE_TIER_QUIET
          - BACKFILL_QUIET
      required:
      - agentJobId
      - bodyTruncated
      - channel
      - createdAt
      - deliveryState
      - id
      - observationCount
    ReviewFeedbackCounts:
      type: object
      description: Counts of feedback by delivery state
      properties:
        delivered:
          type: integer
          format: int64
        failed:
          type: integer
          format: int64
        prepared:
          type: integer
          format: int64
        superseded:
          type: integer
          format: int64
        suppressed:
          type: integer
          format: int64
      required:
      - delivered
      - failed
      - prepared
      - superseded
      - suppressed
    ReviewFeedbackDetail:
      type: object
      description: Full feedback detail including the stored composed body
      properties:
        agentJobId:
          type: string
          format: uuid
        artifact:
          $ref: "#/components/schemas/ReviewArtifact"
          description: Work item the feedback targets; null when it is unanchored
        body:
          type: string
          description: "Stored composed body; null when none was produced, and always\
            \ null on the IN_APP and IN_CHAT channels — neither the developer's practice\
            \ pages nor the mentor's queued move is readable by an operator"
        channel:
          type: string
          enum:
          - IN_CONTEXT
          - IN_CHAT
          - IN_APP
        createdAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
          description: When the feedback was placed; null if it was not delivered
        deliveryState:
          type: string
          enum:
          - PREPARED
          - DELIVERED
          - SUPERSEDED
          - SUPPRESSED
          - FAILED
        id:
          type: string
          format: uuid
        observations:
          type: array
          description: Source observations in render order
          items:
            $ref: "#/components/schemas/ReviewBoundObservation"
        placements:
          type: array
          description: Recorded placements; empty when none
          items:
            $ref: "#/components/schemas/ReviewPlacement"
        recipient:
          $ref: "#/components/schemas/ReviewSubject"
          description: Who the feedback is addressed to; null when the identity is
            no longer resolvable
        replacesId:
          type: string
          format: uuid
          description: The feedback this one replaced; null on a first delivery
        subject:
          $ref: "#/components/schemas/ReviewSubject"
          description: Whose work the feedback addresses; may equal the recipient
        suppressionReason:
          type: string
          description: Why the feedback was withheld; null unless the state is SUPPRESSED
          enum:
          - VOLUME_CAPPED
          - COMPOSER_DEDUPED
          - REACTED_DISPUTED
          - REACTED_NOT_APPLICABLE
          - CONVERSATION_EXPIRED
          - ARTIFACT_GONE
          - ARTIFACT_CLOSED
          - ARTIFACT_MERGED
          - ARTIFACT_DRAFT
          - RECIPIENT_OPTED_OUT
          - EMPTY_AFTER_SANITIZE
          - INSTANCE_SILENCED
          - PRACTICE_TIER_QUIET
          - BACKFILL_QUIET
        threadKey:
          type: string
          description: Cross-run continuity key tying successive deliveries together
      required:
      - agentJobId
      - channel
      - createdAt
      - deliveryState
      - id
      - observations
      - placements
    ReviewFeedbackDisposition:
      type: object
      description: Counts of feedback by delivery state
      properties:
        delivered:
          type: integer
          format: int64
          description: Linked feedback delivered
        failed:
          type: integer
          format: int64
          description: Linked feedback whose delivery failed
        prepared:
          type: integer
          format: int64
          description: Linked feedback awaiting delivery
        superseded:
          type: integer
          format: int64
          description: Linked feedback delivered and later replaced
        suppressed:
          type: integer
          format: int64
          description: Linked feedback withheld by policy
      required:
      - delivered
      - failed
      - prepared
      - superseded
      - suppressed
    ReviewObservation:
      type: object
      description: A practice review observation with its linked feedback outcomes
      properties:
        agentJobId:
          type: string
          format: uuid
        area:
          $ref: "#/components/schemas/ReviewPracticeArea"
          description: Practice area; null when the practice is Unassigned
        artifact:
          $ref: "#/components/schemas/ReviewArtifact"
        assessment:
          type: string
          description: "Assessment: GOOD or BAD (null when NOT_APPLICABLE)"
          enum:
          - GOOD
          - BAD
        claimCurrentness:
          type: string
          description: Whether an observation was produced using the current review
            rules
          enum:
          - CURRENT
          - STALE
          - UNVERIFIABLE
        feedbackDisposition:
          $ref: "#/components/schemas/ReviewFeedbackDisposition"
          description: Counts of linked feedback by delivery state
        id:
          type: string
          format: uuid
        observedAt:
          type: string
          format: date-time
        origin:
          type: string
          description: "What occasioned the measurement. BACKFILL came from a confirmed\
            \ campaign over work that already existed, so it is not a point on the\
            \ live trend line."
          enum:
          - LIVE
          - MANUAL
          - BACKFILL
        practiceName:
          type: string
        practiceSlug:
          type: string
        presence:
          type: string
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
        recurrenceKey:
          type: string
          description: Cross-run locus key; null when continuity is unavailable
        severity:
          type: string
          description: Severity band (null unless assessment is BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        subject:
          $ref: "#/components/schemas/ReviewSubject"
          description: Whose work the observation is about; null when the identity
            is no longer resolvable
        summary:
          type: string
      required:
      - agentJobId
      - artifact
      - claimCurrentness
      - feedbackDisposition
      - id
      - observedAt
      - origin
      - practiceName
      - practiceSlug
      - presence
      - summary
    ReviewObservationCounts:
      type: object
      description: Counts of observations by assessment
      properties:
        inconclusive:
          type: integer
          format: int64
          description: Practices that looked at the evidence and could not settle
            the question either way; reported apart from notApplicable because one
            says there was nothing here to judge and the other says we could not tell
        notApplicable:
          type: integer
          format: int64
          description: Practices whose subject did not occur in this work
        problems:
          type: integer
          format: int64
        strengths:
          type: integer
          format: int64
      required:
      - inconclusive
      - notApplicable
      - problems
      - strengths
    ReviewObservationDetail:
      type: object
      description: An observation with evidence and linked feedback
      properties:
        agentJobId:
          type: string
          format: uuid
        area:
          $ref: "#/components/schemas/ReviewPracticeArea"
          description: Practice area; null when the practice is Unassigned
        artifact:
          $ref: "#/components/schemas/ReviewArtifact"
        assessment:
          type: string
          description: "Assessment: GOOD or BAD (null when NOT_APPLICABLE)"
          enum:
          - GOOD
          - BAD
        claimCurrentness:
          type: string
          description: Whether an observation was produced using the current review
            rules
          enum:
          - CURRENT
          - STALE
          - UNVERIFIABLE
        evidence:
          $ref: "#/components/schemas/ObservationEvidence"
        evidenceRationale:
          type: string
        feedback:
          type: array
          description: "Linked feedback, newest first"
          items:
            $ref: "#/components/schemas/ReviewBoundFeedback"
        id:
          type: string
          format: uuid
        observedAt:
          type: string
          format: date-time
        practiceName:
          type: string
        practiceRevisionId:
          type: integer
          format: int64
          description: "Criteria revision selected as of job start, when available"
        practiceSlug:
          type: string
        presence:
          type: string
          enum:
          - PRESENT
          - ABSENT
          - NOT_APPLICABLE
          - INCONCLUSIVE
        recurrenceKey:
          type: string
          description: Cross-run locus key; null when continuity is unavailable
        severity:
          type: string
          description: Severity band (null unless assessment is BAD)
          enum:
          - CRITICAL
          - MAJOR
          - MINOR
          - INFO
        subject:
          $ref: "#/components/schemas/ReviewSubject"
          description: Whose work the observation is about; null when the identity
            is no longer resolvable
        summary:
          type: string
      required:
      - agentJobId
      - artifact
      - claimCurrentness
      - feedback
      - id
      - observedAt
      - practiceName
      - practiceSlug
      - presence
      - summary
    ReviewPlacement:
      type: object
      description: A recorded message placement
      properties:
        anchorEndLine:
          type: integer
          format: int32
          description: Last anchored line (1-based)
        anchorKind:
          type: string
          description: Anchor granularity; null for non-inline placements
          enum:
          - LINE
          - RANGE
          - FILE
          - IMAGE
        anchorPath:
          type: string
          description: Head-side path of the anchored file
        anchorSide:
          type: string
          description: Diff side of the anchor
          enum:
          - OLD
          - NEW
        anchorStartLine:
          type: integer
          format: int32
          description: First anchored line (1-based)
        chatMessageId:
          type: string
          format: uuid
          description: Mentor chat-message identifier
        id:
          type: string
          format: uuid
        placementType:
          type: string
          enum:
          - SUMMARY
          - INLINE
          - CONVERSATION_TURN
        postedCommentRef:
          type: string
          description: Provider-native comment identifier
      required:
      - id
      - placementType
    ReviewPracticeArea:
      type: object
      properties:
        color:
          type: string
          description: Palette color key
        icon:
          type: string
          description: Lucide icon name
        name:
          type: string
        slug:
          type: string
      required:
      - name
      - slug
    ReviewRequestOutcome:
      type: object
      description: What came of asking for a review
      properties:
        jobId:
          type: string
          format: uuid
          description: The review that is now running; absent when none was started
        reason:
          type: string
          description: The controlled-vocabulary reason nothing was started; absent
            when a review was started
          enum:
          - GATE_SKIPPED
          - COOLDOWN_ACTIVE
          - REQUEST_COOLDOWN_ACTIVE
          - REQUESTER_QUOTA_EXHAUSTED
          - CONCURRENT_DUPLICATE
          - OUT_OF_REVIEW_SCOPE
          - WORKSPACE_INACTIVE
          - PRACTICES_DISABLED
          - NO_ACTIVE_PRACTICE
          - REVIEW_MODEL_UNBOUND
          - PRACTICE_TIER_OFF
          - BUDGET_EXHAUSTED
          - SUBJECT_UNLINKED
          - MODEL_UNAVAILABLE
          - PENDING_DEADLINE_EXCEEDED
          - ARTIFACT_GONE
        reasonDescription:
          type: string
          description: "The reason as one sentence for the person who asked. Render\
            \ it verbatim: it is written next to the reason it explains so that every\
            \ surface says the same thing, and a re-worded copy is how a screen and\
            \ a support answer come to disagree."
        status:
          type: string
          description: "Whether a review is now running, or nothing was started"
          enum:
          - SUBMITTED
          - REFUSED
      required:
      - status
    ReviewRunSummary:
      type: object
      description: A review run with observation and feedback outcome counts
      properties:
        createdAt:
          type: string
          format: date-time
        feedback:
          $ref: "#/components/schemas/ReviewFeedbackCounts"
        id:
          type: string
          format: uuid
        observations:
          $ref: "#/components/schemas/ReviewObservationCounts"
        status:
          type: string
          enum:
          - QUEUED
          - RUNNING
          - COMPLETED
          - FAILED
          - TIMED_OUT
          - CANCELLED
        target:
          $ref: "#/components/schemas/ReviewRunTarget"
      required:
      - createdAt
      - feedback
      - id
      - observations
      - status
      - target
    ReviewRunTarget:
      type: object
      description: Work reviewed by an agent job
      properties:
        channelName:
          type: string
        id:
          type: integer
          format: int64
          description: "Internal artifact entity ID, when recorded"
        number:
          type: integer
          format: int32
          description: Provider-visible work-item number
        provider:
          type: string
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        repositoryName:
          type: string
        title:
          type: string
        type:
          type: string
        url:
          type: string
      required:
      - title
      - type
    ReviewSubject:
      type: object
      properties:
        avatarUrl:
          type: string
        id:
          type: integer
          format: int64
        login:
          type: string
          description: Login on the source provider
        name:
          type: string
          description: "Display name, when known"
      required:
      - id
      - login
    ReviewSweepSchedule:
      type: object
      description: "A standing instruction to review recent work on a cadence, as\
        \ an admin sees it."
      properties:
        artifactKind:
          type: string
        cadence:
          type: string
          enum:
          - DAILY
          - WEEKLY
        createdAt:
          type: string
          format: date-time
        createdByAccountId:
          type: integer
          format: int64
        enabled:
          type: boolean
        id:
          type: string
          format: uuid
        lastRunAt:
          type: string
          format: date-time
          description: When a campaign was last opened from this schedule; absent
            until the first one
        lookbackDays:
          type: integer
          format: int32
          description: |-
            how far back each sweep looks. Bounded at write time to twice the cadence and
                 never more than a week, which is what keeps a sweep's observations admissible in the same trend line
                 as reviews that events triggered.
        nextRunAt:
          type: string
          format: date-time
          description: |-
            when the next sweep is due. Shown because it is the only way to tell a schedule that
                 is working from one whose workspace has been skipping it.
      required:
      - artifactKind
      - cadence
      - createdAt
      - createdByAccountId
      - enabled
      - id
      - lookbackDays
      - nextRunAt
    ReviewTierAssignment:
      type: object
      description: "The autonomy tier in force here, whether it was set here or inherited,\
        \ and the level that decided it"
      properties:
        effective:
          type: string
          description: "The tier actually in force. OFF = not reviewed · PROPOSE =\
            \ reviewed and recorded, feedback held back and nothing sent · DELIVER\
            \ = feedback delivered without asking"
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        inherited:
          type: boolean
          description: True when this practice or area holds no tier of its own and
            follows a level above
        override:
          type: string
          description: "The tier set on this practice or area itself, or null when\
            \ it holds none and inherits. Send null to the tier endpoint to clear\
            \ it back to this state."
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        source:
          type: string
          description: "Which level decided the effective tier: PRACTICE, AREA or\
            \ WORKSPACE"
          enum:
          - PRACTICE
          - AREA
          - WORKSPACE
      required:
      - effective
      - inherited
      - source
    ReviewTierRollup:
      type: object
      description: "Practice counts per autonomy tier, for the workspace and for each\
        \ of its areas"
      properties:
        areas:
          type: array
          description: "The same counts per area, in catalogue order"
          items:
            $ref: "#/components/schemas/AreaReviewTierRollup"
        counts:
          type: object
          additionalProperties:
            type: integer
            format: int32
          description: Practice count per effective tier across the whole workspace;
            every tier is a key
        workspaceDefault:
          $ref: "#/components/schemas/ReviewTierAssignment"
          description: The workspace-level decision every area and practice falls
            back to
      required:
      - areas
      - counts
      - workspaceDefault
    RevokeSessionsResult:
      type: object
      properties:
        revoked:
          type: integer
          format: int32
    SessionView:
      type: object
      properties:
        current:
          type: boolean
        expiresAt:
          type: string
          format: date-time
        ip:
          type: string
        issuedAt:
          type: string
          format: date-time
        jti:
          type: string
          format: uuid
        userAgent:
          type: string
    SlackChannelCandidate:
      type: object
      description: A Slack channel the app can offer in the workspace channel picker
      properties:
        archived:
          type: boolean
          description: Whether Slack reports the channel as archived
        channelName:
          type: string
          description: Current Slack channel name
        consentState:
          type: string
          description: "Existing Hephaestus monitoring state, if already allow-listed"
          enum:
          - PENDING
          - ACTIVE
          - PAUSED
          - REVOKED
        member:
          type: boolean
          description: Whether the app bot is already a member
        privateChannel:
          type: boolean
          description: Whether this is a private channel
        slackChannelId:
          type: string
          description: Stable Slack channel id
      required:
      - channelName
      - slackChannelId
    SlackChannelConsentEvent:
      type: object
      description: An immutable Slack channel consent-transition audit entry
      properties:
        actorUserId:
          type: integer
          format: int64
          description: Workspace user id of the admin who made the change
        createdAt:
          type: string
          format: date-time
          description: When the transition was recorded
        fromState:
          type: string
          description: State the channel left (absent for the very first record)
          enum:
          - PENDING
          - ACTIVE
          - PAUSED
          - REVOKED
        id:
          type: integer
          format: int64
          description: Audit entry id
        reason:
          type: string
          description: Optional free-text reason the admin supplied
        slackChannelId:
          type: string
          description: Slack channel id the transition applied to
        toState:
          type: string
          description: State the channel entered
          enum:
          - PENDING
          - ACTIVE
          - PAUSED
          - REVOKED
      required:
      - createdAt
      - id
      - slackChannelId
      - toState
    SlackMonitoredChannel:
      type: object
      description: An allow-listed Slack channel with its consent state and person
        opt-out count
      properties:
        channelName:
          type: string
          description: "Human-readable channel name, if known"
        consentAnnouncedAt:
          type: string
          format: date-time
          description: When the in-channel consent announcement was posted (stamped
            on first activation)
        consentState:
          type: string
          description: Current per-channel consent lifecycle state
          enum:
          - PENDING
          - ACTIVE
          - PAUSED
          - REVOKED
        createdAt:
          type: string
          format: date-time
          description: When the channel was first discovered / allow-listed
        id:
          type: integer
          format: int64
          description: Internal allow-list row id
        optedOutMemberCount:
          type: integer
          format: int64
          description: Number of workspace members who have opted out of ingestion
            (workspace-wide)
        slackChannelId:
          type: string
          description: Slack channel id (stable C…/G… id; the natural key)
        slackTeamId:
          type: string
          description: Slack team (workspace) id the channel belongs to
      required:
      - consentState
      - createdAt
      - id
      - optedOutMemberCount
      - slackChannelId
      - slackTeamId
    SlackTestMessageRequest:
      type: object
      description: |-
        Optional body for the Slack test-message probe. When <code>channelId</code> is present and non-blank,
         the probe targets that channel (so an admin can validate a typed-but-unsaved channel); otherwise
         it falls back to the persisted notification channel.
      properties:
        channelId:
          type: string
    SlackTestMessageResponse:
      type: object
      description: Result of the Slack test-message probe; carries the Slack error
        code on failure.
      properties:
        channelId:
          type: string
        ok:
          type: boolean
        slackError:
          type: string
    SlackUserPreferences:
      type: object
      properties:
        workspaces:
          type: array
          items:
            $ref: "#/components/schemas/SlackUserWorkspacePreferences"
      required:
      - workspaces
    SlackUserWorkspacePreferences:
      type: object
      properties:
        activeMonitoredChannelCount:
          type: integer
          format: int64
        channelMessagesAllowed:
          type: boolean
        slackDisplayName:
          type: string
        slackTeamId:
          type: string
        slackTeamName:
          type: string
        slackUserId:
          type: string
        workspaceName:
          type: string
        workspaceSlug:
          type: string
      required:
      - slackTeamId
      - slackUserId
      - workspaceName
      - workspaceSlug
    SortObject:
      type: object
      properties:
        empty:
          type: boolean
        sorted:
          type: boolean
        unsorted:
          type: boolean
    SyncJob:
      type: object
      description: One sync_job row — a single INITIAL/RECONCILIATION/BACKFILL pass
        for a connection
      properties:
        cancelRequested:
          type: boolean
          description: Whether a cooperative cancel was requested
        createdAt:
          type: string
          format: date-time
          description: When the job row was created
        errorSummary:
          type: string
          description: "Truncated error summary, set on FAILED"
        finishedAt:
          type: string
          format: date-time
          description: When the job finished (any terminal status)
        id:
          type: integer
          format: int64
          description: Job id
        itemsProcessed:
          type: integer
          format: int32
          description: "Coarse progress: items processed so far"
        itemsTotal:
          type: integer
          format: int32
          description: "Coarse progress: total items, if known"
        progress:
          type: object
          additionalProperties: {}
          description: "Per-phase progress detail, integration-specific shape"
        startedAt:
          type: string
          format: date-time
          description: When the job started running
        status:
          type: string
          description: Job status
          enum:
          - PENDING
          - RUNNING
          - SUCCEEDED
          - SUCCEEDED_WITH_WARNINGS
          - FAILED
          - CANCELLED
        trigger:
          type: string
          description: What initiated this job
          enum:
          - SCHEDULED
          - MANUAL
          - LIFECYCLE
          - SYSTEM
        triggeredByUserId:
          type: integer
          format: int64
          description: "Account id of the admin who triggered this job, if MANUAL"
        type:
          type: string
          description: What kind of pass this is
          enum:
          - INITIAL
          - RECONCILIATION
          - BACKFILL
      required:
      - cancelRequested
      - createdAt
      - id
      - status
      - trigger
      - type
    SyncResourceCount:
      type: object
      description: "One entity class mirrored within a resource (issues, pull requests,\
        \ comments, messages, …)"
      properties:
        count:
          type: integer
          format: int64
          description: Mirrored row count for this class
        key:
          type: string
          description: Stable machine token for this class
          enum:
          - issues
          - pullRequests
          - issueComments
          - reviews
          - reviewComments
          - commits
          - messages
          - documents
          example: pullRequests
        label:
          type: string
          description: Display name
          example: Pull requests
        lastSyncedAt:
          type: string
          format: date-time
          description: When this class was last synced. Null means the integration
            does not track a per-class watermark — not that the class has never synced.
      required:
      - count
      - key
      - label
    SyncResourceState:
      type: object
      description: One synced resource (repository / channel / collection) — unified
        read-model row
      properties:
        backfillCompletedThrough:
          type: string
          format: date-time
          description: Per-resource backfill horizon
        backfillPercent:
          type: integer
          format: int32
          description: Per-resource backfill percent
        counts:
          type: array
          description: "Per-entity-class breakdown behind itemCount. One entry per\
            \ class the integration actually mirrors — 6 for an SCM repository, 1\
            \ for a Slack channel or Outline collection. Never null; empty when the\
            \ resource has never synced."
          items:
            $ref: "#/components/schemas/SyncResourceCount"
        externalId:
          type: string
          description: Vendor-side identifier
        id:
          type: integer
          format: int64
          description: The integration's own row id
        itemCount:
          type: integer
          format: int64
          description: Headline mirrored item count — the rollup of `counts`
        lastError:
          type: string
          description: "Last sync error, if any"
        lastSyncedAt:
          type: string
          format: date-time
          description: Last successful sync timestamp across all entity classes
        name:
          type: string
          description: Display name
        state:
          type: string
          description: Integration-defined status string
        type:
          type: string
          description: Resource kind
          enum:
          - REPOSITORY
          - CHANNEL
          - COLLECTION
        upstreamCount:
          type: integer
          format: int64
          description: "Vendor-reported upstream count, if cheaply available"
      required:
      - counts
      - externalId
      - id
      - name
      - state
      - type
    TeamInfo:
      type: object
      description: "Detailed information about a team including members, repositories,\
        \ and labels"
      properties:
        description:
          type: string
          description: Description of the team
        hidden:
          type: boolean
          description: Whether the team is hidden from leaderboard display
        htmlUrl:
          type: string
          description: URL to the team's page on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the team
        labels:
          type: array
          description: Labels configured as filters for this team
          items:
            $ref: "#/components/schemas/LabelInfo"
        members:
          type: array
          description: Members of the team (excluding bots)
          items:
            $ref: "#/components/schemas/UserInfo"
        membershipCount:
          type: integer
          format: int32
          description: Total count of team memberships
        name:
          type: string
          description: Name of the team
        organization:
          type: string
          description: Organization the team belongs to
        parentId:
          type: integer
          format: int64
          description: "ID of the parent team, if this is a sub-team"
        privacy:
          type: string
          description: Privacy level of the team (SECRET or VISIBLE)
          enum:
          - SECRET
          - VISIBLE
        repoPermissionCount:
          type: integer
          format: int32
          description: Count of repository permissions
        repositories:
          type: array
          description: Repositories the team has access to
          items:
            $ref: "#/components/schemas/RepositoryInfo"
      required:
      - hidden
      - id
      - labels
      - members
      - membershipCount
      - name
      - repoPermissionCount
      - repositories
    TeamSummary:
      type: object
      description: Lightweight summary of a team without member/repository details
      properties:
        description:
          type: string
          description: Description of the team
        hidden:
          type: boolean
          description: Whether the team is hidden from leaderboard display
        htmlUrl:
          type: string
          description: URL to the team's page on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the team
        name:
          type: string
          description: Name of the team
        organization:
          type: string
          description: Organization the team belongs to
        parentId:
          type: integer
          format: int64
          description: "ID of the parent team, if this is a sub-team"
        privacy:
          type: string
          description: Privacy level of the team (SECRET or VISIBLE)
          enum:
          - SECRET
          - VISIBLE
      required:
      - hidden
      - id
      - name
    TracedArtifact:
      type: object
      description: "An artifact this workspace recorded something about, and how much\
        \ of it turned into review"
      properties:
        artifactId:
          type: integer
          format: int64
        artifactKind:
          type: string
        container:
          type: string
          description: "Repository, collection or channel it sits in"
        lastSignalAt:
          type: string
          format: date-time
        number:
          type: integer
          format: int32
          description: "The number the provider shows, for kinds that have one"
        reviewedSignalCount:
          type: integer
          format: int32
          description: How many of them started a review
        signalCount:
          type: integer
          format: int32
          description: Occurrences recorded on this artifact
        title:
          type: string
        url:
          type: string
          description: Where to open it upstream; absent for a deleted or unlinkable
            artifact
      required:
      - artifactId
      - artifactKind
      - lastSignalAt
      - reviewedSignalCount
      - signalCount
      - title
    TracedSignal:
      type: object
      description: "One thing that happened to this artifact, and what the system\
        \ did about it"
      properties:
        discoveredVia:
          type: string
          description: "How we came to know: by event, by sync, by hand, or by backfill"
          enum:
          - EVENT
          - SYNC
          - MANUAL
          - BACKFILL
          - SWEEP
        displayName:
          type: string
          description: "Human label for the signal, from the artifact kind's descriptor"
        id:
          type: string
          format: uuid
          description: This occurrence's own identity; what a practice's occasionedById
            points at
        occurredAt:
          type: string
          format: date-time
          description: "When it happened upstream; for a sync discovery, only as precise\
            \ as the sync"
        reviewId:
          type: string
          format: uuid
          description: "The review this occurrence started, when it started one"
        revision:
          type: string
          description: Which version of the artifact this occurrence is about; the
            reason editing a description can be re-measured while the commits stay
            put
        signal:
          type: string
          description: "Signal name, e.g. scm.pull_request.ready"
        state:
          type: string
          enum:
          - RECORDED
          - TRIGGERED
          - SUPPRESSED
          - PENDING
          - LAPSED
        stateReason:
          type: string
          description: Why it ended in that state; null once it triggered a review
          enum:
          - GATE_SKIPPED
          - COOLDOWN_ACTIVE
          - REQUEST_COOLDOWN_ACTIVE
          - REQUESTER_QUOTA_EXHAUSTED
          - CONCURRENT_DUPLICATE
          - OUT_OF_REVIEW_SCOPE
          - WORKSPACE_INACTIVE
          - PRACTICES_DISABLED
          - NO_ACTIVE_PRACTICE
          - REVIEW_MODEL_UNBOUND
          - PRACTICE_TIER_OFF
          - BUDGET_EXHAUSTED
          - SUBJECT_UNLINKED
          - MODEL_UNAVAILABLE
          - PENDING_DEADLINE_EXCEEDED
          - ARTIFACT_GONE
      required:
      - discoveredVia
      - displayName
      - id
      - occurredAt
      - revision
      - signal
      - state
    TriggerSyncJobRequest:
      type: object
      description: Manual sync trigger request body
      properties:
        type:
          type: string
          description: "RECONCILIATION for a full re-sync, BACKFILL for historical\
            \ data"
          enum:
          - INITIAL
          - RECONCILIATION
          - BACKFILL
      required:
      - type
    UpdateAccountRequest:
      type: object
      properties:
        appRole:
          type: string
    UpdateConnectionStatusRequest:
      type: object
      description: Request to update a connection's lifecycle status
      properties:
        reason:
          type: string
          description: Optional human-readable reason recorded on the audit trail
        state:
          type: string
          description: "Target lifecycle state. Admin-settable: ACTIVE, SUSPENDED,\
            \ UNINSTALLED."
          enum:
          - PENDING
          - ACTIVE
          - SUSPENDED
          - UNINSTALLED
      required:
      - state
    UpdateCuratedStatusRequest:
      type: object
      description: Whether new workspaces receive this catalog entry
      properties:
        status:
          type: string
          enum:
          - AVAILABLE
          - RETIRED
      required:
      - status
    UpdateInstanceLlmSettingsRequest:
      type: object
      description: Update instance-wide LLM governance settings (all fields optional)
      properties:
        allowWorkspaceConnections:
          type: boolean
          description: Whether workspaces may register their own LLM connections
        allowedEgressHosts:
          type: string
          description: Comma/newline-delimited egress host allowlist; blank clears
            it
    UpdateLeaderboardDigestRequest:
      type: object
      description: Request to update the entire weekly leaderboard digest configuration
        atomically
      properties:
        channelId:
          type: string
          description: Slack channel ID for notifications
          example: C01234567AB
          pattern: "^[CG][A-Z0-9]{8,}$"
        day:
          type: integer
          format: int32
          description: "Day of week (1=Monday, 7=Sunday)"
          example: 1
          maximum: 7
          minimum: 1
        enabled:
          type: boolean
          description: Whether leaderboard notifications are enabled
        team:
          type: string
          description: Team name for filtering leaderboard notifications
        time:
          type: string
          description: Time in 24-hour format (HH:mm)
          example: 09:00
          pattern: "^([01]\\d|2[0-3]):[0-5]\\d$"
      required:
      - day
      - time
    UpdateLlmBudgetRequest:
      type: object
      description: Set or clear a monthly LLM budget cap
      properties:
        monthlyBudgetUsd:
          type: number
          format: decimal
          description: "Cap in USD; 0 pauses the affected work immediately, null removes\
            \ the cap. The purse it governs is the one the path names, and only that\
            \ one: /admin/workspaces/{workspaceSlug}/llm/budget caps spend on shared\
            \ (instance) models, /workspaces/{workspaceSlug}/llm/budget caps the workspace's\
            \ spend on its own connected provider. The two are never added together."
          minimum: 0.00
    UpdateLlmConnectionRequest:
      type: object
      description: Update an instance LLM provider connection (all fields optional)
      properties:
        apiKey:
          type: string
          description: New API key (write-only; never returned)
        clearApiKey:
          type: boolean
          description: Set true to clear the stored API key
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the connection is enabled
    UpdateLlmModelPriceRequest:
      type: object
      description: Reprice a model; supersedes the current price
      properties:
        note:
          type: string
          description: "Note; required when the model is free (e.g. self-hosted, no\
            \ cost)"
          maxLength: 500
          minLength: 0
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: "Cache-read rate per 1M tokens (USD), if applicable"
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: "Cache-write rate per 1M tokens (USD), if applicable"
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD); required when the model has
            a price
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD); required when the model has
            a price
        pricingMode:
          type: string
          description: PRICED shows the price itself; NO_CHARGE is a deliberate no-cost
            declaration; UNPRICED shows "No price set"
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
      required:
      - pricingMode
    UpdateLlmModelRequest:
      type: object
      description: Update a model's metadata (all fields optional; pricing and sharing
        are separate)
      properties:
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Active toggle (off = existing settings stop working)
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
    UpdateLlmModelSharingRequest:
      type: object
      description: "Share with: all workspaces, or a selected set"
      properties:
        visibility:
          type: string
          description: Share with all workspaces (PUBLIC) or only the selected ones
            (GRANTED)
          enum:
          - PUBLIC
          - GRANTED
        workspaceIds:
          type: array
          description: Workspace ids to share with when visibility is GRANTED; empty
            or omitted stages the model without workspace access
          items:
            type: integer
            format: int64
      required:
      - visibility
    UpdateLoginProviderRequest:
      type: object
      properties:
        baseUrl:
          type: string
        clientId:
          type: string
        clientSecret:
          type: string
          description: Omit to leave the existing secret unchanged
        displayName:
          type: string
        enabled:
          type: boolean
        scopes:
          type: string
    UpdateOutlineCollectionStateRequest:
      type: object
      description: Transition a mirrored Outline collection to a target mirror state
        (pause / resume)
      properties:
        state:
          type: string
          description: Target mirror state
          enum:
          - ENABLED
          - PAUSED
      required:
      - state
    UpdatePracticeAreaRequest:
      type: object
      description: Request to update an existing practice area (PATCH — only non-null
        fields applied)
      properties:
        color:
          type: string
          description: Optional palette colour key for the area's chip
          maxLength: 32
          minLength: 0
          pattern: "^[a-z]+$"
        description:
          type: string
          description: What this area develops
          maxLength: 5000
          minLength: 0
        displayOrder:
          type: integer
          format: int32
          description: Sort order within the workspace
        icon:
          type: string
          description: Optional lucide icon name giving the area a glanceable identity
          maxLength: 64
          minLength: 0
          pattern: "^[A-Za-z0-9]+$"
        name:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 3
          pattern: .*\S.*
        visibleInPracticeDashboards:
          type: boolean
          description: Whether this area is shown in practice dashboards
    UpdatePracticeRequest:
      type: object
      description: Request to update a practice; omitted fields remain unchanged
      properties:
        area:
          $ref: "#/components/schemas/BindPracticeAreaRequest"
          description: Catalog placement to apply with the definition update; omit
            to leave unchanged
        automatedReviewPolicy:
          $ref: "#/components/schemas/PracticeAutomatedReviewPolicy"
          description: "Replacement review settings; omit to preserve them, or to\
            \ take the recommended ones when the bindings move the practice to a different\
            \ kind of work"
        bindings:
          type: array
          description: Replacement occasion and its evidence; omit to leave it unchanged
          items:
            $ref: "#/components/schemas/PracticeBinding"
          maxItems: 1
          minItems: 1
        clear:
          type: array
          description: Optional fields to clear before applying supplied values
          items:
            type: string
            enum:
            - PRECOMPUTE_SCRIPT
            - WHY_IT_MATTERS
            - WHAT_GOOD_LOOKS_LIKE
          uniqueItems: true
        criteria:
          type: string
          description: Practice review criteria
          maxLength: 50000
          minLength: 0
          pattern: .*\S.*
        name:
          type: string
          description: Human-readable name
          example: PR Description Quality
          maxLength: 128
          minLength: 3
          pattern: .*\S.*
        precomputeScript:
          type: string
          description: TypeScript/Bun static analysis run before automated review
          maxLength: 100000
          minLength: 0
        whatGoodLooksLike:
          type: string
          description: Concrete example shown to the developer; not review criteria
          maxLength: 2000
          minLength: 0
          pattern: .*\S.*
        whyItMatters:
          type: string
          description: Plain-language rationale shown to the developer
          maxLength: 2000
          minLength: 0
          pattern: .*\S.*
    UpdatePracticeReviewSettingsRequest:
      type: object
      description: Update per-workspace practice-review policy. Null fields unchanged;
        'reset' clears to inherit.
      properties:
        cooldownMinutes:
          type: integer
          format: int32
          description: Minimum minutes between reviews for the same PR; 0 disables
            the cooldown
          maximum: 1440
          minimum: 0
        defaultReviewTier:
          type: string
          description: How much autonomy the system has over practices and areas that
            hold no tier of their own. The one decision that moves a whole workspace
            at once. Null leaves it unchanged; name DEFAULT_REVIEW_TIER in 'reset'
            to clear it. PROPOSE is not selectable yet.
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
        deliverToMerged:
          type: boolean
          description: Deliver feedback to already-merged PRs/MRs
        reset:
          type: array
          description: Fields to reset back to inherit
          items:
            type: string
            enum:
            - RUN_FOR_ALL_USERS
            - SKIP_DRAFTS
            - DELIVER_TO_MERGED
            - COOLDOWN_MINUTES
            - REVIEW_SCOPE
            - DEFAULT_REVIEW_TIER
          uniqueItems: true
        reviewScope:
          $ref: "#/components/schemas/WorkspaceReviewScope"
          description: "Replaces the review scope wholesale (the lists ARE the setting,\
            \ so a merge could only ever add). Null leaves it unchanged; two empty\
            \ lists clear it back to unrestricted."
        runForAllUsers:
          type: boolean
          description: Run practice review for all developers (vs only the run_practice_review
            role)
    UpdatePracticeReviewTierRequest:
      type: object
      description: "Set how much autonomy the system has here, or clear it back to\
        \ inherit"
      properties:
        reviewTier:
          type: string
          description: "OFF = not reviewed at all · PROPOSE = the review runs and\
            \ every observation is recorded, and nothing is sent · DELIVER = feedback\
            \ is delivered without asking. Send null (or omit the field) to hold no\
            \ tier here and inherit — a practice inherits its area's, an area inherits\
            \ the workspace default."
          enum:
          - "OFF"
          - PROPOSE
          - DELIVER
    UpdateRepositorySettingsRequest:
      type: object
      description: Request to update repository contribution visibility settings in
        a workspace
      properties:
        hiddenFromContributions:
          type: boolean
          description: Whether contributions from this repository should be hidden
            from leaderboard calculations
      required:
      - hiddenFromContributions
    UpdateReviewBackfillRunStatusRequest:
      type: object
      description: |-
        The lifecycle transitions an admin can ask for.

         <p><code>RUNNING</code> is the confirmation: it is the point at which somebody accepts the estimate they
         were shown and authorises the spend. <code>CANCELLED</code> stops a campaign for good. Every other
         transition — pausing on an exhausted budget, resuming when it clears, completing at the end of the
         scope — belongs to the driver, and is refused here so the state on screen always reflects something
         the system decided or something a person did, never a mixture.
      properties:
        status:
          type: string
          description: RUNNING confirms the estimate and starts the campaign; CANCELLED
            stops it for good
          enum:
          - RUNNING
          - CANCELLED
      required:
      - status
    UpdateReviewSweepScheduleRequest:
      type: object
      description: |-
        Replace a sweep schedule's terms.

         <p>Every field is required: a schedule is three numbers that only mean anything together — a cadence
         changed without its lookback can silently make a window illegal, or leave days nothing ever sweeps.
         The artifact kind is not among them; a schedule for a different kind of work is a different schedule.
      properties:
        cadence:
          type: string
          description: How often the sweep runs
          enum:
          - DAILY
          - WEEKLY
        enabled:
          type: boolean
          description: Whether the scheduler acts on this row; a disabled schedule
            keeps its terms
        lookbackDays:
          type: integer
          format: int32
          description: "How far back each sweep looks, in days"
          example: 2
          maximum: 7
          minimum: 1
      required:
      - cadence
      - enabled
      - lookbackDays
    UpdateSilentModeRequest:
      type: object
      properties:
        engaged:
          type: boolean
        reason:
          type: string
          maxLength: 500
          minLength: 0
      required:
      - engaged
    UpdateSlackChannelConsentRequest:
      type: object
      description: Transition a Slack channel to a target consent state
      properties:
        consentState:
          type: string
          description: Target consent state
          enum:
          - PENDING
          - ACTIVE
          - PAUSED
          - REVOKED
        reason:
          type: string
          description: Optional free-text reason recorded in the immutable audit trail
          maxLength: 2000
          minLength: 0
      required:
      - consentState
    UpdateSlackUserPreferencesRequest:
      type: object
      properties:
        channelMessagesAllowed:
          type: boolean
      required:
      - channelMessagesAllowed
    UpdateSyncJobRequest:
      type: object
      properties:
        cancelRequested:
          type: boolean
      required:
      - cancelRequested
    UpdateTeamSettingsRequest:
      type: object
      description: Request to update team visibility settings in a workspace
      properties:
        hidden:
          type: boolean
          description: Whether the team should be hidden from the leaderboard
      required:
      - hidden
    UpdateWorkspaceFeaturesRequest:
      type: object
      description: Request to update workspace feature flags. Null fields are left
        unchanged.
      properties:
        achievementsEnabled:
          type: boolean
          description: Enable the achievements system
        leaderboardEnabled:
          type: boolean
          description: Enable the leaderboard ranking page
        leaguesEnabled:
          type: boolean
          description: Enable league tiers and rankings
        mentorEnabled:
          type: boolean
          description: Enable the Pi mentor chat feature
        practiceReviewAutoTriggerEnabled:
          type: boolean
          description: Enable automatic practice reviews triggered by PR events
        practiceReviewManualTriggerEnabled:
          type: boolean
          description: Enable manual practice reviews triggered via bot command
        practicesEnabled:
          type: boolean
          description: Enable the practice review feature
        progressionEnabled:
          type: boolean
          description: Enable the league/progression system
    UpdateWorkspaceLlmConnectionRequest:
      type: object
      description: Update your AI provider connection (all fields optional)
      properties:
        apiKey:
          type: string
          description: New API key (write-only; never returned)
        clearApiKey:
          type: boolean
          description: Set true to clear the stored API key
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Whether the connection is active
    UpdateWorkspaceLlmModelRequest:
      type: object
      description: Update a model on your AI provider (all fields optional)
      properties:
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        displayName:
          type: string
          description: Human-readable name
          maxLength: 128
          minLength: 0
        enabled:
          type: boolean
          description: Active toggle
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: "Cache-read rate per 1M tokens (USD), if applicable"
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: "Cache-write rate per 1M tokens (USD), if applicable"
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD)
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD)
        priceNote:
          type: string
          description: "Note; required when the model is free (e.g. self-hosted, no\
            \ cost)"
          maxLength: 500
          minLength: 0
        pricingMode:
          type: string
          description: "Pricing mode; when given, replaces the price wholesale (see\
            \ class docs)"
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
    UpdateWorkspaceNotificationsRequest:
      type: object
      description: Request to update leaderboard notification settings
      properties:
        channelId:
          type: string
          description: Slack channel ID for notifications
          example: C01234567AB
          pattern: "^[CG][A-Z0-9]{8,}$"
        enabled:
          type: boolean
          description: Whether leaderboard notifications are enabled
        team:
          type: string
          description: Team name for filtering leaderboard notifications
    UpdateWorkspacePublicVisibilityRequest:
      type: object
      description: Request to update workspace public visibility setting
      properties:
        isPubliclyViewable:
          type: boolean
          description: Whether the workspace should be publicly viewable without authentication
      required:
      - isPubliclyViewable
    UpdateWorkspaceScheduleRequest:
      type: object
      description: Request to update the leaderboard notification schedule
      properties:
        day:
          type: integer
          format: int32
          description: "Day of week (1=Monday, 7=Sunday)"
          example: 1
          maximum: 7
          minimum: 1
        time:
          type: string
          description: Time in 24-hour format (HH:mm)
          example: 09:00
          pattern: "^([01]\\d|2[0-3]):[0-5]\\d$"
      required:
      - day
      - time
    UpdateWorkspaceStatusRequest:
      type: object
      description: Request to update the workspace lifecycle status
      properties:
        status:
          type: string
          description: "Target lifecycle status. PURGED is not a status transition:\
            \ purge is irreversible and owner-only, so it lives at DELETE /workspaces/{workspaceSlug}."
          enum:
          - ACTIVE
          - SUSPENDED
      required:
      - status
    UpdateWorkspaceTokenRequest:
      type: object
      description: Request to update the workspace's SCM Personal Access Token
      properties:
        personalAccessToken:
          type: string
          description: SCM Personal Access Token for API access
          minLength: 1
      required:
      - personalAccessToken
    UserInfo:
      type: object
      description: Information about a user from the git provider
      properties:
        avatarUrl:
          type: string
          description: URL to the user's avatar image
        email:
          type: string
          description: "Email address of the user, if public"
        htmlUrl:
          type: string
          description: URL to the user's profile on the git provider
        id:
          type: integer
          format: int64
          description: Unique identifier of the user
        leaguePoints:
          type: integer
          format: int32
          description: League points earned by the user in the current scope
          example: 150
        login:
          type: string
          description: Login/username of the user
        name:
          type: string
          description: Display name of the user
      required:
      - avatarUrl
      - htmlUrl
      - id
      - login
      - name
    UserSettings:
      type: object
      description: User preferences and settings
      properties:
        participateInResearch:
          type: boolean
          description: Whether the user consents to participate in research studies
        practiceFeedbackDeliveryEnabled:
          type: boolean
          description: "Whether new practice-feedback comments may be delivered on\
            \ issues, pull requests, and merge requests authored by the user, together\
            \ with related Slack reminders"
      required:
      - participateInResearch
      - practiceFeedbackDeliveryEnabled
    UserTeams:
      type: object
      properties:
        email:
          type: string
        hidden:
          type: boolean
        id:
          type: integer
          format: int64
        login:
          type: string
        name:
          type: string
        teams:
          type: array
          items:
            $ref: "#/components/schemas/TeamSummary"
          uniqueItems: true
        url:
          type: string
      required:
      - id
      - login
      - name
      - teams
      - url
    Workspace:
      type: object
      description: Complete workspace information including configuration and settings
      properties:
        accountLogin:
          type: string
          description: Git provider account login associated with this workspace
        achievementsEnabled:
          type: boolean
          description: Whether the achievements system is enabled
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the workspace was created
        displayName:
          type: string
          description: Human-readable name of the workspace
        gitlabWebhookRegistered:
          type: boolean
          description: Whether a GitLab webhook has been auto-registered for this
            workspace
        hasPersonalAccessToken:
          type: boolean
          description: Whether a Personal Access Token is configured
        hasSlackToken:
          type: boolean
          description: Whether Slack token is configured
        id:
          type: integer
          format: int64
          description: Unique identifier of the workspace
        installationId:
          type: integer
          format: int64
          description: "GitHub App installation ID, if linked"
        installationLinkedAt:
          type: string
          format: date-time
          description: Timestamp when the GitHub App installation was linked
        isPubliclyViewable:
          type: boolean
          description: Whether the workspace is publicly viewable without authentication
        kind:
          type: string
          description: Integration kind backing this workspace (GITHUB or GITLAB)
        leaderboardEnabled:
          type: boolean
          description: Whether the leaderboard is enabled
        leaderboardNotificationChannelId:
          type: string
          description: Slack channel ID for leaderboard notifications
        leaderboardNotificationEnabled:
          type: boolean
          description: Whether leaderboard notifications are enabled
        leaderboardNotificationTeam:
          type: string
          description: Team name for leaderboard notifications
        leaderboardScheduleDay:
          type: integer
          format: int32
          description: "Day of week for leaderboard notifications (1=Monday, 7=Sunday)"
          example: 1
        leaderboardScheduleTime:
          type: string
          description: Time for leaderboard notifications in HH:mm format
          example: 09:00
        leaguesEnabled:
          type: boolean
          description: Whether league tiers and rankings are enabled
        mentorEnabled:
          type: boolean
          description: Whether the Pi mentor chat feature is enabled
        practiceReviewAutoTriggerEnabled:
          type: boolean
          description: Whether automatic practice reviews triggered by PR events are
            enabled
        practiceReviewManualTriggerEnabled:
          type: boolean
          description: Whether manual practice reviews triggered via bot command are
            enabled
        practicesEnabled:
          type: boolean
          description: Whether the practice review feature is enabled
        progressionEnabled:
          type: boolean
          description: Whether the league/progression system is enabled
        providerType:
          type: string
          description: High-level git provider type for the workspace's SCM connection
            (null if none bound)
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        serverUrl:
          type: string
          description: Custom server URL for self-hosted instances (null for cloud
            defaults)
        slackConnectionId:
          type: integer
          format: int64
          description: "ID of the active Slack connection, if any — addresses PATCH\
            \ /connections/{id}/status"
        status:
          type: string
          description: "Current lifecycle status of the workspace (PENDING, ACTIVE,\
            \ ARCHIVED)"
        updatedAt:
          type: string
          format: date-time
          description: Timestamp when the workspace was last updated
        workspaceSlug:
          type: string
          description: URL-friendly identifier for the workspace
          example: my-workspace
      required:
      - accountLogin
      - achievementsEnabled
      - createdAt
      - displayName
      - gitlabWebhookRegistered
      - hasPersonalAccessToken
      - hasSlackToken
      - id
      - isPubliclyViewable
      - leaderboardEnabled
      - leaguesEnabled
      - mentorEnabled
      - practiceReviewAutoTriggerEnabled
      - practiceReviewManualTriggerEnabled
      - practicesEnabled
      - progressionEnabled
      - status
      - updatedAt
      - workspaceSlug
    WorkspaceListItem:
      type: object
      description: Summary information about a workspace for list views
      properties:
        accountLogin:
          type: string
          description: Git provider account login associated with this workspace
        achievementsEnabled:
          type: boolean
          description: Whether the achievements system is enabled
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the workspace was created
        displayName:
          type: string
          description: Human-readable name of the workspace
        id:
          type: integer
          format: int64
          description: Unique identifier of the workspace
        leaderboardEnabled:
          type: boolean
          description: Whether the leaderboard is enabled
        leaguesEnabled:
          type: boolean
          description: Whether league tiers and rankings are enabled
        mentorEnabled:
          type: boolean
          description: Whether the Pi mentor chat feature is enabled
        practicesEnabled:
          type: boolean
          description: Whether the practice review feature is enabled
        progressionEnabled:
          type: boolean
          description: Whether the league/progression system is enabled
        providerType:
          type: string
          description: "High-level git provider type (GITHUB or GITLAB), or null if\
            \ no SCM connection bound"
          enum:
          - GITHUB
          - GITLAB
          - SLACK
          - OUTLINE
        status:
          type: string
          description: "Current lifecycle status of the workspace (PENDING, ACTIVE,\
            \ ARCHIVED)"
        workspaceSlug:
          type: string
          description: URL-friendly identifier for the workspace
          example: my-workspace
      required:
      - accountLogin
      - achievementsEnabled
      - createdAt
      - displayName
      - id
      - leaderboardEnabled
      - leaguesEnabled
      - mentorEnabled
      - practicesEnabled
      - progressionEnabled
      - status
      - workspaceSlug
    WorkspaceLlmConnection:
      type: object
      description: Your AI provider connection (API key redacted)
      properties:
        apiKeyLast4:
          type: string
          description: "Last four characters of the stored API key, if any"
        apiProtocol:
          type: string
          description: Wire protocol
          example: openai-completions
        authMode:
          type: string
          description: Credential shape
          enum:
          - BEARER
          - API_KEY
        baseUrl:
          type: string
          description: Provider base URL
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        displayName:
          type: string
          description: Human-readable name
        enabled:
          type: boolean
          description: Whether the connection is active
        hasApiKey:
          type: boolean
          description: Whether an API key is stored
        id:
          type: integer
          format: int64
          description: Connection ID
        slug:
          type: string
          description: Unique slug within the workspace
          example: openai-prod
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
      required:
      - apiProtocol
      - authMode
      - baseUrl
      - createdAt
      - displayName
      - enabled
      - hasApiKey
      - id
      - slug
    WorkspaceLlmModel:
      type: object
      description: A model on your AI provider
      properties:
        connectionDisplayName:
          type: string
          description: Owning connection's display name
        connectionId:
          type: integer
          format: int64
          description: Owning connection id
        contextWindow:
          type: integer
          format: int32
          description: Context window in tokens
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        currency:
          type: string
          description: Currency code
        displayName:
          type: string
          description: Human-readable name
        enabled:
          type: boolean
          description: Active toggle
        id:
          type: integer
          format: int64
          description: Model id
        maxOutputTokens:
          type: integer
          format: int32
          description: Maximum output tokens
        per1mCacheReadUsd:
          type: number
          format: decimal
          description: Cache-read rate per 1M tokens (USD)
        per1mCacheWriteUsd:
          type: number
          format: decimal
          description: Cache-write rate per 1M tokens (USD)
        per1mInputUsd:
          type: number
          format: decimal
          description: Input rate per 1M tokens (USD)
        per1mOutputUsd:
          type: number
          format: decimal
          description: Output rate per 1M tokens (USD)
        priceNote:
          type: string
          description: Price note
        pricingMode:
          type: string
          description: Pricing mode
          enum:
          - PRICED
          - NO_CHARGE
          - UNPRICED
        slug:
          type: string
          description: Unique slug within the workspace
        supportsReasoning:
          type: boolean
          description: Whether the model supports a reasoning mode
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
        upstreamModelId:
          type: string
          description: Upstream provider model id
      required:
      - connectionDisplayName
      - connectionId
      - createdAt
      - currency
      - displayName
      - enabled
      - id
      - pricingMode
      - slug
      - supportsReasoning
      - upstreamModelId
    WorkspaceLlmProbeResult:
      type: object
      description: Result of testing your AI provider connection
      properties:
        message:
          type: string
          description: Human-readable diagnostic when not reachable
        modelCount:
          type: integer
          format: int32
          description: How many models the provider listed (0 if unreachable)
        reachable:
          type: boolean
          description: Whether the provider answered
      required:
      - modelCount
      - reachable
    WorkspaceLlmSettings:
      type: object
      description: The instance LLM policy as it applies to this workspace (read-only)
      properties:
        ownProviderAllowed:
          type: boolean
          description: Whether this workspace may register its own LLM provider connections
      required:
      - ownProviderAllowed
    WorkspaceLlmUsageReport:
      type: object
      description: "One calendar month of a workspace's LLM spend, rolled up from\
        \ the usage ledger"
      properties:
        byDay:
          type: array
          items:
            $ref: "#/components/schemas/LlmUsageByDay"
        byJobType:
          type: array
          items:
            $ref: "#/components/schemas/LlmUsageByJobType"
        fx:
          $ref: "#/components/schemas/FxRateInfo"
          description: Display-only conversion when the instance has a display currency.
            Absent = show USD only.
        instanceBudgetVerdict:
          type: string
          description: "Whether host-funded (shared-model) spend is within its cap,\
            \ has reached it, or can't be confirmed because some shared-model usage\
            \ has no price set."
          enum:
          - WITHIN
          - EXHAUSTED
          - UNVERIFIABLE
        instanceMonthlyBudgetUsd:
          type: number
          format: decimal
          description: Monthly cap in USD on spend the host pays for (shared models);
            null = uncapped. Set by instance admins only — a workspace admin can see
            it but not change it.
        instancePaused:
          type: boolean
          description: "Whether work on SHARED models is currently paused for this\
            \ workspace. Authoritative — it mirrors the live gate rather than being\
            \ derivable from the verdict alone. Always false for a past month, which\
            \ cannot pause anything."
        instanceTotalCostUsd:
          type: number
          format: decimal
          description: "This month's confirmed spend on shared (instance) models,\
            \ in USD — the figure instanceMonthlyBudgetUsd compares against. A floor,\
            \ not the full total, while unpricedEventCount is non-zero."
        month:
          type: string
          description: "Calendar month (UTC), ISO yyyy-MM"
          example: 2026-07
        ownProviderBudgetVerdict:
          type: string
          description: "The same verdict for spend on this workspace's own provider,\
            \ against its own cap."
          enum:
          - WITHIN
          - EXHAUSTED
          - UNVERIFIABLE
        ownProviderMonthlyBudgetUsd:
          type: number
          format: decimal
          description: Monthly cap in USD on spend this workspace pays for through
            its own connected provider; null = uncapped. Set by this workspace's own
            admins.
        ownProviderPaused:
          type: boolean
          description: Whether work on this workspace's OWN provider is currently
            paused.
        ownProviderTotalCostUsd:
          type: number
          format: decimal
          description: "This month's confirmed spend on this workspace's own connected\
            \ provider(s), in USD — the figure ownProviderMonthlyBudgetUsd compares\
            \ against. Different money from instanceTotalCostUsd: the two are never\
            \ added together."
        unpricedEventCount:
          type: integer
          format: int64
          description: "Calls this month (either purse) whose price is not yet known.\
            \ They are excluded from both totals above, so a non-zero value means\
            \ the real spend may be higher than shown."
      required:
      - byDay
      - byJobType
      - instanceBudgetVerdict
      - instancePaused
      - instanceTotalCostUsd
      - month
      - ownProviderBudgetVerdict
      - ownProviderPaused
      - ownProviderTotalCostUsd
      - unpricedEventCount
    WorkspaceMembership:
      type: object
      description: A user's membership in a workspace
      properties:
        createdAt:
          type: string
          format: date-time
          description: Timestamp when the membership was created
        hidden:
          type: boolean
          description: Whether the member is hidden from the leaderboard
        leaguePoints:
          type: integer
          format: int32
          description: League points earned by the user in this workspace
          example: 150
        role:
          type: string
          description: "Role of the user in this workspace (OWNER, ADMIN, MEMBER)"
          enum:
          - OWNER
          - ADMIN
          - MEMBER
        userId:
          type: integer
          format: int64
          description: Unique identifier of the user
        userLogin:
          type: string
          description: Login/username of the user
        userName:
          type: string
          description: Display name of the user
    WorkspaceProviders:
      type: object
      description: Available workspace creation providers and their configuration
      properties:
        creationPolicy:
          type: string
          description: Who may create workspaces — ADMIN_ONLY restricts creation to
            instance admins
          enum:
          - ADMIN_ONLY
          - SELF_SERVICE
        github:
          $ref: "#/components/schemas/GitHubProvider"
          description: "GitHub workspace provider config, null if not available"
        gitlab:
          $ref: "#/components/schemas/GitLabProvider"
          description: "GitLab workspace provider config, null if not available"
    WorkspaceReviewScope:
      type: object
      description: |-
        The workspace's answer to "which of our work is reviewed at all", ANDed onto every practice binding.
         It exists because a binding says <code>scm.pull_request.merged</code> and cannot say <em>merged into
         what</em>: a trunk is named <code>main</code> here and <code>develop</code> there, which is a deployment fact
         about one workspace that a centrally curated catalogue cannot carry.

         <p>The scope only ever narrows, never widens: an empty or absent list means "no restriction on this
         axis".

         <h2>What this cannot express, and why</h2>

         <ul>
           <li><strong>Changed paths.</strong> Not decidable here: the detection gate holds the
               <code>PullRequest</code> row, not the diff, and changed paths do not exist until the evidence stage,
               by which point the review has been admitted and paid for. A path axis here would be a predicate
               that quietly never narrows anything.
           <li><strong>Branch patterns.</strong> Exact names only; adding globs later stays backward
               compatible, taking them away would not.
           <li><strong>Any third key.</strong> The vocabulary is closed at the column
               (<code>chk_workspace_review_scope</code>), not by this type: a reader configured to ignore unknown
               fields would drop the key in silence, leaving a workspace believing a restriction was in force.
         </ul>
      properties:
        repositories:
          type: array
          description: |-
            exact <code>owner/name</code> repository identifiers. Empty = every monitored
                 repository. This narrows <em>review</em> within the set the workspace already syncs; it never
                 adds one.
          items:
            type: string
        targetBranches:
          type: array
          description: exact target-branch names (a PR's base ref). Empty = every
            branch.
          items:
            type: string
    WorkspaceTeamRepositorySettings:
      type: object
      description: Repository contribution visibility settings for a specific team
        within a workspace
      properties:
        hiddenFromContributions:
          type: boolean
          description: Whether contributions from this repository are hidden from
            leaderboard calculations
        repositoryId:
          type: integer
          format: int64
          description: The repository ID these settings apply to
        teamId:
          type: integer
          format: int64
          description: The team ID these settings apply to
        workspaceId:
          type: integer
          format: int64
          description: The workspace ID these settings belong to
      required:
      - hiddenFromContributions
      - repositoryId
      - teamId
      - workspaceId
    WorkspaceTeamSettings:
      type: object
      description: Team visibility settings for a specific workspace
      properties:
        hidden:
          type: boolean
          description: Whether the team is hidden in the leaderboard for this workspace
        teamId:
          type: integer
          format: int64
          description: The team ID these settings apply to
        workspaceId:
          type: integer
          format: int64
          description: The workspace ID these settings belong to
      required:
      - hidden
      - teamId
      - workspaceId
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: Hephaestus-native JWT bearer authentication. The SPA normally authenticates
        via the `__Host-HEPHAESTUS_AT` session cookie; this scheme documents the equivalent
        bearer token.
      scheme: bearer
      type: http
