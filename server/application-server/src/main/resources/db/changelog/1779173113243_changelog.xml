<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Drop orphan Hibernate-synthesized UNIQUE constraints on discussion / discussion_category.

        Background: prior `Discussion`/`DiscussionCategory` entities declared `@UniqueConstraint`
        WITHOUT an explicit `name`. Under `ddl-auto: update` (dev local) Hibernate's
        `ImplicitNamingStrategy` synthesized hash-style names such as
            uknlcwyn2relkgw95s8okgpkqrt   (discussion.repository_id+number)
            uk6cjmvjyh5jc9bfnn8i9wggbo5   (discussion_category.repository_id+slug)
        in parallel with the Liquibase-managed `uq_discussion_repo_number` and
        `uq_discussion_category_repo_slug`. With `validate` mode and explicit `name=` on the
        entities (introduced alongside this changeset), the orphans must go.

        Idempotent: `IF EXISTS` for the known names, plus a defensive scan dropping any
        `uk[a-z0-9]{20,}` named UNIQUE constraint on the two tables that may exist from
        earlier `ddl-auto: update` runs against revisions we no longer have.

        Production never had these constraints (prod runs `ddl-auto: none`); on prod this
        changeset is a no-op. On dev DBs it cleans up the accumulated drift.
    -->
    <changeSet author="FelixTJDietrich" id="1779173113243-1" context="dev or prod">
        <comment>Drop orphan Hibernate-synthesized UNIQUE constraints on discussion / discussion_category</comment>
        <sql dbms="postgresql" splitStatements="false">
            ALTER TABLE discussion DROP CONSTRAINT IF EXISTS uknlcwyn2relkgw95s8okgpkqrt;
            ALTER TABLE discussion_category DROP CONSTRAINT IF EXISTS uk6cjmvjyh5jc9bfnn8i9wggbo5;

            DO $$
            DECLARE
                r RECORD;
            BEGIN
                FOR r IN
                    SELECT con.conname, cls.relname
                    FROM pg_constraint con
                    JOIN pg_class cls ON con.conrelid = cls.oid
                    JOIN pg_namespace nsp ON cls.relnamespace = nsp.oid
                    WHERE nsp.nspname = current_schema()
                      AND cls.relname IN ('discussion', 'discussion_category')
                      AND con.contype = 'u'
                      AND con.conname ~ '^uk[a-z0-9]{20,}$'
                LOOP
                    EXECUTE format('ALTER TABLE %I DROP CONSTRAINT %I', r.relname, r.conname);
                END LOOP;
            END
            $$;
        </sql>
        <rollback>
            <!-- Intentionally empty: the orphan constraints are duplicates of named ones;
                 there is no meaningful rollback. Restore from backup if absolutely needed. -->
            <comment>No rollback: dropped constraints were duplicates of the named uq_* constraints</comment>
        </rollback>
    </changeSet>

</databaseChangeLog>
