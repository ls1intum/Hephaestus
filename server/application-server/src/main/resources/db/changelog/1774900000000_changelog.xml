<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- Step 1: Rename practice.detection_prompt → practice.criteria -->
    <changeSet id="1774900000000-1" author="hephaestus">
        <comment>Rename detection_prompt to criteria to match domain model</comment>
        <renameColumn tableName="practice"
                      oldColumnName="detection_prompt"
                      newColumnName="criteria"
                      columnDataType="TEXT"/>
    </changeSet>

    <!-- Step 2: Simplify verdict enum — remove NOT_APPLICABLE and NEEDS_REVIEW -->
    <!-- First delete findings with removed verdicts (they are noise, not signal) -->
    <changeSet id="1774900000000-2" author="hephaestus">
        <comment>Delete NOT_APPLICABLE and NEEDS_REVIEW findings before tightening constraint</comment>
        <delete tableName="practice_finding">
            <where>verdict IN ('NOT_APPLICABLE', 'NEEDS_REVIEW')</where>
        </delete>
    </changeSet>

    <!-- Drop old CHECK constraint and add tightened one -->
    <changeSet id="1774900000000-3" author="hephaestus">
        <comment>Tighten verdict CHECK constraint to POSITIVE/NEGATIVE only</comment>
        <sql>
            ALTER TABLE practice_finding DROP CONSTRAINT IF EXISTS chk_practice_finding_verdict;
            ALTER TABLE practice_finding ADD CONSTRAINT chk_practice_finding_verdict
                CHECK (verdict IN ('POSITIVE', 'NEGATIVE'));
        </sql>
    </changeSet>

</databaseChangeLog>
