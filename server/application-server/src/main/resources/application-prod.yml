spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      # TODO: Change to none and use liquibase
      ddl-auto: none
  url: ${APPLICATION_SERVER_URL}

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_URL}/realms/${KEYCLOAK_REALM}

nats:
    enabled: ${NATS_ENABLED:false}
    timeframe: ${MONITORING_TIMEFRAME:7}
    durableConsumerName: ${NATS_DURABLE_CONSUMER_NAME}
    server: ${NATS_SERVER}

monitoring:
    runOnStartup: ${MONITORING_RUN_ON_STARTUP:true}
    # Fetching timeframe in days
    timeframe: ${MONITORING_TIMEFRAME:7}
    repository-sync-cron: ${MONITORING_REPOSITORY_SYNC_CRON:0 0 * * * *}
    repositories: ${MONITORING_REPOSITORIES:ls1intum/Artemis}

github:
  authToken: ${GITHUB_AUTH_TOKEN}
slack:
    runScheduledMessage: ${SLACK_RUN_SCHEDULED_MESSAGE:true}
    cronSchedule: ${SLACK_CRON_SCHEDULE:0 0 9 ? * TUE}
    token: ${SLACK_BOT_TOKEN}
    signingSecret: ${SLACK_SIGNING_SECRET}
    channelId: ${SLACK_CHANNEL_ID:G6TCVL6HL}
