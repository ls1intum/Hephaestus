query GetMergeRequestApprovals($fullPath: ID!, $iid: String!, $first: Int!, $after: String) {
    project(fullPath: $fullPath) {
        mergeRequests(iids: [$iid]) {
            nodes {
                approvedBy(first: $first, after: $after) {
                    count
                    pageInfo {
                        hasNextPage
                        endCursor
                    }
                    nodes {
                        ...GitLabUserFields
                    }
                }
            }
        }
    }
}
