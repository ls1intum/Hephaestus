query GetGroupMembers($fullPath: ID!, $first: Int!, $after: String) {
    group(fullPath: $fullPath) {
        groupMembers(first: $first, after: $after, relations: [DIRECT, INHERITED]) {
            pageInfo {
                hasNextPage
                endCursor
            }
            nodes {
                user {
                    ...GitLabUserFields
                }
                accessLevel {
                    stringValue
                    integerValue
                }
            }
        }
    }
}
