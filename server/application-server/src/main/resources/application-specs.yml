nats:
    enabled: false

spring:
    docker:
        compose:
            enabled: ${SPRING_DOCKER_COMPOSE_ENABLED:false}

monitoring:
    run-on-startup: false

github:
    app:
        id: 0
        privateKey: ""
        privateKeyLocation: ""
    auth-token: ""

hephaestus:
    workspace:
        init-default: false
