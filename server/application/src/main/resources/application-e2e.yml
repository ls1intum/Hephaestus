hephaestus:
  security:
    encryption-key: ${HEPHAESTUS_SECURITY_ENCRYPTION_KEY:hephaestusLocalDevEncryptKey0123}
    credential-encryption-key: ${HEPHAESTUS_SECURITY_CREDENTIAL_ENCRYPTION_KEY:hephaestusLocalDevEncryptKey0123}
  auth:
    dev-login-enabled: true
    cookie-secure: false
    cookie-name: HEPHAESTUS_AT
  workspace:
    init-default: false
    creation-policy: SELF_SERVICE
    auto-seed-membership: true
  features:
    flags:
      gitlab-workspace-creation: true
  dev:
    trigger-enabled: true
  agent:
    enabled: true
  sync:
    run-on-startup: false
    filters:
      allowed-organizations: []
      allowed-repositories: []
    nats:
      enabled: true
      server: nats://localhost:4222
      durable-consumer-name: hephaestus-e2e
  integration:
    gitlab:
      enabled: true
  llm:
    egress:
      allow-loopback: true
