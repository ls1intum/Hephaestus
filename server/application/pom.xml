<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.tum.cit.aet</groupId>
        <artifactId>hephaestus-server-parent</artifactId>
        <version>0.0.0-development</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>hephaestus-application</artifactId>
    <name>Hephaestus</name>
    <description>AI-driven learning for better software development</description>
    <url />
    <licenses>
        <license />
    </licenses>
    <developers>
        <developer />
    </developers>
    <scm>
        <connection />
        <developerConnection />
        <tag />
        <url />
    </scm>
    <properties>
        <app.profiles>local</app.profiles>
        <maven.test.failure.ignore>false</maven.test.failure.ignore>
        <hephaestus.surefire.timeout>1200</hephaestus.surefire.timeout>
        <hephaestus.surefire.forkCount>1</hephaestus.surefire.forkCount>
        <!-- 4g sized for the unit+arch run (ArchUnit + Modulith class graph). -->
        <hephaestus.surefire.maxHeap>4096m</hephaestus.surefire.maxHeap>
        <!-- Surefire expands @{argLine}. JaCoCo's prepare-agent sets it in the lifecycle; the empty
             default lets a bare surefire:test goal run without that phase. -->
        <argLine />
        <surefire.excludedGroups>live</surefire.excludedGroups>
        <surefire.includedGroups>unit</surefire.includedGroups>
        <failsafe.excludedGroups>live</failsafe.excludedGroups>
        <failsafe.includedGroups>integration</failsafe.includedGroups>
        <!-- Coverage Configuration. Enabled in the unit-test CI job, which is the tier that exercises
             the gated pure-class packages; -DskipCoverage=true turns the agent and the gates off for
             the local fast path and for the arch/integration jobs, whose reports would cover a
             different slice of the code and cannot be judged against these floors. -->
        <skipCoverage>true</skipCoverage>
        <postgres.port>5432</postgres.port>
        <resilience4j.version>2.4.0</resilience4j.version>
        <okhttp.version>5.5.0</okhttp.version>
        <docker-java.version>3.7.1</docker-java.version>
        <archunit.version>1.5.0</archunit.version>
        <spring-modulith.version>2.1.1</spring-modulith.version>
        <liquibase.version>5.0.4</liquibase.version>
        <liquibase-hibernate7.version>5.0.4</liquibase-hibernate7.version>
        <springdoc.version>3.1.0</springdoc.version>
        <sentry.version>8.53.0</sentry.version>
        <logstash-logback-encoder.version>9.0</logstash-logback-encoder.version>
        <slack-bolt.version>1.50.0</slack-bolt.version>
        <jnats.version>2.26.2</jnats.version>
        <jgit.version>7.7.1.202607240634-r</jgit.version>
        <java-jwt.version>4.6.0</java-jwt.version>
        <therapi.version>0.15.0</therapi.version>
        <error-prone.version>2.50.0</error-prone.version>
        <nullaway.version>0.14.0</nullaway.version>
        <shedlock.version>7.9.0</shedlock.version>
        <bucket4j.version>8.19.0</bucket4j.version>
        <!-- Transitive only, via docker-java-core: bcpkix -> bcutil -> bcprov. Not managed by the
             Spring Boot BOM, so the version it resolves to is docker-java's, and 1.82 carries
             CVE-2025-14813 (critical) and CVE-2026-5598, both fixed in 1.84. Managed here so the
             image scan stays clean and Renovate keeps it current; drop it once docker-java ships a
             fixed floor of its own. -->
        <bouncycastle.version>1.84</bouncycastle.version>
        <maven.build.cache.skipCache>true</maven.build.cache.skipCache>
        <maven.build.cache.skipSave>true</maven.build.cache.skipSave>
    </properties>
    <dependencies>
        <dependency>
            <groupId>de.tum.cit.aet</groupId>
            <artifactId>hephaestus-generated-clients</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- JSpecify annotations also drive the OpenAPI required-field converter. -->
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>${logstash-logback-encoder.version}</version>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>context-propagation</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!-- ShedLock: distributed lock for @Scheduled methods so multi-pod replicas don't double-run.
             JDBC-template provider piggybacks on the existing DataSource (no new infra). -->
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-spring</artifactId>
            <version>${shedlock.version}</version>
        </dependency>
        <dependency>
            <groupId>net.javacrumbs.shedlock</groupId>
            <artifactId>shedlock-provider-jdbc-template</artifactId>
            <version>${shedlock.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Worker control channel (WSS over TLS-443). Server-side @WebSocketHandler + client-side
             JDK java.net.http.WebSocket. ADR 0009. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-restclient</artifactId>
        </dependency>
        <dependency>
            <groupId>com.auth0</groupId>
            <artifactId>java-jwt</artifactId>
            <version>${java-jwt.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <!-- WebClient.Builder autoconfig; not transitive via spring-boot-starter-webflux. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webclient</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-liquibase</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-logging</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-docker-compose</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aspectj</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
            <version>${spring-modulith.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <version>${spring-modulith.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-simple</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webtestclient</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>tools.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- springdoc-openapi reads class/method Javadoc via therapi-runtime-javadoc to inject
             description="..." into the generated openapi.yaml. Removing this strips descriptions
             from the public API contract, so it stays load-bearing despite no in-source callers. -->
        <dependency>
            <groupId>com.github.therapi</groupId>
            <artifactId>therapi-runtime-javadoc</artifactId>
            <version>${therapi.version}</version>
        </dependency>
        <dependency>
            <groupId>io.nats</groupId>
            <artifactId>jnats</artifactId>
            <version>${jnats.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-api</artifactId>
            <version>${docker-java.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-core</artifactId>
            <version>${docker-java.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.docker-java</groupId>
            <artifactId>docker-java-transport-httpclient5</artifactId>
            <version>${docker-java.version}</version>
        </dependency>
        <dependency>
            <groupId>com.slack.api</groupId>
            <artifactId>bolt</artifactId>
            <version>${slack-bolt.version}</version>
            <exclusions>
                <!-- Slack pulls okhttp:4.12.0; conflicts with our okhttp-jvm:5.x on TaskRunner. -->
                <exclusion>
                    <groupId>com.squareup.okhttp3</groupId>
                    <artifactId>okhttp</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- okhttp 5.x: bare 'okhttp' is a KMP shim (square/okhttp#8913); use okhttp-jvm. -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp-jvm</artifactId>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>logging-interceptor</artifactId>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver3</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate.validator</groupId>
            <artifactId>hibernate-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.sentry</groupId>
            <artifactId>sentry-spring-boot-4-starter</artifactId>
            <version>${sentry.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-graphql</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot4</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-micrometer</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jgit</groupId>
            <artifactId>org.eclipse.jgit</artifactId>
            <version>${jgit.version}</version>
        </dependency>
        <!--
            Bucket4j token-bucket rate limiting for the hot auth endpoints (core.auth.ratelimit).
            Library-only (no Spring Boot starter): the community starter
            `com.giffing.bucket4j.spring.boot.starter` does not yet declare Boot 4.0 compatibility,
            and Bucket4j itself is framework-agnostic, so we wire a small OncePerRequestFilter by
            hand. JDK17 classifier artifacts (8.16.0+ split by JDK). The postgresql module backs a
            SELECT … FOR UPDATE ProxyManager so limits are SHARED across replicas (no Redis).
        -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-postgresql</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit</artifactId>
                <version>${archunit.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp-bom</artifactId>
                <version>${okhttp.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
            <!-- All three move together: Bouncy Castle only supports a matched provider, util and
                 PKIX set. docker-java's TLS path (CertificateUtils, LocalDirectorySSLConfig) is the
                 only consumer, and it links against PEMParser, JcaPEMKeyConverter,
                 JcaX509CertificateConverter and BouncyCastleProvider, all unchanged in 1.84. -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcutil-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcpkix-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <upToDateChecking>
                        <enabled>true</enabled>
                        <indexFile>${project.build.directory}/spotless-index</indexFile>
                    </upToDateChecking>
                    <java>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                        </palantirJavaFormat>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <id>check-java-formatting</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <compilerArgs>
                        <arg>-Xlint:all,-deprecation</arg>
                        <arg>-XDcompilePolicy=simple</arg>
                        <arg>--should-stop=ifError=FLOW</arg>
                        <arg>-XDaddTypeAnnotationsToSymbol=true</arg>
                        <!-- javac requires the Error Prone plugin and its options in one compiler argument. -->
                        <arg>-Xplugin:ErrorProne -XepDisableAllChecks -Xep:NullAway:ERROR -Xep:RequireExplicitNullMarking:ERROR -XepExcludedPaths:.*[\\/]target[\\/]generated-sources[\\/].* -XepOpt:NullAway:AnnotatedPackages=de.tum.cit.aet.hephaestus -XepOpt:NullAway:JSpecifyMode=true -XepOpt:NullAway:TreatGeneratedAsUnannotated=true -XepOpt:NullAway:HandleTestAssertionLibraries=true</arg>
                    </compilerArgs>
                    <showWarnings>false</showWarnings>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                        </path>
                        <path>
                            <groupId>com.github.therapi</groupId>
                            <artifactId>therapi-runtime-javadoc-scribe</artifactId>
                            <version>${therapi.version}</version>
                        </path>
                        <path>
                            <groupId>com.google.errorprone</groupId>
                            <artifactId>error_prone_core</artifactId>
                            <version>${error-prone.version}</version>
                        </path>
                        <path>
                            <groupId>com.uber.nullaway</groupId>
                            <artifactId>nullaway</artifactId>
                            <version>${nullaway.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <id>default-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <skip>${skipCoverage}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <skip>${skipCoverage}</skip>
                        </configuration>
                    </execution>
                    <execution>
                        <id>check-coverage</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <skip>${skipCoverage}</skip>
                            <rules>
                                <!-- Per-package BRANCH floors. There is deliberately no whole-bundle rule: a minimum any
                                     code can clear reads as a guarantee while providing none.

                                     Whole-package figures, including the Spring-wired credential adapters: a rule's
                                     includes/excludes match the element name, so a class glob cannot narrow a
                                     PACKAGE rule. Floors sit below the measured ratios because surefire runs tests
                                     in parallel and the numbers move several points between runs. They are a
                                     ratchet against regression — raise one whenever its package clears the next
                                     step with room to spare. -->
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>de.tum.cit.aet.hephaestus.integration.core.webhook</include>
                                        <include>de.tum.cit.aet.hephaestus.integration.scm.github.webhook</include>
                                        <include>de.tum.cit.aet.hephaestus.integration.scm.gitlab.webhook</include>
                                        <include>de.tum.cit.aet.hephaestus.integration.outline.webhook</include>
                                        <include>de.tum.cit.aet.hephaestus.agent.context</include>
                                        <include>de.tum.cit.aet.hephaestus.agent.context.providers</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>de.tum.cit.aet.hephaestus.integration.slack.webhook</include>
                                        <include>de.tum.cit.aet.hephaestus.evidence</include>
                                        <include>de.tum.cit.aet.hephaestus.evidence.internal</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.60</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Memory and JVM settings - @{argLine} is replaced by JaCoCo.
                         4GB heap: the unit+architecture surefire run loads ArchUnit + Spring
                         Modulith's full class graph (1000+ classes) in the same fork as the
                         regular unit tests. 2GB OOMs on CI under that load. The dedicated
                         `architecture-tests` profile uses the same 4GB ceiling. -->
                    <argLine>@{argLine} -Xmx${hephaestus.surefire.maxHeap} -Dspring.profiles.active=test -Dfile.encoding=UTF-8
                        -XX:+EnableDynamicAgentLoading -XX:+ExitOnOutOfMemoryError
                    </argLine>
                    <printSummary>true</printSummary>
                    <redirectTestOutputToFile>false</redirectTestOutputToFile>
                    <forkCount>${hephaestus.surefire.forkCount}</forkCount>
                    <reuseForks>true</reuseForks>
                    <!-- Integration tests override method parallelism because they share database state. -->
                    <parallel>methods</parallel>
                    <threadCount>4</threadCount>
                    <perCoreThreadCount>true</perCoreThreadCount>
                    <forkedProcessTimeoutInSeconds>${hephaestus.surefire.timeout}</forkedProcessTimeoutInSeconds>
                    <forkedProcessExitTimeoutInSeconds>180</forkedProcessExitTimeoutInSeconds>
                    <!-- Enable process checker so forkedProcessTimeoutInSeconds actually works.
                         Without this, Surefire 3.x ignores the timeout and hung forks run forever. -->
                    <enableProcessChecker>ping</enableProcessChecker>
                    <groups>${surefire.includedGroups}</groups>
                    <excludedGroups>${surefire.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
<!-- EnableDynamicAgentLoading mirrors surefire: without it Byte Buddy cannot
                         self-attach on JDK 21+ and every Mockito-using integration test fails to
                         initialize. -->
                    <argLine>@{argLine} -Xmx4096m -Dspring.profiles.active=test -Dfile.encoding=UTF-8
                        -XX:+EnableDynamicAgentLoading
                    </argLine>
                    <groups>${failsafe.includedGroups}</groups>
                    <excludedGroups>${failsafe.excludedGroups}</excludedGroups>
                    <includes>
                        <include>**/*IntegrationTest.java</include>
                        <include>**/*LiquibaseTest.java</include>
                    </includes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <profiles>${app.profiles}</profiles>
                    <jvmArguments>-XX:+EnableDynamicAgentLoading --enable-native-access=ALL-UNNAMED</jvmArguments>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.28.0</version>
                <configuration>
                    <rulesets>
                        <ruleset>${maven.multiModuleProjectDirectory}/pmd-ruleset.xml</ruleset>
                    </rulesets>
                    <targetJdk>${java.version}</targetJdk>
                    <includeTests>false</includeTests>
                    <linkXRef>false</linkXRef>
                    <printFailingErrors>true</printFailingErrors>
                    <failOnViolation>true</failOnViolation>
                    <excludeRoots>
                        <excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
                        <excludeRoot>${project.build.directory}/generated-test-sources</excludeRoot>
                    </excludeRoots>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.liquibase</groupId>
                <artifactId>liquibase-maven-plugin</artifactId>
                <version>${liquibase.version}</version>
                <configuration>
                    <changeLogFile>db/master.xml</changeLogFile>
                    <diffChangeLogFile>${project.basedir}/src/main/resources/db/changelog_new.xml</diffChangeLogFile>
                    <searchPath>${project.basedir}/src/main/resources</searchPath>
                    <driver>org.postgresql.Driver</driver>
                    <url>jdbc:postgresql://localhost:${postgres.port}/hephaestus</url>
                    <username>root</username>
                    <password>root</password>
                    <referenceUrl>
                        hibernate:spring:de.tum.cit.aet.hephaestus?dialect=org.hibernate.dialect.PostgreSQLDialect&amp;hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy&amp;hibernate.implicit_naming_strategy=org.springframework.boot.hibernate.SpringImplicitNamingStrategy
                    </referenceUrl>
                    <!--
                        Schema objects Hibernate cannot emit, excluded by type and naming convention so
                        the JPA-vs-DB diff stays empty without a hand-maintained name list:
                        - shedlock and auth_rate_limit_bucket have no JPA entity;
                        - auth_event_default and auth_event_p<n> are PostgreSQL partitions of auth_event,
                          which Liquibase reads as extra tables (liquibase/liquibase#1874);
                        - sfk_* is a foreign key backing a scalar id column with no JPA association
                          (ADR 0017). An association's foreign key keeps the fk_* name and is
                          drift-checked; a misnamed constraint fails the gate in either direction.
                    -->
                    <diffExcludeObjects>table:shedlock,table:auth_rate_limit_bucket,table:auth_event_default,table:auth_event_p\d+,foreignkey:sfk_.*</diffExcludeObjects>
                    <verbose>true</verbose>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.liquibase.ext</groupId>
                        <artifactId>liquibase-hibernate7</artifactId>
                        <version>${liquibase-hibernate7.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-starter-data-jpa</artifactId>
                        <version>${spring-boot.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>jakarta.validation</groupId>
                        <artifactId>jakarta.validation-api</artifactId>
                        <version>${jakarta-validation.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>${postgresql.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <id>live-tests</id>
            <properties>
                <surefire.excludedGroups />
                <surefire.includedGroups>live</surefire.includedGroups>
                <failsafe.excludedGroups />
                <failsafe.includedGroups />
                <!--
                  A single-test invocation (`-Dtest=…LiveLlmTest`) can match zero classes when
                  the gating env var is unset. Without these flags Maven fails the build on
                  "no tests matched", which masks the real "gate skipped" outcome.
                -->
                <surefire.failIfNoSpecifiedTests>false</surefire.failIfNoSpecifiedTests>
                <failsafe.failIfNoSpecifiedTests>false</failsafe.failIfNoSpecifiedTests>
            </properties>
        </profile>
        <profile>
            <id>architecture-tests</id>
            <properties>
                <surefire.excludedGroups />
                <surefire.includedGroups>architecture</surefire.includedGroups>
                <failsafe.excludedGroups />
                <failsafe.includedGroups />
            </properties>
        </profile>
        <profile>
            <id>pitest</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>1.30.0</version>
                        <dependencies>
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>1.2.3</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <!-- Surefire's JaCoCo argLine is invalid in PIT minion JVMs. -->
                            <parseSurefireConfig>false</parseSurefireConfig>
                            <!-- Keep peak PIT resource use within the advisory runner budget. -->
                            <threads>1</threads>
                            <failWhenNoMutations>true</failWhenNoMutations>
                            <!-- Account for cold minion startup on the application classpath. -->
                            <timeoutConstant>30000</timeoutConstant>
                            <jvmArgs>
                                <value>-Xmx1g</value>
                                <value>-XX:+EnableDynamicAgentLoading</value>
                            </jvmArgs>
                            <targetClasses>
                                <param>de.tum.cit.aet.hephaestus.core.security.ServerUrlValidator</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.PrivateAddressGuard</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.OutlineOriginPolicy</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.ImpersonationGuard</param>
                                <param>de.tum.cit.aet.hephaestus.integration.core.oauth.state.HmacOAuthStateService</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.AdminBootstrapPolicy</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.ReturnToValidator</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.AuthIntentCookie</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.CookieOAuth2AuthorizationRequestRepository</param>
                                <param>de.tum.cit.aet.hephaestus.integration.core.connection.EncryptionContext</param>
                                <param>de.tum.cit.aet.hephaestus.integration.scm.gitlab.webhook.GitlabWebhookSignatureVerifier</param>
                            </targetClasses>
                            <targetTests>
                                <param>de.tum.cit.aet.hephaestus.core.security.ServerUrlValidatorTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.PrivateAddressGuardTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.OutlineOriginPolicyTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.security.ImpersonationGuardTest</param>
                                <param>de.tum.cit.aet.hephaestus.integration.core.oauth.state.HmacOAuthStateServiceTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.AdminBootstrapPolicyTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.ReturnToValidatorTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.AuthIntentCookieTest</param>
                                <param>de.tum.cit.aet.hephaestus.core.auth.oauth.CookieOAuth2AuthorizationRequestRepositoryTest</param>
                                <param>de.tum.cit.aet.hephaestus.integration.core.connection.EncryptionContextTest</param>
                                <param>de.tum.cit.aet.hephaestus.integration.scm.gitlab.webhook.GitlabWebhookSignatureVerifierTest</param>
                            </targetTests>
                            <outputFormats>
                                <param>HTML</param>
                                <param>XML</param>
                            </outputFormats>
                            <timestampedReports>false</timestampedReports>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- AOT output is used only when the runtime enables spring.aot.enabled. -->
        <profile>
            <id>aot</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>process-aot</id>
                                <goals>
                                    <goal>process-aot</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
