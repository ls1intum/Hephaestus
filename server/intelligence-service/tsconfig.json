{
	"compilerOptions": {
		// ═══════════════════════════════════════════════════════════════════════════
		// Target & Module
		// ═══════════════════════════════════════════════════════════════════════════
		"target": "ES2023",
		"module": "ESNext",
		"moduleResolution": "Bundler",
		"lib": ["ES2023"],

		// ═══════════════════════════════════════════════════════════════════════════
		// JSX for Hono
		// ═══════════════════════════════════════════════════════════════════════════
		"jsx": "react-jsx",
		"jsxImportSource": "hono/jsx",

		// ═══════════════════════════════════════════════════════════════════════════
		// Path Aliases
		// ═══════════════════════════════════════════════════════════════════════════
		"baseUrl": "./",
		"paths": {
			"@/*": ["./src/*"]
		},

		// ═══════════════════════════════════════════════════════════════════════════
		// STRICTEST Type Checking (from @tsconfig/strictest + extras)
		// https://github.com/tsconfig/bases/blob/main/bases/strictest.json
		// ═══════════════════════════════════════════════════════════════════════════
		"strict": true,
		"allowUnusedLabels": false,
		"allowUnreachableCode": false,
		// exactOptionalPropertyTypes: requires distinguishing between undefined and missing properties
		// This is the gold standard but requires refactoring all optional interfaces
		// TODO: Enable after initial migration: "exactOptionalPropertyTypes": true,
		"noFallthroughCasesInSwitch": true,
		"noImplicitOverride": true,
		"noImplicitReturns": true,
		// noPropertyAccessFromIndexSignature: forces bracket notation for index signatures
		// This prevents accidental property access on objects with index signatures
		// TODO: Enable after initial migration: "noPropertyAccessFromIndexSignature": true,
		"noUncheckedIndexedAccess": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"forceConsistentCasingInFileNames": true,

		// ═══════════════════════════════════════════════════════════════════════════
		// Module Interop
		// ═══════════════════════════════════════════════════════════════════════════
		"esModuleInterop": true,
		"allowSyntheticDefaultImports": true,
		"resolveJsonModule": true,
		"isolatedModules": true,
		// verbatimModuleSyntax: more strict than isolatedModules
		// Requires explicit type-only imports. Enable after initial migration.
		// TODO: Enable after initial migration: "verbatimModuleSyntax": true,

		// ═══════════════════════════════════════════════════════════════════════════
		// Output
		// ═══════════════════════════════════════════════════════════════════════════
		"outDir": "./dist",
		"declaration": true,
		"declarationMap": true,
		"sourceMap": true,

		// ═══════════════════════════════════════════════════════════════════════════
		// Type Roots
		// ═══════════════════════════════════════════════════════════════════════════
		"typeRoots": ["../../node_modules/@types", "./node_modules/@types"],
		"types": ["node"],

		// ═══════════════════════════════════════════════════════════════════════════
		// Performance
		// ═══════════════════════════════════════════════════════════════════════════
		"skipLibCheck": true,
		"incremental": true
	},
	"include": ["src/**/*", "test/**/*", "scripts/**/*", "*.config.ts"],
	"exclude": ["node_modules", "dist"],
	"tsc-alias": {
		"resolveFullPaths": true
	}
}
